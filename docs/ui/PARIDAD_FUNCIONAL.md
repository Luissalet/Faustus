# Paridad funcional — el libro que impide perder funciones

Regla (Luis, 04-09-2026): **redistribuir y cambiar, sí; tener menos funciones,
nunca.** Nada de la interfaz anterior se retira hasta que su fila de abajo
diga «Migrado» y esté verificado en el 7001. Mientras tanto, cada función
sigue accesible desde «Interfaz anterior» (`?shell=legacy`), que se conserva
**idéntica** durante todo el piloto.

Cómo leerlo: **Migrado** = existe en Studio con lo mismo o más; **Parcial** =
existe pero le falta algo concreto (dicho al lado); **Anterior** = solo en la
interfaz anterior, alcanzable desde Studio con un clic. Las herramientas del
agente (85 en `src/tool_schemas.py`) no aparecen: viven en el servidor y no
las toca ninguna pantalla.

## 1. Destinos y páginas

| Función | Interfaz anterior | Studio | Estado |
|---|---|---|---|
| Chat / agente | `/` + `chat.js` | `/studio` | **Parcial** — ver §3 y §4 |
| Proyectos (lista, detalle, contexto, memoria, objetivos) | `projects-modal` | `/projects` (filtro, fijados primero, chip Archivados, Nuevo proyecto) y `/projects/{id}` con pestañas: Resumen (empezar una conversación en el proyecto con modelo y frase opcional → `/studio?s=` con la sesión ya en la carpeta del proyecto; instrucciones; los mandos del agente con su estado; raíces de trabajo con añadir por diálogo nativo o ruta y quitar), Chats (las conversaciones de la carpeta, abrir, borrar), Objetivos (añadir con prioridad y bloqueos, estado y prioridad en sitio, notas, descartar, ver descartados, bloqueos, pistas ⚡, registro), Memoria (archivos, leer, editar y guardar, crear los archivos), Actividad del agente (turnos que cambiaron archivos: petición, ficheros, tests/revisión/sin verificar/checkpoint, abrir la conversación en la respuesta exacta `?m=`, vaciar), Contexto (el bloque literal) y Ajustes (nombre, grupo fijo, carpeta con Examinar…, instrucciones con coste, los cinco mandos del agente, comando de tests, modelo revisor, escribir AGENTS.md; zona de peligro: borrar); fijar, exportar todas las conversaciones en zip por formato, archivar/restaurar; `/projects/new` crea. En Studio: chip del proyecto en la cabecera de la conversación y el workspace sigue al proyecto (misma clave `odysseus-project-workspace`) | **Migrado** — la ruta `DELETE /api/projects/{id}/session/{sid}` responde 500 (atributo `folder`); la anterior tampoco la usaba: se borra la conversación como cualquier otra |
| Biblioteca de documentos | `/library`, `documentLibrary.js` | `/library` con modos Todo (federado), Imágenes, Documentos, Chats, Investigación y Archivo (`?type=`): **Documentos** con chips por lenguaje con recuento, buscar (resaltado), ordenar, importar desde disco o arrastrando (PDF por el servidor, Word→Markdown, hojas de cálculo→CSV por hoja, código y texto), Ordenar (títulos vacíos, documentos rotos y criba con IA), fila con vista previa desplegable y acciones (abrir en el editor, abrir en su chat, duplicar, exportar, archivar, borrar) y selección múltiple (archivar, duplicar, exportar —zip a partir de 5—, borrar); **Chats** con carpetas como chips, ordenar, vista previa de los últimos mensajes, abrir, copiar la conversación, archivar, borrar, selección múltiple y Ordenar (auto-sort con IA); **Investigación** con informes, resumen y fuentes desplegables, abrir el informe, exportar en los formatos del servidor, comentar en un chat nuevo (spinoff), archivar, borrar, limpiar fallidos; **Archivo** con documentos, chats e informes archivados (restaurar, borrar). **Editor de documentos** a pantalla completa en `/documents/{id}` (desde la biblioteca, el panel lateral —«Editor completo»— y las tarjetas): título y lenguaje editables, barra Markdown (negrita, cursiva, tachado, código, H1–H3, cita, listas, tareas, bloque, enlace, tabla, línea), buscar y sustituir (Ctrl+F, siguiente, uno/todos), números de línea, vistas Editar/Ambos/Vista previa (Markdown, tabla CSV, HTML en vivo), Ejecutar (Python y bash en el servidor, JavaScript en sandbox, HTML → vista previa) con panel de salida, versiones con Comparar (revisión por bloques con palabras resaltadas, tomar/conservar, aplicar) y Restaurar, exportar (archivo, HTML, PDF del servidor, DOCX), enviar por correo (abre Redactar con el texto), archivar y borrar con consecuencia, Ctrl+S, aviso al salir con cambios; **PDF importados**: páginas renderizadas con campos del formulario (texto, casilla, lista, firma, «Hoy» en fechas), anotaciones de texto / marca / firma colocadas con un clic, deshacer, rellenar con IA (modelo de visión), firmas guardadas (elegir, dibujar con suavizado, subir PNG, borrar), descargar el PDF relleno, «Firmar y responder» cuando el PDF llegó por correo | **Migrado** (los borradores de correo desde el documento pasan por Redactar de Correo; el adjunto del PDF firmado en Redactar llega con el lote de Correo) |
| Galería de imágenes | `/gallery`, `gallery.js`, `galleryEditor.js` | `/library?type=imagen` (`&img=` abre el visor, `&album=`, `&tag=`, `&fav=1`, `&sort=`): rejilla con favoritas y álbumes como chips (crear, renombrar, borrar álbum), etiquetas como filtro apilable, modelo, orden reciente/antiguo/al azar, subir (botón y arrastrar sobre la rejilla, con progreso) al álbum abierto, «Ver más» paginado, estadísticas; menú Etiquetas (etiquetar con IA las que faltan, fusionar duplicadas, vaciar mías / de la IA con confirmación); seleccionar varias: añadir/quitar de álbum, favorita, zip, borrar; visor: imagen grande con flechas (← →), renombrar, favorita, a una conversación (la de origen o una nueva con la imagen adjunta, `/studio?image=`), girar, descargar, borrar, pie/prompt, etiquetas propias editables y de la IA con «Etiquetar con IA», álbum, modelo, tamaño, fechas, cámara, GPS con enlace, calidad; **editor de píxeles** en `/library/edit?img=` (`?draft=` retoma un borrador, `?new=AnchoxAlto` lienzo en blanco; sin parámetros: portada con lienzo nuevo y borradores): capas con desplazamiento, opacidad, visibilidad, bloqueo, renombrar, duplicar, reordenar (arrastrar o menú), fusionar hacia abajo / todas, copia aplanada, máscaras por capa (pintar con pincel/goma, invertir, vaciar, fusionar, borrar) y capas de ajuste (brillo/contraste, tono/saturación, niveles con histograma, equilibrio de color; editar, opacidad, ocultar, fundir, borrar); herramientas mover (imán a lienzo y capas, Ctrl libre), recortar (tercios, mover, Mayús cuadrado), transformación libre (asas, rotación, voltear, proporción, numérico), pincel/goma/clonar (tamaño log 1–800, opacidad, flujo, suavidad; Alt+clic origen), lazo, varita (tolerancia, en directo, nuevo/sumar/restar), selección inteligente SAM (puntos, Alt excluye, por nombre), suavizado y borde de la selección, invertir, copiar/cortar a capa, a máscara, borrar píxeles, seleccionar todo; inpaint (máscara pintada, prompt, modelo de imagen, intensidad, Generar / Quitar / Extender, suavizado y borde del resultado en directo, igualar color), quitar fondo con aviso si falta rembg y limpieza del borde en directo, enfocar / quitar ruido / mejorar caras, armonizar (ajuste de color, costura), estilo img2img, escalar 2×/4× y con IA; Imagen: tamaño del lienzo (anclaje), girar 90/180/270, voltear; Filtro: desenfoque gaussiano / zoom / movimiento con vista previa; deshacer/rehacer con historial saltable; zoom (rueda, ±, ajustar, 1:1), desplazar con espacio o botón central, pellizco; importar como capa (archivo, portapapeles/Ctrl+V, biblioteca, arrastrar); guardar sobre la original (con consecuencia), como copia, descargar PNG, proyecto .json (guardar/abrir); borrador automático que se retoma al reabrir; campo «Pide» en lenguaje natural con sugerencias (girar, voltear, quitar fondo, escalar, ruido, caras, enfocar, estilo); atajos completos (?) | **Migrado** (el enlace «Instalar desde el Cookbook» abre la anterior hasta que llegue el Cookbook; SAM, inpaint, armonizar y estilo necesitan modelo/servidor, como en la anterior) |
| Tareas programadas | `/tasks`, `tasks.js` | `/automations` (`?task=id` enlazable): lista de recetas con nombre, integrada/editada, disparador en palabras (cron habituales traducidos), acción, último y próximo paso, estado; buscar, chips por categoría (Chats, Documentos, Memoria, Investigación, Calendario, Correo, Asistente, Otras) y filtro activas/pausadas; Pausar todas / Reanudar todas con confirmación; seleccionar varias y borrar; panel con la receta completa (próxima y última ejecución, contador de eventos, modelo, encadenado, persona, avisos, URL del webhook con copiar y emitir nueva, prompt) y acciones Ejecutar ahora (409 → diálogo para lanzar otra en paralelo), Pausar/Reanudar, Editar, Detener, Volver a la de fábrica (integradas editadas), Vaciar caché (acciones de correo), Borrar; historial de ejecuciones con estado, modelo, fecha y resultado plegable; Nueva: frase → borrador del servidor (`/api/tasks/parse`, hora local→UTC) o siete formas; formulario completo (nombre opcional, tipo prompt/acción/investigación, prompt + persona, acción con cuenta de correo y reglas de triaje `urgent_email_prompt`, disparador horario/evento/webhook, frecuencia diaria/semanal/mensual/una vez/cron con día, fecha y hora nativas en hora local, evento y cada N, salida sesión/aviso/correo con cuenta y destinatario/herramientas MCP/ninguna, modelo por endpoint, encadenar, avisos); primer uso crea las integradas (`/api/tasks/onboarding`) | **Migrado** — las pestañas Completadas/Actividad del modal viven en `/activity` (ver esa fila); las notificaciones de escritorio por sondeo: PENDIENTES 86 |
| Actividad (runs de tareas, media, aprobaciones) | repartido en `/tasks` (pestañas Completadas y Actividad), `/gallery`, pill de aprobaciones | `/activity` (`?status=`, `?run=` enlazable; Inicio enlaza aquí lo que espera decisión): pestañas Todo / Necesita acción / En curso / Fallido, filtro por texto, chips por tipo (tareas, renders, aprobaciones, avisos — las entregas «notification» fuera de Todo, como antes), filas idénticas apiladas (×n, misma regla que tasks.js) y sondeo cada 5 s mientras algo corre o espera; panel del run: aprobación con el plan entero (acción, detalle, destinatarios, coste, secretos, permisos, produce, caduca, usos) y Aprobar / Denegar con nota; tarea con resultado en Markdown, error, modelo, tokens, entrega, acción y Abrir en una conversación (sesión nueva con el resultado inyectado, mismo modelo si sigue servido), Abrir el informe (investigación), Detener, Ejecutar de nuevo / otra al lado, Copiar, Vaciar caché, enlace a la automatización; render con sus campos, cancelar en curso y enlace a las imágenes | **Migrado** — los avisos de escritorio del sondeo: PENDIENTES 86 |
| Inicio | pantalla `welcome` | `/` | **Migrado** (y más: continuaciones, aprobaciones que abren su ficha en Actividad, quick starts) |
| Notas | `/notes` | `/notes` (grupo «Herramientas» de la barra, paleta, `/notes`, Ctrl+Alt+atajo `open_notes`): añadir rápido (nota o lista), tarjetas nota / lista / objetivo con color, etiquetas, fijar, recordatorio con hora y repetición, archivar con deshacer, borrar con confirmación, orden manual (arrastrar o subir/bajar), lista o cuadrícula, filtros «Hoy» / objetivos / etiqueta, copiar, «Resolver con el agente» (abre Studio en agente y enlaza la conversación a la nota), archivadas con recuperar, `/notes?n=<id>` | **Parcial** — dibujar en lienzo, foto adjunta y fondo de imagen propio, selección múltiple, edición táctil a pantalla completa: anterior. Los recordatorios los dispara la anterior mientras siga cargada debajo (Studio tiene su bucle, apagado si detecta `#notes-pane`) |
| Calendario | `/calendar` | `/calendar` («Herramientas», paleta, `/calendar`, atajo `open_calendar`, `?d=YYYY-MM-DD`): mes con panel del día, semana, agenda (60 días), año con densidad; evento con título, todo el día, inicio/fin, repetición (RRULE), lugar, calendario, color propio, notas; borrar (en recurrentes: solo esta vez o toda la serie); añadir en tus palabras (`/quick-parse`, con formulario de respaldo si el modelo falla); filtros por calendario; calendarios (nombre, color, nuevo, borrar, exportar .ics, importar .ics); sincronizar CalDAV si hay cuenta; teclado (←/→, T, N, m/w/a/y) | **Parcial** — configuración de cuentas CalDAV y prueba de conexión, arrastrar para mover o crear en la semana con horas, zoom de la semana, buscar eventos, recordatorio a Notas desde el evento, deshacer al borrar, fondo de imagen, insignia del carril: anterior |
| Correo | `/email`, `emailInbox.js`, `emailLibrary.js` (8.771 líneas), correo dentro de `document.js`, ajustes de correo en `settings.js` | `/email` («Herramientas», paleta, `/email`; `?uid=&folder=` abre un correo, `#email=carpeta:uid` de la anterior sigue funcionando; `?compose=handoff` recibe un borrador de otra pantalla): **carril** con cuenta (si hay varias), carpetas con el recuento de no leídos en la bandeja (sondeo cada minuto), **Bandeja de salida** (programados con cancelar y borradores de agentes esperando aprobación: aprobar y enviar / descartar, con contador), triaje (todos, sin leer, sin hacer, con estrella, recordatorios, pendientes 30 días, más de 30 días, con adjuntos) y etiquetas del triaje automático (urgente, responder pronto, requiere acción, facturas, recibo, viajes, boletín, spam) como filtros `tag:`; **lista** agrupada por día (con estrella primero), avatar con color estable por remitente, no leídos en negrita con punto (rojo/ámbar según la urgencia del `urgency-state`), borde de urgencia, estrella / hecho / clip, extracto, etiquetas en la fila, acciones rápidas al pasar (archivar, borrar, estrella, hecho), búsqueda con ámbito (carpeta, bandeja, enviados, todo), filtro «solo de este remitente», selección múltiple con acciones en lote (hecho, leído, no leído, archivar, mover a…, borrar), paginación; atajos j/k, Intro, e, #, s, d, u, x, c, /; **lectura**: responder / a todos / reenviar (reenviar vuelve a adjuntar los ficheros del original), hecho / no hecho, estrella, archivar, borrar, no leído, recordarme responder (hoy más tarde, mañana, la semana que viene, elegir hora, nota sin fecha → nota con enlace al correo), mover a…, más (resumir con IA, abrir en un chat, guardar remitente en contactos, solo de este remitente, HTML original, no es spam, mover a spam, borrar definitivamente); remitente con detalles plegados (para, cc, fecha, Message-ID), etiquetas pulsables (calendario abre el evento con `/calendar?event=`), aviso «parece spam», resumen (en caché o a petición) y traducción (idioma por defecto de la configuración, diez idiomas, otro) como paneles plegables con el modelo; cuerpo saneado en el tema (colores del correo fuera, imágenes remotas retenidas con «Mostrar N imágenes», `cid:` por `/inline-image`), historial y firma plegados, **vista de conversación** con burbujas (mías a la derecha, según los alias de la cuenta) cuando el servidor da `thread_turns`, vista «HTML original» en marco aislado; adjuntos con abrir, descargar, descargar todos (zip) y «abrir como documento» (→ `/documents/{id}`), diferidos si el correo es grande; **redactar** en la columna del lector (la lista sigue a la vista): desde, para/CC/CCO con fichas y autocompletado de contactos, asunto, «Redactar con IA» (rápido o completo, con indicación, el modelo usado visible, conserva la cita), cuerpo con arrastrar ficheros, adjuntar (fichero del equipo, desde la biblioteca: documentos e imágenes, en zip si son varios), guardar borrador, enviar más tarde (en una hora, mañana, el lunes, elegir hora), enviar (Ctrl+Intro; aviso si el texto menciona un adjunto y no hay), remitentes nuevos a contactos, aviso si la cuenta no puede enviar; **revisión de bajas** (candidatos con puntuación, motivos, darse de baja por `mailto` o abrir el enlace, opción de mover a spam; después, mover los resueltos a spam o borrarlos); **ajustes del correo**: respuesta de ausencia (fechas, asunto, mensaje, cadencia, saltar automáticos, pausar notificaciones, banner cuando está activa), funciones automáticas (resumir, etiquetar, spam, calendario), estilo de escritura (extraer de los enviados, guardar); las cuentas siguen en Ajustes → Integraciones (enlace) | **Migrado** (y más: conversación en burbujas, vista de lectura en el tema, atajos de teclado, bandeja de salida con aprobaciones, bajas con limpieza) |
| Brain / memoria (`memory-modal`, `/memory`) | sí | `/memory` («Herramientas», paleta, `/brain`, atajo `open_memory`): recuerdos con añadir (texto + categoría), editar en sitio, fijar, borrar, buscar, filtro por categoría, ordenar (recientes, antiguas, alfabético, más usadas), seleccionar varias y borrar en lote, «Ordenar con el modelo» (`/audit`), sugerencias de una conversación (`/extract`) o de un fichero (`/import`) con elección de cuáles guardar, exportar JSON, interruptores «Memoria activa» y «Extraer sola» (`/api/prefs`); reglas aprendidas (`/api/memory-engine`): lista con nivel, madurez, confianza, puntuación y % dañina, filtros, añadir, útil/dañina, borrar, curador con informe, «Ver el paquete» | **Parcial** — los interruptores y ajustes de Skills que vivían en el mismo modal (skills activas, auto-skills, auto-aprobar, confianza mínima, máximo inyectado) van con la fila de Skills; el arrastre para reordenar y la animación del tidy: anterior |
| Cookbook (servir/descargar modelos) | `cookbook-modal`, `/cookbook` | — | **Anterior** |
| Deep Research | panel propio | — | **Anterior** |
| Compare (varios modelos a la vez) | `compare/` | — | **Anterior** |
| Tournament | `tournament-modal` | — | **Anterior** |
| Workers (dispatch) | `/workers` | `/agents` (pestaña Workers; barra «Herramientas», paleta, `/workers`): tareas (línea en blanco o lista = un worker, máx. 4), carpeta, paralelo, revisor, modelo (con el configurado como pista), verificar con (con el runner de tests detectado en la carpeta como pista), rondas de arreglo, **agente** y **runner** (nuevo: la anterior guardaba la elección y no la usaba), Ctrl+Intro; lista de trabajos con estado, hora, duración, ficheros cambiados, verificación, errores; desplegable con fase y progreso por worker (ronda, herramienta, atascado, estado propio), cambios en disco, verificación (fallos, salida, rondas previas), prueba (veredicto, confianza, dudas), workers (rondas, herramientas, tokens, reclamaciones, resumen), bloqueos, tareas caídas, tareas del trabajo; Tablero → chat de Workers en Studio (`/studio?s=`); Cancelar; SSE por trabajo vivo con sondeo cada 3 s de respaldo | **Migrado** |
| Expertos | `experts-modal` | `/agents?t=experts` (`/experts`): galería (buscar, ordenar por nombre/corpus/aceptadas, aviso si `agent_experts` está apagado), nuevo, borrar con confirmación; ficha: nombre, descripción, modelo, temperatura, top p, activado, instrucciones, rúbrica (una por línea), corpus (subir varios, borrar con confirmación, reindexar con resumen, enlace a cada fichero, páginas «desconocidas» cuando no se sabe), buscar en el corpus (nivel léxico avisado), «ver lo que ve el modelo»; panel de revisión: pegar resultado, texto marcado por tramo, tarjetas por corrección (severidad, operación, regla, etiqueta corpus/opinión, citas con marcador desconocido, notas), aceptar/rechazar, resultado aplicado de derecha a izquierda, copiar, enviar desenlace | **Migrado** (`@expert:` en el compositor se escribe a mano, ver Menciones) |
| Procedencia (grafo) | `provenance-modal` | — | **Anterior** |
| Historial importado | `history-modal` | — | **Anterior** |
| Agent runners | `agent-runners-modal` | `/agents?t=runners`: la advertencia de la guardia arriba (no en tooltip), estado de `agent_external_runners`, contadores, buscar, actualizar; tabla con licencia literal (abierta / suscripción / desconocida), instalado y versión, guardia (`gate`), «puede ser worker» separado de «instalado», comando de instalación con copiar y **Lanzar** (`ollama launch`, salida en directo), «Usar en un trabajo» → Workers con el runner puesto | **Migrado** |
| Agent definitions | `agent-defs` | `/agents?t=defs`: reglas resueltas por tarjeta (sí/no, qué, detalle), modo y origen con su explicación, modelo/endpoint/runner, delega o no con el techo nombrado, salvedades, ficheros que no cargaron con su motivo, la frase de lo que una regla de ruta no promete, buscar, actualizar, copiar slug, «Usar en un trabajo» → Workers con el agente puesto (con la carpeta activa `odysseus-workspace` se piden también las del repo) | **Migrado** |
| Workspace de la sesión (elegir carpeta, `Change folder`) | pantalla de bienvenida y compositor | chip de carpeta → diálogo nativo del sistema (`/api/workspace/pick`), con el diálogo en página solo como respaldo | **Migrado** (y más: la anterior también abre el nativo) |
| Skills (auditoría, publicar, importar) | `skills-audit-panel` (pestaña dentro de Brain) | `/skills` («Herramientas», paleta, `/skills`, `?skill=nombre` enlazable): lista con estado, confianza (color por tramo, marca ✓ si pasó una auditoría), usos, origen profesor, aviso (duplicada #n con grupo y candidata a conservar —misma agrupación por tokens ≥0,38 o nombre base—, genérica, posiblemente irrelevante, fallida, necesita trabajo, por debajo del umbral, sin auditar); buscar; filtros Todas / Borradores / Publicadas / **Necesitan atención** (el criterio del «borrar no aptas» hecho visible); ordenar confianza / más usadas / A–Z / recientes; confianza ≤ 95…70; panel de detalle con Resumen (cuándo, procedimiento, trampas, verificación, estado, confianza, usos y último uso, origen, última auditoría con modelo y profesor, categoría, etiquetas, creada, archivo), SKILL.md (leer, editar y guardar, descargar; si la entrada no tiene archivo, escribirlo desde sus campos), Prueba (lanzar, registro en vivo, caja de aprobación permitir una vez / denegar, veredicto con confianza, resumen y problemas, aprobar/despublicar, reintentar, copiar, editar, borrar); publicar/despublicar; auditar una; borrar con confirmación; Auditar todas (las visibles) con diálogo (saltar ya auditadas), banner con progreso, resumen por resultado, profesor, registro y cancelar, que se reengancha si ya corre al abrir; seleccionar varias con todas, aprobar, auditar, borrar no aptas (umbral de prefs), borrar; Nueva skill (título, cuándo, cómo, etiquetas) e importar desde URL de GitHub/skills.sh en el panel; interruptor «Skills activas» y popover Aprendizaje (auto-redactar, la auditoría publica, confianza mínima, skills por petición) sobre las mismas `/api/prefs` | **Migrado** — las tarjetas «builtin» del legacy estaban apagadas (`showBuiltin=false`); el menú kebab por fila se sustituye por las acciones del panel |
| Ajustes (todas las secciones, 81 opciones del agente, endpoints, modelos locales, atajos) | `settings-modal` | `/settings` (pie de la barra, paleta, `/setup`, Ctrl+,): Modelos (endpoints: añadir con prueba, releer modelos, activar/desactivar, quitar), IA por defecto (chat, tareas, utilidad y alternativas, visión, workers, Deep Research, imágenes, profesor, salida estructurada, estilo de documentos, versiones del chat), Voz (TTS/STT), Búsqueda (proveedor, claves, SafeSearch, cadena de respaldo), Recordatorios (canal, correo, ntfy, webhook, síntesis), Agente (las 97 opciones que describe `/api/agent/settings/schema`, agrupadas, con filtro y clave visible), Atajos (grabar combinación, por defecto, duplicados marcados), Sistema (URL pública, compartir valores, rutas extra, urgencia del correo, GPU, opciones de carga, skills). Se guarda por sección, solo lo que cambia | Cuenta (quién ha entrado, cerrar sesión con limpieza del navegador, contraseña con la política del servidor, 2FA: alta con QR y secreto, verificación, códigos de respaldo, baja con contraseña), Usuarios (registro abierto, compartir valores, lista con hacer/quitar admin, renombrar, quitar, permisos por función, límite diario, modelos permitidos todos/ninguno/lista), Herramientas (activar/desactivar por familia y una a una, lista reconstruida de una lectura fresca), Sistema para admin (registro con nivel, últimas N, búsqueda y auto-refresco; copia de seguridad exportar/importar; zona de peligro por categoría y todo, doble confirmación) , Integraciones (la lista unificada de la anterior: claves de API con plantillas del servidor, auth bearer/cabecera/basic/ninguna, prueba; cuentas CalDAV con prueba; contactos: sincronización CardDAV, libreta con buscar, añadir, editar, borrar, importar .vcf/.csv, exportar; cuentas de correo con proveedores, OAuth de Google, IMAP/SMTP, «igual que IMAP», prueba, por defecto; servidores MCP: stdio/SSE/HTTP, OAuth con espera y pegado manual, reconectar, activar/desactivar, herramientas una a una; tokens de agente Codex/Claude con permisos por alcance, comandos de instalación, revocar; bóveda Bitwarden: configuración, iniciar sesión, desbloquear, bloquear, salir) , Modelos locales (el gestor de Ollama: tarjeta(s) con modelos/otros/libre, reserva y presupuesto, por tarjeta con lo que tiene cargado, runners huérfanos con Liberar, política de ubicación «llenar primero»; cargados ahora con ubicación, reparto GPU/CPU, contexto y keep-alive, descargar de memoria; instalados con tamaño y veredicto cabe/justo/no cabe/repartido, cuant./parám., capacidades, contexto, cargar/descargar, por defecto, opciones num_ctx/num_gpu/main_gpu/keep_alive con aviso de fijado, borrar; descargar por nombre con progreso en vivo por EventSource que se reengancha al abrir, cancelar; catálogo con filtro, fit por etiqueta y descargar; selector de endpoint; solo lectura sin admin) , Apariencia (ver la fila Tema) | **Migrado** |
| Tema (editor de colores, importar/exportar) | `theme-modal` | Ajustes → Apariencia: idioma y claro/oscuro/sistema; paleta con los 16 temas de la anterior y la de Studio; colores (fondo, texto, panel, borde, acento) en directo; generador de armonía (complementaria, análoga, triádica, monocromática; claro/oscuro); fuente (Inter, mono, sans, serif, OpenDyslexic y las de `static/fonts/custom/`), densidad, tamaño de texto, cristal esmerilado; fondo: los 8 efectos (puntos, sinapsis, lluvia, constelaciones, flujo Perlin, pétalos, destellos, brasas) con color, intensidad y tamaño; guardar hasta 8 temas propios, borrar, copiar/descargar JSON, importar pegando o desde fichero. Mismo almacenamiento que la anterior (`odysseus-theme`, `odysseus-custom-themes`, prefs `theme` y `custom-themes`), así que el tema es el mismo en las dos. Los toggles de visibilidad del panel Apariencia de la anterior (secciones de su barra, botones de su compositor) no tienen equivalente porque Studio no tiene esa barra; los que eran del chat sí: bloques de razonamiento, emojis, difuminar secretos, pantalla de bienvenida, ancho completo | **Migrado** |
| Fondos | `/backgrounds` | la ruta existía en `app.py` pero `static/backgrounds.html` no: devolvía 404. Los fondos viven en Apariencia | **Migrado** (no había nada que migrar) |
| Login | `/login` | — | **Migrado** (no cambia) |

## 2. Barra lateral y sesiones

| Función | Studio | Estado |
|---|---|---|
| Lista de conversaciones, buscar (Ctrl+K) | sí (`/studio`, filtro + paleta) | **Migrado** |
| Nueva conversación | sí | **Migrado** |
| Renombrar, archivar, borrar, favorito, carpeta | diálogo «…» por conversación (mover a una carpeta existente, nueva o ninguna) | **Migrado** |
| Selección múltiple (archivar/borrar/exportar zip) | menú ⇅ → «Seleccionar varias»: casillas, «todas», barra con Zip / Archivar / Borrar (confirmación) | **Migrado** |
| Ordenar sesiones | menú ⇅: última actividad, fecha de creación, nombre, por carpeta (grupos plegables); misma clave `odysseus-session-sort` | **Migrado** |
| Manage Chats (biblioteca de chats) | «Archivadas…» con búsqueda y «Recuperar»; «Ordenar en carpetas con IA» y «Limpiar vacías» (`/api/sessions/auto-sort`) ; biblioteca completa en `/library?type=chats` (recuento, carpetas, ordenar por reciente/antiguo/mensajes/A–Z, vista previa, copiar, archivar, borrar, selección múltiple, Ordenar con IA) y archivados en `/library?type=archivo&kind=chats` | **Migrado** |
| Exportar conversación (md, txt, json, html, docx, pdf) | diálogo «…» y `/export`; en lote como zip desde la selección múltiple | **Migrado** |
| Truncar y compactar (`/truncate`, `/compact`) | sí | **Migrado** |
| Versiones y restaurar (`/versions`, `/restore`), checkpoints (`/checkpoints`) | sí | **Migrado** |
| Fork de conversación | «Bifurcar desde aquí» en cada mensaje (`/api/session/{id}/fork` con `keep_count`), «Duplicar» en el diálogo, y `/fork` | **Migrado** |
| Modo Nobody / incógnito | chip «Incógnito» y `/incognito` (Ctrl+Alt+I): la sesión se llama «Incógnito», no aparece en la lista y se borra al salir del modo o al recargar; se envía `incognito=true`. Clave propia (`faustus_studio_incognito`), no la de la anterior | **Migrado** |
| Colapsar barra lateral | cajón en ≤1023 px; en pantallas anchas la columna se oculta con el chip «Conversaciones» o Ctrl+B y se recuerda | **Migrado** |
| Barra de navegación: ancho y plegado | borde arrastrable (cursor de doble flecha; 168–400 px; al comprimir por debajo de 120 px se pliega a carril de iconos; arrastrar de vuelta o doble clic la reabre; teclado ←/→/Inicio/Fin en el separador); se recuerda (`faustus_studio_nav`); en <1280 px se pliega sola salvo elección explícita. La anterior no tenía ancho ajustable | **Migrado** (nuevo respecto a la anterior) |
| Idioma de la interfaz | Ajustes → General: inglés (por defecto) o español; aplica al momento y se guarda en `ui_language` (sigue al usuario a otro navegador) además de `localStorage`. Fechas, horas, meses y días con el `locale` del idioma. La anterior es solo inglés; los modelos contestan en el idioma en que se les escribe | **Migrado** (nuevo respecto a la anterior) |
| Apariencia: sistema / claro / oscuro | Ajustes → General: la elección pone `data-theme` en `<html>` (tokens.css lo resuelve antes que `prefers-color-scheme`); «Seguir al sistema» quita el atributo; se guarda en `ui_theme` y `localStorage`. El editor de colores de la anterior sigue en la fila «Tema» | **Migrado** |

## 3. Compositor y turno

| Función | Studio | Estado |
|---|---|---|
| Enviar, Shift+Enter, parar, Escape | sí | **Migrado** |
| Modo chat / agente, plan | sí | **Migrado** |
| Web search, shell access | sí | **Migrado** |
| RAG por sesión | chip «Docs» (misma clave que la anterior) | **Migrado** |
| Deep Research, Group chat, Persona, Compare desde el compositor | — | **Anterior** |
| Elegir modelo y endpoint, refrescar | paleta y `/models`, con «Refrescar la lista de modelos» (`?refresh=true`) | **Migrado** |
| Carpeta de workspace (elegir, quitar, indicador) | diálogo nativo del sistema (Explorador en Windows) y, si el navegador no está en la misma máquina, diálogo con árbol de carpetas; misma clave que la anterior | **Migrado** |
| Adjuntos (ficheros, imágenes, pegar, arrastrar) | sí | **Migrado** (ver PENDIENTES §26 sobre el 7001) |
| Menciones `@fichero` | lista con búsqueda difusa | **Migrado** (`@ruta:línea` y `@expert:` se escriben a mano; sin fichas pulsables) |
| `#` regla permanente, `/remember` | sí | **Migrado** |
| Comandos `/` | 26 propios (help, models, compact, truncate, versions, restore, checkpoints, temp, maxtokens, topp, think, gen, remember, export, rename, stats, agents, doc, browser, open, incognito, preset, fork, tts…) y el resto enruta a su pantalla o a la anterior | **Parcial** |
| `/agents` (delegar a sub-agentes, `[ficheros]`, `{modelo}`, `--review`, `--serial`) | sí, mismo campo `delegate_tasks` | **Migrado** |
| Citar selección ❝ | al seleccionar texto de un mensaje aparece «Citar»: lo añade al borrador como `> cita` | **Migrado** |
| ↑ recuperar último mensaje | ↑ con el borrador vacío recupera lo último enviado | **Migrado** |
| Controles por chat (`/temp`, `/maxtokens`, `/topp`, `/think`, `/gen`) | sí, con chip visible | **Migrado** |
| Presets / personajes / prompt del sistema | chip de preset con paleta (incluidos y tuyos), «Nuevo preset…» y borrar los tuyos (`/api/presets/templates`), `/preset nombre`; se envía `preset_id` | **Migrado** |
| Dictado (STT) y lectura (TTS) | micrófono en el compositor (`/api/stt/transcribe`, o reconocimiento del navegador si el servidor no tiene STT); «Leer en voz alta» por mensaje, `/tts` y Alt+Shift+T (`/api/tts/synthesize`, o `speechSynthesis` si el servidor responde 503) | **Migrado** (sin probar con micrófono real en el 7001: el navegador pide permiso) |

## 4. Transcript

| Función | Studio | Estado |
|---|---|---|
| Streaming, razonamiento, métricas, fuentes web, imagen generada | sí | **Migrado** |
| Traza de herramientas con comando y salida | sí (en el carril), con el diff coloreado de cada escritura, la captura de las herramientas de escritorio y «Ver el fichero» / «Abrir el documento»; se reconstruye del historial (`tool_events`) al recargar, igual que la tarjeta del arnés (`harness`) | **Migrado** (más visible que antes) |
| Aprobación de herramienta (aprobar / toda la tarea / denegar) | sí | **Migrado** |
| `ask_user` con opciones | sí (botones) | **Migrado** |
| Fallback de modelo, errores del servidor | sí | **Migrado** |
| Editar, regenerar, borrar mensajes | sí (queda versión en el servidor) | **Migrado** (Undo/restaurar: anterior) |
| Copiar mensaje / código | botones en mensaje y en cada bloque de código | **Migrado** |
| Markdown completo (tablas, notas al pie) | lector reducido | **Parcial** |
| Tarjetas 🛡 del arnés (Turn summary, Verified/Unverified, comprobaciones por ronda, tests, análisis estático, cambios frente a lo afirmado) | tarjeta bajo la respuesta (`harness_check`, `harness_summary`) | **Migrado** |
| Panel Progress (`todowrite`), plan en vivo (`plan_update`) | lista de progreso y tarjeta de plan dentro del turno | **Migrado** |
| Tablero de sub-agentes (`delegate_agents`): tarjetas por worker con estado, actividad, tiempo, rondas, herramientas, tokens, ficheros, última línea, dirigir (steer), parar, abrir su chat, repetir; se reconstruye del historial | tablero dentro del turno (`SubagentBoard`) | **Migrado** (en vivo verificado con el reductor y una secuencia sintética: el modelo de prueba no llegó a llamar a `delegate_agents`; restaurado verificado con sesiones reales) |
| Vista en vivo del navegador (`browser_view`), capturas del escritorio | pestaña «Navegador» del panel lateral: fotograma, título y URL, tira de los últimos 8, «en vivo», abrir solo (misma clave `odysseus.browserView.auto`) | **Migrado** (sin provocar un `browser_view` real en el 7001; las capturas de `tool_output` van por el mismo camino) |
| Documentos del editor (`doc_stream_*`, `doc_update`, `doc_suggestions`) | pestaña «Documento» del panel lateral: se abre solo cuando el agente escribe, con editor, guardar (versiona), renombrar, vista previa, versiones y restaurar, PDF, archivar, y las sugerencias del agente una a una (aplicar / saltar / todas) | **Migrado** (el panel conserva el editor básico y enlaza a `/documents/{id}` para el modo diff por bloques y palabras, el PDF anotado y la firma) |
| Ficheros editados con diff y revertir, volver al checkpoint del turno, confirmar en git con mensaje propuesto | en la tarjeta del arnés | **Migrado** |
| Context ledger | porcentaje de contexto en el pie del turno | **Migrado** (ledger detallado: anterior) |
| Pill de uso de GPU, salud de servicios | «Vitales» en la cabecera de Studio: traza de uso de GPU (últimos dos minutos), un tanque de VRAM por tarjeta, temperatura, modelo cargado (o «sin modelo» / «Ollama apagado»), aviso de desbordamiento a PCIe; clic → panel con salud (puntuación y señales), GPUs (conjunto y cada tarjeta con sus modelos), runners huérfanos con Liberar, Ollama (modelos con reparto GPU/CPU, tamaño, ubicación, contexto, keep-alive), memoria de GPU compartida (aviso de paginado con los pasos), equipo (RAM, CPU), hora y cadencia; sondeo cada 5 s, 1,5 s mientras responde; `/usage on|off` y ojo en el panel para ocultarlo (`faustus_studio_usage`) | **Migrado** (las vistas «combinada/separada» de la anterior se funden: el conjunto y cada tarjeta se ven a la vez) |
| Visor lateral de ficheros | pestaña «Fichero» del panel: desde «Ver el fichero» en el carril o `/open ruta` | **Migrado** |
| Fichas de mención pulsables | — | **Anterior** |
| Scroll to bottom | automático mientras sigues el stream | **Migrado** |

## 5. Atajos de teclado

| Atajo | Studio | Estado |
|---|---|---|
| Ctrl+K buscar / navegar | sí | **Migrado** |
| Escape cancelar | sí (parar el stream) | **Migrado** |
| Ctrl+Alt+N nuevo chat, Ctrl+B barra, Ctrl+Alt+F favorito, Ctrl+Alt+D borrar (dos veces), Alt+Shift+T TTS, Ctrl+Alt+I incógnito, Ctrl+, ajustes, Ctrl+/ foco, Ctrl+Alt+C calendario, y los reconfigurables | sí, leyendo los keybinds guardados (`/api/auth/settings`) con los mismos valores por defecto; ajustes, calendario, notas y memoria abren la pantalla anterior; galería, biblioteca y tareas sus rutas de Studio. Se capturan antes de que los vea la anterior para que no actúe dos veces | **Migrado** |

## 6. Orden de cierre de la deuda

Lo que más se usa a diario va primero. Cada bloque cierra sus filas y solo
entonces se retira la parte legacy correspondiente (DECISIONES_UI.md §4):

1. Compositor: adjuntos, `@` menciones, `#`/`/remember`, comandos `/`,
   workspace elegible, RAG, presets.
2. Transcript: editar/regenerar/borrar con versiones, copiar código,
   tarjetas del arnés, plan y Progress, ficheros editados con diff.
3. Sesiones: renombrar/archivar/borrar/favorito/carpeta, exportar,
   fork/compactar, incógnito.
4. Sub-agentes, navegador en vivo, documentos del editor.
5. Ajustes completos, tema, atajos reconfigurables.
6. Notas, calendario, correo, brain, cookbook, research, compare, tournament,
   workers, expertos, procedencia, importación, runners, skills, fondos.

Hasta el punto 6 la barra de navegación de Studio no puede quitar «Interfaz
anterior», y `?shell=legacy` no puede dejar de servir la aplicación completa.
