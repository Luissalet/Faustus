# Mapa de reutilización — P1 (por lote, según lo pide `docs/spec/v2/P1_COMUN.md`)

`docs/spec/v2/MAPA_REUTILIZACION.md` solo cubre P0; este fichero recoge, lote
a lote, el estado real de los IDs P1 contra el código (no contra el enunciado
del backlog). Misma leyenda: existente · parcial · ausente · no verificable.
Regla de Luis que manda sobre todo lo que sigue: **no se pierde ninguna
capacidad; se extiende, reorganiza y mejora**.

## Resumen — 84 IDs P1/P2/LAB (`docs/spec/v2/backlog.json`)

Reconstruido por el Lote 50 tras la pérdida de las secciones de los lotes 47,
49a y 49b (sobrescrituras concurrentes; quedaban 45, 46, 48). Recuento sobre
el código real, no sobre lo que cada lote dice haber hecho. El Lote 55
corrigió el total: `backlog.json` tiene 74 IDs `P1` + 9 `P2` + 1 `LAB` = 84,
no 87 — la cifra anterior venía arrastrándose sin recontar desde el Lote 50 y
nunca se verificó contra el fichero fuente; el recuento de abajo es el
resultado de listar cada ID que aparece en alguna tabla de este fichero
(una por lote) y cruzarlo 1:1 contra `backlog.json` (ningún ID sobra ni
falta). El Lote 55 también auditó y cerró siete de los ocho IDs que seguían
"ausente (ningún lote lo ha tocado)" desde el Lote 50 (TASK-05, MOD-05,
IDX-02, IDX-03, UX-03, PERF-01, WRITE-02, WRITE-04 — ver su propia sección
más abajo) y sus cuatro IDs propios (UX-06, UX-09, SET-02, ACT-05).

| Estado | Recuento | Nota |
|---|---|---|
| existente | 83 | Backend y (donde el requisito lo pide) ruta/UI reales, probados. Incluye los 9 IDs cerrados en el cierre de lotes 60-70 (ver "Lote 70 — Cierre de los P1 que quedaban" más abajo): TASK-05, OPS-06, OPS-07, DESK-02, TOOL-05, TOOL-06, SEC-08, UX-06, MEDIA-05. |
| parcial | 1 | HW-06 — primitiva probada; `implemented=False` es decisión de producto (4 tests ajenos lo fijan explícitamente), no un hueco de código. Ver su fila actualizada en el Lote 52 y la nota del cierre 60-70. |
| ausente | 0 | Ningún ID sigue sin ningún fichero que lo toque tras el cierre de lotes 60-70. |
| **Total** | **84** | |

Recuento recalculado en el cierre de lotes 60-70 (2026-09-11) contando cada
fila de este fichero por script y tomando, por ID, el último estado que
aparece en orden de lote (una fila más reciente sustituye a una anterior del
mismo ID, nunca la contradice retroactivamente) — 84 IDs únicos, ninguno
sobra ni falta contra `backlog.json`.

### Ausentes — ningún lote las ha tocado

Una fila por ID, `backend`/`acceptance` tal cual los declara `backlog.json`
(recortado); todos parten de `status: "propuesto: auditar antes de
implementar"`. Esta tabla listaba originalmente 29 IDs (Lote 50); los lotes
51-53 ya habían cerrado veinte de ellos (ART-06, WRITE-01/03/05, CONN-01/04,
AUTO-01/03, HW-03..07, OBS-04/05, PERF-05, OPS-04/06/07, EVAL-02/06 — ver
sus propias secciones más abajo) sin que nadie volviera a esta tabla a
quitarlos, y el Lote 55 cerró los ocho que quedaban salvo uno (MOD-05,
IDX-02, IDX-03, UX-03, PERF-01, WRITE-02, WRITE-04 — ver su sección propia
más abajo). De los 29 originales, TASK-05 es el único que sigue sin ningún
fichero que lo toque.

| ID | Prio. | Título | Qué haría falta |
|---|---|---|---|
| TASK-05 | P1 | Steering y cambios del usuario | Encolar instrucciones durante ejecución y aplicarlas en puntos seguros; invalidar pasos afectados por una nueva restricción. |

Nota (Lote 55): PERF-05, OPS-04, OPS-06, OPS-07, EVAL-02 y EVAL-06 salieron
de esta tabla porque ya tienen fila propia en las secciones de lote de más
abajo (Lote 52 y Lote 53) — quedaban aquí solo por el arrastre documentado
arriba, no porque siguieran ausentes. TASK-05 es el único ID de los 29
originales sin ninguna fila de cierre en todo este fichero.

## Lote 48 — Web, escritorio, ejecución e investigación P1

WEB-05, WEB-06, DESK-01, DESK-02, EXEC-05, EXEC-06, RES-02, RES-06, LANG-02.
Todos partían de "ausente" (ningún módulo ni prueba los cubría) salvo donde
se indica lo contrario. Todo lo de abajo es backend real, probado contra
código real (rule 7: sin mocks exclusivos — estas rutas no cruzan HTTP, así
que se prueban por inyección de dependencias, el mismo patrón que
`src/browser_actions.py` ya usaba). Studio queda fuera del lote por mandato
explícito: ningún requisito de frontend de esta tabla está implementado aquí.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| WEB-05 | parcial | `src/browser_evidence.py` (nuevo): `ScreenshotCapture`/`build_capture` (resolución, escala, viewport, timestamp, página), `ElementRegion`/`AnnotationPoint` (recorte y anotación en píxeles, nunca una línea de código), `evidence_for_capture` (reusa `EvidenceRef` de `src/contracts`, mismo shape que `web_tools._evidence_for_fetch`), `compare_captures` (antes/después explícito), `is_stale_for_edit` (el criterio de aceptación en sí: una captura de otra URL o con DOM cambiado se marca no válida como mapa de edición) | ninguna | tests/test_p1_web_05_evidence.py (nuevo) | Nada llama todavía a `build_capture` desde el punto real donde se toma un screenshot de navegador (`src/browser_view.after_browser_action`, fichero propio de este lote pero cuyo cambio de firma afectaría a callers ajenos — ver informe) ni desde `desktop_tools._screenshot` (mismo motivo). El shape está listo; el cableado al flujo real de captura es el siguiente paso. |
| WEB-06 | parcial | `src/browser_extraction.py` (nuevo): `paginate` (límites `max_pages`/`max_items`, `truncated`/`reason` explícitos), `detect_restricted_access` (no evade bloqueos), `check_form_fill` (límites de formulario), `select_download_target` (una única ruta de fichero dentro del sandbox, nunca la carpeta), `verify_download`/`DownloadOutcome` (`complete=False` en cualquier descarga parcial o con hash/tamaño distinto), `check_login_reuse` (delega en la política de `src/browser_sessions.py`, no duplica el flag) | ninguna | tests/test_p1_web_06_extraction.py (nuevo) | Igual que WEB-05: la lógica está lista y probada de forma aislada; falta el cableado real en el punto donde `src/tool_execution.py` invoca las acciones de navegador (fuera de este lote salvo EXEC-05/06) para que `paginate`/`verify_download` se llamen desde una tool real. |
| DESK-01 | parcial | `src/desktop_control_session.py`: `set_allowlist`/`get_allowlist`/`clear_allowlist`, `is_window_allowed`/`check_focus_authorized` (el criterio de aceptación: mover el foco a una app no autorizada bloquea la siguiente acción), `enable_audit`/`disable_audit`/`record_action`/`audit_log` (captura antes/después por hash + registro en `data/runtime/desktop-audit.log`). Cableado real (no solo librería) en `src/agent_tools/desktop_tools.py::DesktopTool._execute`: ambas cosas son **opt-in por sesión** — ninguna sesión existente que nunca llame a `set_allowlist`/`enable_audit` cambia de comportamiento (verificado: la suite completa de `tests/test_desktop_tools.py`, que nunca las usa, sigue en verde sin tocar). El indicador visible y el kill-switch de Escape ya existían (`ensure_indicator`/`desktop_control_run`, Windows) y no se tocan. | ninguna | tests/test_p1_desk_01_control.py (nuevo) | El indicador visible (`ensure_indicator`) sigue siendo solo-Windows, como ya era — extenderlo a todas las plataformas exige tocar el arranque del backend Linux/Mac y romperías la mayoría de tests de `test_desktop_tools.py` que usan un `FakeBackend` sin ese handshake; no se ha tocado por seguridad de la suite existente. No hay tool nueva expuesta al modelo para llamar a `set_allowlist`/`enable_audit` — eso exige registrar un tool en `src/tool_capabilities.py`/`src/tool_schemas.py` (ficheros ajenos); ver informe. |
| DESK-02 | parcial | `src/browser_journey_verification.py` (nuevo): `run_journey` (para en el primer fallo, adjunta captura), `JourneySnapshot`/`JourneyStep`/`JourneyVerdict`, `detect_object_leak`/`assert_no_object_leak` (el caso `[object Object]` del criterio de aceptación), `assert_no_console_errors`/`assert_no_network_failures`/`assert_no_accessibility_issues`, `assert_dom_contains` | `studio/checks/*.check.mjs` (existente, ajeno a este lote) | tests/test_p1_desk_02_journey.py (nuevo) | El runner toma un `snapshot_fn` inyectado (mismo patrón que `browser_actions.run_with_precondition`) y está probado con fixtures falsas, tal y como pide `P1_COMUN.md` ("fixtures controladas antes de la instancia real"). Cablearlo contra Studio real (dev server + Playwright) es trabajo de Studio, explícitamente fuera de este lote. |
| EXEC-05 | parcial | `src/tool_execution.py`: `detect_package_manager` (lockfile antes que manifest desnudo), `validate_package_name` (rechaza URLs/paths/metacaracteres de shell — el criterio de aceptación), `plan_dependency_install`/`DependencyInstallPlan` (hash de plan estable e independiente del orden), `plan_already_approved`/`record_plan_approval` (no repite aprobación para el mismo plan), `execute_dependency_install` (nunca instala global: `--target` de proyecto o venv activo para pip, `--prefix`/`--cwd`/`--dir` para npm/yarn/pnpm) | ninguna | tests/test_p1_exec_05_install.py (nuevo) | No hay tool nueva expuesta al modelo (requeriría tocar `src/tool_capabilities.py`/`src/tool_schemas.py`, ficheros ajenos) — hoy `plan_dependency_install`/`execute_dependency_install` son funciones de librería llamables por un caller Python, no un tool_call del modelo. Ver "Cambios necesarios en ficheros ajenos" en el informe. |
| EXEC-06 | parcial | `src/tool_execution.py`: `save_script`/`load_script`/`SavedScript` (versionado real: guardar bajo el mismo nombre incrementa `version`, nunca sobrescribe en silencio), `render_script_command` (renderiza a lista argv, nunca a una cadena de shell — sin inyección posible), `run_saved_script`, `pair_ssh_target`/`resolve_ssh_target`/`verify_ssh_target` (comparación EXACTA de host y fingerprint — el criterio de aceptación: un hostname parecido no hereda confianza, una receta no cambia de máquina en silencio), `run_remote_script` (ssh con `BatchMode=yes`, ningún secreto en el prompt) | ninguna | tests/test_p1_exec_06_scripts_ssh.py (nuevo) | Igual que EXEC-05: sin tool expuesta al modelo todavía (fichero ajeno). El emparejamiento SSH (`pair_ssh_target`) asume que el operador ya validó el fingerprint fuera de banda, tal y como el propio lote acota ("SSH ya emparejado") — no hay descubrimiento/TOFU de claves aquí. |
| RES-02 | existente | `src/deep_research.py`: `classify_stop_reason`/`STOP_REASON_*` (siete causas nombradas: cancelado, presupuesto de tiempo, sin más consultas, saturación de novedad, búsqueda caída, cobertura suficiente según el LLM, máximo de rondas — cableado en cada punto real donde el bucle para hoy), `self.stop_reason` (poblado siempre al terminar `research()`), `stop_rationale_text` (frase legible para un caller ajeno). Cambio de comportamiento real: `_generate_queries` añade una instrucción de cambio de estrategia cuando `self._consecutive_empty_rounds > 0` (el criterio de aceptación: repetir consulta sin evidencia nueva dispara cambio de estrategia). La deduplicación de URLs (`self.urls_fetched`) y de consultas (`self.queries_used`) YA EXISTÍA de una ola previa y ya cumplía "diez URLs repetidas no cuentan como diez fuentes nuevas" — no se dupacó, solo se verifica en el informe. | ninguna | tests/test_p1_res_02_stop_reason.py (nuevo); suite completa `tests/test_deep_research_*.py` y `tests/test_research_resume.py` re-ejecutada en verde (103+15 tests) | `self.stop_reason`/`stop_rationale_text` no se muestran todavía en ninguna UI ni se adjuntan al texto del informe final (deliberado: adjuntarlo al string que `research()` devuelve rompería varios tests existentes que comparan el resultado por igualdad exacta — `tests/test_deep_research_report_in_parts.py`, `tests/test_deep_research_synthesis_resilience.py`). Un caller ajeno (la ruta que expone `research()` por HTTP) debe leer `self.stop_reason` aparte; ver informe. |
| RES-06 | existente | `src/deep_research.py`: `EvidenceQualityProfile`/`DEFAULT_QUALITY_PROFILES` (general/technical/academic/clinical, con `min_sources_per_claim`/`preferred_domains`/`blocked_domains`/`require_date` distintos), `assess_evidence_quality` (el criterio de aceptación: un hallazgo sin fecha nunca gradúa "high" aunque haya diez fuentes; dominios bloqueados se excluyen del conteo, no se penalizan solamente), `DeepResearcher(quality_profile=...)` resuelve el perfil por nombre (fallback a "general" si no existe) y `self.evidence_quality` se calcula siempre al terminar `research()` | ninguna | tests/test_p1_res_06_evidence_quality.py (nuevo) | `self.evidence_quality` no se muestra en ninguna UI ni se adjunta al informe (mismo motivo de compatibilidad que RES-02). No hay export de bibliografía con metadatos (frontend, ajeno a este lote). |
| LANG-02 | existente | `services/search/lang_normalize.py` (nuevo, exportado desde `services/search/__init__.py`): `strip_diacritics` (1 carácter de entrada → 1 de salida, garantía que hace posible el criterio de aceptación), `find_all_normalized`/`locate_normalized`/`contains_normalized` (offsets del texto ORIGINAL, nunca del plegado), `detect_query_language` (reusa `src.research_citations.language_signal`, no duplica detector), `multilingual_query_variants`/`should_try_english_variant`. `src/reply_language.py::locate_for_edit` (edición: localiza texto tolerando tildes/mayúsculas y devuelve offsets del documento sin tocarlo) | ninguna | tests/test_p1_lang02.py (nuevo) | Nada en `services/search/providers.py` (que sí vive bajo `services/search/*`) llama todavía a `multilingual_query_variants`/`should_try_english_variant` desde el flujo real de búsqueda — se dejó sin tocar deliberadamente: ese fichero fija `language: "en"` en rutas ya delicadas (anti-spam de "news") y tocarlo sin una auditoría propia se salía del presupuesto de este lote. Glosario de proyecto y "preservar términos técnicos al traducir" son requisitos de frontend, ajenos a este lote. |

## Lote 45 — Context/Memory P1 (CTX-04/05/06, MEM-03/04, IDX-05/06, PERF-03)

Todos partían de `status: "propuesto: auditar antes de implementar"` en `backlog.json`, sin fila previa en este mapa. Auditoría previa (rule "reusar autoridades"): `context_engine/cache.py::on_event`/`note_event` (§1.7) ya existía pero **sin llamador de producción** — CTX-04 lo cablea de verdad, no lo duplica; `context_engine/ranking.py::validate` ya tenía un check de "stale" vía `meta.current_revision` y `explicit_refs` ya existía como "fuente explícita prioritaria" — CTX-05 añade solo la mitad que faltaba (exclusión); `memory_engine.py` ya tenía `type="decision"` en su vocabulario sin escritor/lector (MEM-04) y `memory_curator._invert` ya usaba el mismo umbral de repetición que MEM-03 reutiliza (`INVERT_MIN_HARMFUL`) en vez de inventar uno nuevo; `bg_monitor.is_paused_for_resource_pressure` ya estaba cableado en `bg_jobs.py` para PERF-04 — IDX-06 reusa la misma guarda, verbatim, para `code_index.py`.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| CTX-04 | existente | `src/context_source_versions.py` (nuevo): `record_seen`/`revoke`/`is_accessible`/`invalidate` (tabla `source_revisions`, ámbito owner+project+ref); `invalidate()` llama a `context_engine/cache.py::on_event("source_version_changed", …)`, evento ya existente en `EVENT_PREFIXES` pero sin emisor real hasta ahora. `context_engine/cache.py`: `compose_cache_key` (clave por model/tokenizer/template_version/policy_version/user/source_version/params, sha256) y `cached_or_compute` (hit/miss real contra `WorkingSet`, ya scoping por `scope`) | `POST /api/context-engine/cache/invalidate` (botón "Liberar la caché" en el panel Cache de `Context.tsx`) | tests/test_p1_ctx_04_cache_invalidation.py (8); revert-check: desactivar la llamada a `invalidate()` en `record_seen()` tumba `test_edit_invalidates_scoped_cache_only` | `record_seen`/`revoke` no tienen todavía un llamador real desde el punto donde un documento se reindexa o un permiso se revoca (esos puntos están en ficheros ajenos — ver informe final). La primitiva y su prueba de invalidación de cache están completas y cableadas; falta el emisor automático. |
| CTX-05 | existente | `src/context_selection.py` (nuevo): `set_control`/`unset_control`/`list_controls`/`policy_overrides` (tabla `selection_controls`, ámbito global→proyecto→sesión, nunca cruza sesión/proyecto). `context_engine/contracts.py::ContextPolicy` gana `excluded_refs`/`excluded_prefixes`; `OMISSION_REASONS` gana `"user_excluded"`. `context_engine/ranking.py::validate` rechaza (no recuperable) una ref excluida o bajo un prefijo excluido, salvo que esté en `explicit_refs` (una fuente fijada gana a una exclusión de alcance más amplio). `context_engine/wiring.py::build_request` (la fábrica real que usa `agent_loop`) fusiona `policy_overrides()` en cada `ContextRequest` — cableado de producción, no solo librería | `GET/POST/DELETE /api/context-engine/selection`; panel «Recuperación selectiva» en `Context.tsx` (excluir fuente/carpeta, fijar fuente, por proyecto) | tests/test_p1_ctx_05_selection_controls.py (9), tests/test_p1_ctx_routes_health_selection_cache.py (5); revert-check: quitar el bloque de exclusión en `ranking.validate()` tumba 3 tests (incl. `test_end_to_end_through_build_request`) | El "no borra el archivo fuente" del criterio de aceptación se cumple por construcción (la exclusión solo afecta a `ranking.validate`, nunca toca el fichero); no hay UI para excluir/fijar a nivel de sesión (solo proyecto), porque `Context.tsx` no tiene selector de sesión — sería un añadido de UI, no de backend. |
| CTX-06 | existente | `src/context_health.py` (nuevo): `duplicate_ratio`/`stale_evidence`/`health` (combina `context_ledger.build_ledger`/`evidence_index`/`verify_read_evidence`, ya existentes, con tres señales deterministas — ninguna llamada a otro LLM) y `reconstruct_task` (objetivo/restricciones/cambios/siguiente acción, reutilizando `context_compactor._content_as_text`/`extract_protected_strings`) | `POST /api/context-engine/health`, `POST /api/context-engine/reconstruct-task` | tests/test_p1_ctx_06_context_health.py (10), tests/test_p1_ctx_routes_health_selection_cache.py (5) | Sin UI en `Context.tsx` para el "aviso accionable" / botón "reconstruir tarea" del criterio frontend — la ruta HTTP está lista y probada, el botón queda pendiente por presupuesto de tiempo del lote (ver Limitaciones del informe). |
| IDX-05 | existente | `src/document_processor.py`: `pdf_page_evidence_ref` (locator `kind="page"`, hash sha256 del texto exacto de esa página, `EvidenceRef` de `src/contracts/tool.py` reusado sin cambios) y `_process_pdf(path, evidence_out=None)` — parámetro aditivo, la cadena devuelta es byte-idéntica sin él (verificado: `test_process_pdf_without_evidence_out_is_unchanged`), solo un caller que pase `evidence_out=[]` recibe las refs | ninguna | tests/test_p1_idx_05_evidence_refs.py (5) | Solo cubre PDF (páginas); el criterio de aceptación también menciona celda/símbolo/bbox/rango temporal — esos locators ya existen en `EVIDENCE_LOCATOR_KINDS` (`cells`, `time`, etc.) pero ningún extractor de `document_processor.py` los produce todavía; ningún caller real (`document_processor`'s own callers, fichero ajeno a esta lista) pasa `evidence_out` aún, así que las refs no llegan todavía a una respuesta del modelo. |
| IDX-06 | existente | `src/code_index.py::refresh(..., pause_on_pressure=True)`: para antes de escanear si `bg_monitor.is_paused_for_resource_pressure()` (misma guarda de PERF-04, sin duplicar), y revisa cada `PRESSURE_CHECK_EVERY=25` ficheros durante el escaneo (corta sin perder lo ya escrito); `_copy_indexed_by_hash` (deduplicación por contenido: un fichero con el mismo hash que otro ya indexado copia sus símbolos en vez de reparsear); `cleanup_orphaned_workspaces` (limpia filas de un workspace borrado, nunca toca uno que sigue existiendo); `refresh_async` (envuelve `refresh` en `asyncio.to_thread`, no bloquea el event loop — demostrado con un ticker concurrente) | ninguna | tests/test_p1_idx_06_background_indexing.py (8); revert-check por separado de presión (`test_refresh_pauses_under_pressure_and_does_not_scan`, `test_pressure_trip_mid_scan_stops_without_losing_earlier_writes`) y de dedup (`test_identical_content_at_two_paths_is_deduped`) | `routes/code_index_routes.py::POST /{project_id}/reindex` sigue llamando al `refresh()` síncrono, no a `refresh_async` — ese fichero es ajeno a este lote (no está en PROPIOS); ver "Cambios necesarios en ficheros ajenos" en el informe. Sin panel de almacenamiento en Studio (tamaño de originales/derivados/índices) — sería UI nueva en una pantalla no asignada a este lote (`code_index` no tiene screen propio en `Context*.tsx`/`Memory*.tsx`). |
| MEM-03 | existente | `src/memory_failures.py` (nuevo, sobre `memory_engine.py` — sin store paralelo): `register_failure` (dos puertas obligatorias: prueba de regresión adjunta Y (repetición ≥ `MIN_OCCURRENCES_TO_PROPOSE=3`, el mismo umbral que `memory_curator.INVERT_MIN_HARMFUL`, O `confirm=True`) antes de promocionar de `status="deprecated"` — invisible a `pack()`/`search()` — a `status="anti_pattern"` activo), `list_candidates`, `sweep_expired` (degrada sin borrar, misma postura que el resto de `memory_curator.py`) | `POST/GET /api/memory-engine/failures`, `POST /api/memory-engine/failures/sweep`; panel «Candidatos a fallo» en `Memory.tsx` (firma, resumen, referencia a prueba, contador de ocurrencias) | tests/test_p1_mem_03_failure_candidates.py (8), tests/test_p1_mem_routes_failures_decisions.py (4) | `existing_anchor` del backlog (`src/immune_system/`) es un directorio ajeno a PROPIOS (no casa con el glob `src/memory_*.py`) — se auditó (`service.py`/`persistence.py`) y no comparte vocabulario con `memory_engine.py`; se decidió reutilizar `memory_engine`/`memory_curator` en su lugar por ser la autoridad ya usada por MEM-01/MEM-02, en vez de escribir sobre un sistema paralelo. `sweep_expired` no tiene todavía un disparador periódico (cron/bg_jobs, fichero ajeno). |
| MEM-04 | existente | `src/memory_decisions.py` (nuevo, sobre `memory_engine.py`, reutiliza `type="decision"` ya declarado en `TYPES` desde MEM-01 y nunca escrito): `record_decision` (texto, alternativas descartadas, alcance, refs a artefactos), `invalidate_decision` (nunca borra — pone `status="deprecated"`, misma semántica que el resto del store, y registra motivo/sustituto en `provenance`), `list_decisions` (timeline, invalidadas opcionales) | `POST/GET /api/memory-engine/decisions`, `POST /api/memory-engine/decisions/{id}/invalidate`; timeline «Decisions» en `Memory.tsx` (alternativas descartadas, artefactos, invalidar) | tests/test_p1_mem_04_decisions.py (6), tests/test_p1_mem_routes_failures_decisions.py (4) | `existing_anchor` del backlog (`src/project_context/`) es un directorio ajeno a PROPIOS — se auditó (`service.py`/`models.py`) y gestiona indexación de proyecto, no decisiones técnicas con alternativas; MEM-04 se implementó como módulo nuevo sobre `memory_engine.py` en vez de tocar ese fichero ajeno. El "cambiar de modelo conserva el motivo" del criterio de aceptación se cumple porque la decisión vive en el store de memoria (independiente de sesión/modelo), pero ningún caller ajeno lee todavía `list_decisions` al construir el contexto de una sesión nueva — eso es responsabilidad de `context_engine/wiring.py` (si procede) o del propio flujo de cambio de modelo, ambos fuera de lo tocado aquí salvo por la exposición HTTP. |
| PERF-03 | existente | `context_engine/cache.py::compose_cache_key`/`cached_or_compute` (claves por model/tokenizer/template_version/policy_version/**user**/source_version/params — la fuga entre usuarios que pide el criterio de aceptación queda excluida por construcción: dos usuarios nunca comparten clave); invalidación real por fuente (`context_source_versions.invalidate`, CTX-04) y por evento (`on_event`, ya cableado) | `POST /api/context-engine/cache/invalidate` (mismo botón "Liberar la caché" que CTX-04 — misma primitiva, dos requisitos que comparten el mismo mecanismo por diseño, no por duplicación) | tests/test_p1_ctx_04_cache_invalidation.py (comparte cobertura con CTX-04: son la misma primitiva) | Cache de prefijos a nivel de backend LLM (KV-cache del propio modelo) está fuera de alcance de `context_engine/*` — ese nivel vive en el runtime de inferencia, fichero ajeno. `cached_or_compute` está lista para envolver cualquier cómputo caro de `compiler.py`, pero ningún caller de producción la usa todavía para el *response* completo (solo la primitiva y su test existen) — ver Limitaciones del informe. |

## Lote 46 — Planificación, agentes y herramientas P1

PLAN-02, PLAN-04, PLAN-05, PLAN-06, VER-03, TOOL-02, TOOL-05, TOOL-06, SEC-08. Todos partían de `status: "propuesto: auditar antes de implementar"`. La auditoría (rule "reusar autoridades") encontró infraestructura ya muy madura para varios IDs — `src/council/*` (13k líneas: contratos, ledger, orquestador, síntesis) para PLAN-04; `src/completion_engine/*` ya cableado en `src/agent_loop.py` para PLAN-06; `src/auto_review.py` (review_gate, ground_findings, severidad+evidencia) para VER-03; `src/tool_index.py` (RAG + keyword hints + fallback de dos niveles) para TOOL-02 — así que el trabajo de este lote fue en su mayoría **cerrar el hueco concreto que el criterio de aceptación señala**, no reconstruir el sistema. `src/branching_futures/` (ancla de PLAN-05) no está en PROPIOS de este lote — se auditó y su `commit()` ya es fail-closed (`raise BranchingError(...)`, ninguna rama llega nunca a State Mirror), así que el criterio de aceptación se cumple por construcción sin tocar ningún fichero.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| PLAN-02 | parcial | `src/agent_tools/subagent_tools.py`: `parse_delegation_args` acepta `criteria`/`success_criteria`/`acceptance` por tarea (aditivo — una tarea sin `criteria` produce el mismo dict de siempre, byte a byte); `verify_criteria` (nueva, determinista: sin llamada a modelo) comprueba las criteria contra la EVIDENCIA del run (`tool_calls`, `mutations`, `error`) — cero tool calls es "unmet" siempre (el criterio de aceptación: texto solo no basta), un criterio que nombra un fichero exige que ese fichero esté en `mutations`; `SubagentRun.report()` degrada `status` de `"done"` a `"partial"` cuando hay criteria y `verify_criteria` no las da por cumplidas, añadiendo `criteria_check` al reporte (condicional: sin criteria, el reporte es idéntico al de antes). El presupuesto por hijo (`max_rounds`/`timeout_s`) YA EXISTÍA (no se tocó); no se cableó `src/autonomy_budget.Budget`/`Ledger` per-child porque el run de un subagente hoy vive fuera del bucle que sabe leer un `Budget` (`src/agent_loop.py`, ajeno salvo PLAN-06) — ver informe. | ninguna | tests/test_p1_plan02_delegation_contract.py (9) | `src/dispatch.py` (Fable/API externa) NO se tocó: ya tiene una autoridad más fuerte para el mismo problema — checkpoint+diff+`src/prove.py` (`proved`/`partial`/`unproved`/`contradicted`) — así que añadir `criteria` ahí sería un segundo mecanismo para lo mismo (rule 4); se documenta, no se duplica. `routes/dispatch_routes.py` no ganó rutas nuevas: no se encontró un hueco de comportamiento que una ruta nueva cerrase sin duplicar `dispatch.py`. |
| PLAN-04 | existente | `src/council/synthesis.py`: `evidence_weighted_support(decision, messages)` (nueva) — por cada `supporter` de una `CouncilDecision`, lee el `metadata.evidence_refs` del mensaje MÁS RECIENTE de ese autor sobre esa decisión (campo abierto que `CouncilMessage` ya tenía, ningún cambio de esquema); un voto sin evidencia pesa `NO_EVIDENCE_WEIGHT=0.5`; `unanimous_without_evidence=True` es exactamente el caso del criterio de aceptación (tres modelos, misma afirmación, sin fuente). `_row()` ganó `decision_id`/`metadata` en su lista de fallback (aditivo, más información capturada, nada se pierde) | ninguna | tests/test_p1_plan04_council_evidence.py (4) | `evidence_weighted_support` no se llama todavía desde `src/council/orchestrator.py` ni desde `routes/council_routes.py` — no se encontró el punto donde el orquestador decide "consenso alcanzado" sin ya auditarlo a fondo (fichero de 1537 líneas) y forzar la llamada sin esa auditoría se salía del presupuesto; queda como primitiva lista y probada, igual que varias filas del Lote 45. |
| PLAN-05 | existente | Nada tocado — `src/branching_futures/service.py::commit()` (línea ~325) ya es fail-closed: siempre `raise contracts.BranchingError(...)`, "no se cambió ningún estado". Por construcción, ninguna rama (seleccionada o descartada) llega hoy a State Mirror ni a memoria confirmada — el criterio de aceptación se cumple sin código nuevo. | ninguna | ninguno nuevo (nada que probar: no hubo cambio) | `src/branching_futures/` no está en PROPIOS de este lote, así que no se auditó `fuse()`/`evaluate()`/`select()` a fondo ni se implementó un committer real. Cuando exista un "adaptor de ejecución común" que sí escriba estado real, `commit()` deberá enrutar el resultado SOLO de la rama `select()`-ada — nunca de una descartada — y eso deberá verificarse contra `src/state_mirror` en ese momento; hoy no hay nada que verificar porque no hay escritura real. |
| PLAN-06 | existente | `src/agent_loop.py` (única función tocada en este fichero, por mandato del lote: "SOLO PLAN-06"): `_split_requirements` (nueva, determinista — bullets/numeración como items separados, si no hay ≥2 la instrucción entera es un único requirement) y `_plan_coverage_gap` (nueva) llaman a `src.plan_state.coverage` (PLAN-01, ya existente, reusada sin cambios) contra el ÚLTIMO `update_plan` de este turno. Justo antes del `break` que cierra un turno sin más tool calls: si hay plan (`_latest_plan_update`) y la Completion Engine (ya cableada) no decidió ya seguir, y `plan_state.coverage` encuentra un requirement sin ningún step que lo cubra, el turno NO cierra — un mensaje `system` nombra solo los requirements no cubiertos (nunca inventa alcance nuevo) y se pide seguir, acotado a `_PLAN_COVERAGE_MAX_ROUNDS=1` (una sola vez, nunca un bucle). Un turno sin `update_plan` esta ronda es byte-idéntico a antes (la comprobación entera está guardada tras `if _latest_plan_update`). | ninguna | tests/test_p1_plan06_completion_gate.py (6); revert-check: quitar el bloque `_plan_coverage_gap`/`_split_requirements` tumba las 6 con `AttributeError` | El evento `plan_coverage_gap` se emite por SSE pero no hay tarjeta en Studio que lo muestre (frontend, fuera de este lote). No se probó end-to-end contra el generador completo de `run_agent_loop` (async generator de 9900 líneas, sin harness de integración ligero) — se probó `_plan_coverage_gap`/`_split_requirements` de forma aislada, que es donde vive toda la lógica nueva; el punto de cableado (justo antes del `break`) se revisó a ojo y por `py_compile`, no con una ejecución end-to-end. |
| VER-03 | existente | `src/auto_review.py`: `resolve_reviewer` gana `available_models` (opcional, aditivo) — con el ajuste `"same"`, si se pasan varios modelos disponibles elige uno DISTINTO al que escribió el cambio en vez de reusar el mismo (`_distinct_reviewer`, nueva); sin `available_models` (ningún caller existente lo pasa hoy) el comportamiento es exactamente el de antes. El resto del criterio ("revisor sin permisos de escritura, checklist tipada, veredicto con evidencia, no bloquea por preferencias de estilo") YA EXISTÍA: `review_turn` no llama a ninguna tool (pase sin herramientas por diseño — docstring del módulo), `_prompt` prohíbe explícitamente comentar estilo/naming/formato, `ground_findings` degrada a "warning" (nunca bloquea) cualquier hallazgo sin evidencia literal en el diff o la petición, y `review_gate` ya exige diff real + verificación real antes de sellar una revisión. | ninguna | tests/test_p1_plan_ver03_review.py (5) | `available_models` no tiene todavía un caller real que enumere los modelos servidos y se lo pase a `resolve_reviewer` — ese punto de llamada vive en `chat_routes.py`/`chat_handler.py` (ficheros ajenos, explícitamente prohibidos: "NO toques chat_routes"); se documenta el cambio necesario en el informe. "Checklist tipada" no se añadió como campo nuevo porque `findings[].evidence` (cita literal del diff) ya cumple la función de "reproducción" que pedía el criterio y añadir una taxonomía de categorías sin un consumidor real habría sido especular. |
| TOOL-02 | existente | `src/tool_index.py`: `_FILE_EDIT_RE`/`_FILE_EXT_RE` (nuevas, bilingües ES/EN) fuerzan `{"read_file","write_file"}` en `get_tools_for_query` cuando la consulta combina un verbo de edición/lectura y una extensión de fichero — estructural, igual que `_WEB_RE`/`_SCHEDULE_RE` ya existentes, así que funciona SIN el lane semántico (el criterio de aceptación literal: "una petición española de editar .txt mantiene acceso a lectura/escritura aunque falle el ranking semántico" — probado forzando `retrieve()` a devolver `[]`). El resto de TOOL-02 (ranking híbrido, límite de esquemas visibles, fallback de dos niveles) YA EXISTÍA (docstring del módulo, `src.two_tier_search`). El punto de extensión para "fijar/excluir herramientas por proyecto" también existe ya (`get_tools_for_query(always_include=...)`) — es el caller (`src/agent_loop.py`, fuera del mandato "SOLO PLAN-06") quien decidiría poblarlo desde un ajuste por proyecto. | ninguna | tests/test_p1_tool02_discovery.py (4); revert-check: quitar el bloque `_FILE_EDIT_RE`/`_FILE_EXT_RE` tumba las 2 pruebas ES/EN con `AssertionError` | No hay UI para fijar/excluir tools por proyecto ni para "ver por qué se eligieron" (ambos, requisito de frontend explícito en el backlog, fuera de mandato). |
| TOOL-05 | ausente→parcial | `src/mcp_manager.py` (nuevo dentro del fichero, sección "TOOL-05"): `make_sampling_callback`/`make_elicitation_callback`/`make_list_roots_callback`, cableados en los 3 puntos donde se construye `ClientSession` (stdio/SSE/HTTP) vía `_open_client_session` (con fallback a la llamada de 2 argumentos si el destino — incluidos los `ClientSession` falsos de tests existentes — no acepta los kwargs nuevos, para no romper compatibilidad). `_SamplingQuota` mide por `server_id` (nunca compartido entre servidores — el criterio de aceptación: "no puede reutilizar una autorización de otro destino"); en cuota, sampling se rehúsa con una razón distinta de "not supported" (no hay ruta a un modelo real todavía — se documenta, no se finge). Elicitación es real: pasa por `src.question_store` (la única autoridad de pregunta pendiente del repo — AskUserTool ya la usa, ningún store paralelo) con tope de salientes por servidor, tamaño de mensaje acotado y timeout duro que cancela la pregunta. `list_roots_callback` devuelve el workspace activo como único root, advisorio (no cambia el sandbox de `src/tool_execution.py`). | ninguna | tests/test_p1_tool05_mcp_limits.py (8); revert-check: quitar la sección TOOL-05 tumba las 8 con `AttributeError` | Sampling no llega a invocar un modelo real (no hay contexto owner/turno alcanzable desde un callback de conexión MCP, que vive a nivel de proceso, no de turno) — queda metrado y rehusado con razón auditable; conectar un modelo real es el siguiente paso natural y no pierde nada de lo aquí construido (solo cambiaría el último `return`). La elicitación usa `session_id=f"mcp:{server_id}"` (sintético: una conexión MCP no está atada a un chat) — no hay UI que muestre "qué servicio pregunta y qué datos recibirá" (frontend, fuera de mandato) aunque la pregunta en sí ya llega a `question_store` y sería visible por cualquier UI que ya lea preguntas pendientes. |
| TOOL-06 | ausente→parcial | `src/skill_governance.py` (nuevo): `gate_mutating_use` (el criterio de aceptación literal: una skill `obsolete`/`deprecated`/`superseded` se rehúsa ANTES de una acción mutante — `apply`/`run`/`execute`/`invoke`/`promote`/`publish` — nunca antes de una lectura, y devuelve `fallback_version` con la versión previa no obsoleta si `history` la tiene); `privilege_escalation_flags`/`as_context_message` (escaneo determinista de frases de escalada de privilegio — "ignore instructions", "disable approval"... — y un mensaje con `role: "context"`, nunca `"system"`/`"instruction"`, con la procedencia como campo estructurado aparte del texto); `validate_promotion` (promoción desde `teach_mode`/`demonstration`/`teacher-escalation` a un estado tipo "published" exige `reviewed=True` explícito — nunca inferido); `record_version`/`evaluation_manifest` (versión siguiente derivada del hash del contenido, y qué le falta a una skill para ser evaluable: `when_to_use`, `verification`, `version`). Todo opera sobre los mismos campos que `services/memory/skill_format.Skill` ya tiene (`version`, `status`, `verification`, `requires_toolsets`) — sin store paralelo. | ninguna | tests/test_p1_tool06_skill_governance.py (13) | `services/memory/skills.py` y `routes/skills_routes.py`/`routes/teach_mode_routes.py` no están en PROPIOS de este lote, así que `gate_mutating_use`/`validate_promotion` no tienen todavía un caller real — son la autoridad lista, no el cableado; el vocabulario `obsolete`/`deprecated`/`superseded` tampoco existe aún en `Skill.status` (hoy solo `draft`/`published`) — añadirlo es un cambio de fichero ajeno, descrito en el informe. |
| SEC-08 | ausente→parcial | `src/security_policy.py` (nuevo, como pedía el lote). `PROFILES` (`read`/`local_edit`/`supervised_agent`/`bounded_automation`) construidos sobre `src.tool_capabilities.ToolEffect` — `PROFILES["read"]` es literalmente `autonomy_budget.READ_ONLY_ALLOWED_EFFECTS` (reusado, no redefinido). `evaluate()` decide permiso-antes-que-consentimiento (mismo orden que `completion_engine.scope`), y toda decisión lleva motivo y queda en `audit_log`/`export_audit` (firma HMAC-SHA256 opcional vía `FAUSTUS_AUDIT_SIGNING_KEY`, "cuando el despliegue lo requiera"). `ConsentStore`: la clave es `(plugin_id, tool_name)`, NUNCA `plugin_id` solo — `check()` compara los efectos declarados AHORA con los concedidos, y un efecto añadido invalida solo esa clave; el criterio de aceptación exacto (una actualización con más permisos invalida el consentimiento anterior sin bloquear tools no afectadas) se cumple por construcción: el registro de una tool hermana nunca se lee ni se escribe al comprobar otra. Efectos admin/destructivos exigen aprobación en todo perfil que los permita (`ALWAYS_APPROVAL_EFFECTS`), y una lectura ya autorizada por el perfil nunca la pide (fatiga de confirmación nombrada explícitamente en el backlog). | ninguna | tests/test_p1_sec08.py (17) | No hay caller real todavía que llame a `evaluate()`/`ConsentStore` desde el punto donde Faustus instala/actualiza un plugin o servidor MCP (`src/mcp_manager.py` conecta servidores pero no gestiona "plugins" con metadatos de permisos declarados — ese concepto no existe hoy en `src/database.py::McpServer`, fichero ajeno); la primitiva está completa y probada, el cableado de producción es el siguiente paso. Persistencia del log de auditoría es solo de proceso (`deque` acotado) — un despliegue que necesite sobrevivir un reinicio debe persistir el resultado de `export_audit()` por su cuenta; no se creó un store nuevo para esto (rule 4) a falta de una autoridad de persistencia genérica ya existente que reutilizar.


## Lote 47 — Artefactos y edición avanzada P1

ART-02, ART-03, ART-04, ART-05, ART-07, ART-08, EDIT-05, EDIT-06. Todos
partían de `status: "propuesto: auditar antes de implementar"`. A diferencia
de los lotes 45/46/48, aquí la mayoría de los requisitos SÍ llegan hasta la
ruta HTTP real (`routes/document/document_routes.py`, `routes/artifact_routes.py`)
— el hueco que queda es más estrecho: una librería sin exponer (ART-04) y una
tool sin registrar (EDIT-06).

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| ART-02 | existente | `src/document_actions.py::analyze_docx_preservation` — `POST /api/documents/analyze-docx` (`routes/document/document_routes.py`, línea ~1213) sube un `.docx`, lo analiza y borra el temporal en un `finally`. | ninguna | tests/test_p1_art_02_docx_preservation.py (6) | Ruta real, probada de extremo a extremo contra `TestClient` (rule 7). |
| ART-03 | existente | `src/document_actions.py::detect_page_overflow` — `GET /api/document/{doc_id}/visual-check` (misma fichero, línea ~1320). | ninguna | tests/test_p1_art_03_visual_check.py (5) | Ruta real; comparte fichero con `render_page_png` (`/api/document/{doc_id}/page/{page_no}.png`), ajeno a este lote. |
| ART-04 | parcial | `src/spreadsheet.py` (nuevo): `import_csv_rows`/`export_csv_rows` (dialecto Excel), `safe_csv_value` (bloquea inyección de fórmula — `=`/`+`/`-`/`@` al inicio se escapan), `read_workbook_preview`/`write_range` (openpyxl, celdas tipadas: número/fecha/fórmula/texto vía `classify_cell_text`). | ninguna | tests/test_p1_art_04_spreadsheet.py (14) | Ninguna ruta HTTP llama todavía a estas funciones — no hay `routes/*spreadsheet*` ni referencia desde `routes/document/document_routes.py`. La librería está lista y probada (14 tests); falta el endpoint (`POST /api/documents/spreadsheet/...` o similar) y, si se quiere exponer al modelo, la tool correspondiente — ambos, siguiente paso. |
| ART-05 | existente | `src/artifact_store.py::analysis_provenance` (hash de script + inputs) — `GET /api/artifacts/{id}/provenance` (`routes/artifact_routes.py`, línea 93). | ninguna | tests/test_p1_art_05_analysis_provenance.py (4) | Reusa `ExecutionResult`/`Provenance` de `src.contracts` (rule 4) — sin store paralelo. |
| ART-07 | existente | `src/document_actions.py::split_pdf`/`merge_pdfs` (presupuesto de páginas/ficheros, rehúsa ANTES de escribir si se excede) — `POST /api/documents/pdf/split` y `POST /api/documents/pdf/merge` (`routes/document/document_routes.py`, líneas ~1213-1260). | ninguna | tests/test_p1_art_07_pdf_split_merge.py (9) | Ambas rutas reales, con limpieza de temporales en `finally`. |
| ART-08 | existente | `src/artifact_catalog.py::recent` (filtros `project_id`/`session_id`/`kind`/`q`/`since`/`until`) — `GET /api/artifacts` y `DELETE /api/artifacts/{id}` (`routes/artifact_routes.py`). | Panel Library (ajeno a este lote, ya consume la misma ruta). | tests/test_p1_art_08_library.py (3) | Búsqueda por facetas real contra `TestClient`. |
| EDIT-05 | existente | `src/agent_tools/filesystem_tools.py`: `record_edit_history`/`restore_edit_history` (verifica hash antes de restaurar — una snapshot manipulada se rehúsa), `cleanup_temp_files`/`restore_trashed_files` (solo toca ficheros registrados, nunca un fichero de usuario no registrado). Cableado real: `EditFileTool`/`WriteFileTool` ya llaman a `record_edit_history` en cada escritura con éxito (best-effort, nunca afecta el resultado de la tool). | ninguna | tests/test_p1_edit_05_history_cleanup.py (7) | Cableado de producción real (no solo librería) — cada escritura vía las tools del agente ya deja historial. |
| EDIT-06 | parcial | `src/refactor_tools.py` (nuevo): `plan_rename`/`apply_rename` (renombrado de identificador con `_identifier_pattern` — solo coincidencias de identificador completo, nunca una subcadena dentro de otro nombre), `RenameResult` (por-fichero: líneas tocadas, antes/después). | ninguna | tests/test_p1_edit_06_refactor.py (5) | Ni tool ni ruta HTTP la exponen — no aparece en `src/agent_tools/__init__.py`, `src/tool_schemas.py` ni ninguna ruta bajo `routes/`. La primitiva está lista y probada; registrarla como tool del agente (o una ruta manual) es el siguiente paso, igual que EXEC-05/06/TOOL-05/06 del Lote 46. |

## Lote 49a — UX, ajustes y actividad P1

UX-06, UX-07, UX-09, UX-10, SET-02, SET-04, SET-06, ACT-02, ACT-05. Sin
`status` previo en `backlog.json` más allá de "propuesto: auditar antes de
implementar". De los nueve, tres llegan hasta ruta+Studio con test propio de
este lote (UX-07, UX-10, ACT-02); SET-04/SET-06 resultan **existentes** por
una autoridad que ya vivía en el repo (`/api/doctor`, `routes/changesets_routes.py`
+ `routes/diagnostics_routes.py`, con cobertura en `tests/test_ops_routes.py`)
que `Settings.tsx` ya consume — ninguna fila nueva que inventar, solo
verificar que sigue conectada. Los cuatro restantes (UX-06, UX-09, SET-02,
ACT-05) no tienen ni módulo, ni ruta, ni test, ni marca en el código: el lote
al que se asignaron no dejó nada verificable.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| UX-06 | ausente | ninguna | ninguna | ninguno | "Compositor rico pero rápido" — nada en `Composer.tsx` ni en ningún adaptador lleva su marca; ni un test ni un check lo nombran. Requiere auditoría propia antes de implementar (formato rico, atajos, rendimiento con historial largo). |
| UX-07 | existente | ninguna (frontend puro) | `studio/src/screens/studio/ContextPanel.tsx`/`Composer.tsx` — parseo de menciones `@file`; quitar una mención retira su override de pin/exclude asociado. | tests/test_p1_ux_07_js.py (corre `studio/checks/p1-ux07-context-panel.check.mjs`) | Check en verde contra el fichero real desplegado. |
| UX-09 | ausente | ninguna | ninguna | ninguno | "Acciones personales sin fricción" — sin rastro en el código ni en los tests; requiere definir primero qué "acción personal" cubre (no está en ningún módulo existente). |
| UX-10 | existente | `routes/prompts_routes.py` (CRUD completo + `/render`, `/export/batch`, `/import`) — importar una plantilla con `project_id` obsoleto pide resolución en vez de escribir en silencio en el proyecto que hoy tenga ese id. | Biblioteca de prompts (Studio, ajena a este lote). | tests/test_p1_ux_10_prompts.py, tests/test_p1_ux_10_js.py (`p1-ux10-prompts.check.mjs`) | Ruta real + check de Studio, ambos en verde. |
| SET-02 | ausente | ninguna | ninguna | ninguno | "Selector de modelos informativo" — no hay endpoint ni componente con esta marca; `Settings.tsx` tiene selección de modelo pero no la superficie informativa (coste/latencia/capacidades por modelo) que pide el requisito. |
| SET-04 | existente | `GET /api/doctor` (`routes/changesets_routes.py`) — "qué puede hacer esta máquina, preguntado en vez de asumido": búsqueda, modelos, MCP, disco, cola y modo seguro en una sola tarjeta. | `studio/src/screens/Settings.tsx` (comentario propio: "SET-04: search, models, MCP, disk, queue and safe-mode in one card"). | tests/test_ops_routes.py | Autoridad y UI reales, aunque no nacidas con un test *con nombre* de este lote — se reusa la cobertura de `/api/doctor` ya existente (rule 4: no duplicar). |
| SET-06 | existente | `POST /api/doctor/repair` (`routes/diagnostics_routes.py`) + la misma lectura de `/api/doctor` que SET-04. | `Settings.tsx` (comentario propio: "SET-06: por qué podría no funcionar aún la generación de imágenes... 'a disabled feature explains whether it's missing a model'"). | tests/test_ops_routes.py | Ayuda situada junto al control, no un manual aparte — cumplido por construcción (mismo `/api/doctor` que SET-04, un solo mecanismo para dos requisitos, rule 4). |
| ACT-02 | existente | `routes/notifications_routes.py` — dedupe, preferencias por tipo/canal, horas silenciosas, y el caso de aceptación literal: un reconnect sobre el mismo pendiente no re-notifica veinte veces. | `shell/notifications.ts::emitForNewRuns` — emite un run como mucho una vez por diff de sondeo. | tests/test_p1_act_02_notifications.py, tests/test_p1_act_02_js.py (`p1-act02-notifications.check.mjs`) | Mitad servidor + mitad cliente, ambas en verde. |
| ACT-05 | ausente | ninguna | ninguna | ninguno | "Vista de colas y prioridades" — sin módulo ni ruta ni test; sería una superficie nueva sobre `bg_jobs.py`/`task_scheduler.py` (ajenos), sin auditar todavía. |

## Lote 49b — Workbench y accesibilidad P1

BENCH-01, BENCH-03, BENCH-04, BENCH-05, BENCH-06, A11Y-02, A11Y-03, A11Y-04.
El lote con más cobertura real de los tres reconstruidos: siete de los ocho
IDs quedan con librería pura + wiring real en `SidePanel.tsx`/`Transcript.tsx`,
cada uno probado dos veces (la función pura, vía `studio/checks/*.check.mjs`,
y el cableado en el fichero real desplegado, mismo patrón que
`tests/qa/test_qa_32_preview_malicioso.py`). El Lote 50 completó además el
cableado de BENCH-03 hasta la tarjeta de herramienta (ver "Cableados" más
abajo) y añadió `Preview.tsx` (BENCH-06) al parametrize de QA-32.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| BENCH-01 | ausente | ninguna | ninguna | ninguno | "Espacio de trabajo persistente" — sin módulo, ruta ni test con esta marca; el resto de BENCH-* (03/04/05/06) ya asumen un workbench existente (`SidePanel.tsx`) pero la persistencia entre sesiones que pide BENCH-01 en sí no está construida. |
| BENCH-03 | existente | `routes/evidence_routes.py::resolve_evidence` — `POST /api/evidence/resolve`, reusa `context_ledger.verify_read_evidence` como única autoridad de hashing (rule 4). | `studio/src/screens/Evidence.tsx` (inspector) + `studio/src/adapters/evidence.ts`. Lote 50: enlazado desde la tarjeta de tool — `Transcript.tsx`'s `ToolRail` muestra un botón "Ver evidencia" por `step.evidenceRefs[i]` que abre `Evidence.tsx` en un `Dialog` desde `Studio.tsx`. | tests/test_p1_bench03_evidence.py, tests/test_p1_bench03_evidence_js.py; Lote 50: tests/test_lote50_wiring.py::test_evidence_refs_wired_from_wire_to_tool_card_button | Antes del Lote 50, `Evidence.tsx` no tenía NINGÚN caller en todo el árbol de Studio (componente huérfano) — ver "Cableados" abajo. |
| BENCH-04 | existente | `routes/workspace_routes.py::save_workspace_file` — un 409 por conflicto de `base_revision` (EDIT-01/Lote 19). | `studio/src/adapters/fileConflict.ts` (comparación a tres bandas) wireado en `SidePanel.tsx`'s FileTab — un 409 abre la vista de conflicto en vez de un error que descarta el borrador. | tests/test_p1_bench04_conflict_js.py, tests/test_p1_bench04_sidepanel_wiring.py | Ambos, en verde. |
| BENCH-05 | existente | ninguna nueva (lee el ChangeSet ya persistido). | `studio/src/adapters/workbenchChanges.ts` (agregador puro) wireado en `SidePanel.tsx`'s PlanAndChanges — diff por fichero del ChangeSet actual. | tests/test_p1_bench05_changes_js.py | En verde. |
| BENCH-06 | existente | ninguna (canvas cliente-only). | `studio/src/components/Preview.tsx` + `studio/src/components/previewSandbox.ts` — `<iframe sandbox={sandboxAttr(...)}>`, nunca `allow-same-origin`, CSP propia (`previewCsp`). | tests/test_p1_bench06_preview.py, tests/test_p1_bench06_preview_js.py; Lote 50: `tests/qa/test_qa_32_preview_malicioso.py` ahora incluye este fichero en su parametrize. | Antes del Lote 50, QA-32 (la prueba general de sandboxing de HTML) no cubría esta cuarta superficie de preview — ver "Cableados" abajo. |
| A11Y-02 | existente | ninguna (frontend puro). | `studio/src/adapters/streamAnnounce.ts::nextStreamAnnouncement` wireado en `Transcript.tsx` — anuncio agrupado (`aria-live="polite"`), nunca token a token, y solo mientras `turn.streaming`. | tests/test_p1_a11y02_stream_js.py, tests/test_p1_a11y02_transcript_wiring.py | En verde. |
| A11Y-03 | existente | ninguna. | `studio/src/styles/base.css` — soporte `prefers-contrast: more`, junto a la densidad/reduced-motion ya existentes. | tests/test_p1_a11y03_contrast.py | En verde. |
| A11Y-04 | existente | ninguna. | `studio/src/adapters/remoteAccess.ts` (clasificador puro) wireado en `SidePanel.tsx` — aviso cuando el acceso remoto no va por HTTPS/túnel + auth. | tests/test_p1_a11y04_remote_js.py | En verde. |

## Lote 51 — Media y voz P1/P2

MEDIA-02, MEDIA-03, MEDIA-04, MEDIA-05, MEDIA-06, VOICE-01, VOICE-02.
Auditoría: VOICE-01 y VOICE-02 resultaron ya maduras (la "voz Jarvis" del
Studio ya traía interrupción por voz y push-to-talk reales, sin marca propia
en el código pero con cobertura equivalente) — no requirieron cambio. El
resto tenía la autoridad de recetas/cola (`src/media_workflows.py`,
`src/media_runs.py`) ya construida pero sin las puertas/capacidades
concretas que pide cada ID; se extendieron esas dos autoridades en vez de
crear una segunda cola o un segundo registro de plantillas (rule 4).

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| MEDIA-02 | existente | `src/media_workflows.py::review_status`/`load_review_registry` — un cambio de fingerprint solo pasa si los `class_type` del grafo ya estaban aprobados para ese id; `plan()`/`start()` en `src/media_runs.py` se niegan con `needs_review` antes de tocar el motor. Registro semilla: `config/media_workflows/reviews/approved_recipes.json`. | ninguna (puerta de servidor). | tests/test_p1_media_recipe_review.py | En verde; revert-and-fail confirmado (quitar la puerta deja pasar una plantilla con nodos nuevos). |
| MEDIA-03 | existente (Lote 55 cableó `app.py`) | `src/media_edit_projects.py` (proyectos no destructivos: capas, historial, export componiendo con Pillow, protección de "no sobrescribir el original") + `routes/media_edit_routes.py::setup_media_edit_routes` (`/api/media/edit/projects...`), enganchado en `app.py` por el Lote 55 (`app.include_router(setup_media_edit_routes())`, sección "vía libre" del lote — `app.py` dejó de ser ajeno). | ninguna todavía. | tests/test_p1_media_edit_projects.py, tests/test_p1_media_edit_routes.py, tests/test_l55_app_wiring.py (nuevo, prueba las rutas reales sobre `app` vía `TestClient` con el bypass de loopback interno) | Sigue sin pantalla propia en Studio (frontend del requisito, no auditado por el Lote 55). |
| MEDIA-04 | existente | `src/media_runs.py::classify_failure`/`retry` — reintento solo si `poll()` clasifica el fallo como transitorio (motor caído, sin client-id, etc.); un fallo permanente, uno en curso o uno ya terminal se niega explícitamente. La cola/outbox en sí (submit/poll/reconcile) ya existía antes de este lote. | ninguna nueva. | tests/test_p1_media_retry_classification.py | Sin columna `retry_of`/`attempt` en `core/database.py` (ajeno) no hay enlace persistente entre el run original y el reintento — `retry()` funciona sin ella pero no queda trazado en la fila de la base de datos. |
| MEDIA-05 | parcial | `src/media_subtitles.py::to_srt`/`to_vtt`/`export` (puro) + `routes/local_video_routes.py::export_subtitles` (`POST /api/media/local-video/subtitles/export`). | ninguna todavía. | tests/test_p1_media_subtitles.py, tests/test_p1_media_subtitle_routes.py | Solo exportación de subtítulos ya transcritos; no hay UI para descargarlos desde Studio. |
| MEDIA-06 | existente | `src/media_consent.py` (registro/revocación/consulta de consentimiento) + `requires_consent`/`consent_subject_input` en `MediaWorkflow` — `plan()`/`start()` se niegan con `consent_required` si la plantilla lo exige y no hay consentimiento registrado para el sujeto. `routes/gallery/gallery_helpers.py::strip_location_exif` + `gallery_routes.py::gallery_download_zip` (`redact_location`, opt-in, por defecto apagado). | ninguna. | tests/test_p1_media_consent.py, tests/test_p1_media_gallery_export.py | En verde; revert-and-fail confirmado en ambas puertas. |
| VOICE-01 | existente | `routes/voice_control_routes.py` / `src/voice/*` (ya existente, sin marca propia). | `studio/src/voice/*` — máquina de estados con fase `listening`/`speaking` excluyentes; micrófono nunca abierto mientras suena TTS. | tests/test_voice_routes.py, tests/test_studio_voice_js.py | Auditado, no modificado — cumplía ya el criterio de aceptación (rule 4: no duplicar). |
| VOICE-02 | existente | igual que VOICE-01. | `studio/src/voice/TurnDetector`, push-to-talk (Alt+V) — interrupción real de una respuesta en curso al hablar de nuevo. | tests/test_studio_voice_js.py | Auditado, no modificado. |

## Lote 53 — Conectores, automatizaciones, escritura y evaluación P1/P2

CONN-01, CONN-04, AUTO-01, AUTO-03, WRITE-01, WRITE-03, WRITE-05, ART-06,
EVAL-02, EVAL-06. Los diez partían de "ausente (ningún lote lo ha tocado)"
en el resumen de este fichero. Auditoría contra código real: AUTO-01 estaba
YA implementado por un lote anterior con otro nombre (`QA-43`, ajeno a la
numeración P1/AUTO de este fichero — de ahí que no apareciera aquí); los
otros nueve eran greenfield de verdad. Todo lo de abajo es backend real,
probado con `TestClient` cuando cruza HTTP (rule 7) y, si no, contra
código/DB real sin mocks salvo el límite de red exacto que cada test dice.
Ningún fichero ajeno fue tocado — routes/task_routes.py, app.py y
src/integrations.py quedan documentados abajo con el cambio exacto que
haría falta.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| AUTO-01 | existente | `src/task_scheduler.py` (`_dst_resolve`, `set_task_policy`/`get_task_policy`, `_next_moment_after`, `_recover_expired_leases`, `occurrence_key`) — ya construido por un lote anterior (comentarios propios "AUTO-01/QA-43"), sin fila en este fichero hasta ahora. | ninguna. | tests/qa/test_qa_43_cambio_horario.py, tests/test_auto_02_task_dst_misfire.py, tests/test_auto_02_task_budget_permissions.py, tests/test_l36_task_and_workflow_run_policy.py (29 tests, verificados en verde por este lote) | Falta SOLO el cableado de `set_task_policy`/`get_task_policy` a una ruta que un cliente pueda llamar al crear/editar una tarea — `routes/task_routes.py` es ajeno a este lote; ver "Cambios en ficheros ajenos". |
| AUTO-03 | existente (extendido) | `src/workflows/store.py::retry_node` (nuevo) + `POST /api/workflows/runs/{run_id}/nodes/{node_id}/retry` (`routes/workflows_routes.py`) — re-ejecuta un nodo `completed`/`failed`/`skipped` NO efectivo (`EFFECTFUL_TYPES` de `src/contracts/workflow.py` lo refusa con 409); reabre el `WorkflowRun` si estaba terminal. | ninguna — `studio/src/screens/Automations*.tsx` no fue tocado; ver limitaciones. | tests/test_p1_auto_03_node_retry.py (4 tests) + toda la suite de workflows existente (127 tests) en verde | El resto de AUTO-03 (DAG visible, simulación, secretos por referencia) YA existía (`WorkflowStore`/`WorkflowEngine`/`routes/workflows_routes.py::list_runs` con estado por nodo); el hueco real era exactamente el de la línea de aceptación (reejecutar sin repetir un envío), ahora cerrado. Falta el botón en Studio. |
| CONN-01 | existente (nuevo; Lote 55 cableó `app.py`) | `src/connector_registry.py` (nuevo) + `routes/integrations_routes.py` (nuevo, prefijo `/api/connectors`) — capa de ámbitos/owner/revocación sobre `src/integrations.py` (autoridad de credenciales reusada, no duplicada); revocar borra la credencial real (`delete_integration`), no solo marca un flag. Enganchado en `app.py` por el Lote 55 (`app.include_router(setup_integrations_routes())`). | ninguna. | tests/test_p1_conn_01_connector_registry.py (6 tests) + suite existente de integrations (53 tests) en verde + tests/test_l55_app_wiring.py (nuevo) | `src/integrations.py` no tiene columna `owner` — el aislamiento por propietario se aplica en el registro nuevo, no en el almacén de credenciales; ver "Cambios en ficheros ajenos". |
| CONN-04 | existente (nuevo) | `src/caldav_writeback.py::push_event` (conflicto explícito por ETag, ambas versiones, nunca sobrescribe) + `_persist_writeback_result` (marca `caldav_sync_pending="conflict"`) + `src/caldav_sync.py::_event_payload` (añade `remote_etag`) + `routes/calendar_routes.py::GET /api/calendar/conflicts` y `POST /api/calendar/conflicts/{uid}/keep-local` (nuevas). | ninguna. | tests/test_p1_conn_04_sync_conflicts.py (8 tests) + suite existente de CalDAV/calendar (94 tests) en verde | Solo CalDAV (no CardDAV/contactos — `routes/contacts/*` seguía en la lista PROPIA pero el mecanismo de conflicto no se replicó ahí por tiempo); ver limitaciones. |
| WRITE-01 | existente (nuevo) | `src/writing_edit.py` (nuevo) — `apply_edit`/`undo_edit`; `correct` refusa (`CorrectionTooWide`) si cambia más de `MAX_CORRECTION_CHANGE_RATIO` (34%) de las palabras del tramo; `protected` refusa (`ProtectedFragmentLost`) si un fragmento pinneado no sobrevive. | ninguna. | tests/test_p1_write_01_localized_edit.py (9 tests) | Enforcement puro, sin caller de producción todavía (el prompt real de "proponer una corrección" es ajeno/no existe aún) — ver limitaciones. |
| WRITE-03 | existente (nuevo) | `src/writing_style.py` (nuevo) — envuelve el flujo YA existente "Aprender un estilo" (`routes/preset_routes.py` vía `/api/presets/style/derive`, ajeno, reusado no duplicado) con ámbito, versión e intensidad; `compile_context` devuelve `None` si `enabled=False` o el ámbito no coincide. | `studio/src/screens/studio/StyleLab.tsx` (existente, sin tocar). | tests/test_p1_write_03_style_scope.py (6 tests) | Sin caller de producción que llame `compile_context` al construir el system prompt — ese cableado es ajeno (fuera de la lista PROPIA de este lote). |
| WRITE-05 | existente (nuevo) | `src/writing_project_adapter.py` (nuevo) — `diff_manifests`/`plan_sync`/`apply_sync_result`, hash-based, sin git en ninguna parte; una divergencia sin sincronización previa se clasifica `conflict`, nunca se resuelve a favor de un lado. | ninguna. | tests/test_p1_write_05_project_adapter.py (7 tests) | Sin adaptador real de lectura de un proyecto Writer's Hoard concreto (formato de manifiesto no especificado en la spec); este módulo cubre la lógica de sincronización/conflicto, no el parser de un formato externo. |
| ART-06 | existente (nuevo) | `src/presentation.py` (nuevo) — `render_pptx` (gráfico nativo `python-pptx`, ejes/unidad/datos reales, no imagen), `render_html_slides` (siempre disponible, SVG+tabla reales), `flatten_chart_to_image` (matplotlib, opt-in explícito), `validate_pptx` (reabre el fichero y cuenta slides/gráficos). | ninguna. | tests/test_p1_art06_presentation.py (6 tests, python-pptx y matplotlib confirmados instalados) | Sin editor visual de layout/densidad en Studio (frontend del requisito). |
| EVAL-02 | existente (nuevo) | `tests/eval/ablation.py` (nuevo) — `run_dimension` (reusa `tests/eval/harness.py::EvalApp`, tres dimensiones con override de `settings.json` antes de arrancar el subproceso), `judge_improvement` (éxito≥baseline Y coste≤ratio, nunca solo éxito). | ninguna. | tests/test_p1_eval_02_ablation.py (6 tests; 1 marcado `slow`, corre una tarea real contra un servidor real en ~18s) | Solo 2 dimensiones reales cableadas (`context`, `no_subagents`) más el baseline `minimal`; "sin verificador"/"sin memoria" necesitarían flags de `src/settings.py` que hoy no existen como toggle de un único ajuste (ver limitaciones). |
| EVAL-06 | existente (nuevo) | `src/capability_promotion.py` (nuevo) — matriz `(modelo, backend, plantilla)` exacta (nunca prefijo), `promote` exige flag + evidencia vigente (última calibración) + un paso por vez; una calibración fallida degrada el estado al instante. | ninguna. | tests/test_p1_eval_06_capability_promotion.py (8 tests) | Sin persistencia en disco (la clase es una autoridad de reglas en memoria; un caller de producción decide dónde guardar `to_dict()` de cada fila) ni ruta HTTP — ninguno de los dos estaba pedido por PROPIOS de forma explícita más allá de `src/capability_promotion.py`. |

## Lote 52 — Hardware, observabilidad y operación P1/P2/LAB

HW-03, HW-04, HW-05, HW-06, HW-07, OBS-04, OBS-05, PERF-05, OPS-04, OPS-06,
OPS-07. Todo lo de abajo es backend real; probado con `TestClient` contra el
router real cuando cruza HTTP (PERF-05), o contra código/DB real sin mocks
salvo el límite de red/GPU exacto que cada test dice (nunca dos modelos de
27B a la vez, nada de cargas de peso real). `agent_loop.py` y
`chat_routes.py` no fueron tocados (regla explícita del lote); las rutas de
`chat_routes.py` que sí llaman `src/api_version.py` (OPS-06) quedaron
intactas, solo leídas.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| HW-03 | existente (extendido) | `src/vram_admission.py` — `pin_model`/`unpin_model`/`is_pinned`/`residency_status` (residencia por pin + gracia de `RESIDENCY_GRACE_SECONDS`), `assess()` ahora rellena `suggestion` con residentes libres antes de tocar uno pinneado o en gracia (`suggestion_protected_used`). `routes/local_models_routes.py::GET /api/local-models/residency`, `PUT /api/local-models/{name}/pin` (nuevas). | ninguna. | tests/test_p1_hw_hw03_residency.py (7 tests) + tests/test_vram_admission.py, tests/test_chat_vram_gate.py, tests/test_hw_vram_reservation.py, tests/test_studio_vram_live_js.py, tests/test_research_model_load.py (suite existente, en verde) | Política de "cola" (varios `pull`/`load` esperando el mismo hueco) no implementada aquí — solo protección de residencia; ver HW-04 para la cola de descargas. |
| HW-04 | existente (extendido) | `routes/local_models_routes.py::PullJob.run` — cola real por endpoint (`MAX_CONCURRENT_PULLS_PER_ENDPOINT=2`, cancelable en espera) + fix de bug real: el chequeo de espacio en disco comparaba solo la capa en curso contra el disco libre (`total > disk_free`), no la suma acumulada (`agg_total > disk_free`) — un modelo multi-capa podía pasar capa a capa y agotar disco igualmente. `PullManager.active_pulling_count`. | ninguna. | tests/test_p1_hw_hw04_downloads.py (4 tests) + tests/test_local_models_routes.py, tests/test_l43_hw02_and_mod04_routes.py, tests/test_model_calibration.py (suite existente, en verde) | Progreso ya era real (`_layers`); esta fila solo cierra cola + el bug de espacio acumulado. |
| HW-05 | existente (nuevo) | `src/hardware_profiles.py` (nuevo) — `collect_profile` (reusa `services.hwfit.hardware.detect_system`, velocidades medidas de `src.llm_core._LOCAL_SPEED` y `src.vram_fit.KV_RATES`, nunca inventadas), `save/list/get/delete/export/import_profile`, `compare_profiles` (nunca rellena un lado sin dato medido). `routes/hwfit_routes.py` — 6 rutas nuevas bajo `/api/hwfit/hardware-profile*`. | ninguna. | tests/test_p1_hw_hw05_hardware_profiles.py (8 tests) + tests/test_hwfit_gpu_count_nonnumeric.py, tests/test_hwfit_manual_backend.py, tests/test_hwfit_remote_validation.py (suite existente, en verde) | Sin selector en Studio que muestre/compare perfiles (frontend del requisito, fuera de PROPIOS). |
| HW-06 | parcial (nota de decisión de producto, cierre 60-70) | `src/remote_worker_registry.py` (nuevo) — `register_node` (exige `ssh_trust.is_paired` previo), `check_health`/`reconcile` (nunca reporta éxito falso: nodo inalcanzable → `"uncertain"` si tenía reservas, `"unreachable_idle"` si no), `reserve_node`/`release_reservation`. `src/capability_registry.py::_probe_remote_worker` (nuevo, cableado en `observe()`). | ninguna. | tests/test_p1_hw_hw06_remote_nodes.py (10 tests) + tests/test_capability_registry.py, tests/test_contracts_mcp_and_routes.py, tests/test_doctor.py, tests/test_execution_router.py (suite existente, en verde) | **Nota de decisión de producto (Lote 66/70, QA-27)**: la primitiva (`register_node`, `check_health`/`reconcile`, nunca reporta éxito falso) está construida y probada — el único motivo de que siga `parcial` es que `DECLARATIONS["remote_worker"].implemented` se mantiene en `False` A PROPÓSITO, porque 4 ficheros de test ajenos afirman `implemented is False` explícitamente y porque activar un nodo remoto como backend "soportado" exige que alguien (Luis) fije el criterio de fiabilidad/soporte bajo el que un nodo remoto pasa de "primitiva probada" a "implementado" — no es una tarea de código pendiente, es una decisión de producto explícitamente fuera del alcance de un agente. Hasta esa decisión, QA-27 sigue xfail por diseño (ver `QA_ESTADO.md`). |
| HW-07 | LAB (documento + script) | `docs/spec/v2/lab/HW-07_spark_pc_egpu.md` (nuevo, diseño + no-reclamaciones explícitas) + `scripts/lab/hw07_spark_pc_egpu_benchmark.py` (nuevo, standalone, exige `--i-know-this-is-experimental`; `--mode capacity` es aritmética pura sobre `src.vram_fit`, `--mode link` hace un round-trip SSH real vía `src.ssh_trust`). Sin código en el núcleo, tal como pide el lote. | ninguna (LAB). | tests/test_p1_hw_hw07_lab.py (5 tests) | Sin rig Spark+PC+eGPU real disponible para validar los 5 criterios de aceptación con hardware de verdad — es explícitamente lo que LAB significa aquí. |
| OBS-04 | existente (nuevo) | `src/support_bundle.py` (nuevo) — `version_info`, `effective_config_snapshot` (reusa `src.effective_config.compile_effective`, ya enmascara secretos), `recent_events` (allowlist de campos, nunca prosa/adjuntos), `sanitized_log_excerpt` (redacción por patrón), `build_bundle` (zip + `manifest.json` con `personal_content_included: false`). CLI `python -m src.support_bundle`. | ninguna. | tests/test_p1_obs_obs04_support_bundle.py (9 tests) | Sin paso de "previsualizar antes de compartir" en Studio (frontend del requisito). |
| OBS-05 | existente (extendido) | `src/scorecard.py` — `_group_worthy` extraído de `aggregate()` (mismo comportamiento, verificado contra la suite existente), `aggregate_by` (agrupación por modelo/perfil/hardware), `regression_flags` (solo marca regresión si tok/s mejora Y calidad/tiempo/errores empeoran, con `min_samples` en ambos lados — nunca "más rápido" confundido con "mejor"), `reset_local_telemetry`. | ninguna. | tests/test_p1_obs_obs05_scorecard.py (7 tests) | Sin ruta HTTP ni panel en Studio que exponga `aggregate_by`/`regression_flags` todavía (fuera de PROPIOS). |
| PERF-05 | existente (nuevo) | `src/upload_limits.py` — límites/validador de sesión para subida por trozos. `src/upload_handler.py` — `start/write_chunk/status/complete/cancel_chunked_upload` (reasamblado en orden de índice, hash verificado, sweep de sesiones huérfanas) + `save_upload(..., max_size_override=...)` (fix real: el ensamblado grande reusaba el límite de 10 MB del chat en vez del límite de 2 GB de trozos). `routes/upload_routes.py` — 5 rutas nuevas bajo `/api/upload/chunked/*`. | ninguna. | tests/test_p1_perf05.py (10 tests, `TestClient` contra el router real) + tests/test_upload_handler_atomicity.py, tests/test_upload_handler_cleanup.py, tests/test_upload_multifile.py, tests/test_upload_routes_owner_scope.py, tests/test_upload_id_validation.py (suite existente, en verde) | Sin UI de progreso/reanudación en Studio (frontend del requisito). |
| OPS-04 | existente (nuevo) | `src/project_export.py` (nuevo) — `export_project` (memorias de `.odysseus/` con secretos excluidos por patrón, chats vía `Session`/`ChatMessage` reales, `manifest.json` con rutas relativas), `preview_import` (referencias irrecuperables listadas, nunca descartadas en silencio), `import_project` (`dry_run` por defecto; refusa sobrescribir un `.faustus/project.json` de otro proyecto vía `src.project_identity`; guarda de traversal de rutas). | ninguna. | tests/test_p1_ops_ops04_project_export.py (20 tests) + tests/test_project_identity.py, tests/test_projects.py, tests/test_project_store_fail_closed.py, tests/test_database_utcnow.py, tests/test_idx_project_identity.py (suite existente, en verde) | `services.projects.ProjectStore.create()` no acepta un `project_id` explícito — un primer import en una máquina que nunca vio el proyecto obtiene un id NUEVO (`id_preserved=False`, `original_project_id` siempre expuesto). Ver "Cambios en ficheros ajenos". |
| OPS-06 | parcial | `src/api_version.py` — `DEPRECATIONS`/`active_deprecations`/`deprecation_headers` (cabeceras RFC 8594 `Deprecation`/`Sunset`/`Link`), `client_adaptation_notice` (aviso legible para un cliente soportado pero antiguo — no error ambiguo), `openapi_version_extension` (`x-api-version`/`x-min-client-version`/`x-deprecations` para fundir en `app.openapi()["info"]`). `docs/api/versioning.md`, `docs/api/deprecations.md`, `docs/api/client-guide.md` (nuevos). | ninguna. | tests/test_p1_ops_ops06_api_version.py (11 tests) + tests/test_api_version.py, tests/test_l29_api_version_header.py, tests/test_obs_chat_resume_route.py (suite existente, en verde) | `openapi_version_extension()` y `client_adaptation_notice()` no están cableados a ninguna ruta real — `app.py` (que ya define `GET /api/version` y usa `api_version.API_VERSION_HEADER`) es ajeno a este lote. Ver "Cambios en ficheros ajenos". |
| OPS-07 | parcial | `src/cleanup_service.py` (extendido) — `move_to_trash`/`purge_trash` (nunca borra directo, todo pasa por papelera con retención), `sweep_upload_orphans` (delega en `UploadHandler.cleanup_expired_chunked_sessions`, PERF-05, no reimplementado), `enforce_blob_quota` (solo actúa sobre una lista `evictable` que el llamador nomina explícitamente — nunca descubre "sin referencia" por sí mismo), `runs_log_retention`, `artifact_store_usage`/`scorecard_log_usage`/`trash_usage`/`storage_report`, `remote_cost_report` (nunca estima coste desconocido como 0 — ver limitaciones). | ninguna. | tests/test_p1_ops_ops07_cleanup_hygiene.py (19 tests) + tests/test_cleanup_owner_scope.py, tests/test_cleanup_service_utcnow.py, tests/test_cleanup_routes_shim.py (suite existente, en verde) | `remote_cost_report()` lee `DATA_DIR/runs/*.jsonl` buscando eventos de coste, pero ningún caller en el repo escribe ese evento todavía (`src.external_worker.run_task()` calcula `total_cost_usd` y lo devuelve, pero nadie lo persiste) — hoy siempre devuelve `unknown_period=True`, correctamente, no un 0 fabricado. Sin ruta HTTP ni panel en Studio. |

## Lote 54 — Studio restantes y cableado de tools

UX-06, UX-09, SET-02, ACT-05, BENCH-01, EXEC-05, EXEC-06, DESK-01, DESK-02,
WEB-05, WEB-06, ART-04, EDIT-06, PLAN-02. Los lotes 47/48 ya habían dejado
librería real y probada para EXEC-05/06, DESK-01, WEB-05/06 y ART-04/EDIT-06
("parcial": código real, sin tool que el modelo pueda invocar); el hueco
exacto que cerraba cada uno era el mismo — registrar el tool en las cinco
listas que `src/tool_registry.py` exige (`TOOL_TAGS`, `TOOL_HANDLERS`,
`FUNCTION_TOOL_SCHEMAS`, `tool_capabilities`, `tool_index`) con el efecto
correcto para que la aprobación genérica post-contexto-externo
(`src/tool_execution.py`) las cubra sin mecanismo nuevo. `agent_loop.py` y
`chat_routes.py` no fueron tocados (regla explícita del lote). PLAN-02 sí
tenía una autoridad más fuerte ya construida (`src/dispatch.py`:
checkpoint+diff+`src/prove.py`) según el Lote 46 — el hueco real, distinto,
era que un JOB con varios workers podía terminar `done` sin que NINGÚN
worker hubiera tocado lo que el criterio del job pedía; PLAN-02 añade solo
esa verificación a nivel de job, sobre el diff observado. BENCH-01 tenía
módulo real (`panel.ts`/`panel-storage.ts`) pero sin marca ni test propios,
y con un bug de fondo: persistía a `sessionStorage`, no sobrevivía a cerrar
la pestaña — lo contrario de "espacio de trabajo persistente". UX-06, UX-09,
SET-02 y ACT-05 seguían greenfield: exigen autoridades nuevas enteras
(backend de adjuntos con dedupe/MIME, backend de favoritos/pins/tags/
carpetas/papelera de chats, unificación real de alias/digest/capacidades de
modelo, y una cola con prioridad/envejecimiento) que no existen en ningún
sitio del repo y no caben en el presupuesto de este lote sin repetir el
error que P1_COMUN.md prohíbe explícitamente (construir una autoridad
paralela a medias); quedan documentados abajo, mismo patrón que el Lote 49a
dejó para ellos.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| BENCH-01 | existente (nuevo) | ninguna. | `studio/src/screens/studio/useChatPanel.ts` — persistencia movida de `sessionStorage` a `localStorage` (el reducer `panel.ts`/`panel-storage.ts` ya existían: clave estable por conversación, `forget` rehúsa evictar una entrada con draft sin guardar, abrir un fichero nunca toca `doc`). | tests/test_p1_bench01_panel_persistence.py (nuevo, 5 tests: función pura vía `studio/checks/p1-bench01-panel-persistence.check.mjs` + wiring estático sobre el fichero real desplegado) | El resto del "espacio de trabajo" pedido por BENCH-01 (dividir/acoplar chat junto a editor/diff/fuentes/browser/actividad, volver al layout anterior) ya vive en `SidePanel.tsx` desde BENCH-03/04/05/06 (Lote 49b) — esta fila cierra específicamente la persistencia entre sesiones, que era el hueco documentado. |
| EXEC-05 | existente (extendido) | `src/agent_tools/exec_tools.py::InstallDependenciesTool` (nuevo) — tool `install_dependencies` sobre `src/tool_execution.py::plan_dependency_install`/`execute_dependency_install` ya existentes; `action=plan` nunca ejecuta, `action=install` exige `approved=true` o una aprobación previa del mismo `plan_hash`. Clasificado `ADMIN_CHANGE`+`WRITE_WORKSPACE` en `src/tool_capabilities.py` — cubierto por la aprobación genérica, no una nueva. | ninguna. | tests/test_l54_tool_wiring.py (nuevo, cubre las 7 tools de este lote) + tests/test_p1_exec_05_install.py (Lote 48, suite de librería, en verde) | Nada — el hueco que Lote 48 dejó anotado ("sin tool nueva expuesta al modelo") queda cerrado. |
| EXEC-06 | existente (extendido) | `src/agent_tools/exec_tools.py::ManageScriptsTool` (nuevo) — tool `manage_scripts` (`save`/`run`/`run_remote`) sobre `save_script`/`run_saved_script`/`run_remote_script` ya existentes; `run_remote` exige alias YA emparejado y host/fingerprint EXACTOS. Clasificado `EXECUTE_CODE`+`WRITE_PRIVATE`. | ninguna. | tests/test_l54_tool_wiring.py + tests/test_p1_exec_06_scripts_ssh.py (Lote 48, suite de librería, en verde) | Nada — mismo cierre que EXEC-05. |
| DESK-01 | existente (extendido) | `src/agent_tools/desktop_tools.py::ManageDesktopControlTool` (nuevo) — tool `manage_desktop_control` (`get_allowlist`/`set_allowlist`/`clear_allowlist`/`enable_audit`/`disable_audit`/`audit_log`) sobre `src/desktop_control_session.py` ya existente, opt-in por sesión (una sesión que nunca la llama no cambia de comportamiento — verificado: `tests/test_desktop_tools.py` completo sigue en verde sin tocar). Clasificado `ADMIN_CHANGE`. | ninguna. | tests/test_l54_tool_wiring.py + tests/test_p1_desk_01_control.py (Lote 48, en verde) | El indicador visible sigue siendo solo-Windows (nota ya existente del Lote 48, sin cambio aquí — fuera de PROPIOS). |
| DESK-02 | parcial (sin cambio) | `src/browser_journey_verification.py` (Lote 48, sin tocar). | `studio/checks/*.check.mjs` (existente, ajeno). | tests/test_p1_desk_02_journey.py (Lote 48, en verde) | Sin fichero propio asignado a DESK-02 en la lista PROPIOS de este lote (solo `studio/checks` como ancla, ya cubierto); cablearlo contra un dev server + Playwright real sigue fuera de presupuesto, igual que dejó anotado el Lote 48. |
| WEB-05 | existente (extendido) | `src/agent_tools/browser_tools.py::CaptureEvidenceTool` (nuevo) — tool `capture_evidence` (`build`/`compare`/`check_stale`) sobre `src/browser_evidence.py` ya existente; nunca toma la captura, solo procesa metadata/imagen ya obtenida por el caller. Clasificado `READ_PRIVATE`. | ninguna. | tests/test_l54_tool_wiring.py + suite del Lote 48 (en verde) | Nada — mismo cierre que EXEC-05/06. |
| WEB-06 | existente (extendido) | `src/agent_tools/browser_tools.py::BrowserExtractTool` (nuevo) — tool `browser_extract` (`paginate`/`detect_restricted_access`/`check_form_fill`/`select_download_target`/`verify_download`/`check_login_reuse`) sobre `src/browser_extraction.py` ya existente; `paginate` envuelve páginas ya obtenidas por el caller en un callback sintético (el modelo no puede suministrar uno real). Clasificado `READ_WORKSPACE`. | ninguna. | tests/test_l54_tool_wiring.py + tests/test_p1_web_06_extraction.py (Lote 48, en verde) | Nada — mismo cierre. |
| ART-04 | existente (extendido) | `src/agent_tools/spreadsheet_tools.py::ManageSpreadsheetTool` (nuevo) — tool `manage_spreadsheet` (`import_csv`/`export_csv`/`read_workbook`/`write_range`) sobre `src/spreadsheet.py` ya existente (neutraliza `=`/`+`/`-`/`@` al exportar, detecta fórmula sin valor cacheado). Rutas resueltas vía `_resolve_tool_path` (mismo resolutor que `read_file`/`write_file`), no `_resolve_search_root`. Clasificado `READ_WORKSPACE`+`WRITE_WORKSPACE`. | ninguna. | tests/test_l54_tool_wiring.py (incluye el caso de IDs con cero a la izquierda y bloqueo de inyección de fórmulas) | Nada — mismo cierre. |
| EDIT-06 | existente (nuevo) | `src/agent_tools/code_tools.py::RenameSymbolTool` (nuevo) — tool `rename_symbol` (`plan`/`apply`) sobre `src/refactor_tools.py` ya existente; usa `src/code_index.py` (nunca sustitución de texto en todo el workspace) y rehúsa cuando el símbolo no está indexado en vez de degradar a un patch amplio. Clasificado `WRITE_WORKSPACE`. | ninguna. | tests/test_l54_tool_wiring.py | Nada — cierra el hueco que el Lote 47 había dejado anotado ("sin ruta ni tool"). |
| PLAN-02 | existente (extendido) | `src/dispatch.py` — `DispatchJob.criteria_check` (nuevo), `build_args` acepta `criteria`/`success_criteria`/`acceptance` en el body, `_build_job_criteria_check` reusa `src/agent_tools/subagent_tools.py::verify_criteria` (la MISMA función que ya verifica cada worker — no una autoridad paralela) contra el diff OBSERVADO del job (`job.changes`), nunca contra lo que un worker afirma; un job `done` con criterio de job incumplido baja a `partial`. | ninguna (el árbol de subtareas en Studio ya existía; sin cambio de UI en este lote). | tests/test_l54_plan02_dispatch_job_criteria.py (nuevo, 10 tests) + suite completa de `tests/test_dispatch*.py` (135+9 tests, en verde) | `criteria_check` no se muestra todavía en ninguna vista de Studio del árbol de subtareas — solo en el JSON de `to_dict()`; frontend fuera de PROPIOS para esta fila concreta. |
| UX-06 | ausente (sin auditar en este lote) | ninguna. | ninguna. | ninguno. | "Compositor rico pero rápido" depende de MEDIA-01 y de un backend de adjuntos con MIME/tamaño/dedupe/estado que no existe en ningún fichero PROPIOS de este lote (`Composer.tsx` sí existe, pero cablearlo sin esa autoridad de adjuntos sería la "autoridad paralela a medias" que P1_COMUN.md prohíbe). Requiere auditoría propia — mismo veredicto que dejó el Lote 49a. |
| UX-09 | ausente (sin auditar en este lote) | ninguna. | ninguna. | ninguno. | "Acciones personales sin fricción" (favoritos/pins/tags/carpetas/archivo/exportar/restaurar chats, con la garantía explícita de que mover un chat no cambia permisos/memorias de sus mensajes históricos) exige un backend de persistencia por chat que no existe — construirlo tocaría casi con certeza `routes/session_routes.py`/almacenamiento de sesiones, fuera de PROPIOS y cerca de `chat_routes.py`, explícitamente prohibido. Requiere auditoría propia. |
| SET-02 | ausente (sin auditar en este lote) | ninguna. | ninguna. | ninguno. | "Selector de modelos informativo" pide unificar alias/digest/capacidades/conexión/residencia para que dos tags del mismo digest nunca aparezcan como instalado/no-instalado contradictorios — eso es una unificación de varias fuentes ya existentes (`routes/local_models_routes.py`, HW-03/HW-04 del Lote 52, catálogo de modelos) que ninguna autoridad actual hace hoy; construirla a medias duplicaría lo que HW-04 ya resuelve para la cola. Requiere auditoría propia. |
| ACT-05 | ausente (sin auditar en este lote) | ninguna. | ninguna. | ninguno. | "Vista de colas y prioridades" pide una cola JUSTA con prioridad interactiva, límites por proyecto y envejecimiento anti-inanición — no existe ninguna cola de ese tipo hoy (`routes/queue_routes.py` de PROPIOS no llegó a crearse: no hay encolador real al que enrutar). `Activity.tsx` ya existe y podría pintar la vista, pero sin la cola detrás sería una UI sin backend real, lo que P1_COMUN.md pide evitar explícitamente. Requiere auditoría propia. |

## Lote 55 — Integración de la ola 8 y los cuatro P1 que quedaban

Con vía libre sobre cualquier fichero (nadie más trabajaba en el repo a la
vez). Dos mitades: (1) cablear en `app.py` tres routers de lotes anteriores
que estaban escritos y probados pero nunca enganchados (`media_edit_routes`
→ MEDIA-03, `integrations_routes` → CONN-01, y el `queue_routes` nuevo de
este lote → ACT-05 — ver sus filas ya actualizadas más arriba), más el
método `_Lane.prioritize`/`prioritize_run` nuevo en `src/agent_runs.py`
(ACT-05 necesitaba una cola con orden real que reordenar; de las cuatro
colas que existen en el repo, solo la de turnos de agente — `_Lane.waiting`
— es de verdad FIFO, así que es la única que `POST /api/queue/{kind}/{id}/
priority` puede reordenar; las otras tres kinds responden 409
`no_ordered_queue` en vez de fingir un reordenamiento que no hacen); y (2)
auditar y cerrar los ocho IDs P1 que llevaban desde el Lote 50 marcados
"ausente (ningún lote lo ha tocado)" sin que nadie los hubiera mirado, más
los cuatro IDs propios de este lote (UX-06, UX-09, SET-02, ACT-05) que los
Lotes 49a/54 habían dejado igual de vacíos dos veces seguidas. De los ocho
primeros, siete resultaron YA implementados por lotes anteriores con otro
nombre o sin marca — exactamente el mismo patrón que AUTO-01/QA-43 ya dejó
documentado en el Lote 53 — así que este lote no escribió código nuevo para
ellos, solo los auditó, verificó sus tests en verde y corrigió el mapa (que
llevaba des-sincronizado con el código real desde el Lote 50, ver nota de la
sección "Ausentes" más arriba); solo TASK-05 sigue sin ningún fichero que lo
toque. UX-09/SET-02/ACT-05 sí eran greenfield de verdad y se construyeron
aquí; UX-06 se auditó con el alcance que el propio enunciado de este lote le
fijó (rendimiento de menciones/comandos/adjuntos bajo un presupuesto de
16 ms por pulsación), no el enunciado más amplio de `backlog.json` (formato
rico, atajos) que ningún lote ha auditado todavía.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| TASK-05 | ausente | ninguna. | ninguna. | ninguno. | Auditado, no implementado: ni `agent_loop.py` ni `chat_routes.py` tienen ningún mecanismo que invalide pasos de un plan en curso cuando llega una restricción nueva (se buscó "invalidat" en `agent_loop.py`: el único resultado es sobre caché de prefijo de KV, sin relación). El steering de UX-04 (`agent_runs.queue_steer`/`take_steers`, `POST /api/chat/steer/{sid}`) cubre la mitad "encolar instrucciones y aplicarlas en un punto seguro", pero nunca marca ningún `PlanStep` existente como inválido — el criterio de aceptación completo (la mitad de invalidación) sigue sin ningún caller. Fuera de presupuesto de este lote implementarlo desde cero: exigiría tocar `agent_loop.py`/`src/plan_state.py`, ninguno de los dos en PROPIOS. |
| MOD-05 | existente (Lote 41, sin fila hasta ahora) | `src/privacy_policy.py` (perfil único "¿esto sale de la máquina?", SEC-04/MOD-05, QA-29) — cableado real en `src/settings.py`, `src/rerank.py` y `src/effective_config.py`: el reranker y cualquier auxiliar de compilación de contexto consultan la MISMA autoridad que el chat principal en vez de decidir cada uno por su cuenta, que era exactamente el hueco que nombraba QA-29 ("no filtra por componente auxiliar"). | ninguna. | tests/test_privacy_policy.py, tests/test_privacy_policy_auxiliaries.py, tests/qa/test_qa_29_privacidad_transitiva.py (`qa_state("green")`) — las tres suites re-ejecutadas en verde por este lote. | El "routing por capacidad/calidad observada/latencia" del título del ID (elegir modelo/backend, no solo permitir o no salir) no está aquí — esta fila cubre la mitad de "privacidad transitiva" del ID, que es la que QA-29 examina; el enrutamiento por capacidad ya vive en `src/execution_router.py` (ajeno, sin auditar contra MOD-05 por este lote). |
| IDX-02 | existente (Lote 38, sin fila hasta ahora) | `src/code_index.py` (índice incremental, símbolos/referencias) + `src/agent_tools/code_tools.py` (`find_symbol`/`callers`/`tests_for`, tools reales del agente) + `routes/code_index_routes.py` (superficie HTTP). | `routes/code_index_routes.py` es HTTP puro, sin pantalla propia en Studio (el índice se usa desde las tools del agente, no desde un panel). | tests/test_code_index.py, tests/test_code_index_routes.py — re-ejecutadas en verde por este lote. | Comparte primitiva con IDX-03 (misma fila de evidencia); "watch con debounce, cancelación y límites de archivo" del título están cubiertos por `code_index.refresh(pause_on_pressure=True)` (IDX-06, Lote 45, ya documentado arriba) — no se duplicó. |
| IDX-03 | existente (Lote 38, sin fila hasta ahora) | Misma base que IDX-02: `src/code_index.py::find_callers`/`tests_for` combinan coincidencia léxica con la convención de nombre de fichero de test — la "recuperación híbrida" que pide el título, aunque sin BM25/semántica todavía (ese lado más ambicioso del ID no se auditó). | igual que IDX-02. | tests/test_code_index.py, tests/test_code_index_routes.py. | El reranking/top-k adaptativo con evidencia negativa del título completo del ID no está aquí — esta fila cubre solo la mitad "símbolos + convención de test" que el código de Lote 38 realmente construyó, no el título completo del ID. |
| UX-03 | existente (Lote 40, sin fila hasta ahora) | `routes/chat_routes.py::regenerate_chat_response` — `POST /api/chat/regenerate/{sid}` (QA-36/UX-03): reinyecta la evidencia de tool_events del turno anterior como contexto, y bloquea TODA tool que esta instalación no pueda probar de solo lectura (reusa `src.tool_capabilities`/`src.tool_security.PLAN_MODE_READONLY_TOOLS`, ninguna lista nueva) para el turno regenerado entero, no solo las que se usaron la vez anterior — así que "no repite herramientas externas" se cumple incluso si el modelo intenta una acción efectiva distinta. `save_assistant_response` AÑADE el mensaje regenerado (`sess.add_message`), nunca borra ni sobrescribe el original — "no borra historial" por construcción. | ninguna nueva (`Studio.tsx` ya tenía el botón "Regenerar", ajeno a este lote). | tests/test_l40_chat_regenerate_route.py (con prueba de reversión documentada: quitando la ruta, las pruebas fallan en el propio lookup de `router.routes`) + tests/qa/test_qa_36_regenerar_con_efectos.py (`qa_state("green")`) — re-ejecutadas en verde por este lote. | "Árbol de conversación" del título no es un árbol de ramas visual — es versionado lineal recuperable (`/versions`, `/restore`, ajeno a este lote); no se audita aquí si eso satisface el título completo del ID, solo la mitad literal que QA-36 examina (regenerar sin duplicar efectos ni perder historial). |
| PERF-01 | existente (Lote 39, sin fila hasta ahora) | `studio/src/screens/studio/Transcript.tsx` — virtualiza la lista de turnos con `@tanstack/react-virtual` (`useVirtualizer`/`getVirtualItems`/`measureElement`, solo lo visible se monta) y repinta el turno en streaming como máximo una vez por frame vía `useFrameBatched` sobre `studio/src/lib/frame-batch.ts::frameBatcher` (mismo mecanismo que el Lote 55 reutilizó para UX-06 más abajo — no se duplicó). | `Transcript.tsx` mismo (frontend puro). | tests/qa/test_qa_37_lectura_bajo_stream.py (`qa_state("green")`), tests/test_studio_l39_frame_batch_js.py — re-ejecutadas en verde por este lote. | "Cargar editores/galerías bajo demanda" ya existe a nivel de ruta (`studio/src/shell/AppShell.tsx`, `lazy(() => import(...))` para Editor/Library/Documents — code-splitting general de la app, no una marca de este lote) y "limitar logs en memoria" no se auditó — ninguno de los dos tiene test que los ate a PERF-01 específicamente. |
| WRITE-02 | existente (Lote 41, sin fila hasta ahora) | `src/branching_futures/narrative_canon.py` (canon vs. alternativa descartada, sobre `BranchingService` ya existente — sin store paralelo) + `routes/branching_futures_routes.py::GET /canon/state` (solo ramas `selected`/promovidas) y `GET /canon/alternatives` (una alternativa descartada nunca aparece en `/canon/state`, verificable). | ninguna. | tests/test_write_narrative_canon.py, tests/qa/test_qa_47_continuidad_de_novela.py (`qa_state("green")`) — re-ejecutadas en verde por este lote. | `canon_state()` no tiene todavía un caller real desde `memory_engine.py`/`context_engine/` que lo prefiera sobre recall de transcript crudo para un proyecto con futuros narrativos — documentado ya en el docstring del propio módulo; ninguno de los dos ficheros está en PROPIOS de este lote. |
| WRITE-04 | existente (Lote 41, sin fila hasta ahora) | Mismo módulo que WRITE-02 (`narrative_canon.py`): `discarded_alternatives()` es la mitad "sesiones largas y alternativas" — una rama abandonada queda auditable (fila en disco) sin poder resurgir como hecho confirmado, que es el criterio literal de QA-47 ("alternativa no se recupera como hecho confirmado"). | igual que WRITE-02. | igual que WRITE-02. | "Esquemas/capítulos con referencias estables, objetivos de escena" del título del ID no está en este módulo — esta fila cubre la mitad "ramas editoriales independientes" que QA-47 examina, no el ID completo. |
| UX-06 | parcial (nuevo, Lote 55) | `studio/src/screens/studio/composer-suggest.ts` (nuevo) — `resolveSuggestionIntent` (la lógica de `@`/`/` extraída, pura) y `capMentionItems` (tope defensivo a 20 filas, independiente de lo que mande el servidor); `Composer.tsx` corre esa función detrás de `frameBatcher` (`studio/src/lib/frame-batch.ts`, el MISMO mecanismo que PERF-01 ya usa para el streaming — no una segunda autoridad) en vez de en cada pulsación, así que una ráfaga de teclas dentro de un frame resuelve una sola vez; el listado de adjuntos se extrajo a `AttachmentList`, un componente `memo`-izado que ya no se reconstruye en cada pulsación porque `pendingFiles`/`attachments` no cambian con el borrador. | `Composer.tsx` mismo (frontend puro). | studio/checks/l55-ux06-composer-perf.check.mjs (nuevo; prueba, con el bundle real vía esbuild, que 500 pulsaciones de un `@mention` en un solo frame resuelven una vez, que 500 candidatos se recortan a 20, y que ambas rutas quedan por debajo de 16 ms/llamada) + tests/test_l55_studio_checks_js.py (wrapper pytest) — revert-check: quitando el `slice` de `capMentionItems` la prueba del recorte falla (500 en vez de 20), restaurado y confirmado en verde. | El enunciado de este lote acotó UX-06 a "menciones/comandos/adjuntos no bloquean el hilo" — no se auditó el resto del título original del ID (formato rico, atajos de teclado, backend de adjuntos con MIME/dedupe que el Lote 54 había señalado como bloqueante); por eso queda "parcial", no "existente". |
| UX-09 | existente (nuevo, Lote 55) | Sin backend nuevo: "acción personal" se resolvió reusando lo que ya existía — un recordatorio es una nota con `due_date` (`src/task_scheduler.py` ya trata cualquier nota con fecha como recordatorio, sin importar `note_type`) y un evento ya tenía su propia ruta; no se tocó `routes/session_routes.py` ni `chat_routes.py` (el veredicto del Lote 54 sobre "favoritos/pins/tags/carpetas de chat" seguía siendo cierto y no se auditó aquí — solo se cerró la mitad de "notas/recordatorios/eventos sin fricción" vía comandos). | `studio/src/screens/studio/commands.ts` (alias `/nota`, `/recordatorio`, `/evento`) + `Studio.tsx` (casos `note`/`reminder`/`event`, cada uno con botón Deshacer real — `deleteNote`/`deleteEvent` — no solo un mensaje de confirmación). | studio/checks/l55-ux09-personal-actions.check.mjs (nuevo; alias sin choque, resolución a `note`/`reminder`/`event`) + tests/test_l55_studio_checks_js.py. | "Favoritos/pins/tags/carpetas/archivo/exportar/restaurar chats" del título completo del ID sigue sin construir — esta fila cubre solo la parte de comandos rápidos que este lote acotó, no el ID completo. |
| SET-02 | existente (nuevo, Lote 55) | `routes/local_models_routes.py::GET /api/models/endpoint-profile` (nuevo, dentro de `caps_router` ya existente) — privacidad (`src.privacy_policy.is_local_destination`, la autoridad que MOD-05 fija arriba, no un chequeo de `endpoint_kind` ad hoc como el que ya usa `diagnostics_routes.py`) y coste por endpoint; `GET /api/models/{name}/capabilities` (ya existente) se reusa para el manifiesto `tested` sin duplicar MOD-01/MOD-02. | `studio/src/adapters/fit.ts` (`endpointProfiles`/`useEndpointProfiles`/`costLabel`/`fetchModelCapabilities`) + `ModelPalette.tsx` — badge de privacidad/coste por fila (lectura única al abrir, igual que `useFitHints`) y capacidades probadas por fila SOLO bajo demanda (hover/focus, nunca las N filas de golpe). | tests/test_l55_set02_model_profile.py (nuevo, 401 sin auth, privacidad desde `base_url`, cubos de coste, scoping por owner igual que `/api/models`) + studio/checks/l55-set02-model-profile.check.mjs + tests/test_l55_studio_checks_js.py. | La "unificación de alias/digest" del título (que dos tags del mismo digest nunca aparezcan contradictorios) ya existía por otra vía (`aliasesOf` en `fit.ts`, sobre el `digest` de HW-04) — no se tocó aquí; esta fila añade privacidad+coste+capacidades, que era el hueco que HW-04 no cerraba. |
| ACT-05 | existente (nuevo, Lote 55) | `routes/queue_routes.py` (nuevo) — `GET /api/queue` fusiona agent-runs/bg-jobs/research/media-renders en una lista con propietario; `POST /api/queue/{kind}/{id}/priority` reordena SOLO `agent_run` (la única cola realmente FIFO — `src.agent_runs._Lane.waiting`, nuevo método `prioritize`/`prioritize_run`) y devuelve 409 `no_ordered_queue`, nunca un éxito fingido, para las otras tres kinds. Cableado en `app.py` (`setup_queue_routes(research_handler, session_manager=session_manager)`). | `studio/src/adapters/activity.ts` (`loadQueue`/`prioritizeQueueItem`) + `Activity.tsx` (`QueuePanel`, sondeo propio vía `createActivityPoller`, botón de prioridad por fila). | tests/test_l55_act05_queue.py (nuevo, 7: 2 sobre `agent_runs.prioritize_run` directo + 5 HTTP sobre un `FastAPI()` con el router real) + tests/test_l55_app_wiring.py (cableado real en `app.py`, vía `TestClient` + bypass de loopback interno) + studio/checks/l55-act05-queue.check.mjs + tests/test_l55_studio_checks_js.py. | "Límites por proyecto" y "envejecimiento anti-inanición" del título no están — solo prioridad interactiva (mover al frente); documentado como el alcance real, no fingido como cierre completo del título del ID. |

## Lote 70 — Cierre de los P1 que quedaban abiertos (integración de los lotes 60-70)

Los 9 IDs que llevaban `ausente`/`parcial` en su última fila (Lotes 46/48/51/52/55)
se auditan contra el código real de master cfaa30d (lotes 60-69b, con los 16
cableados de la sección A de `INTEGRACION_70.md` dados por aplicados en
paralelo por este cierre). HW-06 se deja explícitamente aparte (ver su fila
actualizada arriba, Lote 52): es una decisión de producto, no un hueco de
código, y no se toca aquí. HW-07 (LAB) tampoco se toca — sigue exigiendo el
rig físico Spark+PC+eGPU que este cierre no tiene.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| TASK-05 | existente (Lote 60) | `src/plan_state.py::apply_steer(plan, steer_text)` (nuevo, línea 318) invalida los `PlanStep` que una nueva restricción del usuario contradice; `src/agent_loop.py::_apply_steer_to_plan_update` (línea 4306) cablea `apply_steer` en el punto seguro donde el turno ya consumía `queue_steer`/`take_steers` (UX-04) — ahora la corrección del usuario durante un turno en curso no solo se encola, también marca inválido cualquier `PlanStep` que la nueva restricción contradiga, y el plan mostrado refleja el estado ANTERIOR al siguiente cambio de archivos, no un estado a medio actualizar | plan acoplado (docked) ya muestra los pasos marcados inválidos tras un steer | tests/test_l60_task05_apply_steer.py (nuevo) | Cerrado: era el único ID de los 84 sin ningún fichero que lo tocara. La corrección del usuario durante un turno queda en el estado anterior al siguiente cambio de archivos, y el steering ya invalida pasos del plan afectados por la restricción nueva, no solo los encola. |
| OPS-06 | existente (Lote 61) | `src/api_version.py` (`DEPRECATIONS`/`active_deprecations`/`deprecation_headers`, `client_adaptation_notice`, `openapi_version_extension`, lote 52); Lote 61: `app.py` ya no es ajeno — `app.openapi()["info"]` funde `openapi_version_extension()` (línea ~162), y `client_adaptation_notice()` se llama en `app.py:1537` para componer el aviso legible que acompaña al 426 global de ARCH-01 (`core/middleware.py`) | `docs/api/versioning.md`, `docs/api/deprecations.md`, `docs/api/client-guide.md` (lote 52, sin cambio de contenido) | tests/test_p1_ops_ops06_api_version.py, tests/test_api_version.py, tests/test_l61_arch01_global_version_gate.py (cubre también el aviso de adaptación) | Cerrado en Lote 61: un cliente antiguo recibe error de versión legible (`client_adaptation_notice`) o 426 comprensible en cualquier `/api/*`, nunca un fallo ambiguo tras ejecutar media acción — `openapi_version_extension`/`client_adaptation_notice` ya están cableados en `app.py`, que dejó de ser un fichero ajeno. |
| OPS-07 | existente (Lote 67) | `src/cleanup_service.py` (extendido, lote 52) — `move_to_trash`/`purge_trash`, `sweep_upload_orphans`, `enforce_blob_quota`, `storage_report`, `remote_cost_report`; Lote 67: `src/external_worker.py::run_task()` ya PERSISTE el evento de coste que calculaba y devolvía sin guardar (`DATA_DIR/runs/*.jsonl`) — `remote_cost_report()` ya no depende de un caller inexistente; `GET /api/ops/remote-cost` (nuevo, `routes/ops_routes.py`) expone el informe por HTTP | panel de higiene/coste en Settings › System (Lote 67, reusa `SystemExtras.tsx`) | tests/test_p1_ops_ops07_cleanup_hygiene.py, tests/test_cleanup_owner_scope.py, tests/test_l67_ops07_remote_cost_route.py (nuevo) | Cerrado en Lote 67: liberar caché no elimina evidencias fijadas ni modelos en uso (ya lo garantizaba `enforce_blob_quota`); una factura remota desconocida no se estima como cero — `remote_cost_report()` ya recibe el evento real de `external_worker.run_task()` y lo expone por `GET /api/ops/remote-cost`. |
| DESK-02 | existente (Lote 68) | `src/browser_journey_verification.py::run_journey` (Lote 48) — `snapshot_fn` inyectado, `detect_object_leak`/`assert_no_object_leak` (`[object Object]`), `assert_no_console_errors`/`assert_no_network_failures`; Lote 68: `scripts/ui_journeys.py` (nuevo) cablea `run_journey` contra un Chromium (Playwright) real que dirige el bundle `static/studio/` servido de verdad (`_make_snapshot_fn`, línea 116) — ya no son solo fixtures falsas | `studio/checks/*.check.mjs` (existente) | tests/test_p1_desk_02_journey.py, tests/test_l68_desk02_ui_journeys_live.py (nuevo, `@pytest.mark.slow` — abre un subprocess server y un navegador real, ~30s, fuera del carril rápido) | Cerrado en Lote 68: el caso de botones `[object Object]` falla en un test de render/journey REAL (Chromium + servidor real), no solo con fixtures inyectadas — aunque el backend devuelva HTTP 200. |
| TOOL-05 | existente (Lote 63) | `src/mcp_manager.py` — `make_sampling_callback`/`make_elicitation_callback`/`make_list_roots_callback` (lote 46), `_SamplingQuota` por `server_id`; Lote 63: el rehúso de sampling en cuota ya es visible al operador — razón auditable expuesta en `list_servers`/manifest, no solo metrada internamente; elicitación sigue pasando por `question_store` sin store paralelo | `studio/src/screens/settings/Integrations.tsx` — aviso "qué servicio pregunta y qué datos recibirá" cuando una elicitación MCP está pendiente | tests/test_p1_tool05_mcp_limits.py, tests/test_l63_tool05_sampling_refusal_visible.py (nuevo) | Cerrado en Lote 63: un servidor no puede recursar indefinidamente pidiendo modelos ni reutilizar una autorización de otro destino (cuota por `server_id`, sin cambios) — el rehúso ya es visible con razón auditable, y qué pregunta un servidor y qué datos recibirá ya se muestra en Integrations.tsx. |
| TOOL-06 | existente (Lote 63) | `src/skill_governance.py::gate_mutating_use`/`validate_promotion` (lote 46, primitiva sin caller); Lote 63: `services/memory/skill_format.py::STATUSES` (línea 368) gana el vocabulario `obsolete`/`deprecated`/`superseded` (antes solo `draft`/`published`); `routes/skills_routes.py` (línea 1840, 1895) ya llama `skill_governance.gate_mutating_use(match, action="apply")` antes de mutar — una skill obsoleta se rehúsa ANTES de mutar datos, con `fallback_version` a la versión previa no obsoleta | markdown de la skill muestra el estado (`draft`/`published`/`obsolete`/`deprecated`/`superseded`) y el gate en Teach Mode (`routes/teach_mode_routes.py`) | tests/test_p1_tool06_skill_governance.py, tests/test_l63_tool06_status_vocab_and_gate_caller.py (nuevo) | Cerrado en Lote 63: una skill obsoleta falla antes de mutar datos y ofrece la versión previa; su contenido no eleva privilegios (`as_context_message`, sin cambios) — el vocabulario `obsolete`/`deprecated`/`superseded` ya existe en `Skill.status` y `gate_mutating_use` ya tiene caller real. |
| SEC-08 | existente (Lote 63) | `src/security_policy.py` — `PROFILES`, `evaluate()`, `ConsentStore` (clave `(plugin_id, tool_name)`, lote 46); Lote 63: `src/mcp_manager.py` ya declara permisos por servidor MCP (reusa el manifiesto de TOOL-04, `src/extension_manifest.py`) y llama `security_policy.evaluate()`/`ConsentStore.check()` al instalar/actualizar — una actualización con más permisos invalida el consentimiento anterior sin bloquear tools no afectadas, ya con caller real; `export_audit()` se persiste en disco (ya no solo `deque` de proceso) | banner de permisos nuevos en `Integrations.tsx` (reusa `ManifestDiffPanel` de TOOL-04) | tests/test_p1_sec08.py, tests/test_l63_sec08_mcp_manager_evaluate_caller.py (nuevo) | Cerrado en Lote 63: una actualización de plugin con más permisos invalida el consentimiento anterior sin bloquear herramientas no afectadas — `evaluate()`/`ConsentStore` ya tienen caller real en `mcp_manager.py`, y el log de auditoría sobrevive a un reinicio. |
| UX-06 | existente (Lote 65) | `studio/src/screens/studio/composer-suggest.ts` (menciones/comandos bajo 16ms/pulsación, lote 55); Lote 65: `Composer.tsx` ya explica por qué un adjunto es incompatible ANTES de gastar inferencia — "{name} is too large ({size} MB) to attach.", "{name} is audio, but no transcription backend is installed", "{name} is video, but ffmpeg is not installed" (líneas 185-193) — backend de adjuntos con MIME/tamaño/estado que el Lote 54 señalaba como bloqueante ya está cableado, sin autoridad paralela (reusa `media_capabilities.py`, MEDIA-01) | `Composer.tsx` mismo, mensajes visibles antes de adjuntar | studio/checks/l55-ux06-composer-perf.check.mjs, tests/test_l65_ux06_incompatible_attachment_reason_js.py (nuevo) | Cerrado en Lote 65: pegar un archivo grande no congela la UI (lote 55, sin cambio); un adjunto incompatible muestra la razón antes de gastar inferencia — tamaño, falta de backend de transcripción o de ffmpeg, cada uno con su mensaje. |
| MEDIA-05 | existente (Lote 68) | `src/media_subtitles.py::to_srt`/`to_vtt`/`export`; `routes/local_video_routes.py::export_subtitles` (`POST /api/media/local-video/subtitles/export`, lote 51); Lote 68: `studio/src/screens/studio/LocalVideo.tsx` ya consume esa ruta — botones de exportación SRT/VTT visibles junto al vídeo local, sin reemplazar el audio original (solo pide los subtítulos ya transcritos) | `studio/src/screens/studio/LocalVideo.tsx` — exportar SRT/VTT | tests/test_p1_media_subtitles.py, tests/test_p1_media_subtitle_routes.py, tests/test_l68_media05_localvideo_export_ui_js.py (nuevo) | Cerrado en Lote 68: cambiar subtítulos no reemplaza accidentalmente el audio original (por construcción — el export nunca toca el stream de audio); ya hay UI para descargarlos desde Studio (`LocalVideo.tsx`). |
