# Mapa de reutilización — P1 (por lote, según lo pide `docs/spec/v2/P1_COMUN.md`)

`docs/spec/v2/MAPA_REUTILIZACION.md` solo cubre P0; este fichero recoge, lote
a lote, el estado real de los IDs P1 contra el código (no contra el enunciado
del backlog). Misma leyenda: existente · parcial · ausente · no verificable.
Regla de Luis que manda sobre todo lo que sigue: **no se pierde ninguna
capacidad; se extiende, reorganiza y mejora**.

## Resumen — 87 IDs P1/P2/LAB (`docs/spec/v2/backlog.json`)

Reconstruido por el Lote 50 tras la pérdida de las secciones de los lotes 47,
49a y 49b (sobrescrituras concurrentes; quedaban 45, 46, 48). Recuento sobre
el código real, no sobre lo que cada lote dice haber hecho:

| Estado | Recuento | Nota |
|---|---|---|
| existente | 35 | Backend y (donde el requisito lo pide) ruta/UI reales, probados. |
| parcial | 11 | Primitiva real y probada, sin el cableado final a un caller de producción o una tool expuesta al modelo — el "siguiente paso" queda descrito en la fila de cada lote. |
| ausente (tocado, sin resultado) | 5 | El lote al que se asignó no dejó código ni test: BENCH-01, UX-06, UX-09, SET-02, ACT-05. |
| ausente (ningún lote lo ha tocado) | 36 | Ver tabla siguiente — una línea por ID con lo que haría falta. |
| **Total** | **87** | |

### Ausentes — ningún lote (45-49b) los ha tocado

Una fila por ID, `backend`/`acceptance` tal cual los declara `backlog.json`
(recortado); todos parten de `status: "propuesto: auditar antes de
implementar"`.

| ID | Prio. | Título | Qué haría falta |
|---|---|---|---|
| TASK-05 | P1 | Steering y cambios del usuario | Encolar instrucciones durante ejecución y aplicarlas en puntos seguros; invalidar pasos afectados por una nueva restricción. |
| MOD-05 | P1 | Router medido y privacidad transitiva | Routing por capacidad/calidad observada/latencia/residencia/política; los auxiliares heredan restricciones de datos. |
| IDX-02 | P1 | Índice incremental y análisis de código | Símbolos y referencias con parsers/LSP; watch con debounce, ignorados, cancelación y límites de archivos. |
| IDX-03 | P1 | Recuperación híbrida medible | Combinar literal, BM25/semántica, símbolos y vecindad; reranking opcional, top-k adaptativo, evidencia negativa. |
| ART-06 | P2 | Presentaciones y gráficos | Diapositivas editables y gráficos con datos fuente, notas y validación visual — no una captura por slide. |
| WRITE-01 | P1 | Edición localizada con intención | Separar corregir/reescribir/resumir/desarrollar/criticar; conservar fragmentos protegidos, registrar cambios de significado. |
| WRITE-02 | P1 | Canon y continuidad | Memoria de personajes/lugares/cronología/relaciones/hechos con fuente/capítulo; distinguir canon, borrador y alternativa. |
| WRITE-03 | P1 | Estilo editable y no invasivo | Reusar el aprendizaje de estilo existente (`existing_anchor`: "Aprender un estilo"); versionar reglas con ejemplos/prohibiciones/ámbito; A/B sobre texto de prueba. |
| WRITE-04 | P1 | Sesiones largas y alternativas | Esquemas/capítulos con referencias estables, objetivos de escena, resúmenes con fuente; ramas editoriales independientes. |
| WRITE-05 | P2 | Integración sin duplicación | Adaptador de proyecto para importar/exportar manifiestos/capítulos/entidades/recursos de Writer's Hoard; resolver conflictos y versiones. |
| MEDIA-02 | P1 | Recetas versionadas y verificadas | Workflows ComfyUI aprobados con hash, nodos requeridos, licencias, entradas tipadas (`existing_anchor`: `config/media_workflows/`); preflight antes de cargar modelos. |
| MEDIA-03 | P1 | Edición no destructiva de imágenes | Capas, máscaras y referencia original; guardar proyecto de edición además del resultado plano; coordenadas/color/alpha preservados. |
| MEDIA-04 | P1 | Colas de render y recuperación | Jobs con IDs externos, estados y reconciliación; descargar con integridad; no duplicar render tras perder una conexión. |
| MEDIA-05 | P2 | Audio y vídeo editables | Transcripción con timestamps, separación de pistas, cortes, subtítulos, export SRT/VTT; transformación con FFmpeg acotada. |
| VOICE-01 | P1 | Voz útil con control del usuario | Estados permiso/escuchando/transcribiendo/pensando/hablando (`existing_anchor`: "Voz Jarvis"); push-to-talk por defecto, detección de silencio, cancelación. |
| VOICE-02 | P2 | Interrupción y confirmación por voz | Barge-in con separación de audio generado/capturado, evitar autoescucha; confirmar efectos sensibles por canal visual. |
| MEDIA-06 | P1 | Procedencia y consentimiento | Guardar modelo/versión, licencia, semilla, receta y hashes; identificar contenido sintético; minimizar metadatos sensibles. |
| CONN-01 | P1 | Centro de conexiones con ámbitos | Credenciales en vault del sistema o almacén cifrado, tokens limitados, rotación/revocación, aislamiento por propietario. |
| CONN-04 | P1 | Sincronización y conflictos | ETag/revisión/updated_at, webhooks verificados o polling incremental; tokens de sincronía por ámbito, reconciliación tras desconexión. |
| AUTO-01 | P1 | Scheduler durable y correcto en el tiempo | Recurrencias con IANA/DST, política de misfire, zona por tarea; jobs persistentes con lease y deduplicación por ocurrencia. |
| AUTO-03 | P2 | Flujos visibles y depurables | DAG de pasos con entradas/salidas, condiciones, reintentos, cron/webhook, simulación; secretos por referencia. |
| HW-03 | P1 | Residencia y concurrencia inteligentes | Política LRU con pins y coste de recarga; cola por dispositivo; evitar precargas especulativas sin presupuesto. |
| HW-04 | P1 | Catálogo y descargas fiables | Metadatos por revisión/digest/licencia/cuantización; descargas reanudables con hash, límites de red, eliminación segura. |
| HW-05 | P1 | Perfiles de hardware medidos | Benchmark opt-in por modelo/backend/topología: TTFT, prefill, decode, contexto, memoria, concurrencia; límites térmicos/potencia. |
| HW-06 | P2 | Nodos remotos como servicio controlado | Registrar endpoint/identidad/versión/capacidades/seguridad/salud; tolerar desconexión; separar administración del nodo de inferencia. |
| HW-07 | LAB | Spark + PC + eGPU: laboratorio | Prototipo opt-in; medir reparto de capas/KV/enlaces/carga/latencia; no afirmar RDMA/pooling/failover sin probarlos. |
| OBS-04 | P1 | Reproducción y soporte local | Export de configuración efectiva, versiones, traza saneada, fixtures con consentimiento; modo diagnóstico sin contenido personal. |
| OBS-05 | P1 | Calidad y rendimiento por perfil | Métricas de éxito/intervención humana/loops/tiempo/recursos/coste externo ligadas a modelo+harness; no comparar workloads distintos. |
| UX-03 | P1 | Editar, regenerar y ramificar | Árbol de conversación con mensajes/versiones; regenerar crea alternativa, no borra historial ni repite herramientas externas. |
| PERF-01 | P1 | Frontend incremental | Virtualizar listas cuando compense, memoizar bloques terminados, cargar editores/galerías bajo demanda, limitar logs en memoria. |
| PERF-05 | P1 | Transferencias y adjuntos grandes | Streaming con cuotas, reanudación, hashes incrementales, thumbnails fuera de ruta crítica. |
| OPS-04 | P1 | Portabilidad de proyectos y conocimiento | Export/import versionado de chats/fuentes/memorias/skills/artefactos con referencias relativas; secretos excluidos por defecto. |
| OPS-06 | P1 | APIs y cliente extensibles | OpenAPI versionado, SDK ligero, eventos documentados; compatibilidad hacia atrás, migraciones de configuración, deprecaciones anunciadas. |
| OPS-07 | P1 | Higiene y costes operativos | Rotación de logs, cuotas de blobs, retención de runs, detección de huérfanos y jobs pendientes; no borrar lo referenciado. |
| EVAL-02 | P1 | Ablaciones y coste comparable | Ejecutar mínimo→contexto→verificador→memoria→subagentes cambiando una dimensión; presupuestos y repeticiones con seeds. |
| EVAL-06 | P1 | Promoción controlada de capacidades | Matriz por tupla modelo/backend/plantilla: no probado/experimental/compatible/recomendado; cambiar de estado solo con evidencia vigente. |

## Lote 48 — Web, escritorio, ejecución e investigación P1

WEB-05, WEB-06, DESK-01, DESK-02, EXEC-05, EXEC-06, RES-02, RES-06, LANG-02.
Todos partían de "ausente" (ningún módulo ni prueba los cubría) salvo donde
se indica lo contrario. Todo lo de abajo es backend real, probado contra
código real (rule 7: sin mocks exclusivos — estas rutas no cruzan HTTP, así
que se prueban por inyección de dependencias, el mismo patrón que
`src/browser_actions.py` ya usaba). Studio queda fuera del lote por mandato
explícito: ningún requisito de frontend de esta tabla está implementado aquí.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| WEB-05 | parcial | `src/browser_evidence.py` (nuevo): `ScreenshotCapture`/`build_capture` (resolución, escala, viewport, timestamp, página), `ElementRegion`/`AnnotationPoint` (recorte y anotación en píxeles, nunca una línea de código), `evidence_for_capture` (reusa `EvidenceRef` de `src/contracts`, mismo shape que `web_tools._evidence_for_fetch`), `compare_captures` (antes/después explícito), `is_stale_for_edit` (el criterio de aceptación en sí: una captura de otra URL o con DOM cambiado se marca no válida como mapa de edición) | ninguna | tests/test_p1_web_05_evidence.py (nuevo) | Nada llama todavía a `build_capture` desde el punto real donde se toma un screenshot de navegador (`src/browser_view.after_browser_action`, fichero propio de este lote pero cuyo cambio de firma afectaría a callers ajenos — ver informe) ni desde `desktop_tools._screenshot` (mismo motivo). El shape está listo; el cableado al flujo real de captura es el siguiente paso. |
| WEB-06 | parcial | `src/browser_extraction.py` (nuevo): `paginate` (límites `max_pages`/`max_items`, `truncated`/`reason` explícitos), `detect_restricted_access` (no evade bloqueos), `check_form_fill` (límites de formulario), `select_download_target` (una única ruta de fichero dentro del sandbox, nunca la carpeta), `verify_download`/`DownloadOutcome` (`complete=False` en cualquier descarga parcial o con hash/tamaño distinto), `check_login_reuse` (delega en la política de `src/browser_sessions.py`, no duplica el flag) | ninguna | tests/test_p1_web_06_extraction.py (nuevo) | Igual que WEB-05: la lógica está lista y probada de forma aislada; falta el cableado real en el punto donde `src/tool_execution.py` invoca las acciones de navegador (fuera de este lote salvo EXEC-05/06) para que `paginate`/`verify_download` se llamen desde una tool real. |
| DESK-01 | parcial | `src/desktop_control_session.py`: `set_allowlist`/`get_allowlist`/`clear_allowlist`, `is_window_allowed`/`check_focus_authorized` (el criterio de aceptación: mover el foco a una app no autorizada bloquea la siguiente acción), `enable_audit`/`disable_audit`/`record_action`/`audit_log` (captura antes/después por hash + registro en `data/runtime/desktop-audit.log`). Cableado real (no solo librería) en `src/agent_tools/desktop_tools.py::DesktopTool._execute`: ambas cosas son **opt-in por sesión** — ninguna sesión existente que nunca llame a `set_allowlist`/`enable_audit` cambia de comportamiento (verificado: la suite completa de `tests/test_desktop_tools.py`, que nunca las usa, sigue en verde sin tocar). El indicador visible y el kill-switch de Escape ya existían (`ensure_indicator`/`desktop_control_run`, Windows) y no se tocan. | ninguna | tests/test_p1_desk_01_control.py (nuevo) | El indicador visible (`ensure_indicator`) sigue siendo solo-Windows, como ya era — extenderlo a todas las plataformas exige tocar el arranque del backend Linux/Mac y romperías la mayoría de tests de `test_desktop_tools.py` que usan un `FakeBackend` sin ese handshake; no se ha tocado por seguridad de la suite existente. No hay tool nueva expuesta al modelo para llamar a `set_allowlist`/`enable_audit` — eso exige registrar un tool en `src/tool_capabilities.py`/`src/tool_schemas.py` (ficheros ajenos); ver informe. |
| DESK-02 | parcial | `src/browser_journey_verification.py` (nuevo): `run_journey` (para en el primer fallo, adjunta captura), `JourneySnapshot`/`JourneyStep`/`JourneyVerdict`, `detect_object_leak`/`assert_no_object_leak` (el caso `[object Object]` del criterio de aceptación), `assert_no_console_errors`/`assert_no_network_failures`/`assert_no_accessibility_issues`, `assert_dom_contains` | `studio/checks/*.check.mjs` (existente, ajeno a este lote) | tests/test_p1_desk_02_journey.py (nuevo) | El runner toma un `snapshot_fn` inyectado (mismo patrón que `browser_actions.run_with_precondition`) y está probado con fixtures falsas, tal y como pide `P1_COMUN.md` ("fixtures controladas antes de la instancia real"). Cablearlo contra Studio real (dev server + Playwright) es trabajo de Studio, explícitamente fuera de este lote. |
| EXEC-05 | parcial | `src/tool_execution.py`: `detect_package_manager` (lockfile antes que manifest desnudo), `validate_package_name` (rechaza URLs/paths/metacaracteres de shell — el criterio de aceptación), `plan_dependency_install`/`DependencyInstallPlan` (hash de plan estable e independiente del orden), `plan_already_approved`/`record_plan_approval` (no repite aprobación para el mismo plan), `execute_dependency_install` (nunca instala global: `--target` de proyecto o venv activo para pip, `--prefix`/`--cwd`/`--dir` para npm/yarn/pnpm) | ninguna | tests/test_p1_exec_05_install.py (nuevo) | No hay tool nueva expuesta al modelo (requeriría tocar `src/tool_capabilities.py`/`src/tool_schemas.py`, ficheros ajenos) — hoy `plan_dependency_install`/`execute_dependency_install` son funciones de librería llamables por un caller Python, no un tool_call del modelo. Ver "Cambios necesarios en ficheros ajenos" en el informe. |
| EXEC-06 | parcial | `src/tool_execution.py`: `save_script`/`load_script`/`SavedScript` (versionado real: guardar bajo el mismo nombre incrementa `version`, nunca sobrescribe en silencio), `render_script_command` (renderiza a lista argv, nunca a una cadena de shell — sin inyección posible), `run_saved_script`, `pair_ssh_target`/`resolve_ssh_target`/`verify_ssh_target` (comparación EXACTA de host y fingerprint — el criterio de aceptación: un hostname parecido no hereda confianza, una receta no cambia de máquina en silencio), `run_remote_script` (ssh con `BatchMode=yes`, ningún secreto en el prompt) | ninguna | tests/test_p1_exec_06_scripts_ssh.py (nuevo) | Igual que EXEC-05: sin tool expuesta al modelo todavía (fichero ajeno). El emparejamiento SSH (`pair_ssh_target`) asume que el operador ya validó el fingerprint fuera de banda, tal y como el propio lote acota ("SSH ya emparejado") — no hay descubrimiento/TOFU de claves aquí. |
| RES-02 | existente | `src/deep_research.py`: `classify_stop_reason`/`STOP_REASON_*` (siete causas nombradas: cancelado, presupuesto de tiempo, sin más consultas, saturación de novedad, búsqueda caída, cobertura suficiente según el LLM, máximo de rondas — cableado en cada punto real donde el bucle para hoy), `self.stop_reason` (poblado siempre al terminar `research()`), `stop_rationale_text` (frase legible para un caller ajeno). Cambio de comportamiento real: `_generate_queries` añade una instrucción de cambio de estrategia cuando `self._consecutive_empty_rounds > 0` (el criterio de aceptación: repetir consulta sin evidencia nueva dispara cambio de estrategia). La deduplicación de URLs (`self.urls_fetched`) y de consultas (`self.queries_used`) YA EXISTÍA de una ola previa y ya cumplía "diez URLs repetidas no cuentan como diez fuentes nuevas" — no se dupacó, solo se verifica en el informe. | ninguna | tests/test_p1_res_02_stop_reason.py (nuevo); suite completa `tests/test_deep_research_*.py` y `tests/test_research_resume.py` re-ejecutada en verde (103+15 tests) | `self.stop_reason`/`stop_rationale_text` no se muestran todavía en ninguna UI ni se adjuntan al texto del informe final (deliberado: adjuntarlo al string que `research()` devuelve rompería varios tests existentes que comparan el resultado por igualdad exacta — `tests/test_deep_research_report_in_parts.py`, `tests/test_deep_research_synthesis_resilience.py`). Un caller ajeno (la ruta que expone `research()` por HTTP) debe leer `self.stop_reason` aparte; ver informe. |
| RES-06 | existente | `src/deep_research.py`: `EvidenceQualityProfile`/`DEFAULT_QUALITY_PROFILES` (general/technical/academic/clinical, con `min_sources_per_claim`/`preferred_domains`/`blocked_domains`/`require_date` distintos), `assess_evidence_quality` (el criterio de aceptación: un hallazgo sin fecha nunca gradúa "high" aunque haya diez fuentes; dominios bloqueados se excluyen del conteo, no se penalizan solamente), `DeepResearcher(quality_profile=...)` resuelve el perfil por nombre (fallback a "general" si no existe) y `self.evidence_quality` se calcula siempre al terminar `research()` | ninguna | tests/test_p1_res_06_evidence_quality.py (nuevo) | `self.evidence_quality` no se muestra en ninguna UI ni se adjunta al informe (mismo motivo de compatibilidad que RES-02). No hay export de bibliografía con metadatos (frontend, ajeno a este lote). |
| LANG-02 | existente | `services/search/lang_normalize.py` (nuevo, exportado desde `services/search/__init__.py`): `strip_diacritics` (1 carácter de entrada → 1 de salida, garantía que hace posible el criterio de aceptación), `find_all_normalized`/`locate_normalized`/`contains_normalized` (offsets del texto ORIGINAL, nunca del plegado), `detect_query_language` (reusa `src.research_citations.language_signal`, no duplica detector), `multilingual_query_variants`/`should_try_english_variant`. `src/reply_language.py::locate_for_edit` (edición: localiza texto tolerando tildes/mayúsculas y devuelve offsets del documento sin tocarlo) | ninguna | tests/test_p1_lang02.py (nuevo) | Nada en `services/search/providers.py` (que sí vive bajo `services/search/*`) llama todavía a `multilingual_query_variants`/`should_try_english_variant` desde el flujo real de búsqueda — se dejó sin tocar deliberadamente: ese fichero fija `language: "en"` en rutas ya delicadas (anti-spam de "news") y tocarlo sin una auditoría propia se salía del presupuesto de este lote. Glosario de proyecto y "preservar términos técnicos al traducir" son requisitos de frontend, ajenos a este lote. |

## Lote 45 — Context/Memory P1 (CTX-04/05/06, MEM-03/04, IDX-05/06, PERF-03)

Todos partían de `status: "propuesto: auditar antes de implementar"` en `backlog.json`, sin fila previa en este mapa. Auditoría previa (rule "reusar autoridades"): `context_engine/cache.py::on_event`/`note_event` (§1.7) ya existía pero **sin llamador de producción** — CTX-04 lo cablea de verdad, no lo duplica; `context_engine/ranking.py::validate` ya tenía un check de "stale" vía `meta.current_revision` y `explicit_refs` ya existía como "fuente explícita prioritaria" — CTX-05 añade solo la mitad que faltaba (exclusión); `memory_engine.py` ya tenía `type="decision"` en su vocabulario sin escritor/lector (MEM-04) y `memory_curator._invert` ya usaba el mismo umbral de repetición que MEM-03 reutiliza (`INVERT_MIN_HARMFUL`) en vez de inventar uno nuevo; `bg_monitor.is_paused_for_resource_pressure` ya estaba cableado en `bg_jobs.py` para PERF-04 — IDX-06 reusa la misma guarda, verbatim, para `code_index.py`.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| CTX-04 | existente | `src/context_source_versions.py` (nuevo): `record_seen`/`revoke`/`is_accessible`/`invalidate` (tabla `source_revisions`, ámbito owner+project+ref); `invalidate()` llama a `context_engine/cache.py::on_event("source_version_changed", …)`, evento ya existente en `EVENT_PREFIXES` pero sin emisor real hasta ahora. `context_engine/cache.py`: `compose_cache_key` (clave por model/tokenizer/template_version/policy_version/user/source_version/params, sha256) y `cached_or_compute` (hit/miss real contra `WorkingSet`, ya scoping por `scope`) | `POST /api/context-engine/cache/invalidate` (botón "Liberar la caché" en el panel Cache de `Context.tsx`) | tests/test_p1_ctx_04_cache_invalidation.py (8); revert-check: desactivar la llamada a `invalidate()` en `record_seen()` tumba `test_edit_invalidates_scoped_cache_only` | `record_seen`/`revoke` no tienen todavía un llamador real desde el punto donde un documento se reindexa o un permiso se revoca (esos puntos están en ficheros ajenos — ver informe final). La primitiva y su prueba de invalidación de cache están completas y cableadas; falta el emisor automático. |
| CTX-05 | existente | `src/context_selection.py` (nuevo): `set_control`/`unset_control`/`list_controls`/`policy_overrides` (tabla `selection_controls`, ámbito global→proyecto→sesión, nunca cruza sesión/proyecto). `context_engine/contracts.py::ContextPolicy` gana `excluded_refs`/`excluded_prefixes`; `OMISSION_REASONS` gana `"user_excluded"`. `context_engine/ranking.py::validate` rechaza (no recuperable) una ref excluida o bajo un prefijo excluido, salvo que esté en `explicit_refs` (una fuente fijada gana a una exclusión de alcance más amplio). `context_engine/wiring.py::build_request` (la fábrica real que usa `agent_loop`) fusiona `policy_overrides()` en cada `ContextRequest` — cableado de producción, no solo librería | `GET/POST/DELETE /api/context-engine/selection`; panel «Recuperación selectiva» en `Context.tsx` (excluir fuente/carpeta, fijar fuente, por proyecto) | tests/test_p1_ctx_05_selection_controls.py (9), tests/test_p1_ctx_routes_health_selection_cache.py (5); revert-check: quitar el bloque de exclusión en `ranking.validate()` tumba 3 tests (incl. `test_end_to_end_through_build_request`) | El "no borra el archivo fuente" del criterio de aceptación se cumple por construcción (la exclusión solo afecta a `ranking.validate`, nunca toca el fichero); no hay UI para excluir/fijar a nivel de sesión (solo proyecto), porque `Context.tsx` no tiene selector de sesión — sería un añadido de UI, no de backend. |
| CTX-06 | existente | `src/context_health.py` (nuevo): `duplicate_ratio`/`stale_evidence`/`health` (combina `context_ledger.build_ledger`/`evidence_index`/`verify_read_evidence`, ya existentes, con tres señales deterministas — ninguna llamada a otro LLM) y `reconstruct_task` (objetivo/restricciones/cambios/siguiente acción, reutilizando `context_compactor._content_as_text`/`extract_protected_strings`) | `POST /api/context-engine/health`, `POST /api/context-engine/reconstruct-task` | tests/test_p1_ctx_06_context_health.py (10), tests/test_p1_ctx_routes_health_selection_cache.py (5) | Sin UI en `Context.tsx` para el "aviso accionable" / botón "reconstruir tarea" del criterio frontend — la ruta HTTP está lista y probada, el botón queda pendiente por presupuesto de tiempo del lote (ver Limitaciones del informe). |
| IDX-05 | existente | `src/document_processor.py`: `pdf_page_evidence_ref` (locator `kind="page"`, hash sha256 del texto exacto de esa página, `EvidenceRef` de `src/contracts/tool.py` reusado sin cambios) y `_process_pdf(path, evidence_out=None)` — parámetro aditivo, la cadena devuelta es byte-idéntica sin él (verificado: `test_process_pdf_without_evidence_out_is_unchanged`), solo un caller que pase `evidence_out=[]` recibe las refs | ninguna | tests/test_p1_idx_05_evidence_refs.py (5) | Solo cubre PDF (páginas); el criterio de aceptación también menciona celda/símbolo/bbox/rango temporal — esos locators ya existen en `EVIDENCE_LOCATOR_KINDS` (`cells`, `time`, etc.) pero ningún extractor de `document_processor.py` los produce todavía; ningún caller real (`document_processor`'s own callers, fichero ajeno a esta lista) pasa `evidence_out` aún, así que las refs no llegan todavía a una respuesta del modelo. |
| IDX-06 | existente | `src/code_index.py::refresh(..., pause_on_pressure=True)`: para antes de escanear si `bg_monitor.is_paused_for_resource_pressure()` (misma guarda de PERF-04, sin duplicar), y revisa cada `PRESSURE_CHECK_EVERY=25` ficheros durante el escaneo (corta sin perder lo ya escrito); `_copy_indexed_by_hash` (deduplicación por contenido: un fichero con el mismo hash que otro ya indexado copia sus símbolos en vez de reparsear); `cleanup_orphaned_workspaces` (limpia filas de un workspace borrado, nunca toca uno que sigue existiendo); `refresh_async` (envuelve `refresh` en `asyncio.to_thread`, no bloquea el event loop — demostrado con un ticker concurrente) | ninguna | tests/test_p1_idx_06_background_indexing.py (8); revert-check por separado de presión (`test_refresh_pauses_under_pressure_and_does_not_scan`, `test_pressure_trip_mid_scan_stops_without_losing_earlier_writes`) y de dedup (`test_identical_content_at_two_paths_is_deduped`) | `routes/code_index_routes.py::POST /{project_id}/reindex` sigue llamando al `refresh()` síncrono, no a `refresh_async` — ese fichero es ajeno a este lote (no está en PROPIOS); ver "Cambios necesarios en ficheros ajenos" en el informe. Sin panel de almacenamiento en Studio (tamaño de originales/derivados/índices) — sería UI nueva en una pantalla no asignada a este lote (`code_index` no tiene screen propio en `Context*.tsx`/`Memory*.tsx`). |
| MEM-03 | existente | `src/memory_failures.py` (nuevo, sobre `memory_engine.py` — sin store paralelo): `register_failure` (dos puertas obligatorias: prueba de regresión adjunta Y (repetición ≥ `MIN_OCCURRENCES_TO_PROPOSE=3`, el mismo umbral que `memory_curator.INVERT_MIN_HARMFUL`, O `confirm=True`) antes de promocionar de `status="deprecated"` — invisible a `pack()`/`search()` — a `status="anti_pattern"` activo), `list_candidates`, `sweep_expired` (degrada sin borrar, misma postura que el resto de `memory_curator.py`) | `POST/GET /api/memory-engine/failures`, `POST /api/memory-engine/failures/sweep`; panel «Candidatos a fallo» en `Memory.tsx` (firma, resumen, referencia a prueba, contador de ocurrencias) | tests/test_p1_mem_03_failure_candidates.py (8), tests/test_p1_mem_routes_failures_decisions.py (4) | `existing_anchor` del backlog (`src/immune_system/`) es un directorio ajeno a PROPIOS (no casa con el glob `src/memory_*.py`) — se auditó (`service.py`/`persistence.py`) y no comparte vocabulario con `memory_engine.py`; se decidió reutilizar `memory_engine`/`memory_curator` en su lugar por ser la autoridad ya usada por MEM-01/MEM-02, en vez de escribir sobre un sistema paralelo. `sweep_expired` no tiene todavía un disparador periódico (cron/bg_jobs, fichero ajeno). |
| MEM-04 | existente | `src/memory_decisions.py` (nuevo, sobre `memory_engine.py`, reutiliza `type="decision"` ya declarado en `TYPES` desde MEM-01 y nunca escrito): `record_decision` (texto, alternativas descartadas, alcance, refs a artefactos), `invalidate_decision` (nunca borra — pone `status="deprecated"`, misma semántica que el resto del store, y registra motivo/sustituto en `provenance`), `list_decisions` (timeline, invalidadas opcionales) | `POST/GET /api/memory-engine/decisions`, `POST /api/memory-engine/decisions/{id}/invalidate`; timeline «Decisions» en `Memory.tsx` (alternativas descartadas, artefactos, invalidar) | tests/test_p1_mem_04_decisions.py (6), tests/test_p1_mem_routes_failures_decisions.py (4) | `existing_anchor` del backlog (`src/project_context/`) es un directorio ajeno a PROPIOS — se auditó (`service.py`/`models.py`) y gestiona indexación de proyecto, no decisiones técnicas con alternativas; MEM-04 se implementó como módulo nuevo sobre `memory_engine.py` en vez de tocar ese fichero ajeno. El "cambiar de modelo conserva el motivo" del criterio de aceptación se cumple porque la decisión vive en el store de memoria (independiente de sesión/modelo), pero ningún caller ajeno lee todavía `list_decisions` al construir el contexto de una sesión nueva — eso es responsabilidad de `context_engine/wiring.py` (si procede) o del propio flujo de cambio de modelo, ambos fuera de lo tocado aquí salvo por la exposición HTTP. |
| PERF-03 | existente | `context_engine/cache.py::compose_cache_key`/`cached_or_compute` (claves por model/tokenizer/template_version/policy_version/**user**/source_version/params — la fuga entre usuarios que pide el criterio de aceptación queda excluida por construcción: dos usuarios nunca comparten clave); invalidación real por fuente (`context_source_versions.invalidate`, CTX-04) y por evento (`on_event`, ya cableado) | `POST /api/context-engine/cache/invalidate` (mismo botón "Liberar la caché" que CTX-04 — misma primitiva, dos requisitos que comparten el mismo mecanismo por diseño, no por duplicación) | tests/test_p1_ctx_04_cache_invalidation.py (comparte cobertura con CTX-04: son la misma primitiva) | Cache de prefijos a nivel de backend LLM (KV-cache del propio modelo) está fuera de alcance de `context_engine/*` — ese nivel vive en el runtime de inferencia, fichero ajeno. `cached_or_compute` está lista para envolver cualquier cómputo caro de `compiler.py`, pero ningún caller de producción la usa todavía para el *response* completo (solo la primitiva y su test existen) — ver Limitaciones del informe. |

## Lote 46 — Planificación, agentes y herramientas P1

PLAN-02, PLAN-04, PLAN-05, PLAN-06, VER-03, TOOL-02, TOOL-05, TOOL-06, SEC-08. Todos partían de `status: "propuesto: auditar antes de implementar"`. La auditoría (rule "reusar autoridades") encontró infraestructura ya muy madura para varios IDs — `src/council/*` (13k líneas: contratos, ledger, orquestador, síntesis) para PLAN-04; `src/completion_engine/*` ya cableado en `src/agent_loop.py` para PLAN-06; `src/auto_review.py` (review_gate, ground_findings, severidad+evidencia) para VER-03; `src/tool_index.py` (RAG + keyword hints + fallback de dos niveles) para TOOL-02 — así que el trabajo de este lote fue en su mayoría **cerrar el hueco concreto que el criterio de aceptación señala**, no reconstruir el sistema. `src/branching_futures/` (ancla de PLAN-05) no está en PROPIOS de este lote — se auditó y su `commit()` ya es fail-closed (`raise BranchingError(...)`, ninguna rama llega nunca a State Mirror), así que el criterio de aceptación se cumple por construcción sin tocar ningún fichero.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| PLAN-02 | parcial | `src/agent_tools/subagent_tools.py`: `parse_delegation_args` acepta `criteria`/`success_criteria`/`acceptance` por tarea (aditivo — una tarea sin `criteria` produce el mismo dict de siempre, byte a byte); `verify_criteria` (nueva, determinista: sin llamada a modelo) comprueba las criteria contra la EVIDENCIA del run (`tool_calls`, `mutations`, `error`) — cero tool calls es "unmet" siempre (el criterio de aceptación: texto solo no basta), un criterio que nombra un fichero exige que ese fichero esté en `mutations`; `SubagentRun.report()` degrada `status` de `"done"` a `"partial"` cuando hay criteria y `verify_criteria` no las da por cumplidas, añadiendo `criteria_check` al reporte (condicional: sin criteria, el reporte es idéntico al de antes). El presupuesto por hijo (`max_rounds`/`timeout_s`) YA EXISTÍA (no se tocó); no se cableó `src/autonomy_budget.Budget`/`Ledger` per-child porque el run de un subagente hoy vive fuera del bucle que sabe leer un `Budget` (`src/agent_loop.py`, ajeno salvo PLAN-06) — ver informe. | ninguna | tests/test_p1_plan02_delegation_contract.py (9) | `src/dispatch.py` (Fable/API externa) NO se tocó: ya tiene una autoridad más fuerte para el mismo problema — checkpoint+diff+`src/prove.py` (`proved`/`partial`/`unproved`/`contradicted`) — así que añadir `criteria` ahí sería un segundo mecanismo para lo mismo (rule 4); se documenta, no se duplica. `routes/dispatch_routes.py` no ganó rutas nuevas: no se encontró un hueco de comportamiento que una ruta nueva cerrase sin duplicar `dispatch.py`. |
| PLAN-04 | existente | `src/council/synthesis.py`: `evidence_weighted_support(decision, messages)` (nueva) — por cada `supporter` de una `CouncilDecision`, lee el `metadata.evidence_refs` del mensaje MÁS RECIENTE de ese autor sobre esa decisión (campo abierto que `CouncilMessage` ya tenía, ningún cambio de esquema); un voto sin evidencia pesa `NO_EVIDENCE_WEIGHT=0.5`; `unanimous_without_evidence=True` es exactamente el caso del criterio de aceptación (tres modelos, misma afirmación, sin fuente). `_row()` ganó `decision_id`/`metadata` en su lista de fallback (aditivo, más información capturada, nada se pierde) | ninguna | tests/test_p1_plan04_council_evidence.py (4) | `evidence_weighted_support` no se llama todavía desde `src/council/orchestrator.py` ni desde `routes/council_routes.py` — no se encontró el punto donde el orquestador decide "consenso alcanzado" sin ya auditarlo a fondo (fichero de 1537 líneas) y forzar la llamada sin esa auditoría se salía del presupuesto; queda como primitiva lista y probada, igual que varias filas del Lote 45. |
| PLAN-05 | existente | Nada tocado — `src/branching_futures/service.py::commit()` (línea ~325) ya es fail-closed: siempre `raise contracts.BranchingError(...)`, "no se cambió ningún estado". Por construcción, ninguna rama (seleccionada o descartada) llega hoy a State Mirror ni a memoria confirmada — el criterio de aceptación se cumple sin código nuevo. | ninguna | ninguno nuevo (nada que probar: no hubo cambio) | `src/branching_futures/` no está en PROPIOS de este lote, así que no se auditó `fuse()`/`evaluate()`/`select()` a fondo ni se implementó un committer real. Cuando exista un "adaptor de ejecución común" que sí escriba estado real, `commit()` deberá enrutar el resultado SOLO de la rama `select()`-ada — nunca de una descartada — y eso deberá verificarse contra `src/state_mirror` en ese momento; hoy no hay nada que verificar porque no hay escritura real. |
| PLAN-06 | existente | `src/agent_loop.py` (única función tocada en este fichero, por mandato del lote: "SOLO PLAN-06"): `_split_requirements` (nueva, determinista — bullets/numeración como items separados, si no hay ≥2 la instrucción entera es un único requirement) y `_plan_coverage_gap` (nueva) llaman a `src.plan_state.coverage` (PLAN-01, ya existente, reusada sin cambios) contra el ÚLTIMO `update_plan` de este turno. Justo antes del `break` que cierra un turno sin más tool calls: si hay plan (`_latest_plan_update`) y la Completion Engine (ya cableada) no decidió ya seguir, y `plan_state.coverage` encuentra un requirement sin ningún step que lo cubra, el turno NO cierra — un mensaje `system` nombra solo los requirements no cubiertos (nunca inventa alcance nuevo) y se pide seguir, acotado a `_PLAN_COVERAGE_MAX_ROUNDS=1` (una sola vez, nunca un bucle). Un turno sin `update_plan` esta ronda es byte-idéntico a antes (la comprobación entera está guardada tras `if _latest_plan_update`). | ninguna | tests/test_p1_plan06_completion_gate.py (6); revert-check: quitar el bloque `_plan_coverage_gap`/`_split_requirements` tumba las 6 con `AttributeError` | El evento `plan_coverage_gap` se emite por SSE pero no hay tarjeta en Studio que lo muestre (frontend, fuera de este lote). No se probó end-to-end contra el generador completo de `run_agent_loop` (async generator de 9900 líneas, sin harness de integración ligero) — se probó `_plan_coverage_gap`/`_split_requirements` de forma aislada, que es donde vive toda la lógica nueva; el punto de cableado (justo antes del `break`) se revisó a ojo y por `py_compile`, no con una ejecución end-to-end. |
| VER-03 | existente | `src/auto_review.py`: `resolve_reviewer` gana `available_models` (opcional, aditivo) — con el ajuste `"same"`, si se pasan varios modelos disponibles elige uno DISTINTO al que escribió el cambio en vez de reusar el mismo (`_distinct_reviewer`, nueva); sin `available_models` (ningún caller existente lo pasa hoy) el comportamiento es exactamente el de antes. El resto del criterio ("revisor sin permisos de escritura, checklist tipada, veredicto con evidencia, no bloquea por preferencias de estilo") YA EXISTÍA: `review_turn` no llama a ninguna tool (pase sin herramientas por diseño — docstring del módulo), `_prompt` prohíbe explícitamente comentar estilo/naming/formato, `ground_findings` degrada a "warning" (nunca bloquea) cualquier hallazgo sin evidencia literal en el diff o la petición, y `review_gate` ya exige diff real + verificación real antes de sellar una revisión. | ninguna | tests/test_p1_plan_ver03_review.py (5) | `available_models` no tiene todavía un caller real que enumere los modelos servidos y se lo pase a `resolve_reviewer` — ese punto de llamada vive en `chat_routes.py`/`chat_handler.py` (ficheros ajenos, explícitamente prohibidos: "NO toques chat_routes"); se documenta el cambio necesario en el informe. "Checklist tipada" no se añadió como campo nuevo porque `findings[].evidence` (cita literal del diff) ya cumple la función de "reproducción" que pedía el criterio y añadir una taxonomía de categorías sin un consumidor real habría sido especular. |
| TOOL-02 | existente | `src/tool_index.py`: `_FILE_EDIT_RE`/`_FILE_EXT_RE` (nuevas, bilingües ES/EN) fuerzan `{"read_file","write_file"}` en `get_tools_for_query` cuando la consulta combina un verbo de edición/lectura y una extensión de fichero — estructural, igual que `_WEB_RE`/`_SCHEDULE_RE` ya existentes, así que funciona SIN el lane semántico (el criterio de aceptación literal: "una petición española de editar .txt mantiene acceso a lectura/escritura aunque falle el ranking semántico" — probado forzando `retrieve()` a devolver `[]`). El resto de TOOL-02 (ranking híbrido, límite de esquemas visibles, fallback de dos niveles) YA EXISTÍA (docstring del módulo, `src.two_tier_search`). El punto de extensión para "fijar/excluir herramientas por proyecto" también existe ya (`get_tools_for_query(always_include=...)`) — es el caller (`src/agent_loop.py`, fuera del mandato "SOLO PLAN-06") quien decidiría poblarlo desde un ajuste por proyecto. | ninguna | tests/test_p1_tool02_discovery.py (4); revert-check: quitar el bloque `_FILE_EDIT_RE`/`_FILE_EXT_RE` tumba las 2 pruebas ES/EN con `AssertionError` | No hay UI para fijar/excluir tools por proyecto ni para "ver por qué se eligieron" (ambos, requisito de frontend explícito en el backlog, fuera de mandato). |
| TOOL-05 | ausente→parcial | `src/mcp_manager.py` (nuevo dentro del fichero, sección "TOOL-05"): `make_sampling_callback`/`make_elicitation_callback`/`make_list_roots_callback`, cableados en los 3 puntos donde se construye `ClientSession` (stdio/SSE/HTTP) vía `_open_client_session` (con fallback a la llamada de 2 argumentos si el destino — incluidos los `ClientSession` falsos de tests existentes — no acepta los kwargs nuevos, para no romper compatibilidad). `_SamplingQuota` mide por `server_id` (nunca compartido entre servidores — el criterio de aceptación: "no puede reutilizar una autorización de otro destino"); en cuota, sampling se rehúsa con una razón distinta de "not supported" (no hay ruta a un modelo real todavía — se documenta, no se finge). Elicitación es real: pasa por `src.question_store` (la única autoridad de pregunta pendiente del repo — AskUserTool ya la usa, ningún store paralelo) con tope de salientes por servidor, tamaño de mensaje acotado y timeout duro que cancela la pregunta. `list_roots_callback` devuelve el workspace activo como único root, advisorio (no cambia el sandbox de `src/tool_execution.py`). | ninguna | tests/test_p1_tool05_mcp_limits.py (8); revert-check: quitar la sección TOOL-05 tumba las 8 con `AttributeError` | Sampling no llega a invocar un modelo real (no hay contexto owner/turno alcanzable desde un callback de conexión MCP, que vive a nivel de proceso, no de turno) — queda metrado y rehusado con razón auditable; conectar un modelo real es el siguiente paso natural y no pierde nada de lo aquí construido (solo cambiaría el último `return`). La elicitación usa `session_id=f"mcp:{server_id}"` (sintético: una conexión MCP no está atada a un chat) — no hay UI que muestre "qué servicio pregunta y qué datos recibirá" (frontend, fuera de mandato) aunque la pregunta en sí ya llega a `question_store` y sería visible por cualquier UI que ya lea preguntas pendientes. |
| TOOL-06 | ausente→parcial | `src/skill_governance.py` (nuevo): `gate_mutating_use` (el criterio de aceptación literal: una skill `obsolete`/`deprecated`/`superseded` se rehúsa ANTES de una acción mutante — `apply`/`run`/`execute`/`invoke`/`promote`/`publish` — nunca antes de una lectura, y devuelve `fallback_version` con la versión previa no obsoleta si `history` la tiene); `privilege_escalation_flags`/`as_context_message` (escaneo determinista de frases de escalada de privilegio — "ignore instructions", "disable approval"... — y un mensaje con `role: "context"`, nunca `"system"`/`"instruction"`, con la procedencia como campo estructurado aparte del texto); `validate_promotion` (promoción desde `teach_mode`/`demonstration`/`teacher-escalation` a un estado tipo "published" exige `reviewed=True` explícito — nunca inferido); `record_version`/`evaluation_manifest` (versión siguiente derivada del hash del contenido, y qué le falta a una skill para ser evaluable: `when_to_use`, `verification`, `version`). Todo opera sobre los mismos campos que `services/memory/skill_format.Skill` ya tiene (`version`, `status`, `verification`, `requires_toolsets`) — sin store paralelo. | ninguna | tests/test_p1_tool06_skill_governance.py (13) | `services/memory/skills.py` y `routes/skills_routes.py`/`routes/teach_mode_routes.py` no están en PROPIOS de este lote, así que `gate_mutating_use`/`validate_promotion` no tienen todavía un caller real — son la autoridad lista, no el cableado; el vocabulario `obsolete`/`deprecated`/`superseded` tampoco existe aún en `Skill.status` (hoy solo `draft`/`published`) — añadirlo es un cambio de fichero ajeno, descrito en el informe. |
| SEC-08 | ausente→parcial | `src/security_policy.py` (nuevo, como pedía el lote). `PROFILES` (`read`/`local_edit`/`supervised_agent`/`bounded_automation`) construidos sobre `src.tool_capabilities.ToolEffect` — `PROFILES["read"]` es literalmente `autonomy_budget.READ_ONLY_ALLOWED_EFFECTS` (reusado, no redefinido). `evaluate()` decide permiso-antes-que-consentimiento (mismo orden que `completion_engine.scope`), y toda decisión lleva motivo y queda en `audit_log`/`export_audit` (firma HMAC-SHA256 opcional vía `FAUSTUS_AUDIT_SIGNING_KEY`, "cuando el despliegue lo requiera"). `ConsentStore`: la clave es `(plugin_id, tool_name)`, NUNCA `plugin_id` solo — `check()` compara los efectos declarados AHORA con los concedidos, y un efecto añadido invalida solo esa clave; el criterio de aceptación exacto (una actualización con más permisos invalida el consentimiento anterior sin bloquear tools no afectadas) se cumple por construcción: el registro de una tool hermana nunca se lee ni se escribe al comprobar otra. Efectos admin/destructivos exigen aprobación en todo perfil que los permita (`ALWAYS_APPROVAL_EFFECTS`), y una lectura ya autorizada por el perfil nunca la pide (fatiga de confirmación nombrada explícitamente en el backlog). | ninguna | tests/test_p1_sec08.py (17) | No hay caller real todavía que llame a `evaluate()`/`ConsentStore` desde el punto donde Faustus instala/actualiza un plugin o servidor MCP (`src/mcp_manager.py` conecta servidores pero no gestiona "plugins" con metadatos de permisos declarados — ese concepto no existe hoy en `src/database.py::McpServer`, fichero ajeno); la primitiva está completa y probada, el cableado de producción es el siguiente paso. Persistencia del log de auditoría es solo de proceso (`deque` acotado) — un despliegue que necesite sobrevivir un reinicio debe persistir el resultado de `export_audit()` por su cuenta; no se creó un store nuevo para esto (rule 4) a falta de una autoridad de persistencia genérica ya existente que reutilizar.


## Lote 47 — Artefactos y edición avanzada P1

ART-02, ART-03, ART-04, ART-05, ART-07, ART-08, EDIT-05, EDIT-06. Todos
partían de `status: "propuesto: auditar antes de implementar"`. A diferencia
de los lotes 45/46/48, aquí la mayoría de los requisitos SÍ llegan hasta la
ruta HTTP real (`routes/document/document_routes.py`, `routes/artifact_routes.py`)
— el hueco que queda es más estrecho: una librería sin exponer (ART-04) y una
tool sin registrar (EDIT-06).

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| ART-02 | existente | `src/document_actions.py::analyze_docx_preservation` — `POST /api/documents/analyze-docx` (`routes/document/document_routes.py`, línea ~1213) sube un `.docx`, lo analiza y borra el temporal en un `finally`. | ninguna | tests/test_p1_art_02_docx_preservation.py (6) | Ruta real, probada de extremo a extremo contra `TestClient` (rule 7). |
| ART-03 | existente | `src/document_actions.py::detect_page_overflow` — `GET /api/document/{doc_id}/visual-check` (misma fichero, línea ~1320). | ninguna | tests/test_p1_art_03_visual_check.py (5) | Ruta real; comparte fichero con `render_page_png` (`/api/document/{doc_id}/page/{page_no}.png`), ajeno a este lote. |
| ART-04 | parcial | `src/spreadsheet.py` (nuevo): `import_csv_rows`/`export_csv_rows` (dialecto Excel), `safe_csv_value` (bloquea inyección de fórmula — `=`/`+`/`-`/`@` al inicio se escapan), `read_workbook_preview`/`write_range` (openpyxl, celdas tipadas: número/fecha/fórmula/texto vía `classify_cell_text`). | ninguna | tests/test_p1_art_04_spreadsheet.py (14) | Ninguna ruta HTTP llama todavía a estas funciones — no hay `routes/*spreadsheet*` ni referencia desde `routes/document/document_routes.py`. La librería está lista y probada (14 tests); falta el endpoint (`POST /api/documents/spreadsheet/...` o similar) y, si se quiere exponer al modelo, la tool correspondiente — ambos, siguiente paso. |
| ART-05 | existente | `src/artifact_store.py::analysis_provenance` (hash de script + inputs) — `GET /api/artifacts/{id}/provenance` (`routes/artifact_routes.py`, línea 93). | ninguna | tests/test_p1_art_05_analysis_provenance.py (4) | Reusa `ExecutionResult`/`Provenance` de `src.contracts` (rule 4) — sin store paralelo. |
| ART-07 | existente | `src/document_actions.py::split_pdf`/`merge_pdfs` (presupuesto de páginas/ficheros, rehúsa ANTES de escribir si se excede) — `POST /api/documents/pdf/split` y `POST /api/documents/pdf/merge` (`routes/document/document_routes.py`, líneas ~1213-1260). | ninguna | tests/test_p1_art_07_pdf_split_merge.py (9) | Ambas rutas reales, con limpieza de temporales en `finally`. |
| ART-08 | existente | `src/artifact_catalog.py::recent` (filtros `project_id`/`session_id`/`kind`/`q`/`since`/`until`) — `GET /api/artifacts` y `DELETE /api/artifacts/{id}` (`routes/artifact_routes.py`). | Panel Library (ajeno a este lote, ya consume la misma ruta). | tests/test_p1_art_08_library.py (3) | Búsqueda por facetas real contra `TestClient`. |
| EDIT-05 | existente | `src/agent_tools/filesystem_tools.py`: `record_edit_history`/`restore_edit_history` (verifica hash antes de restaurar — una snapshot manipulada se rehúsa), `cleanup_temp_files`/`restore_trashed_files` (solo toca ficheros registrados, nunca un fichero de usuario no registrado). Cableado real: `EditFileTool`/`WriteFileTool` ya llaman a `record_edit_history` en cada escritura con éxito (best-effort, nunca afecta el resultado de la tool). | ninguna | tests/test_p1_edit_05_history_cleanup.py (7) | Cableado de producción real (no solo librería) — cada escritura vía las tools del agente ya deja historial. |
| EDIT-06 | parcial | `src/refactor_tools.py` (nuevo): `plan_rename`/`apply_rename` (renombrado de identificador con `_identifier_pattern` — solo coincidencias de identificador completo, nunca una subcadena dentro de otro nombre), `RenameResult` (por-fichero: líneas tocadas, antes/después). | ninguna | tests/test_p1_edit_06_refactor.py (5) | Ni tool ni ruta HTTP la exponen — no aparece en `src/agent_tools/__init__.py`, `src/tool_schemas.py` ni ninguna ruta bajo `routes/`. La primitiva está lista y probada; registrarla como tool del agente (o una ruta manual) es el siguiente paso, igual que EXEC-05/06/TOOL-05/06 del Lote 46. |

## Lote 49a — UX, ajustes y actividad P1

UX-06, UX-07, UX-09, UX-10, SET-02, SET-04, SET-06, ACT-02, ACT-05. Sin
`status` previo en `backlog.json` más allá de "propuesto: auditar antes de
implementar". De los nueve, tres llegan hasta ruta+Studio con test propio de
este lote (UX-07, UX-10, ACT-02); SET-04/SET-06 resultan **existentes** por
una autoridad que ya vivía en el repo (`/api/doctor`, `routes/changesets_routes.py`
+ `routes/diagnostics_routes.py`, con cobertura en `tests/test_ops_routes.py`)
que `Settings.tsx` ya consume — ninguna fila nueva que inventar, solo
verificar que sigue conectada. Los cuatro restantes (UX-06, UX-09, SET-02,
ACT-05) no tienen ni módulo, ni ruta, ni test, ni marca en el código: el lote
al que se asignaron no dejó nada verificable.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| UX-06 | ausente | ninguna | ninguna | ninguno | "Compositor rico pero rápido" — nada en `Composer.tsx` ni en ningún adaptador lleva su marca; ni un test ni un check lo nombran. Requiere auditoría propia antes de implementar (formato rico, atajos, rendimiento con historial largo). |
| UX-07 | existente | ninguna (frontend puro) | `studio/src/screens/studio/ContextPanel.tsx`/`Composer.tsx` — parseo de menciones `@file`; quitar una mención retira su override de pin/exclude asociado. | tests/test_p1_ux_07_js.py (corre `studio/checks/p1-ux07-context-panel.check.mjs`) | Check en verde contra el fichero real desplegado. |
| UX-09 | ausente | ninguna | ninguna | ninguno | "Acciones personales sin fricción" — sin rastro en el código ni en los tests; requiere definir primero qué "acción personal" cubre (no está en ningún módulo existente). |
| UX-10 | existente | `routes/prompts_routes.py` (CRUD completo + `/render`, `/export/batch`, `/import`) — importar una plantilla con `project_id` obsoleto pide resolución en vez de escribir en silencio en el proyecto que hoy tenga ese id. | Biblioteca de prompts (Studio, ajena a este lote). | tests/test_p1_ux_10_prompts.py, tests/test_p1_ux_10_js.py (`p1-ux10-prompts.check.mjs`) | Ruta real + check de Studio, ambos en verde. |
| SET-02 | ausente | ninguna | ninguna | ninguno | "Selector de modelos informativo" — no hay endpoint ni componente con esta marca; `Settings.tsx` tiene selección de modelo pero no la superficie informativa (coste/latencia/capacidades por modelo) que pide el requisito. |
| SET-04 | existente | `GET /api/doctor` (`routes/changesets_routes.py`) — "qué puede hacer esta máquina, preguntado en vez de asumido": búsqueda, modelos, MCP, disco, cola y modo seguro en una sola tarjeta. | `studio/src/screens/Settings.tsx` (comentario propio: "SET-04: search, models, MCP, disk, queue and safe-mode in one card"). | tests/test_ops_routes.py | Autoridad y UI reales, aunque no nacidas con un test *con nombre* de este lote — se reusa la cobertura de `/api/doctor` ya existente (rule 4: no duplicar). |
| SET-06 | existente | `POST /api/doctor/repair` (`routes/diagnostics_routes.py`) + la misma lectura de `/api/doctor` que SET-04. | `Settings.tsx` (comentario propio: "SET-06: por qué podría no funcionar aún la generación de imágenes... 'a disabled feature explains whether it's missing a model'"). | tests/test_ops_routes.py | Ayuda situada junto al control, no un manual aparte — cumplido por construcción (mismo `/api/doctor` que SET-04, un solo mecanismo para dos requisitos, rule 4). |
| ACT-02 | existente | `routes/notifications_routes.py` — dedupe, preferencias por tipo/canal, horas silenciosas, y el caso de aceptación literal: un reconnect sobre el mismo pendiente no re-notifica veinte veces. | `shell/notifications.ts::emitForNewRuns` — emite un run como mucho una vez por diff de sondeo. | tests/test_p1_act_02_notifications.py, tests/test_p1_act_02_js.py (`p1-act02-notifications.check.mjs`) | Mitad servidor + mitad cliente, ambas en verde. |
| ACT-05 | ausente | ninguna | ninguna | ninguno | "Vista de colas y prioridades" — sin módulo ni ruta ni test; sería una superficie nueva sobre `bg_jobs.py`/`task_scheduler.py` (ajenos), sin auditar todavía. |

## Lote 49b — Workbench y accesibilidad P1

BENCH-01, BENCH-03, BENCH-04, BENCH-05, BENCH-06, A11Y-02, A11Y-03, A11Y-04.
El lote con más cobertura real de los tres reconstruidos: siete de los ocho
IDs quedan con librería pura + wiring real en `SidePanel.tsx`/`Transcript.tsx`,
cada uno probado dos veces (la función pura, vía `studio/checks/*.check.mjs`,
y el cableado en el fichero real desplegado, mismo patrón que
`tests/qa/test_qa_32_preview_malicioso.py`). El Lote 50 completó además el
cableado de BENCH-03 hasta la tarjeta de herramienta (ver "Cableados" más
abajo) y añadió `Preview.tsx` (BENCH-06) al parametrize de QA-32.

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| BENCH-01 | ausente | ninguna | ninguna | ninguno | "Espacio de trabajo persistente" — sin módulo, ruta ni test con esta marca; el resto de BENCH-* (03/04/05/06) ya asumen un workbench existente (`SidePanel.tsx`) pero la persistencia entre sesiones que pide BENCH-01 en sí no está construida. |
| BENCH-03 | existente | `routes/evidence_routes.py::resolve_evidence` — `POST /api/evidence/resolve`, reusa `context_ledger.verify_read_evidence` como única autoridad de hashing (rule 4). | `studio/src/screens/Evidence.tsx` (inspector) + `studio/src/adapters/evidence.ts`. Lote 50: enlazado desde la tarjeta de tool — `Transcript.tsx`'s `ToolRail` muestra un botón "Ver evidencia" por `step.evidenceRefs[i]` que abre `Evidence.tsx` en un `Dialog` desde `Studio.tsx`. | tests/test_p1_bench03_evidence.py, tests/test_p1_bench03_evidence_js.py; Lote 50: tests/test_lote50_wiring.py::test_evidence_refs_wired_from_wire_to_tool_card_button | Antes del Lote 50, `Evidence.tsx` no tenía NINGÚN caller en todo el árbol de Studio (componente huérfano) — ver "Cableados" abajo. |
| BENCH-04 | existente | `routes/workspace_routes.py::save_workspace_file` — un 409 por conflicto de `base_revision` (EDIT-01/Lote 19). | `studio/src/adapters/fileConflict.ts` (comparación a tres bandas) wireado en `SidePanel.tsx`'s FileTab — un 409 abre la vista de conflicto en vez de un error que descarta el borrador. | tests/test_p1_bench04_conflict_js.py, tests/test_p1_bench04_sidepanel_wiring.py | Ambos, en verde. |
| BENCH-05 | existente | ninguna nueva (lee el ChangeSet ya persistido). | `studio/src/adapters/workbenchChanges.ts` (agregador puro) wireado en `SidePanel.tsx`'s PlanAndChanges — diff por fichero del ChangeSet actual. | tests/test_p1_bench05_changes_js.py | En verde. |
| BENCH-06 | existente | ninguna (canvas cliente-only). | `studio/src/components/Preview.tsx` + `studio/src/components/previewSandbox.ts` — `<iframe sandbox={sandboxAttr(...)}>`, nunca `allow-same-origin`, CSP propia (`previewCsp`). | tests/test_p1_bench06_preview.py, tests/test_p1_bench06_preview_js.py; Lote 50: `tests/qa/test_qa_32_preview_malicioso.py` ahora incluye este fichero en su parametrize. | Antes del Lote 50, QA-32 (la prueba general de sandboxing de HTML) no cubría esta cuarta superficie de preview — ver "Cableados" abajo. |
| A11Y-02 | existente | ninguna (frontend puro). | `studio/src/adapters/streamAnnounce.ts::nextStreamAnnouncement` wireado en `Transcript.tsx` — anuncio agrupado (`aria-live="polite"`), nunca token a token, y solo mientras `turn.streaming`. | tests/test_p1_a11y02_stream_js.py, tests/test_p1_a11y02_transcript_wiring.py | En verde. |
| A11Y-03 | existente | ninguna. | `studio/src/styles/base.css` — soporte `prefers-contrast: more`, junto a la densidad/reduced-motion ya existentes. | tests/test_p1_a11y03_contrast.py | En verde. |
| A11Y-04 | existente | ninguna. | `studio/src/adapters/remoteAccess.ts` (clasificador puro) wireado en `SidePanel.tsx` — aviso cuando el acceso remoto no va por HTTPS/túnel + auth. | tests/test_p1_a11y04_remote_js.py | En verde. |
