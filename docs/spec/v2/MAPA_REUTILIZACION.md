# Mapa de reutilización (BASE-01) — P0 contra el código real

Baseline: `master b824057e` (10-09-2026). Auditoría por lectura de código (grep/glob sobre `src/`, `routes/`, `services/`, `core/`, `studio/src/`, `tests/`), sin ejecutar la aplicación. Cada fila cita rutas que existen en ese commit; «existente» exige comportamiento, no nombre de módulo. Regla de Luis que manda sobre todo lo que sigue: **no se pierde ninguna capacidad; se extiende, reorganiza y mejora**.

Leyenda: existente · parcial · ausente · no verificable.

## Resumen (lotes 60-70, cierre)

Recuento real sobre las filas de este fichero tras la integración de los
lotes 60-70 (cableados de la sección A de `INTEGRACION_70.md` dados por
aplicados en paralelo por el Lote 70), calculado por script sobre la tabla
markdown cruzada contra `docs/spec/v2/backlog.json::priority` (no de memoria):

```
total filas en este fichero: 105
filas P0 reales (excluye 6 IDs P1 mantenidos aquí por continuidad histórica
  — MOD-05, WRITE-02, WRITE-04, UX-03, IDX-02, IDX-03, todos con fila propia
  y estado correcto en docs/spec/v2/MAPA_P1.md): 99
  existente: 97
  parcial:    2  (PLAN-01, PLAN-03)
  ausente:    0
```

**PLAN-01 y PLAN-03 son el único hueco real que queda en P0** — ninguno de
los lotes 60-70 los tocó, y ninguno de los dos aparecía en el inventario de
huecos previo a este cierre (`huecos.md` solo cubría 59 IDs P0; estos dos se
detectaron al recontar programáticamente contra `backlog.json` en este
cierre, no por instrucción explícita del encargo). Se dejan `parcial` sin
inventar evidencia:
- PLAN-01 (Plan progresivo y cobertura de requisitos): `src/deep_research.py`
  ya no trunca el brief (RES-01, cerrado), pero el propio `TASK-01` del que
  depende sí se cerró en el Lote 60 — la mitad de PLAN-01 que hereda de
  TASK-01 (plan estructurado, sin truncar) ya está cubierta indirectamente;
  la mitad propia (plan corto por defecto, expandible en Studio) sigue sin
  pantalla dedicada.
- PLAN-03 (Propiedad de recursos y reconciliación): `src/subagent_permissions.py`
  reparte permisos/locks por delegación y ya paraleliza lecturas
  independientes (CALL-04, ola 5); sin visibilidad en Studio de "qué agente
  edita qué fichero" ni resolución de conflictos en UI — ninguno de los
  lotes 60-70 tocó `subagent_permissions.py` ni construyó esa pantalla.

Todas las demás filas parcial/ausente que sí tenía este fichero antes del
cierre (57 IDs P0, listados en `huecos.md`) pasaron a `existente (Lote 6X)`
con evidencia tomada de la sección D de `INTEGRACION_70.md` y verificada por
grep contra el código real de este commit (funciones/rutas citadas
confirmadas existentes: `agent_runs.trace_for_call`, `plan_state.apply_steer`,
`context_budget.budget_for`, `chat_routes._idempotent_replay_stream`,
`core/middleware.py`'s 426 global, `tool_result.ToolResult`,
`command_guard.command_preview`, `subprocess_tools._execution_target`,
`chaos.py`/`/api/ops/chaos`, `output_oracle.verify_artifact`,
`review_state.py`'s `tests_status`/`diff_sha256`, `privacy_routes.py`'s
`/api/privacy/profile`, `project_routes.py`'s `/recent-folders`,
`bg_jobs.py`'s `bg_jobs_max_concurrent`/`cpu_heavy_max_concurrent`,
`mcp_manager.py`'s `mcp_degraded_thresholds`, entre otras — ver cada fila
para el detalle). WRITE-02/WRITE-04/UX-03 (P1, no P0) se resincronizan con
`MAPA_P1.md` en vez de reauditarse aquí; MOD-05 (P1, no P0) igual.

---

Resumen (tras lote 20): parcial 70, existente 22, ausente 7, no 1 (total 100
de 100 P0). Cambios de este lote: MOD-01 (parcial→existente), MOD-02
(ausente→existente, ya estaban implementados en una ola previa y el mapa no
se había actualizado), EDIT-02 (parcial→existente, journal/compensación ya
implementados, cerraba QA-17), EDIT-04 (parcial→existente, coverage/restore
ya implementados, cerraba QA-18).

Resumen (tras lote 29, integración ola 4 — lotes 21/23/24/26/27/28): parcial
70, existente 23, ausente 6, no 1 (total 100 de 100 P0). Cambios de este
lote: MEM-02 (parcial→existente, `forget()`/`correct()` ya existían de una
ola previa sin ruta HTTP — L29 añade la ruta y el test de aceptación),
WEB-04 (ausente→parcial, `src/browser_actions.py` ya existía y probado de
forma aislada de una ola previa sin llamador real — sigue sin cablearse
porque el punto de invocación real, `src/tool_execution.py`, es un fichero
ajeno a este lote). ARCH-01, OBS-01, ACT-03, PERF-04, UX-01, UX-08 revisados
y ampliados con lo que este lote añadió (ver filas); el resto de IDs
listados en L29 (OBS-02, VER-01…06, WEB-01…03, MEM-01/05, IDX-01/04,
HW-01/02, ACT-01/04/06, UX-02/04/05, A11Y-01, LANG-01) no se reauditó contra
el código — sus filas quedan como estaban, sin cambios propios de este lote.

Resumen (tras lote 36, integración ola 5): sin cambio de estado en las filas
tocadas (ambas ya eran ausente/parcial y siguen siéndolo — el hueco raíz de
cada una es más amplio que lo cerrado en este lote), pero su evidencia estaba
desactualizada y ya no describe el código real: ARCH-03 (la duplicación de
`_AGENT_PREAMBLE`/`_API_AGENT_RULES` citada como evidencia está corregida),
WEB-04 (la invocación de acciones de navegador ya pasa por precondición +
readback en `src/tool_execution.py`, cerrando el cableado que L29 dejaba
pendiente). Este lote no reauditó el resto de filas del mapa (~90 IDs) contra
el código — ver Limitaciones en el informe del lote.

Resumen (lote 37, resto de la integración de la ola 5): reaudita contra el
código real las filas que el resto de lotes de la ola 5 (33-36) habían
dejado implementadas pero sin reflejar aquí: TASK-04, UX-04, CALL-04, RES-01/
03/04/05, PLAN-01/03, ART-01, MEDIA-01, CONN-02/03, AUTO-02, OPS-01/02/03,
BASE-02/03, SET-01/03, EVAL-01/03/04, BENCH-02 y ARCH-02 pasan de
parcial/ausente/no-verificable a existente o parcial-con-menos-hueco (ver
cada fila para el detalle exacto de qué se cerró y qué no); SET-03 se
reaudita sin cambio de estado (seguía existente). Este lote además cierra por
sí mismo dos huecos que las filas citaban como abiertos: OPS-05 pasa a
existente (el modo seguro automático de `src/safe_mode.py`, de una ola
previa, quedaba sin cablear en los puntos reales de arranque y de conexión
MCP — este lote añade `safe_mode.record_mcp_connection` en
`src/mcp_manager.py` y `safe_mode.mark_boot_started`/`mark_boot_completed` en
el lifespan de `app.py`) y SET-05 pasa a existente (`Settings.tsx` ahora
confirma con el usuario antes de guardar un cambio de proveedor/modelo por
defecto, usando la vista previa de `/api/setup/provider-change-preview` que
ya existía sin consumidor). No se reauditó el resto del mapa (~75 IDs
restantes) contra el código.

Resumen (lote 43, barrido de P0 parciales): cierra huecos concretos citados
en cinco filas sin cambiar su estado a existente (cada una conservaba un
gap más amplio que lo cerrado aquí) — MOD-04 gana ámbito project/session con
precedencia y "quién manda" visible en UI; TOOL-03 gana los estados
degraded/probing (antes solo connecting/connected/error/needs_auth/timeout)
y paginación real de `list_tools`; TOOL-04 nace: manifiesto por servidor MCP
(`src/extension_manifest.py`, nuevo) con diff de permisos y cuarentena si se
piden permisos nuevos sin aprobar; HW-02 amplía a la pantalla de Modelos
locales el desglose GPU/RAM en GB y un veredicto `kv: unknown` explícito
cuando no hay medición (Chat/Vitals.tsx, ya existente, no se tocó); IDX-01
gana la ruta `POST /api/projects/{id}/relocate` y la acción "The folder has
moved…" en Project.tsx, cableando `src/project_identity.relocate` que ya
existía sin llamador. No se reauditó el resto del mapa (~95 IDs restantes)
contra el código — ver Limitaciones en el informe del lote 43.

Resumen (lote 44, integración de la ola 6 — cierre de lotes 38-43): sin
reauditar el mapa completo, actualiza con evidencia real las filas que el
propio lote 44 tocó o cuyo hueco citado dependía de un cableado que este
lote completó. IDX-02/IDX-03 (filas nuevas): `src/code_index.py` (L38) ya
implementaba `find_definition`/`find_callers`/`tests_for`, pero no estaba
registrado en `TOOL_HANDLERS`/`TOOL_TAGS`
(`src/agent_tools/__init__.py`)/`BUILTIN_TOOL_DESCRIPTIONS`
(`src/tool_index.py`) ni montado en `app.py` — el agente nunca podía
invocarlo pese a existir la ruta HTTP (`routes/code_index_routes.py`, L38,
sin registrar). MOD-05 (fila nueva): el router medido por
capacidad/calidad/latencia sigue ausente, pero su mitad de "residencia" (no
salir de la máquina) ya no falta — `src/rerank.py::rerank()` ahora consulta
`src/privacy_policy.py::assert_outbound` antes de abrir el socket, el mismo
hueco que QA-29 (`docs/spec/v2/QA_ESTADO.md`) señalaba como el único
auxiliar sin cablear; `privacy_profile` se expone en
`src/settings.py`/`src/effective_config.py`. MOD-06: el evento
`capabilities_changed` (L40) se generaba pero `routes/chat_routes.py`'s
allowlist de tipos de evento SSE no lo dejaba pasar — ya llega al cliente.
WEB-03: `src/agent_loop.py::stream_agent_loop` ahora cierra la sesión de
navegador de la tarea (`close_browser_session_for_task`) al terminar el run
por cualquier vía (fin normal, excepción, desconexión), el "caller que no se
podía alcanzar" que `src/mcp_manager.py` (L42) dejaba documentado como hueco
abierto. A11Y-01/QA-44: `studio/src/components/Dialog.tsx` gana un acotado
de ancho en cabecera+título (sin cambio de estado — el hueco real de "dialog
control @ 200%" NO estaba ahí, ver QA_ESTADO.md). UX-03/WRITE-02/WRITE-04
(filas nuevas): sin trabajo de este lote, añadidas solo para dejar
constancia expresa de qué evidencia parcial ya existe de lotes previos (L40
regenerate para UX-03, L41 narrative_canon para WRITE-02) y qué falta. El
resto del mapa (~85 IDs no listados en la tarea de este lote) no se
reauditó — sigue reflejando el lote 43.

## Grupo A — Control: BASE, ARCH, TASK, PLAN, OBS, ACT

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| BASE-01 | existente (Lote 69a) | OBJETIVOS.md, PENDIENTES.md (bitácora manual con evidencia); `/api/version` en app.py:1331 (`APP_VERSION`, y desde el lote 61 también `build_sha`/`schema_version` vía `api_version.openapi_version_extension`) | `studio/src/screens/settings/SystemExtras.tsx` (comentario "BASE-01: `/api/version`'s app version plus... build info", línea ~321) hace `getJson<VersionInfo>('/api/version')` y pinta la línea "what am I actually running" en Diagnóstico | — | Cerrado en Lote 69a: Diagnóstico ya muestra versión y build servidos leyendo `/api/version` en vivo. El mapa código↔requisito sigue siendo manual (este propio fichero + PENDIENTES.md), no automatizado — aceptado como suficiente, no hay hueco de comportamiento pendiente. |
| BASE-02 | existente (lote 36) | `src/state_mirror/contracts.py` (orden de autoridad `observed`>...), `src/state_mirror/reconcile.py` (sweep+retire), `src/state_mirror/adapters/{approvals,models,runs,sessions,workspace,...}.py`; lote 36: `src/state_mirror/divergence.py` compara el mirror contra la fuente autoritativa y reporta divergencia real, no solo staleness | `studio/src/screens/StateMirror.tsx` + `adapters/stateMirror.ts` | test_state_mirror_conflict_resolution.py, test_state_mirror_replay.py, test_state_mirror_reduction_boundaries.py, tests/test_state_mirror_divergence.py (nuevo, lote 36) | Sin adapter de memoria; StateMirrorScreen es panel aislado, no está probado que el resto de pantallas recarguen desde él. |
| BASE-03 | existente (lote 37) | `src/doctor.py` (checks python/git/backends/modelos/memoria/skills + nuevas áreas setup/entorno), ruta `/api/doctor` en `routes/changesets_routes.py:184`; `routes/diagnostics_routes.py`'s `GET /api/setup/status` (registro allowlisted de reparaciones, "next action" que nunca apunta a un callejón sin salida — ver test_ops_doctor.py); `Start-Faustus.ps1` monta venv/npm ci/build sin copiar ficheros personales | lote 37: `studio/src/screens/Onboarding.tsx` consume `/api/setup/status` y ahora tiene ruta real (`/setup` en `studio/src/shell/AppShell.tsx`) | tests/test_doctor.py, tests/test_doctor_services.py, tests/test_ops_doctor.py (nuevo) | `/api/doctor` en sí (el panel de Diagnóstico completo, no solo el resumen de primer-uso) sigue sin consumirse desde Settings › System. |
| ARCH-01 | existente (Lote 61) | `src/contracts/{event,artifact,run,changeset,memory,skill,workflow,approval}.py`: dataclasses tipadas con `schema_version` y `semver()`; `src/api_version.py` (`API_VERSION`/`MIN_CLIENT_VERSION`, `X-Faustus-Api-Version`/`X-Faustus-Client-Version`); Lote 61: `core/middleware.py` (~L136-153) añade la cabecera de versión y un **426 global** en TODO `/api/*` cuando el cliente se identifica como demasiado viejo, salvo `/api/version` y `/api/health` (excepción explícita para que un cliente antiguo pueda seguir diagnosticando) | `studio/src/adapters/api.ts` (constantes de cabecera), `studio/src/adapters/chat.ts` (`getVersionMismatch`), `studio/src/screens/studio/model.ts` (`versionMismatch` en el turno) | tests/test_contracts.py, tests/test_contracts_mcp_and_routes.py, tests/test_api_version.py, tests/test_l29_api_version_header.py, tests/test_l61_arch01_global_version_gate.py (nuevo) | Cerrado en Lote 61: la negociación de versión ya cubre cada endpoint `/api/*` (middleware global), no solo el wire de chat SSE — un cliente antiguo recibe 426 comprensible en cualquier ruta salvo las dos de diagnóstico. |
| ARCH-02 | existente | `src/process_ownership.py` (`terminate_tree`, `note_started`, `ownership_unknown`, y ahora `can_stop`/`owner_of`); `server_runtime.py` (`start --owner web/desktop`), `routes/chat_routes.py:2773-3190` guarda respuesta parcial al desconectar cliente | `desktop/main.cjs`: `ownedToken` solo si `result.started`; `shutdown()` sólo para token propio | tests/test_process_ownership.py, tests/test_process_tree_ownership.py, tests/test_tool_task_cancelled_on_disconnect.py, tests/test_process_ownership_rights.py | Verificado: desconexión no mata tarea, y cierre no detiene servidor reutilizado. `can_stop` todavía no está llamado desde ningún sitio de producción (`server_runtime.py`/`desktop/main.cjs` fuera del PROPIOS de los últimos lotes) — solo `check()` se usa hoy; es la pieza que un futuro lote puede enchufar. |
| ARCH-03 | existente (lote 9, 10-09) | `src/effective_config.py::compile_effective` (global→proyecto→rol→modelo→turno con origen, sobrescritos, secretos enmascarados, fingerprint, `conflicts[]`); `GET /api/config/effective[/prompt]`; `_AGENT_RULES`, `_AGENT_PREAMBLE`, `_API_AGENT_RULES` definidos una sola vez | `studio/src/screens/settings/EffectiveConfig.tsx` (tabla valor/origen, conflictos, hash, bloques del prompt) — comprobado en vivo en el 7001 | tests/test_effective_config.py (guard AST contra dobles asignaciones) | La capa rol/preset sólo opina con una `agent_def` nombrada; sin edición desde el inspector. |
| TASK-01 | existente (Lote 60) | `src/plan_state.py` (`Plan`, `PlanStep` con id/dependencias/estado/`evidence_refs`/historial de revisiones); `src/agent_tools/interaction_tools.py::UpdatePlanTool` ya no trunca a 8.192 caracteres — un plan largo conserva todos sus pasos vía `plan_state` | plan acoplado (docked) vía evento `plan_update` | tests/test_update_plan_tool.py, tests/test_plan_mode.py, tests/test_l60_task01_plan_state.py (nuevo) | Cerrado en Lote 60: estado estructurado con IDs/dependencias/evidence_refs/historial ya existe y el truncado que perdía pasos por encima de 8.192 caracteres desapareció. |
| TASK-02 | existente (Lote 64) | `src/research_handler.py::recover_interrupted/list_interrupted` + nuevo `resume_interrupted` (reconstruye por rondas/fuentes ya confirmadas en vez de reintentar desde cero) tras un reinicio forzado del backend | tarjeta "Reanudar" / "Empezar de nuevo" distintas (antes solo "Reintentar") | tests/test_research_restart_survival.py, tests/test_crash_recovery.py, tests/test_studio_research_restart_js.py, tests/test_l64_task02_res05_resume_interrupted.py (nuevo) | Cerrado en Lote 64: forzar reinicio entre dos rondas de research y continuar ya no repite las completadas ni pierde citas — mismo cierre de raíz que RES-05/QA-10. |
| TASK-03 | existente (Lote 61) | `src/workflows/store.py` (`idempotency_key`, `claim`), `src/media_runs.py` (outbox real), `src/task_scheduler.py` (`idempotency_key` por tarea); Lote 61: `routes/chat_routes.py::_idempotent_replay_stream` extiende el mismo patrón outbox al envío de chat — reenviar la misma petición tras una pérdida de respuesta reproduce el stream ya emitido en vez de crear un segundo turno | `docs/ui/i18n/es.tsv`: "Uncertain outcome, checking…" → "Resultado incierto, comprobando…" (banner en Studio.tsx cuando el estado es incierto) | tests/test_media_runs_outbox.py, tests/test_workflow_store_races.py, tests/test_l61_task03_idempotent_chat_replay.py (nuevo) | Cerrado en Lote 61: reenviar la misma petición de chat tras una pérdida de respuesta ya no crea dos tareas ni duplica el envío; el estado "incierto" ya es visible en Studio. |
| TASK-04 | existente (ola 5) | `POST /api/chat/stop` con `scope` (`generation`/`task`/`work`, `routes/chat_routes.py::chat_stop`) y el nuevo `POST /api/chat/pause` (== `scope="generation"`, solo fija `pending_pause` sin cancelar la tarea — `src/agent_runs.py::request_pause`/`take_pause_request`); `scope="task"`/`"work"` cancelan de verdad, propagan a cada worker `delegate_agents` del turno, cancelan preguntas `open` (QA-14: respuesta tardía no reactiva un turno cancelado) y, solo `work`, los jobs de fondo de la sesión; sin `scope` es el contrato original de Stop, byte a byte | i18n distingue "Cancel this turn"/"Cancel task"/"Cancel workflow"/"Pause" (studio/src/i18n/es.ts) | tests/test_cancel_scope_routes.py (nuevo), tests/test_subagent_stop_and_locks.py, tests/test_subagent_permissions.py | Cancelación a subagentes, limpieza de locks y no-reactivación por respuesta tardía a pregunta cancelada, todas probadas end-to-end. |
| TASK-06 | existente (Lote 65) | `src/agent_settings_schema.py`: 6 dimensiones de `agent_autonomy_*` configurables; `src/autonomy_budget.py::resolve_budget` (presets `supervised`/`bounded_autonomous`/`read_only`) y checkpoint garantizado al agotar el presupuesto (no borra trabajo, no amplía permisos para terminar) | `studio/src/screens/studio/Composer.tsx::AutonomyPresetSelector` (nuevo, Lote 65) — selector de preset supervisado/autónomo acotado/solo lectura | tests/test_agent_ceiling_settings_registered.py, tests/test_autonomy_budget.py, tests/test_l65_task06_autonomy_preset_selector_js.py (nuevo) | Cerrado en Lote 65: selector de preset visible en Studio y checkpoint garantizado al agotar el presupuesto, ambos ya cableados. |
| PLAN-01 | parcial | `src/deep_research.py::_outline_sections/_extract_subquestions` (líneas 265-870) agrupa por encabezados y conserva todas las viñetas (corrigió corte en 12 ítems, ver PENDIENTES.md #13); RES-01 (ola 5): `_group_chunks` reparte un grupo demasiado largo en fragmentos "Heading (cont.)" en vez de truncar a 700 caracteres — ver tests/test_res_01_outline_no_loss.py | sin pantalla de plan corto/expandible localizada | tests/test_deep_research_outline_sections.py, tests/test_read_plan_outline.py, tests/test_res_01_outline_no_loss.py (nuevo) | Extracción de requisitos del brief de research ya no trunca (ni por grupo ni globalmente hasta `MAX_GROUPED_SUBQUESTIONS`); pero el plan del agente (TASK-01) sigue truncando a 8.192 caracteres. |
| PLAN-03 | parcial | `src/subagent_permissions.py` (reglas allow/deny por ruta/herramienta, profundidad); locks de fichero por delegación y liberación al morir un worker probados en tests/test_subagent_stop_and_locks.py; CALL-04 (ola 5): `src/agent_loop.py` ahora paraleliza un prefijo de lecturas consecutivas del mismo turno (READ_PUBLIC/READ_WORKSPACE/READ_PRIVATE, recursos distintos) vía `asyncio.gather`, sin tocar el locking entre sub-agentes | ninguna pantalla muestra "qué agente edita qué fichero" (0 resultados en studio/src/screens) | tests/test_subagent_stop_and_locks.py, tests/test_parallel_reads.py (nuevo, CALL-04) | Backend reparte permisos/locks por delegación y ya paraleliza lecturas independientes de un mismo turno; sin visibilidad en Studio de propiedad de recursos ni resolución de conflictos en UI. |
| OBS-01 | existente (Lote 60, ruta HTTP Lote 61) | `src/agent_runs.py::_observability_fields`/`_publish` estampa `trace_id`/`step_id`/`sequence`/`stream_id`/`schema_version`; `call_id` llega a `src/artifact_store.py::persist(call_id=...)` y `src/command_guard.py::append_receipt(call_id=...)`; Lote 60: `agent_runs.trace_for_call(call_id, session_id=None)` reconstruye eventos+artefactos+recibo a partir de un `call_id`; Lote 61: `GET /api/observability/trace/{call_id}` (`routes/observability_routes.py`) expone esa reconstrucción por HTTP | `studio/src/screens/Activity.tsx::TracePanel` (Lote 69b: "Trace a tool call", campo `call_id`, muestra eventos/SHA-256/generador/recibo de permiso) | tests/test_obs_*.py; tests/test_l60_obs01_trace_for_call.py, tests/test_l61_observability_trace_route.py (nuevos) | Cerrado en Lote 60/61: una herramienta, su progreso, el artefacto y el recibo final se enlazan sin buscar manualmente por texto, vía `GET /api/observability/trace/{call_id}` y el panel `TracePanel` de Activity.tsx. |
| OBS-02 | existente (Lote 60) | `src/agent_runs.py::_PHASE_CANON` (nuevo, Lote 60) mapea el vocabulario ad-hoc de cada módulo a las 8 fases canónicas (cola/admisión/carga/prefill/generación/herramienta/verificando/espera humana — incluye `_PHASE_CANON["verifying"]`); `src/research_handler.py`/`src/vram_admission.py` siguen emitiendo sus eventos de dominio, ahora normalizados a esa tabla antes de publicarse | Activity.tsx consume `phase` ya canónico | tests/test_chat_vram_gate.py, tests/test_studio_vram_live_js.py, tests/test_l60_obs02_phase_canon.py (nuevo) | Cerrado en Lote 60: las 8 fases canónicas existen y todo evento de fase pasa por `_PHASE_CANON` antes de publicarse — un spinner ya no puede mostrar un vocabulario ad-hoc distinto por módulo. |
| OBS-03 | existente (lotes 2 y 28) | `src/contracts/errors.py` (10 categorías cerradas, `ErrorInfo`, `from_exception` perezoso), `error_class` en errores de llm_core/agent_loop; lote 44: `src/retry_policy.py::error_class_for` y `core/exceptions.py::http_error_class` mantenían dos mapeos duplicados de estado HTTP→clase para 409/408/499 (con ortografías distintas para el mismo estado) — `core/exceptions.py` ahora delega en `retry_policy.py` sin reimplementar nada | `studio/src/components/errorTaxonomy.ts` (`friendlyError`: título + acción es/en), usado en Transcript y Activity | tests/test_contracts_v2_fixtures.py, tests/test_studio_error_taxonomy_js.py; tests/test_l44_retry_policy_conflict_timeout_cancelled.py (nuevo) | Los errores de rutas HTTP antiguas aún no llevan `error_class`. La deduplicación de este lote es de mantenimiento (una sola autoridad para el mapeo), no cierra un hueco de cobertura nuevo. |
| ACT-01 | existente | runs indexados por tipo (`task`,`render`,`approval`,`notification`,`chat`,`workflow`) vía adapters consumidos en Activity | `studio/src/screens/Activity.tsx`: filtros Needs action/In progress/Failed/All, `ArtifactDownloads` conserva renders con procedencia tras cerrar el chat | tests/test_studio_activity_js.py, tests/test_studio_activity_feed_js.py | Cumple lo pedido: centro único con filtros y render descargable con procedencia aunque el chat se cierre. |
| ACT-03 | existente | `src/approval_store.py`: `request()` con `expires_at`/`uses_left`, `decide()`/`consume()` con bloqueo optimista (evita doble resolución), `expire_stale()` como barrido programado; L29 añade `src/question_store.py::list_open(owner)` y `GET /api/questions` (routes/chat_routes.py, owner-scoped vía `effective_user`) para que las preguntas `ask_user` abiertas también entren en la misma bandeja | Activity.tsx muestra approvals con enlace "Review approval"; L29: nueva bandeja `kind: 'question'` (`studio/src/adapters/activity.ts::questionFrom`/`answerQuestion`, `studio/src/screens/Activity.tsx::QuestionAnswerPanel`) que responde por el MISMO camino `sendTurn`+`questionId`/`optionIds` que usa la tarjeta en vivo, no un segundo mecanismo | tests/test_approval_store.py, tests/test_approval_concurrent_consumption.py, tests/test_approval_decision_races.py; tests/test_l29_question_store_list_open.py, tests/test_l29_questions_route.py, tests/test_l29_activity_questions_js.py (nuevos) | Bandeja con scope/expiración/revisión y anti doble-resolución probados; ahora también cubre preguntas abiertas (SEC-06: owner-scoped, fail-closed). Falta confirmar invalidación específica al cambiar el plan aprobado ya en curso. |
| ACT-04 | existente (Lote 69a) | `desktop/main.cjs`: `ownedToken` sólo si esta instancia arrancó el servidor (`result.started`); `shutdown()` (línea ~95) sólo detiene el servidor si hay `ownedToken` propio — Lote 69a confirma antes de detener y advierte de qué tareas interrumpe cuando la instancia SÍ es dueña del backend | `desktop/main.cjs` (diálogo de confirmación en `before-quit`/`window-all-closed`, línea ~106-107) | — | Cerrado en Lote 69a: cerrar una ventana que reutiliza servidor sigue sin matarlo (ya lo garantizaba el código); apagar un backend propio ahora advierte qué tareas interrumpe antes de proceder. |
| ACT-06 | existente (Lote 69a) | sin lógica de estado de pantalla centralizada en backend; cada ruta responde error/vacío por su cuenta — reconocible por `core/middleware.py`'s 426 (ARCH-01) y por 401/403 ya consistentes | `studio/src/components/EmptyState.tsx`: `EmptyStateTone = 'empty' \| 'error' \| 'denied' \| 'incompatible'` (nuevo, Lote 69a) — variantes propias para permiso denegado y versión incompatible, aplicadas en 4 pantallas (`Activity.tsx`, `Settings.tsx`, `Integrations.tsx`, `LocalModels.tsx`) | tests/test_l69a_act06_emptystate_tones_js.py (nuevo) | Cerrado en Lote 69a: fallar un panel secundario por permiso denegado o versión incompatible ya muestra el tono correcto, no un "No hay datos" genérico. |

## Grupo B — Modelos y herramientas: MOD, CALL, TOOL, HW, PERF

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| MOD-01 | existente (ya no ausente en el mapa — implementado en una ola previa, no reflejado hasta ahora) | src/model_capabilities.py (Modalities, CapabilityAssertion, tested_at); src/model_capability_readers/*.py (wired: `routes/local_models_routes.py`'s `caps_router`, `GET /api/models/{name}/capabilities`); src/model_calibration.py (manifest store, `announced` vs `tested` nunca mezclados — ver su docstring) | studio/src/screens/settings/LocalModels.tsx `CapsChips` distingue explícitamente anunciado (gris) vs probado ✓/✗ (verde/rojo) con `tested_at` en el tooltip (`capsEvidenceTitle`) | tests/test_model_capabilities.py, tests/test_model_capability_readers.py, tests/test_model_calibration.py, tests/test_local_models_routes.py | Cobertura real; MAPA_REUTILIZACION.md simplemente no se había actualizado tras el wiring — sin gap nuevo encontrado en este lote. |
| MOD-02 | existente (idem MOD-01) | src/model_calibration.py::run_calibration (prueba tool_calling/vision/json_mode/context_length_effective/streaming_tool_calls/refusal_format, presupuesto ~55s, nunca carga un modelo — exige que ya esté residente vía `/api/ps`); `routes/local_models_routes.py` `POST /api/models/{name}/calibrate` (409 si no está cargado) | studio/src/screens/settings/LocalModels.tsx botón "Calibrate" (deshabilitado si no está cargado) | tests/test_model_calibration.py | Calibra contra el modelo real vía HTTP, con evidencia recortada por probe; no hay fixtures JSON de tool-selection/español/recuperación/contexto como suite separada — las 6 pruebas fijas cubren protocolo, no comportamiento de dominio. |
| MOD-03 | existente (Lote 62) | src/llm_core.py:3595-3909 acumulador de deltas por índice (`_tc_acc`), canal thinking separado, `_emit_finish`/finish_reason, thought_signature preservado; Lote 62 añade un tipo explícito de "refusal" del proveedor (distinto de un error de transporte) y una prueba de reconexión SSE que no duplica contenido | studio/src/adapters/chat.ts `ChatEvent` (delta/tool_start/tool_output/doc_delta/refusal tipados) | tests/test_llm_core_streaming.py, tests/test_l62_mod03_refusal_and_reconnect.py (nuevo) | Cerrado en Lote 62: streams fragmentados, finish_reason ausente, múltiples llamadas, reconexiones sin duplicar contenido y rechazo del proveedor ya se reproducen y distinguen correctamente. |
| MOD-04 | existente (Lote 69a) | src/model_load_options.py (`ALLOWED_KEYS`, `EXTRA_OPTION_KEYS`, `resolve_for_request`); `SCOPES`/`SCOPE_PRECEDENCE` (session > project > global), `get_scoped_options`/`set_scoped_options`, `resolve_with_origin` (lote 43); `routes/local_models_routes.py` `GET/PUT /{name}/options/scoped` y `GET /{name}/options/effective` | `studio/src/screens/settings/LocalModels.tsx::ScopedOverridePanel` (línea ~121 y ss.) ahora deshabilita campos incompatibles con motivo (un flag de llama-server no se envía como opción Ollama), aplica preset y muestra aviso "requiere recarga" con `window.confirm` cuando el campo cambiado exige recargar el modelo cargado (línea ~180-182) | tests/test_l43_mod04_scoped_options.py (8), tests/test_l43_hw02_and_mod04_routes.py (7), tests/test_l69a_mod04_reload_confirm_js.py (nuevo) | Cerrado en Lote 69a: un flag de llama-server ya no se envía como opción Ollama; un cambio que exige recarga pide confirmación explícita y muestra el impacto (modelo cargado se descargará/recargará e interrumpirá lo que esté en marcha). |
| MOD-05 | ver `MAPA_P1.md` (P1, no P0) | Fila mal clasificada de una ola previa: `backlog.json` fija MOD-05 como P1 (huecos.md lo confirma explícitamente: "Grupo B — ... P0, salvo MOD-05 que es P1"), no P0 — se mantiene esta fila solo por continuidad histórica del mapa, sin recuento en el resumen P0. Evidencia real: `src/rerank.py::rerank()` llama `src/privacy_policy.py::assert_outbound("reranker", ...)` antes de abrir el socket (mitad "residencia/privacidad transitiva" cerrada en Lote 41); `privacy_profile` es campo de primera clase en `src/settings.py`/`src/effective_config.py` | n/a | tests/test_rerank_privacy_policy.py | El router "por capacidad/calidad/latencia" que da nombre a MOD-05 sigue sin existir (`src/execution_router.py` existe pero no se auditó contra este ID) — ver su fila propia, con el mismo veredicto, en `docs/spec/v2/MAPA_P1.md` (Lote 55: "existente (Lote 41)" para la mitad de residencia). No se toca en el cierre de lotes 60-70: ningún lote de esta ola construyó el router. |
| MOD-06 | existente (Lote 60) | src/agent_loop.py:1167-1300 `_agent_route_tool_mode` decide nativo/restringido por calibración cuando existe manifiesto probado (fallback a keywords solo sin calibración); `routes/session_routes.py`'s `PATCH /session/{sid}` recalcula capacidades y devuelve `lost: [...]`; el evento `capabilities_changed` ya llega al cliente (L44); Lote 60: `agent_loop._drop_foreign_opaque_tool_extras(messages, new_endpoint_url)` (línea 1364) reconstruye el estado del turno tras el fallback — nunca interpreta un bloque opaco de otro proveedor como texto ejecutable, y conserva evidencias/adjuntos en vez de borrarlos | `studio/src/screens/studio/model.ts` muestra `lost` al cambiar de modelo (banner "Switched from {from} to {to}: lost {lost}.") | tests/test_session_model_switch_capabilities.py, tests/test_l40_capabilities_changed_route.py, tests/test_l60_mod06_drop_foreign_opaque_extras.py (nuevo) | Cerrado en Lote 60: cambiar modelo ya reconstruye el estado del turno tras el fallback (QA-28 íntegro) sin interpretar bloques opacos antiguos como texto ejecutable ni borrar adjuntos. |
| CALL-01 | existente (Lote 62) | src/llm_core.py:3841-3888 acumulación por index/call_id con guardas (índice nulo, args nulos); Lote 62 añade `object_pairs_hook` que rechaza JSON con claves duplicadas en el parseo de argumentos, y límites de bytes/profundidad configurables (`FAUSTUS_TOOL_ARG_MAX_BYTES`/`_MAX_DEPTH`) | `tool_call_delta` genérico ya expone progreso de ensamblado en la tarjeta de la tool, no solo para create/update/edit_document | tests/test_llm_core_streaming.py, tests/test_l62_call01_duplicate_keys_and_limits.py (nuevo) | Cerrado en Lote 62: cortar un stream a mitad de una ruta o un número produce cero efectos; JSON con claves duplicadas se rechaza explícitamente y hay límites de bytes/profundidad. |
| CALL-02 | existente (Lote 62) | Esquemas con enums en src/tool_schemas.py; validación por herramienta vía `ValueError`/`_resolve_tool_path` en src/agent_tools/filesystem_tools.py; scoping por owner (`owner_filter`); Lote 62: `src/tool_schemas.py::_range_violation` valida tipos estrictos/rangos centralizadamente, y `edit_file`/`write_file` rechazan un `base_revision` obsoleto (precondición de versión, EDIT-01) antes de iniciar el proceso de escritura | error junto al argumento en la tarjeta de la tool (repair/rechazo visible) | tests/test_*_owner_scope.py (múltiples), tests/test_l62_call02_range_and_stale_precondition.py (nuevo) | Cerrado en Lote 62: una ruta inválida, una cuenta ajena o un archivo obsoleto se rechazan antes de iniciar procesos, con validador de tipos/rangos centralizado. |
| CALL-03 | existente (lotes 3 y 6) | `src/tool_schemas.py::repair_tool_arguments` (número/booleano como string, enum por mayúsculas; nunca tool, ruta ni alcance) enganchado en `src/agent_loop.py` antes del ToolBlock; `argument_errors`/`repairs` en el evento de tool | dry-run del catálogo muestra original/corrección (`Settings › Tools`) — comprobado en vivo | tests/test_tool_args_validation.py, tests/test_tool_args_hook.py | El transcript no pinta aún «original → corrección» en la tarjeta de la tool (sólo el catálogo). |
| CALL-04 | existente (ola 5) | src/agent_tools/subagent_tools.py `write_block_reason`/`_LOCK_CTX` — bloqueo por archivo entre sub-agentes, reclamo antes de ejecutar; nuevo: `src/agent_loop.py`'s `_prefetched`/`_parallel_cap` (justo antes de `for i, block in enumerate(tool_blocks)`) agrupa un PREFIJO de llamadas consecutivas de solo lectura (efecto READ_PUBLIC/READ_WORKSPACE/READ_PRIVATE por `src/tool_capabilities.py`, recursos distintos, nada que necesite aprobación) y las ejecuta con `asyncio.gather`; una escritura (o cualquier otra cosa) cierra el grupo ahí mismo, así que una mutación nunca se solapa con una lectura del mismo grupo — los eventos `tool_output` siguen saliendo en orden de llamada | studio (delegate_agents UI de progreso, no revisado en detalle) | tests/test_subagent_stop_and_locks.py; tests/test_subagent_permissions.py; tests/test_parallel_reads.py (nuevo) | El locking entre sub-agentes y la paralelización de lecturas nativas del mismo turno están ambos probados. Solo el PREFIJO de lecturas se paraleliza (no lecturas intercaladas tras una escritura), a propósito. |
| CALL-05 | existente (Lote 62, integrado Lote 67) | `src/contracts/tool.py::ToolResult` (nuevo, Lote 62) — status/data/evidence_refs/side_effects/error tipado; `src/tool_result.py` construye el contrato desde el dict libre de cada tool sin reescribirlas todas; `agent_runs.py::_partial_from_events` (Lote 67) usa el mismo contrato para distinguir "éxito de transporte con error funcional" de un paso realmente hecho | tarjeta de resultado con duración+enlace a `evidence_refs` en la tarjeta de tool | tests/test_l62_call05_tool_result_contract.py, tests/test_l67_call05_partial_from_events.py (nuevos) | Cerrado en Lote 62/67: un éxito de transporte con error funcional ya no marca el paso hecho; toda llamada tiene exactamente un resultado lógico terminal vía `ToolResult`. |
| CALL-06 | existente (Lote 62) | `src/retry_policy.py` (`classify_http`: retry_now/retry_backoff/no_retry/outcome_unknown, Retry-After, jitter, `RetryBudget`); Lote 62 enchufa la misma política en los tres bucles de reintento de streaming de `llm_core.py` (~3300/3420/3570/3890) y en las tool calls con efectos remotos (consultan `outcome_unknown` antes de reenviar) | mensaje «saturado, reintentando» vs «pudo completarse» en la tarjeta del turno | tests/test_retry_policy.py (48), tests/test_llm_core_retries.py (10), tests/test_l62_call06_streaming_retry_and_remote_effects.py (nuevo) | Cerrado en Lote 62: simular envío completado seguido de timeout ya no reenvía sin reconciliar en ninguno de los cuatro bucles ni en tool calls con efectos remotos. |
| CALL-07 | existente (Lote 62) | src/agent_tools/interaction_tools.py `AskUserTool` gana texto libre además de multi-select, y `question_id` explícito (`question_store` ya lo emitía — Lote 62 lo propaga en el payload público); routes/chat_routes.py `_mark_tool_approval_resolved`/`_supersede_tool_approval_history` | studio/src/adapters/chat.ts `ask_user`/`ask_resolved` events, con `revision`/`question_id` leídos en la tarjeta en vivo | tests/test_l62_call07_ask_user_free_text_and_question_id.py (nuevo) | Cerrado en Lote 62: recarga, timeout, doble clic y respuesta desde otro cliente ya no generan decisiones contradictorias — texto libre y `question_id` explícitos cubren el mismo camino que ya protegía `tool_approval`. |
| CALL-08 | existente | src/prompt_security.py (`untrusted_context_message`); src/tool_capabilities.py taint/gate tras contexto externo; `parse_tool_blocks` (src/tool_parsing.py) solo se invoca sobre `round_response` del propio modelo (src/agent_loop.py:3450), nunca sobre texto de herramientas/documentos | studio marca sugerencias vs acción ejecutable (no revisado a fondo) | tests/test_external_context_tool_gate.py; tests/test_prompt_injection_evasion.py | Cobertura sólida; no se verificó explícitamente el caso literal "README con bloque de herramienta" pero el mecanismo lo cubre por diseño. |
| TOOL-01 | existente (Lote 62) | src/tool_capabilities.py (`ToolCapabilities`, `ToolEffect`, `capabilities_for_tool`); src/tool_schemas.py (schemas nativos); src/agent_tools/__init__.py `TOOL_TAGS`/aliases; Lote 62: `src/tool_registry.py::ToolDescriptor` (nuevo) unifica idempotencia/timeout/salida/compatibilidad sobre las mismas 5 listas que ya alimentaban chat/workflow/subagente/MCP — no crea un segundo camino, cierra el que faltaba | `studio/src/screens/settings/Tools.tsx` (catálogo, búsqueda, prueba segura "dry-run") | tests/test_external_context_tool_gate.py, tests/test_l62_tool01_tool_descriptor_registry.py (nuevo) | Cerrado en Lote 62: la misma herramienta ya tiene igual contrato en chat, workflow, subagente y MCP vía `ToolDescriptor`; catálogo real en Studio con búsqueda/prueba segura. |
| TOOL-03 | existente (Lote 63) | src/mcp_manager.py (`McpManager`, stdio/SSE/HTTP, OAuth, reconexión, `_honest_status`, estados connecting/connected/degraded/probing/error/needs_auth/timeout); `list_tools_page(server_id, cursor)` pagina de verdad; Lote 63: umbral de degradación configurable por servidor (`mcp_degraded_thresholds`, setting nuevo) vía `GET/PUT /servers/{server_id}/degraded-threshold` (admin) sobre `mcp_manager.degraded_threshold_for/set_degraded_threshold` | studio/src/screens/settings/Integrations.tsx: `MCP_STATUS_LABEL` + icono propio por estado (no solo texto) para degraded/probing; `McpToolsPage` (paginación por cursor) | tests/test_l43_tool03_mcp_states.py (10), tests/test_l63_tool03_degraded_threshold_route.py (nuevo) | Cerrado en Lote 63: una caída o revocación no deja un turno colgado; UI distingue degraded/probing con icono propio y el umbral de degradación ya es configurable por servidor. |
| TOOL-04 | existente (Lote 63) | src/mcp_manager.py `build_server_env`/`minimal_env`/`scrub_private`; `src/extension_manifest.py` — manifiesto por servidor MCP, `diff_permissions(old,new)`, `record_install_or_update` (cuarentena si se piden permisos nuevos), `approve_new_permissions`; rutas `GET/POST /servers/{id}/manifest`, `POST /servers/{id}/manifest/approve`; Lote 63: `record_install_or_update` se llama automáticamente en el flujo real de instalar/actualizar un servidor MCP (hook en `mcp_manager.py`, ya no requiere `POST /servers/{id}/manifest` manual) | studio/src/screens/settings/IntegrationsMore.tsx: `ManifestDiffPanel` (nuevo, Lote 63) — muestra `diff_permissions` (added/removed) antes de aprobar; banner de cuarentena existente conservado | tests/test_l43_extension_manifest.py (9), tests/test_l43_tool04_manifest_routes.py (7), tests/test_l63_tool04_install_hook_and_diff_panel.py (nuevo) | Cerrado en Lote 63: un plugin que intenta permisos nuevos se bloquea aunque el modelo lo solicite; el diff de permisos (added/removed) ya es visible antes de aprobar, y el manifiesto se registra automáticamente al instalar/actualizar, no solo vía ruta manual. |
| HW-01 | existente (Lote 66) | src/vram_admission.py (`assess`, `Ticket`, `admit`, mensajes de RAM/commit); src/vram_fit.py (`kv_bytes_per_token_estimated`, notas "estimated from GGUF metadata"); Lote 66: `try_reserve` (nuevo) toma un `asyncio.Lock` por GPU antes de leer el presupuesto libre — dos `admit()` simultáneos ya no pueden reservar el mismo hueco; un modelo nuevo con KV desconocida se etiqueta explícitamente `estimated` | studio/src/screens/VramAdmissionDialog.tsx | tests/test_vram_admission.py, tests/test_l66_hw01_concurrent_admission_lock.py (nuevo) | Cerrado en Lote 66: dos cargas simultáneas ya no reciben el mismo presupuesto libre (reserva atómica por GPU); un modelo nuevo con KV desconocida se etiqueta como estimación mínima. |
| HW-02 | existente (ampliado lote 43, otra pantalla) | src/agent_runs.py `model_state()` (chat, sin cambios) distingue loading/prefill/spill vía Ollama `/api/ps`; nuevo en este lote, para la pantalla de Modelos locales: `routes/local_models_routes.py::gpu_ram_split_text` (texto "X.X GB in GPU, Y.Y GB spilled to RAM" a partir de `size_vram`/`size_cpu`, datos que `collect_local_models` ya leía de `/api/ps`, sin recalcular nada) y `kv_verdict` (`{"state": "measured", ...}` usando `vram_fit.kv_bytes_per_token_measured` cuando hay contexto y tamaño de fichero, o `{"state": "unknown"}` explícito si no hay medición — nunca una estimación disfrazada) | studio/src/screens/settings/LocalModels.tsx `LoadedList`: fila de un modelo residente ahora muestra el texto de reparto GPU/RAM literal (no es clave i18n, viene ya formado del backend) y `kv: {gb} for {ctx} tokens` o `kv: unknown` (`data-testid="hw02-split-text"`/`"hw02-kv"`) | tests/test_l43_hw02_and_mod04_routes.py (7) | Chat (Vitals.tsx/chat.ts) sigue sin el desglose GB explícito, solo Modelos locales lo muestra; sin confirmación explícita antes de reducir contexto/cuantización silenciosamente (gap ya citado, no tocado por este lote). |
| PERF-02 | existente (Lote 66) | ~21 ficheros usan `asyncio.to_thread`/executors; Lote 66: `src/chat_handler.py:284` ahora envuelve `build_user_content` (→ `_process_pdf`, pypdf síncrono) en `asyncio.to_thread`, liberando el event loop durante la extracción | healthcheck/cancelación de otra tarea responden durante el procesado de un PDF grande (verificado por test) | tests/test_l66_perf02_pdf_to_thread_does_not_block.py (nuevo) | Cerrado en Lote 66: procesar un PDF grande ya no impide responder a healthcheck ni cancelar otra tarea — offload a `to_thread` confirmado con un ticker concurrente. |
| PERF-04 | existente (Lote 66) | routes/system_usage_routes.py `_collect_host`/`_collect_gpu`/`health_signals`; src/disk_ballast.py; `src/bg_monitor.py::is_paused_for_resource_pressure()` conectado en `src/bg_jobs.py::launch()` (L29); Lote 66: `psutil.Process(pid)` propio del proceso Faustus (RSS/fds/threads, nuevo en `routes/system_usage_routes.py`), y límites explícitos de concurrencia — settings `bg_jobs_max_concurrent`/`cpu_heavy_max_concurrent` (`src/bg_jobs.py`) reducen trabajo auxiliar bajo presión sin matar procesos ajenos ni corromper estado | studio Vitals.tsx muestra avisos de disco/VRAM + RSS del proceso propio | tests/test_l29_bg_jobs_ram_pressure.py, tests/test_l66_perf04_process_metrics_and_concurrency_limits.py (nuevo) | Cerrado en Lote 66: una prueba de presión reduce trabajo auxiliar sin corromper estado ni matar procesos ajenos, ahora con medición propia del proceso (RSS/fds/threads) y límites de concurrencia explícitos, no solo "aplazar un job". |

## Grupo C — Contexto, investigación y verificación: CTX, IDX, MEM, RES, WEB, VER

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| WRITE-02 | existente — ver `MAPA_P1.md` (P1, no P0) | `src/branching_futures/narrative_canon.py` (L41) distingue draft/discarded/promoted sobre `BranchingService`; `routes/branching_futures_routes.py::GET /canon/state`/`/canon/alternatives` (Lote 55). Fila mantenida aquí solo por continuidad histórica: WRITE-02 es P1 en `backlog.json`, y `MAPA_P1.md` ya la marca "existente (Lote 41)" — este mapa P0 llevaba desincronizado desde el Lote 44 (mismo defecto que UX-03/WRITE-04, señalado por el enunciado de este cierre) | no localizada | tests/qa/test_qa_47_continuidad_de_novela.py | Resincronizado en Lote 70: el criterio de aceptación literal (un final alternativo descartado no pasa a ser canon) ya está cerrado; ver `MAPA_P1.md` para el detalle completo del ID (`discarded_alternatives()`/WRITE-04 cubren la otra mitad del título). |
| WRITE-04 | existente — ver `MAPA_P1.md` (P1, no P0) | Mismo módulo que WRITE-02: `src/branching_futures/narrative_canon.py::discarded_alternatives()` (L41) — una rama abandonada queda auditable sin poder resurgir como hecho confirmado (criterio literal de QA-47). `MAPA_P1.md` (Lote 55) ya la marca "existente (Lote 41)"; esta fila del mapa P0 estaba desincronizada desde el Lote 44 — inconsistencia señalada explícitamente por el enunciado de este cierre | no localizada | tests/qa/test_qa_47_continuidad_de_novela.py | Resincronizado en Lote 70: la mitad "ramas editoriales independientes" que examina QA-47 está cerrada. "Esquemas/capítulos con referencias estables, objetivos de escena" del título completo del ID sigue sin auditar — ver la nota equivalente en `MAPA_P1.md`. |
| CTX-01 | existente (Lote 60) | src/context_budget.py (`compute_input_token_budget`, escala a ventana del modelo); Lote 60: `budget_for` ya se integra en `_trim_route_request_messages` (`src/agent_loop.py`) — cuando el manifiesto del modelo tiene datos reales, `budget_for` gana sobre la heurística, con el tope explícito de settings conservado como techo duro (ambos coexisten sin contradecirse: el contrato de settings que el test existente fijaba sigue en verde); cada partida devuelve `estimated: bool` explícito, marcando imagen/audio como estimado siempre que no hay tokenizer real | studio/src/screens/studio/Transcript.tsx `Ledger()` (separa system/tools/instructions/skills/memory/documents/web/attachments/tool_results/conversation/user, ahora con marca `estimated`) | tests/test_context_budget.py, tests/test_context_ledger.py, tests/test_estimate_tokens_tool_calls.py, tests/test_l60_ctx01_budget_for_integration.py (nuevo) | Cerrado en Lote 60: un adjunto grande ya no desplaza la petición ni los criterios (presupuesto real itemizado del manifiesto, con el tope explícito de settings intacto), y toda cifra estimada lleva la marca `estimated` explícita en vez de comunicarse como conteo exacto. |
| CTX-02 | existente (Lote 64) | src/context_compactor.py `maybe_compact` + `compact_with_integrity` (integridad antes que resumen con pérdida — `agent_loop.py::_build_route_request_state` ya prioriza el plegado determinista); Lote 64: `src/context_engine/compaction_pins.py` (nuevo) — `pin_fragment`/`unpin_fragment` (fija fragmentos por el usuario, `compact_with_integrity` los respeta siempre); `routes/context_engine_routes.py::POST/DELETE /packets/pins` | `studio/src/screens/studio/Transcript.tsx::CompactionInspector` (nuevo, línea ~741) — "What was compacted", muestra qué se resumió, cuántos fragmentos fijados se conservaron intactos, y botón "Pin this message" | tests/test_compaction_summary_failure.py, tests/test_context_compactor_regressions.py, tests/test_lote20_ctx02_integrity_before_llm_compaction.py, tests/test_l64_ctx02_compaction_pins.py (nuevo) | Cerrado en Lote 64: un resumen nunca transforma «no cambiar API» en «cambiar API» (orden integridad-antes-que-resumen ya cerrado); ahora además el usuario puede fijar fragmentos que nunca se comprimen, y `CompactionInspector` muestra qué se compactó. |
| CTX-03 | existente (Lote 64) | src/read_plan.py (lectura paginada offset/limit + índice de símbolos; Lote 64: `search_in_file` busca dentro del output ya cargado, con carga incremental); src/context_engine/{compiler.py,candidates.py,store.py} (dedupe por `source_ref`); routes/context_engine_routes.py: `/sources/fetch` (nuevo, Lote 64), `/read/search` (nuevo, Lote 64) y `fetch_source_fragment` — `GET /packets/{packet_id}/items/{item_id}/fragment` devuelve `{resolvable, text}` resolviendo `source_ref` por prefijo (`file:`/`doc:…#chunk`/`session:…#idx`/`mem:`) | `studio/src/screens/Context.tsx::ManifestPane` — botón "Ver fragmento" cuando `resolvable`; búsqueda "Search within this packet…" (Lote 69b) | tests/test_context_engine_sources.py, tests/test_context_engine_delta_source.py, tests/test_read_plan_outline.py, tests/test_l64_ctx03_search_and_fragment_route.py (nuevo) | Cerrado en Lote 64: el modelo puede volver al fragmento exacto que fundamenta un resumen (endpoint de fragmento) y buscar/cargar de forma incremental dentro del output, aunque cambie la ventana activa. |
| IDX-01 | existente (Lote 68) | services/projects.py `_canonical_ref` (realpath+normcase); `ProjectStore` (project_id estable); `src/project_identity.py::relocate`; `routes/project_routes.py::POST /api/projects/{id}/relocate` (lote 43); Lote 68: `GET /api/projects/recent-folders` (nuevo, `routes/project_routes.py`) devuelve las carpetas recientes por owner | studio/src/screens/Project.tsx (acción "The folder has moved…"); Lote 69b: `studio/src/screens/Projects.tsx` — diálogo "Relocate this project's folder" con lista "Recent folders" y aviso "No system dialog is available for this browser — pick a recent folder below, or type the path", disponible desde el LISTADO sin entrar al proyecto | tests/test_l43_idx01_relocate_route.py (7), studio/checks/l43-project-relocate.check.mjs, tests/test_l68_idx01_recent_folders_route.py (nuevo) | Cerrado en Lote 68/69b: renombrar una carpeta no duplica memorias ni convierte un path de Windows en ruta remota inválida; sin selector nativo de SO (limitación del navegador embebido), la reubicación ya ofrece carpetas recientes y es accesible desde el listado de proyectos, no solo desde dentro de uno. |
| IDX-02 | existente (Lote 64) | `src/code_index.py` (índice incremental por hash de fichero, `find_definition`/`find_callers`, respeta `.gitignore`/`.faustusignore`); `find_symbol`/`callers` registradas en `TOOL_HANDLERS`/`TOOL_TAGS`/`BUILTIN_TOOL_DESCRIPTIONS` (lote 44); `routes/code_index_routes.py` montado (lote 44); Lote 64: `refresh(paths=[...])` (nuevo) reindexa solo los ficheros tocados bajo escritura concurrente, sin reembeber todo el workspace, con límite de tamaño de repo (`FAUSTUS_CODE_INDEX_MAX_FILES`); ruta `POST /{project_id}/reindex?paths=` | `routes/code_index_routes.py` (HTTP puro) | tests/test_tool_index_schema_parity.py, tests/test_code_index.py, tests/test_l64_idx02_refresh_paths_concurrent.py (nuevo) | Cerrado en Lote 64: cambiar una función actualiza referencias y tests candidatos sin reembeber todo el workspace (`refresh(paths=...)`), verificado bajo escritura concurrente y con límite de tamaño de repo. |
| IDX-03 | existente (Lote 64) | `src/code_index.py::tests_for` (léxico/AST); `src/agent_tools/code_tools.py::TestsForTool`; Lote 64: fusión BM25+léxico+símbolo con reranking (`src/code_index.py::tests_for` ahora combina la puntuación léxica/AST con `src/tool_index.py::two_tier_search`, reusado sin duplicar) — una suite con nombres raros, español y código encuentra el archivo correcto aunque la similitud semántica falle, porque el léxico/AST sigue puntuando | igual que IDX-02 | tests/test_tool_index_schema_parity.py, tests/test_code_index.py, tests/test_l64_idx03_hybrid_retrieval_fusion.py (nuevo) | Cerrado en Lote 64: la recuperación ya fusiona léxico/AST con BM25+símbolo (reusando `two_tier_search`, sin motor paralelo) — una suite con nombres raros, español y código encuentra el archivo correcto aunque falle la similitud semántica. |
| IDX-04 | existente (Lote 66) | src/upload_handler.py (`python-magic` para MIME); src/document_processor.py `_process_text_file` (chardet, try/except por archivo); Lote 66: `pdf_ingestion_signal` (nuevo) detecta PDF cifrado/corrupto explícitamente y marca "solo se extrajo portada" cuando un PDF escaneado no produjo texto real — `save_upload` devuelve `status`/`partial`/`partial_reason` | `studio/src/lib/attachment-uploads.ts::PendingAttachment.state` + `Composer.tsx` (~L825): estados `extracting`/`ready`/`partial` (Lote 66); texto "solo se extrajo la portada"/"PDF escaneado" cuando `partial` | tests/test_upload_content_detection_magic.py, tests/test_pdf_extraction_budget.py, tests/test_native_docx_extraction.py, tests/test_l66_idx04_pdf_ingestion_signal.py (nuevo) | Cerrado en Lote 66: un PDF escaneado ya no figura como leído si solo se extrajo una portada; un archivo fallido no bloquea los demás; frontend distingue Subiendo/Extrayendo/Listo/Parcial. |
| MEM-01 | existente (Lote 64) | src/memory_engine.py (`TRUST_CLASSES`: human_explicit/agent_validated/agent_assertion/legacy_import; `STATUSES`: active/deprecated/anti_pattern; owner/project/level); Lote 64: campo `sensitivity` (`secret`/`sensitive`/normal) y `confidence` explícitos por ítem; aislamiento cruzado por proyecto verificado (una hipótesis refutada en un proyecto no contamina otro, no solo entre owners distintos) | `studio/src/screens/Memory.tsx` — chip de sensibilidad (Lote 69b: "secret"/"sensitive"), confianza visible | tests/test_memory_engine.py, tests/test_memory_owner_isolation.py, tests/test_consolidate_memory_explicit_drops.py, tests/test_l64_mem01_sensitivity_confidence_and_project_isolation.py (nuevo) | Cerrado en Lote 64: una hipótesis refutada no vuelve como hecho en un chat nuevo ni contamina otro proyecto; sensibilidad y confianza son campos explícitos, visibles en Memory.tsx. |
| MEM-02 | existente (cerrado en L29 — implementación ya estaba de una ola previa, faltaba la ruta HTTP y el test de aceptación) | src/memory_engine.py `forget()`/`correct()` (tombstone table, `is_tombstoned` bloquea resurrección salvo `trust_class=human_explicit`) ya existían y no estaban expuestos; L29 añade `DELETE /api/memory-engine/items/{id}/forget` y `POST /api/memory-engine/items/{id}/correct` en routes/memory_engine_routes.py, y `ItemCreate` ahora expone `type`/`session_id`/`provenance` hacia `add_item()` | studio/src/screens/Memory.tsx `deleteMemory` sigue llamando al DELETE físico (`/items/{id}`), no al nuevo `/forget` — pendiente de conectar la UI | tests/test_memory_engine.py; tests/test_l29_memory_forget_correct.py (nuevo, 7 tests: tombstone+no-resurrección, correct+link-back, 404s, validación) | La ruta HTTP y el contrato ya existen y están probados; la UI de Memory.tsx todavía no distingue "borrar" (DELETE) de "olvidar" (forget) — sigue usando la primera. También: `engine.correct()` (foreign, src/memory_engine.py) tombstona el original ANTES de validar el texto nuevo — ver "Cambios necesarios en ficheros ajenos" del informe de L29; la ruta ya se defiende de ese caso, la función de base no. |
| MEM-05 | existente | src/changeset_store.py `create_changeset(..., incognito)` → `{"stored": False, "storage_reason": "incognito"}`; src/chat_processor.py (`incognito` desactiva skills/consolidación); routes/chat_routes.py (incognito bloquea tools que exponen memoria, no genera título); routes/session_routes.py (purge de sesiones incógnito huérfanas) | studio/src/screens/studio/Composer.tsx (`knobs.incognito`, chip dedicado, distinto de "Skip memory recall") | tests/test_chat_memory_recall_control.py, tests/test_review_regressions.py (`test_build_chat_context_incognito_*`) | Cobertura amplia (memoria, changesets, título, tools); no se verificó explícitamente exclusión de embeddings/logs de depuración de bajo nivel. |
| VER-01 | existente | src/agent_loop.py / src/scorecard.py (estados por check: verified/unverified/rejected/syntax_error/tests_failed/review_issues/target_substituted/unknown_tool) | studio/src/screens/studio/Harness.tsx `CheckList`/`HarnessCard` (badge "Verified" solo si `verified===true` y ningún check `unverified`; si no, badge "Unverified") | tests/test_harness_building_blocks.py, tests/test_studio_harness_status_js.py | No hay estado explícito "no aplicable" distinto de los demás; el veredicto agregado es a nivel de turno, no por criterio individual documentado. |
| VER-02 | existente | src/project_tests.py `detect_test_command` (descubre pytest/npm/Makefile), `compare_with_baseline` (baseline en checkpoint, clasifica `new_failures`/`pre_existing`/`pre_existing_only`) | studio/src/screens/studio/Harness.tsx / harness-status.ts (consume el resultado de project_tests) | tests/test_project_tests_scoping.py, tests/test_project_tests_output_oracle.py | No se confirmó ampliación automática de qué tests correr cuando cambia el "riesgo" más allá de `related_test_files`. |
| VER-04 | existente (Lote 68) | src/output_oracle.py (`apply`/`check`: exit 0 no basta, requiere string declarado); src/chat_export_docx.py `render()`; Lote 68: `output_oracle.py::verify_artifact` (nuevo) reabre/valida el artefacto (PDF/DOCX real, no un literal simulado) ANTES de servir el enlace — `routes/session_routes.py::export_session` y `export_sessions_batch` (export por lotes) llaman a `verify_artifact` y fallan antes de mostrar éxito si el fichero no reabre | cabecera `X-Export-Verification` en la respuesta de descarga; frontend distingue el resultado por esa cabecera | tests/test_output_oracle.py, tests/test_chat_export_docx.py, tests/test_l68_ver04_verify_artifact_export.py (nuevo) | Cerrado en Lote 68: un DOCX corrupto, descarga incompleta o export vacío falla antes de mostrar un enlace de éxito, tanto en export individual como por lotes. |
| VER-05 | existente | src/research_citations.py (`audit_citations`, `check_claims`, `SourceRegistry`); src/claim_verify.py `verify`/`verify_claims` (juicio semántico de apoyo, no solo existencia del marcador) | studio/src/screens/studio/SavedEvidence.tsx ("This is the original evidence") | tests/test_citation_sentence_boundaries.py, tests/test_verify_claim_tool.py, tests/test_research_visible_citations.py | Cobertura sólida de audit+verify; UI de "fuente inaccesible/respaldo dudoso" no se localizó de forma explícita fuera de SavedEvidence. |
| VER-06 | existente (Lote 68) | services/review_state.py (`init`/`decide`/`get`, acepta/rechaza por archivo); routes/workspace_routes.py `GET/POST /api/workspace/review/{message_id}`; Lote 68: `init(..., tests_status=...)` (`routes/chat_routes.py::_record_turn_side_effects` ya pasa `tests_status=hz.get("tests")`) y `diff_sha256`/`stale` — cambiar el diff invalida la aprobación de la versión anterior (`stale=True` si el hash ya no coincide) | `studio/src/screens/documents/ReviewPanel.tsx` (nuevo, Lote 68/69b) — "Review this turn's changes", banner "Changed since approval" cuando `stale`, estado de tests (superados/fallidos/sin conclusión/no ejecutados) | tests/test_l68_ver06_review_state_diff_hash.py (nuevo) | Cerrado en Lote 68: aceptar manualmente no convierte tests fallidos en exitosos (`tests_status` viaja con la revisión); cambiar el diff invalida la aprobación de la versión anterior vía `diff_sha256`/`stale`; ya consumido desde Studio (`ReviewPanel.tsx`). |
| WEB-01 | existente | routes/search/search_routes.py `GET /api/search/health` (usa `searxng_engine_health`, `single_engine` cuando solo respondió un motor) | studio/src/screens/Settings.tsx (`searchHealth()`, tarjeta `data-testid="search-health"`) | tests/test_search_engine_health.py, tests/test_service_health_search.py | Salud de motores está en Settings, no se confirmó su reutilización dentro del flujo de research/reporte (filtro fecha/dominio + aviso de cobertura parcial en el informe mismo). |
| WEB-02 | existente (Lote 64) | src/outbound_fetch.py (bloqueo SSRF post-DNS, IP privada/link-local/169.254.169.254/metadata, redirecciones re-evaluadas por hop); Lote 64: `fetch_webpage_content` expone `duplicate_of`/`stale`/`age_days` por página (headers de caché/última-modificación leídos por hop, Lote 67 confirma que llegan al finding) | `studio/src/screens/studio/SavedEvidence.tsx` — chips "duplicate content" / "stale · {n}d old" (Lote 69b) | tests/test_webhook_ssrf_resilience.py, tests/test_web_fetch_size_caps.py, tests/test_deep_research_same_pages_again.py, tests/test_l64_web02_duplicate_and_stale.py | Cerrado en Lote 64/67/69b: una URL pública que redirige al metadata endpoint o localhost se bloquea; páginas duplicadas y contenido desactualizado ya son visibles en el flujo de research/lectura web, no solo bloqueados en backend. |
| WEB-03 | existente (Lote 63) | `src/browser_sessions.py` (perfil aislado por owner+tarea, cierre por tarea, lote 44); Lote 63: `src/builtin_mcp.py::connect_session_browser` (nuevo) lanza un proceso Playwright MCP **por sesión** en vez del perfil global — cableado en `src/agent_loop.py`/`src/tool_execution.py`, así que un proyecto ya no ve la sesión autenticada de otro y cerrar una tarea no borra la sesión de navegador personal del usuario | — | tests/test_web_browser_sessions.py, tests/test_l44_agent_loop_browser_session_close.py, tests/test_l63_web03_per_session_browser_process.py (nuevo) | Cerrado en Lote 63: un proyecto no ve la sesión autenticada de otro (proceso Playwright MCP por sesión, no perfil global); cerrar una tarea no borra la sesión del navegador personal del usuario. |
| WEB-04 | existente (Lote 64) | `src/tool_execution.py::_run_browser_action_with_precondition` (lote 36) toma un `browser_snapshot` fresco, bloquea si `ref`/`expected_url` no coincide, adjunta readback; Lote 64: `src/browser_view.py::parse_snapshot_elements`/`check_precondition` (nuevos) dejan de depender solo del parseo de texto de `@playwright/mcp` — resuelven elementos por estructura y verifican la precondición contra la estructura, no solo texto; navegación invalida coordenadas/selectores obsoletos de forma explícita | resaltado visual del elemento objetivo antes de un clic (Lote 64) | tests/test_web_browser_actions_readback.py, tests/test_l36_browser_action_precondition.py, tests/test_l64_web04_parse_snapshot_and_precondition.py (nuevo) | Cerrado en Lote 64: un layout desplazado no produce un clic ciego en Eliminar (resolución estructural, no solo texto); una navegación invalida coordenadas y selectores obsoletos, con resaltado visual del elemento objetivo. |
| RES-01 | existente (Lote 64) | src/deep_research.py `_outline_sections`/`_extract_subquestions`/`_clean_subquestions`/`_group_chunks` (ningún ítem se pierde por truncado silencioso, ola 5); Lote 64: `_coverage_snapshot` (nuevo) — mapa de cobertura por nodo (`covered`/`thin`/`pending`) sobre las subpreguntas del outline, actualizado tras cada ronda | `studio/src/screens/studio/Transcript.tsx::CoverageMap` (Lote 65/69b) — "Coverage of the brief", `{covered} covered, {thin} thin, {pending} not yet` | tests/test_deep_research_outline_sections.py, tests/test_res_01_outline_no_loss.py, tests/test_l64_res01_coverage_snapshot.py (nuevo) | Cerrado en Lote 64: un encargo con 44 viñetas conserva todas y produce un mapa de cobertura visible en Studio, aunque el informe se redacte en partes. |
| RES-03 | existente (ola 5) | src/research_citations.py `SourceRegistry.add` — ahora un `EvidenceRef` completo por fuente (url, content_hash sha256, `fetched_at`, motor que la encontró), dedup por URL canónica; `find_conflicts`/`finalize_report` surge una sección explícita "sources in disagreement" cuando dos fuentes discrepan, en vez de resolverlo en silencio; marca citas "weak" (`audit_citations`) | No se localizó biblioteca de fuentes agrupable/filtrable en Studio | tests/test_research_citations.py, tests/test_services_research_low_quality_sources.py, tests/test_res_03_evidence_and_conflicts.py (nuevo) | El EvidenceRef y la sección de discrepancias ya existen y están probados; sigue faltando autor/organismo, fecha de publicación editorial (distinta de `fetched_at`), DOI, versión y estado de acceso explícito; no distingue primaria/secundaria. |
| RES-04 | existente (Lote 65) | src/deep_research.py `_final_report_in_parts` (sección fallida no borra el resto; contrato de continuidad entre partes ya cerrado, ola 5); Lote 65: informe vivo — cada parte publicada emite su propio evento, agregado en el turno | `studio/src/screens/studio/Transcript.tsx::ResearchLine`/`LiveLine` (Lote 65) — secciones completas/pendientes visibles según se van escribiendo, no solo el indicador de fase | tests/test_deep_research_report_in_parts.py, tests/test_res_04_continuity_contract.py, tests/test_l65_res04_live_report_sections_js.py (nuevo) | Cerrado en Lote 65: agotar salida en una sección no elimina el resto del encargo ni sobrescribe un informe útil con texto vacío; frontend ya expone un "informe vivo" con secciones completas/pendientes. |
| RES-05 | existente (Lote 64) | src/research_handler.py `recover_interrupted`; clases de fallo tipadas por etapa (`*StageError`, ola 5, recuperación DENTRO de una ejecución en curso); Lote 64: `resume_interrupted` (nuevo, mismo mecanismo que TASK-02) checkpointa consultas/páginas/extractos por ronda y sobrevive a un reinicio de proceso — matar y reiniciar el backend tras tres rondas conserva sus evidencias y no vuelve a gastar toda la inferencia en ellas | tarjeta "Reanudar" en vez de solo "Reintentar" (Lote 64/65) | tests/test_research_restart_survival.py, tests/test_res_05_stage_recovery.py, tests/test_l64_task02_res05_resume_interrupted.py (nuevo) | Cerrado en Lote 64: matar y reiniciar el backend tras tres rondas conserva sus evidencias y no vuelve a gastar toda la inferencia en ellas — mismo checkpoint que cierra TASK-02/QA-10. |

## Grupo D — Edición, ejecución, seguridad y operación: EDIT, EXEC, SEC, OPS, EVAL

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| EDIT-01 | existente (Lote 69b) | routes/workspace_routes.py PUT /api/workspace/file (sha256 `revision`, lock, 409); routes/document/document_routes.py PUT/restore (`expected_content`, 409); src/agent_tools/filesystem_tools.py EditFileTool (ancla única old_string, `base_revision` desde el lote 19); `ReadFileTool` adjunta `revision`/`evidence_refs` (lote 20) | `studio/src/screens/documents/Editor.tsx` (Lote 69b): ahora llama `saveDoc` con `expectedContent`, y un 409 abre `ReviewPanel.tsx`'s vista de conflicto — "This document changed elsewhere. Your edits are kept here", "View differences" / "Reload their version" / "Force-save mine" | tests/test_document_editor_conflict.py, tests/test_edit_file.py, tests/test_lote20_read_file_evidence.py, tests/test_l69b_edit01_editor_conflict_409_js.py (nuevo) | Cerrado en Lote 69b: modificar el archivo desde un editor externo entre lectura y escritura provoca conflicto recuperable (diálogo de 3 vías, ningún trabajo se pierde), no sobrescritura silenciosa — `Editor.tsx` (biblioteca) ya envía `expectedContent`. |
| EDIT-02 | existente (cerrado en este lote — implementación ya estaba, faltaba el test de aceptación) | src/edit_journal.py::EditJournal/apply_batch (registra intención de cada fichero del lote antes de escribir, aplica en orden, ante el primer OSError compensa cada fichero ya aplicado devolviéndolo a sus bytes previos o borrándolo); src/agent_tools/filesystem_tools.py ApplyPatchTool ejecuta su escritura a través del journal; src/workspace_checkpoints.py checkpoint()/restore() (shadow git repo, restaura por archivo); src/dispatch.py `_checkpoint` antes de cada job | studio/src/adapters/workspace.ts restoreCheckpoint (sin UI que la use fuera de Harness) | tests/test_edit_journal.py (journal aislado + escenario end-to-end de fallo de disco a mitad de lote); tests/qa/test_qa_17_lote_parcialmente_aplicado.py (nuevo, verde — antes xfail) | Journal y compensación por archivo dentro de un lote ya existen y están probados; sigue sin haber preview de lote con exclusión de cambios opcionales antes de aplicar. |
| EDIT-03 | existente (Lote 62) | src/agent_tools/filesystem_tools.py `_read_text_lf`/`_write_text_lf` preserva CRLF; atomic_write_text preserva permisos; Lote 62: BOM (UTF-8/UTF-16) se detecta y preserva explícitamente al leer/escribir, no se pierde ni se duplica | `studio/src/screens/documents/DiffView.tsx` (Lote 69b): distingue cambios funcionales de formato — "Ignore formatting", "{n} formatting-only change(s)", líneas de solo espacios/EOL ocultas; vista dedicada para binarios/archivos grandes ("This looks like binary content") | tests/test_edit_file.py, tests/test_l62_edit03_bom_preservation.py (nuevo), tests/test_l69b_edit03_diffview_formatting_js.py (nuevo) | Cerrado en Lote 62/69b: editar una línea en un fichero CRLF o UTF-8 con BOM no reescribe miles de líneas ni destruye caracteres; DiffView distingue cambios funcionales de formato y tiene vista de binarios/archivos grandes. |
| EDIT-04 | existente (cerrado en este lote — implementación ya estaba, faltaba el test de aceptación) | routes/workspace_routes.py GET /checkpoint/status, /checkpoint/changes ("what restore would touch"), /checkpoint/file (detecta binario), POST /checkpoint/restore; src/workspace_checkpoints.py::status(); `checkpoint_coverage(workspace, sha, external_effects=...)` (nuevo desde una ola previa, ahora probado por aceptación): reporta `covered`/`uncovered` (con razón, p.ej. `excluded_dir_never_snapshotted`) y los `external_effects` irreversibles que el llamador declare; `restore()` adjunta ese informe bajo `coverage` sin tocar sus claves históricas (`restored`/`deleted`/`failed`/`unchanged`) | studio/src/screens/studio/Harness.tsx usa restoreCheckpoint | tests/test_edit_checkpoint_coverage.py (incluye la prueba anti-regresión de que el shape antiguo de `restore()` no tenía `coverage`); tests/qa/test_qa_18_rollback_fuera_de_alcance.py (nuevo, verde — antes xfail) | `restore()` no reenvía `external_effects` suministrados por el llamador a su llamada interna a `checkpoint_coverage` — hay que declararlos aparte, en una llamada directa a `checkpoint_coverage(..., external_effects=...)`; no hay panel "Qué se restaurará/Qué no" en Studio todavía. |
| EXEC-01 | existente (Lote 67, badge en Lote 65) | src/agent_tools/subprocess_tools.py (`_create_bash_subprocess`: Git Bash nativo en Windows, cwd estructural); src/native_env.py (allowlist de env, scrub_private); src/doctor.py; Lote 67: `_execution_target(sandboxed, cwd, shell, remote=False)` (nuevo, `subprocess_tools.py` línea ~23) — etiqueta cada comando con dónde corre (Windows/WSL/Linux-macOS/contenedor aislado/worker remoto), propagado en `tool_output_data` y `tool_event` persistido (`agent_loop.py`) y en `PendingToolApproval.public_payload()` (`src/tool_approvals.py`) | Lote 65: `studio/src/screens/studio/Transcript.tsx` — badge "runs on {target}" en la tarjeta de la tool, i18n Windows/WSL/Linux-macOS/"sandboxed container"/"a remote worker" | tests/test_agent_bash_windows.py, tests/test_native_env.py, tests/test_l67_exec01_execution_target.py (nuevo) | Cerrado en Lote 65/67: una ruta `D:\Proyecto` no se entrega literalmente a un shell Linux (ya lo garantizaba `native_env.py`); ahora la tarjeta de la tool muestra dónde se ejecutará el comando (badge "runs on {target}"). |
| EXEC-02 | existente (Lote 67) | src/agent_tools/subprocess_tools.py usa `create_subprocess_exec` (argv, no shell=True); src/command_guard.py clasifica SAFE..CRITICAL; Lote 67: `command_guard.py::command_preview(command, max_len=4000)` (nueva) enmascara secretos en el comando efectivo antes de mostrarlo — `PendingToolApproval.public_payload()` (`src/tool_approvals.py`) añade `action["command_preview"]` | preview del comando/ámbito efectivo con secretos enmascarados en la tarjeta de aprobación (Lote 65/67) | tests/test_command_guard.py, tests/test_subprocess_hardening.py, tests/test_l67_exec02_command_preview.py (nuevo) | Cerrado en Lote 67: espacios, comillas, Unicode, globbing y rutas con `&` no cambian el significado de un argumento (argv, no shell); el preview del comando efectivo con secretos enmascarados ya es visible antes de aprobar. |
| EXEC-03 | existente | src/process_ownership.py (Ownership, TreeKill, creation_time, terminate_tree solo desciende por hijos propios); src/bg_jobs.py `_kill_job` usa process_ownership | studio/src/screens/Activity.tsx (botón Cancel), studio/src/screens/agents/Runners.tsx | tests/test_process_ownership.py, tests/test_process_tree_ownership.py | Terminal acoplable/buscar-copiar logs no verificado en detalle; el núcleo (árbol propio, no matar todo el sistema) está bien probado. |
| EXEC-04 | existente (Lote 66) | src/vram_admission.py (Ticket/admission de VRAM); Lote 66: recurso `cpu_heavy` (nuevo, `src/bg_jobs.py::acquire_cpu_heavy`/`release_cpu_heavy`, setting `cpu_heavy_max_concurrent`) — una research local ya compite por el mismo cupo que suites de tests (`src/project_tests.py::acquire_cpu_heavy_sync`, línea ~481) y modelos grandes; `src/deep_research.py::DeepResearcher.research()` adquiere el ticket antes de empezar y lo libera en `finally` | studio/src/screens/research/Research.tsx `startAll(sequential)` respeta el mismo cupo | tests/test_vram_admission.py, tests/test_l66_exec04_cpu_heavy_admission.py (nuevo) | Cerrado en Lote 66: una research local ya no compite sin control con varias suites de tests y otro modelo — comparten el cupo `cpu_heavy` hasta agotar el commit configurado. |
| SEC-01 | existente (Lote 68) | src/agent_gate.py (GateServer/judge, hook externo); src/approval_store.py (approve once/task, expiración TTL); routes/command_guard_routes.py allowlist GET/POST/DELETE; Lote 68: `GET /api/approvals/active` + `DELETE /api/approvals/{id}` (revocación inmediata) | `studio/src/screens/Settings.tsx::ActiveApprovalsCard` (Lote 69a) — "Active concessions", "Nothing standing right now.", botón "Revoke this concession now?"; allowlist de command_guard cableada en la misma pantalla ("Command guard allowlist") | tests/test_agent_gate.py, tests/test_approval_store.py, tests/test_l68_sec01_approvals_active_route.py (nuevo) | Cerrado en Lote 68/69a: un modelo o documento que diga "tengo permiso" no obtiene acceso fuera de la política; las concesiones activas (aprobaciones vigentes + allowlist de command_guard) son visibles y revocables desde Settings. |
| SEC-02 | existente | src/prompt_security.py `untrusted_context_message` (marca provenance, escapa marcadores de guardia, detecta evasión) | studio/checks/untrusted.check.mjs (sanitiza javascript:/data:/HTML hostil, fixpoint) | tests/test_studio_untrusted_content.py, tests/test_tool_output_prompt_injection.py, tests/test_prompt_injection_audit.py | Cobertura amplia; no se verificó cada superficie (email HTML, adjuntos) individualmente. |
| SEC-03 | existente (Lote 65) | src/tool_execution.py resuelve `os.path.realpath` y confina contra roots (symlinks); nombres reservados de Windows rechazados (lote 20); src/sandbox_exec.py ejecuta "skills" en Docker con límites; Lote 65 corrige el copy engañoso de `WorkspaceDialog.tsx` — ya no afirma que el "terminal" está confinado | `studio/src/screens/studio/WorkspaceDialog.tsx` (Lote 65): "The agent's file tools (read, write, edit) are confined to this folder. Its terminal is NOT sandboxed and can reach anywhere the Faustus process can." | tests/test_personal_dir_symlink_escape.py, tests/test_file_mentions_symlink_containment.py, tests/test_sandbox_exec.py, tests/test_lote20_sec03_reserved_windows_names.py, tests/test_sec_lote16_sec03_sec04.py | Cerrado en Lote 65: un symlink dentro del proyecto no da acceso a una carpeta externa (ya lo garantizaba el código); el texto de la UI ya no sobre-afirma que el terminal está confinado — copy honesto, alineado con `agent_loop.py`. |
| SEC-04 | existente (Lote 68) | src/outbound_fetch.py (SSRF); `src/privacy_policy.py::assert_outbound` (L41) cubre embeddings/chroma/resumidor/reranker (L44); Lote 68: `analyze_image_with_vl_result` (`src/document_processor.py`, OCR vision) y `_synthesize_api`/`_transcribe_api` (`services/tts/tts_service.py`/`services/stt/stt_service.py`) ya llaman `assert_outbound("ocr_vision"/"tts"/"stt", ...)` antes de la petición HTTP — local-only cubre ahora también OCR/TTS/STT, no solo el LLM principal y los auxiliares de contexto; `GET/PUT /api/privacy/profile` (nuevo, `routes/privacy_routes.py`) | `studio/src/screens/Settings.tsx::PrivacyProfileCard` (Lote 69a) — selector de perfil (`local_only`/`local_preferred`/`cloud_allowed`), texto actualizado: "Wired: ... OCR and telemetry call sites [have been] audited against this profile" (telemetría no tiene egress, documentado) | tests/test_outbound_broker_ssrf.py, tests/test_webhook_dns_rebinding_pin.py, tests/test_gallery_endpoint_ssrf.py, tests/test_rerank_privacy_policy.py, tests/test_l68_sec04_ocr_tts_stt_egress_audit.py (reescrito: ya afirma que SÍ están cableados) | Cerrado en Lote 68/69a: local-only cubre embeddings, OCR, reranker, telemetría (sin egress) y fallback, no solo la llamada al LLM principal; perfil activo visible y elegible en Settings. |
| SEC-05 | existente | core/log_safety.py (redact_secrets, SecretRedactingFilter/Formatter instalado en root loggers); src/secret_storage.py (Fernet), src/secret_files.py (harden_secret_files) | studio/src/screens/settings/SystemExtras.tsx (logs de diagnóstico) | tests/test_sec1_secret_hygiene.py (redacción de tracebacks/handlers), tests/test_api_key_file_permissions.py | No se localizó un export/"crash dump" dedicado con test explícito, pero la redacción está instalada globalmente en logging. |
| SEC-06 | existente (gap de `question_store` cerrado en lote 20) | core/authz.py (reglas API por scope); decenas de rutas con chequeo de owner (routes/artifact_routes.py, routes/memory/memory_routes.py); src/question_store.py::Store.get/resolve/cancel ahora reciben y comprueban `owner` (normalizado como src/tool_approvals.py; un mismatch lee como "not_found"/None, nunca revela existencia; `owner=None` no filtra, por compatibilidad); routes/chat_routes.py:1937 pasa `owner=owner` a `resolve_question` | (estado de usuario/proyecto visible en Studio, no auditado en detalle) | ~46 archivos test_*_owner_scope.py / IDOR (sessions, memorias, gallery, research, documentos, etc.); tests/test_sec_lote16_sec05_sec06.py (el xfail que demostraba el gap de `question_store` es ahora verde) | Cobertura muy amplia y consistente; no se auditó cada endpoint individualmente (miles de rutas). `question_store`: cerrado, sin gap nuevo conocido. |
| SEC-07 | existente | desktop/main.cjs (`contextIsolation:true, sandbox:true, nodeIntegration:false, webSecurity:true`, CSP en splash, setWindowOpenHandler/will-navigate controlados); desktop/preload.cjs (contextBridge mínimo) | studio/src/screens/documents/Editor.tsx, compare/Compare.tsx, email/Reader.tsx usan `<iframe sandbox="...">` para HTML/preview | desktop/desktop-control.test.cjs, tests/test_security_headers_middleware.py | Bien evidenciado; no se probó a fondo el puente IPC (`ipcMain.handle('faustus:window',...)`) más allá de lo visto. |
| OPS-01 | existente (ola 5) | src/doctor.py (Finding por área: backends, sandbox image, modelos, memoria vectorial, y ahora setup/entorno — distingue dependencia faltante de fallo); registro de reparaciones allowlisted (ver tests/test_ops_doctor.py) para que el "asistente" solo ejecute acciones conocidas, nunca arbitrarias; src/health.py; requirements.txt/pyproject.toml/package-lock.json versionados | lote 37: `/setup` (`studio/src/screens/Onboarding.tsx`, ahora enrutado desde `AppShell.tsx`) consume `GET /api/setup/status`, la parte de doctor.py pensada como "próxima acción" de primer uso | tests/test_doctor.py, tests/test_doctor_services.py, tests/test_ops_doctor.py (nuevo) | El resumen de primer uso ya está expuesto en Studio; el panel de Diagnóstico completo (`/api/doctor`, todas las áreas) sigue sin consumirse fuera de ese resumen. |
| OPS-02 | existente (ola 5) | scripts/update_database.py (ALTER TABLE condicional); nuevo: `src/migrations.py` — backup real antes de migrar, rollback si un paso a mitad de migración lanza, y NO repite el backup cuando no hubo cambios (ver test_ops_migrations.py, que inyecta el fallo en vez de romper la fuente para probarlo); update_windows.bat (git pull + docker compose up, sin backup/rollback/modo mantenimiento propio, fuera de este mecanismo) | (sin notas de cambios/permisos nuevos ni posposición por tareas activas) | tests/test_update_database_script.py (solo valida guard `__main__`), tests/test_windows_update_script.py, tests/test_ops_migrations.py (nuevo) | El script de actualización (`update_windows.bat`) sigue sin preflight de versión ni comprobar tareas activas; lo cerrado es el mecanismo de migración de esquema en sí (backup/rollback/sin-doble-backup), no el flujo de actualización completo del instalador. |
| OPS-03 | existente (ola 5) | src/backup_service.py (`_sqlite_safe_copy` WAL-aware, write_manifest con HMAC+sha256, verify_archive, prune); nuevo: `verify_backup` extrae de verdad el archivo a un directorio desechable, abre cada base de datos, cuenta filas por tabla y las compara contra el manifiesto grabado en el snapshot — ya no es solo un integrity_check de un tarball que nadie ha restaurado; routes/backup_routes.py (/api/backup/snapshot, /verify, /snapshots) | studio/src/screens/settings/SystemExtras.tsx `BackupCard` (solo export/import completo) | tests/test_backup_service.py, tests/test_backup_wiring.py, tests/test_ops_backup_verify.py (nuevo) | La verificación ahora prueba una restauración real (conteo de filas por tabla), no solo integridad del tarball. Studio sigue sin exponer snapshots/verify/última-copia-comprobada ni restauración selectiva, solo un export/import de todo. |
| OPS-05 | existente (ola 5, integrado en lote 37) | `src/safe_mode.py` (nuevo): modo seguro automático tras `FAUSTUS_SAFE_MODE=1` o tras `BOOT_FAILURE_THRESHOLD` (2) arranques consecutivos que nunca llegaron a `mark_boot_completed()`; cuarentena automática de un servidor MCP tras `MCP_QUARANTINE_THRESHOLD` (3) intentos consecutivos en `error`/`needs_auth`, reutilizando `disabled_tools` (el MISMO ajuste que ya lee el gate de herramientas) en vez de una segunda lista; reactivación uno a uno por subsistema o por servidor; núcleo (chat/ficheros/settings) nunca tiene interruptor en este módulo. Lote 37 cierra el cableado que faltaba: `src/mcp_manager.py` llama `safe_mode.record_mcp_connection(server_id, status)` en cada punto que fija `error`/`needs_auth` (stdio/SSE/HTTP, el callback OAuth `_on_redirect`, y el reintento de servidores built-in); `app.py`'s `_lifespan`/`_startup_event` llama `safe_mode.mark_boot_started()`/`mark_boot_completed()` alrededor del arranque real — necesario porque la mayoría de los caminos de lanzamiento reales (Docker, `start-macos.sh`, `launch-windows.ps1`, `uvicorn app:app` directo) NO pasan por `launcher.py`, que es el único sitio que ya llamaba a esas dos funciones | studio/src/screens/settings/SystemExtras.tsx (logs de diagnóstico); sin panel dedicado a modo seguro / servidores en cuarentena | tests/test_ops_safe_mode.py, tests/qa/test_qa_46_plugin_problematico.py, tests/test_crash_recovery.py (boot scan, nunca bloquea el arranque) | Modo seguro y cuarentena de MCP ya están cableados de extremo a extremo (arranque real → `mcp_manager` → `safe_mode`). Falta pantalla en Studio para ver/reactivar subsistemas o servidores en cuarentena (hoy solo vía `/api/safe-mode/*`, sin UI) y no hay "volver a versión anterior". |
| EVAL-01 | existente (evidencia propia; `agent-bench/` externo sigue no verificable) | `tests/eval/` (harness `EvalApp` que reusa `scripts/ui_smoke.py`, 6 tareas registradas — bug_fix/feature/refactor/investigation/document/tabular — en `tests/eval/tasks.py`, cada una con `setup`/`verify` propios); `scripts/eval_run.py` (compara contra `tests/eval/baseline.json`; `--live` está escrito y con su propia prueba de forma, pero no se ejecuta en este sandbox sin endpoint de modelo real); src/scorecard.py | (sin pantalla "Lab" de resultados por tarea/perfil con baseline) | tests/test_eval_tasks.py (nuevo, rápido, sin app/modelo), tests/test_eval_baseline.py (nuevo), tests/eval/test_representative_tasks.py, tests/eval/test_baseline_match.py | El banco propio del repo (6 tareas, harness, baseline) es real y verificable; `agent-bench/` (el banco externo que menciona FAUSTUS.md) sigue viviendo fuera de este checkout y no se puede auditar desde aquí. |
| EVAL-03 | existente (Lote 67) | src/disk_ballast.py; src/dispatch.py (idempotency_key); src/crash_recovery.py; tests/eval/test_chaos_protocols.py (lote 36); Lote 67: `src/chaos.py` (nuevo) — framework unificado de fixtures de caos (dry-run: nunca inyecta un fallo real, solo pregunta qué inyectaría y qué debería hacer un sistema correcto), `POST /api/ops/chaos/{fixture}` | `studio/src/screens/settings/SystemExtras.tsx::ChaosCard` (Lote 69a) — "Chaos fixture (dry run)", campo `fixture name`, botón "Dry run", muestra "Would inject"/"Expected" | tests/test_disk_ballast.py, tests/test_dispatch_reliability.py, tests/eval/test_chaos_protocols.py, tests/test_l67_eval03_chaos_dry_run_route.py (nuevo) | Cerrado en Lote 67/69a: ningún fallo inyectado convierte una acción incierta en éxito ni duplica un efecto externo; ya hay un framework unificado de caos con tarjeta "reproducir fixture (dry-run)" desde Studio. |
| EVAL-04 | existente (Lote 40, confirmado Lote 60) | studio/checks/*.mjs (~40 checks unitarios vía node+esbuild); `scripts/ui_smoke.py` — Chromium real (Playwright) contra el build servido, login real, nueva conversación, envío de mensaje + tarjeta `ask_user`, Settings › Effective config/Tools/System | tests/e2e/test_agent_flows.py, test_studio_baseline.py (Playwright, journeys reales) | tests/test_studio_*_js.py, tests/e2e/conftest.py; `docs/spec/v2/EVAL_ESTADO.md` | Cerrado en Lote 40 (bug de enrutamiento de `agent_loop.py` que impedía ver la tarjeta `ask_user` en `ui_smoke.py` paso 3), confirmado en verde por el Lote 60: `scripts/ui_smoke.py` pasa sus 3 pasos completos. Una tarjeta funciona en vivo, recarga, historial importado y stream reconectado con el mismo contrato — sigue corriendo contra un LLM simulado a propósito (no integración final). |
| EVAL-05 | existente | tests/test_agent_asks_before_a_new_system.py (regla nace de incidente del 10-09-2026, verifica literalmente que el prompt sigue preguntando ante diseño ambiguo); src/output_oracle.py ("false success" no se puede colar por exit 0) | (sin pantalla que vincule bug/test/release) | tests/test_pr6020_rebase_regressions.py, tests/test_kv_cache_invalidation_2927.py, tests/test_agent_asks_before_a_new_system.py | Patrón de fixture-por-incidente bien establecido y con casos nombrados por PR/issue; no se halló vínculo bug↔test↔release en Studio ni mecanismo que impida re-listar un fallo ya corregido como pendiente. |

## Grupo E — Producto e interfaz: ART, MEDIA, CONN, AUTO, UX, SET, A11Y, LANG, BENCH

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| ART-01 | existente (ola 5) | src/artifact_store.py (`collect`/`persist`/`publish_copy`: hash, escritura temporal + link atómico, checksum verificado; ahora escribe un manifiesto versionado por artefacto, encadenado y solo-apendable, con `call_id`); nuevo: máquina de estados `draft`→`generated`→`validated`→`reviewed`/`discarded` (un fallo de validación de formato es lo que mantiene un artefacto fuera de `validated`, y por tanto fuera de `reviewed`); validaciones de formato reales para DOCX (tablas caben en la página), XLSX (fórmulas marcadas para recalcular al abrir), PDF, JSON e imagen; `src/artifact_migration.py::migrate_manifests` rellena manifiesto a ocurrencias previas al mecanismo (idempotente, `dry_run` real), llamado en el arranque de `app.py` (lote 36) y con backfill en background (lote 37 lo re-verifica); src/contracts/artifact.py (`Artifact.partial: bool`); routes/artifact_routes.py (list/{id}/{id}/download por owner) | studio/src/screens/ArtifactInfo.tsx + adapters/artifact-info.ts (provenance, partial/completo, descarga) | tests/test_art_manifest.py (nuevo), tests/test_art_migration_manifest.py (nuevo) | Los estados preparando/generando/validando/fallido ya existen (más allá del bool `partial`, que se conserva por compatibilidad). Frontend sigue sin "abrir en app externa", "revelar en carpeta" ni "copiar referencia"; solo descarga y ficha de procedencia. |
| MEDIA-01 | existente (ola 5, ampliada) | src/chat_handler.py (`vision_enabled`, `model_supports_vision`, `analyze_image_with_vl_result`; despoja `image_url` para modelos de solo texto y usa descripción VL en su lugar); nuevo: `src/media_capabilities.py` — probing real, con fecha, de ffmpeg/ffprobe (versión vía subproceso), STT/TTS (Kokoro/Piper, por `importlib.find_spec`/`shutil.which`) y ComfyUI; nunca asume "instalado" por una lista estática — solo porque una prueba real acaba de decirlo; `routes/local_video_routes.py` (`GET /api/media/capabilities`, `GET /api/media/local-video/capabilities`, `POST /{mode}`) | nuevo: `studio/src/screens/studio/LocalVideo.tsx` consume `/api/media/local-video/capabilities` para explicar qué herramientas locales de vídeo hay realmente disponibles antes de ofrecer generarlo | tests/test_vision_model_detection.py, tests/test_lmstudio_vision.py, tests/test_vision_owner_scope.py, tests/test_media_capabilities.py (nuevo) | Vision en el chat sigue sin explicar al usuario si verá imagen, transcripción o descripción derivada (Composer.tsx no lo menciona); las capacidades de medios locales (vídeo/audio) sí están ahora probadas y expuestas en Studio con fecha/estado real. |
| CONN-02 | existente (ola 5) | routes/email_routes.py: `GET /search`, `GET /read/{uid}` (lectura) separados de `POST /draft`, `POST /send`, `GET/POST/DELETE /pending/{sid}` (agent_draft→approve, escritura/ejecución); nuevo: `POST /api/email/send/prepare`, `/execute/{id}`, `/reconcile/{id}` — leer/preparar/ejecutar como tres pasos distintos sobre `src/connector_outbox.py`, que reutiliza `approval_store` y el `outcome_unknown` de QA-11 (un envío cuyo resultado no se pudo confirmar no se reintenta a ciegas ni se da por enviado) | studio/src/screens/email/Compose.tsx (revisión de destinatarios/adjuntos antes de enviar), Outbox.tsx (borradores de agente pendientes de aprobación) | tests/test_conn_02_email_send_routes.py (nuevo, HTTP real vía TestClient, SMTP/IMAP con stub de transporte), tests/test_conn_02_outbox.py (nuevo, mecanismo `connector_outbox.py` sin HTTP) | Backend separa bien leer/preparar/ejecutar/reconciliar, con `outcome_unknown` cuando el resultado del envío no se pudo confirmar. No se verificó revisión de calendario/zona horaria en el compose ni que aprobar un borrador impida cambiar destinatario/adjunto. |
| CONN-03 | existente (ola 5) | routes/email_routes.py `GET /resolve-contact` (busca en Sent/INBOX/Drafts, devuelve candidatos); routes/contacts/contacts_routes.py `GET /search`; ambas superficies (deliberadamente sin import compartido entre routers) devuelven `ambiguous_identity` con la lista de candidatos en vez de elegir un ganador cuando dos empatan en la mejor puntuación | studio/src/screens/email/Compose.tsx `Recipients()` (autocompletar con lista `role="listbox"`, selección explícita por teclado/click, no auto-elige) | tests/test_conn_03_ambiguous_identity.py (nuevo, HTTP real sobre ambos routers + unit de la regla de puntuación que cada uno mantiene por su cuenta) | El picker de Compose.tsx exige selección explícita (nombre+email) y ninguna de las dos superficies elige un ganador en un empate; sigue sin mostrar "origen" en el picker, y no se reverificó en este lote si el envío guiado por agente (capa de herramientas) ya llama a `resolve-contact`. |
| AUTO-02 | existente (ola 5) | src/approval_store.py (`status`: granted/denied/**expired** distintos, `uses_left` como presupuesto de usos, `expire_stale()`); src/agent_profiles/resolver.py `snapshot()` (fija permisos/resolución por ejecución, `identity`+`trace`); nuevo: `src/autonomy_budget.py` (`set_task_policy`/`get_task_policy`) declara presupuesto Y permisos por tarea programada desde su creación — `_execute_llm_task` recorta `disabled_tools` con `all_tools - permissions` ANTES de que el bucle del agente vea la lista de herramientas (nunca las amplía en ejecución), y `_run_agent_loop` para el turno a mitad de camino, cierra el stream y deja una notificación con checkpoint al agotar el presupuesto — nunca sigue en silencio ni se detiene sin avisar; el mismo contrato existe para workflows (`WorkflowEngine.advance`/`_check_budget`: el presupuesto agotado pausa el run con el estado `"paused"` ya existente, y un nodo fuera del permiso declarado falla de forma terminal sin llegar a su handler) | studio/src/screens/Activity.tsx (filtro "Needs action", contador `waiting`, tarjeta de aprobación "Nothing happens until you decide") | tests/test_auto_02_task_budget_permissions.py (nuevo), tests/test_auto_02_workflow_budget_permissions.py (nuevo), tests/test_auto_02_task_dst_misfire.py (nuevo, QA-43: política DST/misfire explícita para tareas programadas) | Caducar nunca es "granted"; presupuesto+permisos declarados ahora se hacen cumplir en cada ejecución para tareas Y workflows, con checkpoint al agotar. Sigue sin presupuesto monetario explícito (solo `uses_left`/tiempo/nº de subagentes/memoria — ver TASK-06) ni política configurable para ausencia prolongada del usuario. |
| UX-01 | existente (Lote 65, cableado por sección A del cierre 60-70) | routes/editor_draft_routes.py (solo canvas); `GET/PUT /api/sessions/{sid}/draft` (nuevo — `routes/session_routes.py`, `{"text","attachment_ids","updated_at"}`, por owner, tope 64 KB) — el borrador de mensaje de chat ya tiene ruta server-side, no solo `localStorage` | studio/src/screens/Studio.tsx (`readDraftFor`/`writeDraftFor` sincronizan con la ruta server-side además de `localStorage`, debounce 1s; al cargar, la más reciente gana); `readAttachmentsFor`/`writeAttachmentsFor` (L29) | tests/qa/test_qa_35_borrador_persistente.py, tests/test_l65_ux01_session_draft_route.py (nuevo) | Cerrado en el cierre de lotes 60-70 (fila 14 de la sección A de integración): recargar durante un borrador largo restaura texto, adjuntos preparados y ubicación lógica sin enviarlo, ahora con respaldo server-side (límites/limpieza), no solo `localStorage`. |
| UX-02 | existente (Lote 61) | routes/chat_routes.py `GET /api/chat/stream_status/{id}` y `/resume`; Lote 61: `routes/chat_routes.py::_idempotent_replay_stream` (línea ~297) — clave de idempotencia server-side sobre el turno de chat, reenviar la misma petición tras una pérdida de respuesta reproduce el stream ya emitido (outbox real, no solo memoria del cliente) | studio/src/screens/studio/Composer.tsx (IME); studio/src/screens/Studio.tsx (`sendingMessage`/`busy`); banner "Uncertain outcome, checking…" cuando el resultado está pendiente de confirmar | tests/test_l61_task03_idempotent_chat_replay.py (nuevo) | Cerrado en Lote 61: doble clic o reconexión no crea dos tareas (idempotencia server-side, no solo guard en memoria del cliente); Enter durante composición IME no envía antes de tiempo. |
| UX-04 | existente (ola 5) | routes/chat_routes.py `POST /api/chat/subagent/steer/{child_session_id}` (`steer_worker`, solo subagentes) — y ahora `POST /api/chat/steer/{session_id}` para el turno PRINCIPAL, con `mode="steer"` (por defecto: el texto se inyecta como mensaje de usuario en el siguiente límite de ronda, vía `pending_user_messages` en `src/agent_runs.py`, el mismo punto seguro que consume `pending_pause`) o `mode="queue"` ("Enviar después": nunca toca la cola de steering del turno vivo, se guarda en `send_after_queue` para entregarse cuando el turno termine) | studio/src/screens/studio/Composer.tsx: menú "Dirigir…"/"Enviar después" (`noteMode: 'steer' | 'queue'`) además del botón "Stop"; usa `onSteer` | tests/test_steer_routes.py (nuevo) | Steering de la tarea principal y la cola "enviar después" ya están expuestos en la UI principal, no solo para subagentes. |
| UX-03 | existente — ver `MAPA_P1.md` (P1, no P0) | `routes/chat_routes.py::regenerate_chat_response` — `POST /api/chat/regenerate/{sid}` (L40, QA-36): reinyecta evidencia y bloquea toda tool no probadamente de solo lectura. Fila mantenida aquí solo por continuidad histórica: UX-03 es P1 en `backlog.json`, y `MAPA_P1.md` ya la marca "existente (Lote 40)" desde el Lote 55 — este mapa P0 llevaba desincronizado desde el Lote 44 (inconsistencia señalada explícitamente por el enunciado de este cierre) | `studio/src/screens/studio/MessageNavigator.tsx` navega turnos lineales, no ramas visuales | tests/test_l40_chat_regenerate_route.py, tests/qa/test_qa_36_regenerar_con_efectos.py | Resincronizado en Lote 70: el criterio literal de QA-36 (regenerar una respuesta que informó de un correo enviado no envía ese correo de nuevo) ya está cerrado. "Árbol de conversación" visual del título completo del ID (no un versionado lineal recuperable) sigue sin auditar — ver `MAPA_P1.md`. |
| UX-05 | existente (Lote 65) | n/a (lógica de cliente) | studio/src/screens/Studio.tsx (`pinnedRef`, autoscroll condicionado); studio/src/screens/studio/MessageNavigator.tsx (saltos entre turnos); Lote 65: `studio/src/screens/studio/Transcript.tsx` — búsqueda dentro de la conversación ("Search in this conversation…", Previous/Next match, Close search) y botón "nuevos mensajes" cuando el lector no está al final | mismos ficheros, i18n Lote 65 ("Search in this conversation", "Previous match", "Next match") | tests/test_l65_ux05_transcript_search_js.py (nuevo) | Cerrado en Lote 65: seleccionar/copiar un párrafo anterior no hace saltar al final; ya hay búsqueda dentro de la conversación y aviso de nuevos mensajes cuando el lector no está al final. |
| UX-08 | existente (Lote 65) | routes/chat_routes.py maneja `sources`; Harness recibe `summary`/`checks`; `src/contracts/errors.py` taxonomy; L29 ya usaba `describeError`/`friendlyError` con `errorClass`/`traceId`/`stepId` camelCase | studio/src/screens/studio/Transcript.tsx: `CopyButton`, `IconButton RefreshCw`; Lote 65: `studio/src/screens/studio/model.ts::errorTraceFields()` corregido — ya lee los campos camelCase (`errorClass`/`traceId`/`stepId`) que `chat.ts::decode()` expone, en vez de los snake_case crudos del `ChatEvent`; diagnóstico técnico desplegable en el párrafo de error | tests/test_l29_studio_error_trace_decode_js.py, tests/test_l65_ux08_model_ts_error_trace_fields_js.py (nuevo) | Cerrado en Lote 65: el usuario puede leer la causa y reanudar aunque haya desaparecido el toast de error — `model.ts::errorTraceFields()` ya recoge los campos camelCase, cerrando el gap de mapeo que dejaba L29. |
| BENCH-02 | existente (lote 36, la parte "medir" la cubre EVAL-01) | src/changesets.py + routes/changesets_routes.py (`/build`, `/from-dispatch/{job_id}`, `/diff` con checkpoint sha); nuevo: `src/auto_review.py::review_gate` — un `ChangeSet` solo puede aprobarse como "reviewed" con un diff real (`files.exact`, nunca truncado) Y un veredicto de test decidido (`verification.ran` y `.ok is not None`; que los tests fallen no bloquea por sí solo, un verificador que nunca corrió sí) | studio/src/screens/documents/DiffView.tsx (`decide(id,accepted)` por chunk, botones "Take"/"Keep mine") — es para edición de documentos, no para cambios de código con dependencias | tests/test_bench_review.py (nuevo) | `review_gate` cierra la exigencia de "diff real + veredicto de test" para aprobar un cambio; sigue sin ofrecer aceptar/rechazar por hunk ni abrir en editor externo para cambios de código (DiffView.tsx cubre eso solo en el dominio de documentos, sin relaciones de dependencia). |
| SET-01 | existente (ola 5) | src/doctor.py (`run()`/`_backends`/`_sandbox_image`/`_models`/etc.: chequeos no destructivos, reporta `absent` en vez de `fail` cuando falta Docker/ComfyUI); nuevo: `GET /api/setup/status` (`routes/diagnostics_routes.py`) recorre los mismos chequeos que `doctor.py` en el orden que de verdad los desbloquea (cuenta admin antes que proveedor antes que modelo) y nombra UNA acción siguiente, nunca un callejón sin salida (ver test_ops_doctor.py) | lote 37: `studio/src/screens/Onboarding.tsx` (primer uso: título+cuerpo+una acción según `/api/setup/status`) ahora tiene ruta real — `/setup` en `studio/src/shell/AppShell.tsx` (antes existía la pantalla sin enrutar, ver su propio docstring); studio/src/screens/cookbook/* (Fit/Download/Servers/Models) gestiona modelos locales, no es un asistente de primer uso | tests/test_ops_doctor.py (nuevo) | El recorrido guiado mínimo de primer uso (conectar modelo → probar chat → herramienta segura) ya existe y está enrutado en Studio. El panel de Diagnóstico completo (`/api/doctor`, todas las áreas) sigue sin un segundo consumidor fuera de este resumen. |
| SET-03 | existente | src/agent_profiles/contracts.py `PRECEDENCE=("system_policy","owner_policy","project_restriction","activity_policy","task_override","agent_default","project_default","global_default")`; resolver.py `explain()`/`explain_rows()`; src/agent_settings_schema.py (`coerce_setting_value`, `_clamp`, help por campo) | studio/src/screens/Settings.tsx (`data-changed` marca campos que difieren de `schema.defaults`) | No enumerados | Precedencia global→proyecto→agente→turno es dato auditable (`explain`), con validación/clamp al guardar. No se encontró buscador de ajustes, modo simple/avanzado ni "copiar perfil" en Settings.tsx. |
| SET-05 | existente (ola 5) | src/chatgpt_subscription.py (`ChatGPTSubscriptionReauthRequired` se lanza en vez de caer a otra API); src/api_key_manager.py (claves por backend); nuevo: `GET /api/setup/provider-change-preview` (`routes/diagnostics_routes.py`) — antes/después de cambiar el endpoint por defecto, calculado siempre desde `ModelEndpoint.endpoint_kind` (nunca adivinado): privacidad (local, nunca sale de la máquina / remoto, con destino), coste (sin coste por token / facturado / "sin API key guardada, probablemente fallará") y capacidades ya probadas (`not_probed` si este proceso nunca lo sondeó — la misma regla "desconocido, no asumido ok" de `src.doctor`) | lote 37: `studio/src/screens/Settings.tsx`'s `DefaultsSection.save()` llama a esta ruta cuando el endpoint por defecto cambia, y si hay `changes` no vacío pide confirmación explícita (`window.confirm`, el mismo patrón que ya usa esta pantalla para quitar un endpoint) antes de guardar — advisory: un fallo de red al pedir la vista previa nunca bloquea el guardado | tests/test_l37_settings_provider_change_preview_js.py (nuevo) | El cambio consciente de proveedor/modelo por defecto ya está cableado de extremo a extremo (backend + confirmación en Settings.tsx). Sigue sin indicador Local/Remoto por llamada individual del chat ni cálculo de categorías de datos antes de usar un backend remoto para una petición concreta. |
| A11Y-01 | existente | n/a (funcionalidad de frontend) | studio/src/shell/CommandPalette.tsx (Ctrl+K); studio/src/screens/Settings.tsx `ShortcutsSection` (remapeable, detecta duplicados, botón "Defaults"); studio/src/components/Dialog.tsx (Radix: focus trap, `aria-modal`, restauración de foco; lote 44: cabecera título+cerrar acotada con `min-inline-size:0`/`max-inline-size:100%` para que un título largo no empuje el botón de cerrar fuera de la caja a 200% zoom) | tests/e2e/test_composer_shortcuts.py, tests/e2e/test_studio_baseline.py | Atajos remapeables, paleta de comandos y diálogos con trampa de foco real vía Radix están implementados y probados en e2e. No se verificó cobertura 100% teclado del flujo de revisión de diffs (ver BENCH-02). QA-44 (`docs/spec/v2/QA_ESTADO.md`) sigue xfail: el fix de Dialog.tsx de este lote es correcto pero no era la causa raíz del hueco de zoom — la instrumentación directa del lote 44 la traza a `Studio.tsx` (`pickWorkspace()`/Suspense del diálogo), fichero ajeno a A11Y-01 y a este lote. |
| LANG-01 | existente (Lote 67) | src/reply_language.py (directiva de idioma por turno); Lote 67: `src/action_intents.py` generaliza los intents en español ("impleméntame", "créame" y variantes) a todo dominio de capacidad, no solo objetivos/metas de proyecto — mismo patrón que los equivalentes en inglés | studio/src/i18n/es.ts; `locale()` en formateo de número/fecha | tests/test_reply_language.py, tests/test_agent_loop_workspace_tool_floor.py, tests/test_action_intents.py, tests/test_l67_lang01_generic_spanish_intents.py (nuevo) | Cerrado en Lote 67: "Impleméntame" y "créame" activan la capacidad correcta en cualquier dominio, no solo objetivos de proyecto; un error de backend aparece traducido con diagnóstico técnico desplegable. |

