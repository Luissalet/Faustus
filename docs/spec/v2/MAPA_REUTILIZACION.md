# Mapa de reutilización (BASE-01) — P0 contra el código real

Baseline: `master b824057e` (10-09-2026). Auditoría por lectura de código (grep/glob sobre `src/`, `routes/`, `services/`, `core/`, `studio/src/`, `tests/`), sin ejecutar la aplicación. Cada fila cita rutas que existen en ese commit; «existente» exige comportamiento, no nombre de módulo. Regla de Luis que manda sobre todo lo que sigue: **no se pierde ninguna capacidad; se extiende, reorganiza y mejora**.

Leyenda: existente · parcial · ausente · no verificable.

Resumen (tras lote 20): parcial 70, existente 22, ausente 7, no 1 (total 100
de 100 P0). Cambios de este lote: MOD-01 (parcial→existente), MOD-02
(ausente→existente, ya estaban implementados en una ola previa y el mapa no
se había actualizado), EDIT-02 (parcial→existente, journal/compensación ya
implementados, cerraba QA-17), EDIT-04 (parcial→existente, coverage/restore
ya implementados, cerraba QA-18).

Resumen (tras lote 29, integración ola 4 — lotes 21/23/24/26/27/28): parcial
70, existente 23, ausente 6, no 1 (total 100 de 100 P0). Cambios de este
lote: MEM-02 (parcial→existente, `forget()`/`correct()` ya existían de una
ola previa sin ruta HTTP — L29 añade la ruta y el test de aceptación),
WEB-04 (ausente→parcial, `src/browser_actions.py` ya existía y probado de
forma aislada de una ola previa sin llamador real — sigue sin cablearse
porque el punto de invocación real, `src/tool_execution.py`, es un fichero
ajeno a este lote). ARCH-01, OBS-01, ACT-03, PERF-04, UX-01, UX-08 revisados
y ampliados con lo que este lote añadió (ver filas); el resto de IDs
listados en L29 (OBS-02, VER-01…06, WEB-01…03, MEM-01/05, IDX-01/04,
HW-01/02, ACT-01/04/06, UX-02/04/05, A11Y-01, LANG-01) no se reauditó contra
el código — sus filas quedan como estaban, sin cambios propios de este lote.

## Grupo A — Control: BASE, ARCH, TASK, PLAN, OBS, ACT

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| BASE-01 | parcial | OBJETIVOS.md, PENDIENTES.md (bitácora manual con evidencia); `/api/version` en app.py:1331 devuelve `APP_VERSION="1.0.3"` fijo | ninguna — `/api/version` no se consume en studio/src; `studio/src/screens/settings/SystemExtras.tsx` solo tiene Logs/Backup/Danger | — | No hay mapa código↔requisito automatizado; Diagnóstico no muestra versión ni build servidos. |
| BASE-02 | parcial | `src/state_mirror/contracts.py` (orden de autoridad `observed`>...), `src/state_mirror/reconcile.py` (sweep+retire), `src/state_mirror/adapters/{approvals,models,runs,sessions,workspace,...}.py` | `studio/src/screens/StateMirror.tsx` + `adapters/stateMirror.ts` | test_state_mirror_conflict_resolution.py, test_state_mirror_replay.py, test_state_mirror_reduction_boundaries.py | Sin adapter de memoria; StateMirrorScreen es panel aislado, no está probado que el resto de pantallas recarguen desde él. |
| BASE-03 | parcial | `src/doctor.py` (checks python/git/backends/modelos/memoria/skills), ruta `/api/doctor` en `routes/changesets_routes.py:184`; `Start-Faustus.ps1` monta venv/npm ci/build sin copiar ficheros personales | ninguna — cero referencias a `api/doctor` en studio/src | tests/test_doctor.py, tests/test_doctor_services.py | Backend sólido pero sin asistente de arranque en UI que enseñe el componente ausente y su reparación. |
| ARCH-01 | parcial (revisado L29) | `src/contracts/{event,artifact,run,changeset,memory,skill,workflow,approval}.py`: dataclasses tipadas con `schema_version` y `semver()`; `src/api_version.py` (`API_VERSION`/`MIN_CLIENT_VERSION`, `X-Faustus-Api-Version`/`X-Faustus-Client-Version`, 426 si el cliente se identifica como demasiado viejo) ya existía de una ola previa y no estaba reflejado aquí; L29 añade el mismo header a `app.py`'s `/api/version` (antes solo lo llevaban las respuestas de chat SSE) | `studio/src/adapters/api.ts` (constantes de cabecera), `studio/src/adapters/chat.ts` (`getVersionMismatch`, detecta el desajuste desde la cabecera de respuesta), `studio/src/screens/studio/model.ts` (`versionMismatch` en el turno) | tests/test_contracts.py, tests/test_contracts_mcp_and_routes.py, tests/test_api_version.py, tests/test_l29_api_version_header.py (nuevo) | Sigue sin haber contrato de "turno"/"herramienta" versionado; la negociación cubre solo el wire de chat SSE (ahora también `/api/version`), no cada endpoint. |
| ARCH-02 | existente | `src/process_ownership.py` (`terminate_tree`, `note_started`, `ownership_unknown`), `server_runtime.py` (`start --owner web/desktop`), `routes/chat_routes.py:2773-3190` guarda respuesta parcial al desconectar cliente | `desktop/main.cjs`: `ownedToken` solo si `result.started`; `shutdown()` sólo para token propio | tests/test_process_ownership.py, tests/test_process_tree_ownership.py, tests/test_tool_task_cancelled_on_disconnect.py | Verificado: desconexión no mata tarea, y cierre no detiene servidor reutilizado. Falta ver runners no-Ollama. |
| ARCH-03 | ausente | Ningún compilador de precedencia global/proyecto/rol/modelo/turno con hash+origen; `src/agent_loop.py` define `_AGENT_RULES` DOS VECES (líneas 357 y 473) — la segunda pisa la primera en silencio | ninguna — no hay inspector "qué instrucciones se aplican" | — | Contradice directamente el requisito: exactamente el "orden accidental de definición" que se pide detectar como conflicto. |
| TASK-01 | parcial | `src/agent_tools/interaction_tools.py::UpdatePlanTool` recibe markdown libre y hace `plan = plan[:8192]` (línea ~84) — trunca en silencio | plan acoplado (docked) vía evento `plan_update`, sin ver fichero de pantalla dedicado | tests/test_update_plan_tool.py, tests/test_plan_mode.py | Sin IDs/dependencias/estado estructurado ni evidence_refs ni historial de revisiones; trunca justo en el límite que la aceptación exige conservar. |
| TASK-02 | parcial | `src/research_handler.py::recover_interrupted/list_interrupted`, `src/crash_recovery.py::find_interrupted/mark_interrupted` marcan runs "interrupted" tras reinicio | tarjeta "Reintentar" (según PENDIENTES.md 10-09 #14) que reinicia desde cero | tests/test_research_restart_survival.py, tests/test_crash_recovery.py, tests/test_studio_research_restart_js.py | Detecta interrupción y sobrevive al reinicio, pero Reintentar repite desde cero: no reconstruye por rondas/fuentes confirmadas ni distingue Reanudar/Alternativa. |
| TASK-03 | parcial | `src/workflows/store.py` (`idempotency_key`, `claim`, patrón outbox comentado), `src/media_runs.py` (outbox real, líneas ~53-491), `src/task_scheduler.py` (`idempotency_key` por tarea) | sin texto "resultado incierto, comprobando" localizado en studio/src/i18n/es.ts | tests/test_media_runs_outbox.py, tests/test_workflow_store_races.py | Idempotencia sólo en workflows/media/tasks programadas, no es un diario transaccional general (p.ej. envío de chat); sin estado "incierto" visible. |
| TASK-04 | parcial | `tests/test_subagent_stop_and_locks.py` prueba que parar un worker no afecta a otros y libera locks de fichero; sin `question_id`/versión en ask_user (0 resultados en src/ai_interaction.py, src/agent_loop.py) | i18n distingue "Cancel this turn"/"Cancel task"/"Cancel workflow"/"Pause" (studio/src/i18n/es.ts) | tests/test_subagent_stop_and_locks.py, tests/test_subagent_permissions.py | Cancelación a subagentes y limpieza de locks probada; sin evidencia de que una respuesta tardía a pregunta cancelada no reactive el turno. |
| TASK-06 | parcial (lote 20) | `src/agent_settings_schema.py`: `agent_max_tool_calls`, `agent_input_token_hard_max`, `agent_subagent_depth`, varios `*_timeout_seconds`, y ahora las 4 que faltaban — `agent_autonomy_max_active_seconds`, `agent_autonomy_max_subagents`, `agent_autonomy_max_remote_spend`, `agent_autonomy_max_memory_mb` (`src/settings.py` DEFAULT_SETTINGS + schema); `src/autonomy_budget.py::resolve_budget` ya lee las 6 dimensiones de settings (antes 2 de 6, 4 en constantes fijas) | sin preset "supervisado/autónomo acotado/solo lectura" localizado en studio/src (los presets de `src/autonomy_budget.py` — supervised/bounded_autonomous/read_only — existen server-side, sin selector de UI) | tests/test_agent_ceiling_settings_registered.py, tests/test_autonomy_budget.py | Las 6 dimensiones del presupuesto ya son configurables vía settings (0 = sin límite); falta el selector de preset en Studio y un checkpoint garantizado al agotar el presupuesto. |
| PLAN-01 | parcial | `src/deep_research.py::_outline_sections/_extract_subquestions` (líneas 265-870) agrupa por encabezados y conserva todas las viñetas (corrigió corte en 12 ítems, ver PENDIENTES.md #13) | sin pantalla de plan corto/expandible localizada | tests/test_deep_research_outline_sections.py, tests/test_read_plan_outline.py | Extracción de requisitos del brief de research ya no trunca; pero el plan del agente (TASK-01) sigue truncando a 8.192 caracteres. |
| PLAN-03 | parcial | `src/subagent_permissions.py` (reglas allow/deny por ruta/herramienta, profundidad); locks de fichero por delegación y liberación al morir un worker probados en tests/test_subagent_stop_and_locks.py | ninguna pantalla muestra "qué agente edita qué fichero" (0 resultados en studio/src/screens) | tests/test_subagent_stop_and_locks.py | Backend reparte permisos/locks por delegación; sin visibilidad en Studio de propiedad de recursos ni resolución de conflictos en UI. |
| OBS-01 | parcial (revisado L29) | `src/agent_runs.py::_observability_fields`/`_publish` (de una ola previa, no reflejado aquí) estampa `trace_id`/`step_id`/`sequence`/`stream_id`/`schema_version` aditivamente en cada evento SSE del turno; `call_id` sigue en más ficheros (ahora también `src/artifact_store.py::persist(call_id=...)` y `src/command_guard.py::append_receipt(call_id=...)`, L29, folded into `provenance.note`/`record["call_id"]`) | timeline en `studio/src/screens/Activity.tsx`; `studio/src/adapters/chat.ts` decodifica `trace_id`/`step_id` por evento | tests/test_obs_*.py; tests/test_l29_artifact_store_call_id.py, tests/test_l29_command_guard_call_id.py (nuevos) | `trace_id`/`step_id` ya existen y se propagan por evento; `call_id` ahora también llega a la procedencia de artefactos y a los recibos de command_guard, pero no hay un endpoint que reconstruya "todo lo que produjo esta call_id" a partir de esas tres piezas. |
| OBS-02 | parcial | Fases reales por dominio: `src/research_handler.py`/`src/vram_admission.py` emiten `loading_model`, `unloading_model`, `probing`, `waiting_model`, `vram_blocked`, `warning`; agregadas en `src/agent_runs.py` (`phase`, `phase_since`, `last_event_at`) | Activity.tsx consume esos campos de run | tests/test_chat_vram_gate.py, tests/test_studio_vram_live_js.py | Vocabulario de fases es ad-hoc por módulo, no las 8 fases canónicas (cola/admisión/carga/prefill/generación/herramienta/verificación/espera humana); sin confirmar regla de "% sólo con total medible". Sin cambios de L29 — no se reaudita más allá de lo ya descrito. |
| OBS-03 | ausente | `src/exceptions.py` es un shim con sólo 4 clases (`SessionNotFoundError`, `InvalidFileUploadError`, `LLMServiceError`, `WebSearchError`); `src/tool_outcome.py` sólo clasifica 4 resultados (success/expected_error/cancelled/panic) | sin mapeo de códigos a acciones (reintentar/reanudar/cambiar recurso) localizado en studio/src | tests/test_tool_outcome.py | No hay taxonomía estable de códigos (capacidad, schema, permiso, recurso, transporte, timeout, cancelación, conflicto, verificación, desconocido); granularidad insuficiente. |
| ACT-01 | existente | runs indexados por tipo (`task`,`render`,`approval`,`notification`,`chat`,`workflow`) vía adapters consumidos en Activity | `studio/src/screens/Activity.tsx`: filtros Needs action/In progress/Failed/All, `ArtifactDownloads` conserva renders con procedencia tras cerrar el chat | tests/test_studio_activity_js.py, tests/test_studio_activity_feed_js.py | Cumple lo pedido: centro único con filtros y render descargable con procedencia aunque el chat se cierre. |
| ACT-03 | existente | `src/approval_store.py`: `request()` con `expires_at`/`uses_left`, `decide()`/`consume()` con bloqueo optimista (evita doble resolución), `expire_stale()` como barrido programado; L29 añade `src/question_store.py::list_open(owner)` y `GET /api/questions` (routes/chat_routes.py, owner-scoped vía `effective_user`) para que las preguntas `ask_user` abiertas también entren en la misma bandeja | Activity.tsx muestra approvals con enlace "Review approval"; L29: nueva bandeja `kind: 'question'` (`studio/src/adapters/activity.ts::questionFrom`/`answerQuestion`, `studio/src/screens/Activity.tsx::QuestionAnswerPanel`) que responde por el MISMO camino `sendTurn`+`questionId`/`optionIds` que usa la tarjeta en vivo, no un segundo mecanismo | tests/test_approval_store.py, tests/test_approval_concurrent_consumption.py, tests/test_approval_decision_races.py; tests/test_l29_question_store_list_open.py, tests/test_l29_questions_route.py, tests/test_l29_activity_questions_js.py (nuevos) | Bandeja con scope/expiración/revisión y anti doble-resolución probados; ahora también cubre preguntas abiertas (SEC-06: owner-scoped, fail-closed). Falta confirmar invalidación específica al cambiar el plan aprobado ya en curso. |
| ACT-04 | parcial | `desktop/main.cjs`: `ownedToken` sólo si esta instancia arrancó el servidor (`result.started`); `shutdown()` sólo detiene el servidor si hay `ownedToken` propio | `desktop/main.cjs` (ventana de splash con botón cerrar), sin diálogo de confirmación en cierre | — | Cerrar ventana que reutiliza servidor no lo mata (código lo confirma), pero no hay diálogo "esto detiene trabajo" ni opción visible de mantener el backend. |
| ACT-06 | parcial | sin lógica de estado de pantalla centralizada en backend; cada ruta responde error/])/vacío por su cuenta | `studio/src/components/EmptyState.tsx` + `Skeleton.tsx`/`StatusBadge.tsx` reusados en 41 pantallas (p.ej. Activity.tsx: "Some activity is unavailable") | — | Hay componente genérico de vacío/carga reusado ampliamente, pero sin variante específica para permiso denegado ni versión incompatible. |

## Grupo B — Modelos y herramientas: MOD, CALL, TOOL, HW, PERF

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| MOD-01 | existente (ya no ausente en el mapa — implementado en una ola previa, no reflejado hasta ahora) | src/model_capabilities.py (Modalities, CapabilityAssertion, tested_at); src/model_capability_readers/*.py (wired: `routes/local_models_routes.py`'s `caps_router`, `GET /api/models/{name}/capabilities`); src/model_calibration.py (manifest store, `announced` vs `tested` nunca mezclados — ver su docstring) | studio/src/screens/settings/LocalModels.tsx `CapsChips` distingue explícitamente anunciado (gris) vs probado ✓/✗ (verde/rojo) con `tested_at` en el tooltip (`capsEvidenceTitle`) | tests/test_model_capabilities.py, tests/test_model_capability_readers.py, tests/test_model_calibration.py, tests/test_local_models_routes.py | Cobertura real; MAPA_REUTILIZACION.md simplemente no se había actualizado tras el wiring — sin gap nuevo encontrado en este lote. |
| MOD-02 | existente (idem MOD-01) | src/model_calibration.py::run_calibration (prueba tool_calling/vision/json_mode/context_length_effective/streaming_tool_calls/refusal_format, presupuesto ~55s, nunca carga un modelo — exige que ya esté residente vía `/api/ps`); `routes/local_models_routes.py` `POST /api/models/{name}/calibrate` (409 si no está cargado) | studio/src/screens/settings/LocalModels.tsx botón "Calibrate" (deshabilitado si no está cargado) | tests/test_model_calibration.py | Calibra contra el modelo real vía HTTP, con evidencia recortada por probe; no hay fixtures JSON de tool-selection/español/recuperación/contexto como suite separada — las 6 pruebas fijas cubren protocolo, no comportamiento de dominio. |
| MOD-03 | parcial | src/llm_core.py:3595-3909 acumulador de deltas por índice (`_tc_acc`), canal thinking separado, `_emit_finish`/finish_reason, thought_signature preservado | studio/src/adapters/chat.ts `ChatEvent` (delta/tool_start/tool_output/doc_delta tipados) | tests/test_llm_core_streaming.py (null index, chunked args, parallel calls) | No hay tipo explícito para "rechazo/refusal" del proveedor; sin test de reconexión SSE sin duplicar contenido. |
| MOD-04 | parcial | src/model_load_options.py (`ALLOWED_KEYS`, `EXTRA_OPTION_KEYS`, `resolve_for_request`) — ámbito por endpoint+modelo, per-request `/ctx` gana sobre guardado; comenta explícitamente por qué flags de llama-server NO se aceptan | studio/src/screens/settings/LocalModels.tsx `OptionsForm` (num_ctx/num_gpu/keep_alive/main_gpu) | No se halló test dedicado | Frontend no deshabilita campos incompatibles con motivo, no hay preset ni aviso de "requiere recarga". |
| MOD-06 | parcial (lote 20) | src/agent_loop.py:1167-1300 `_agent_route_tool_mode` sigue decidiendo nativo/restringido por lista de keywords del nombre del modelo, no por calibración, dentro de una llamada; nuevo en este lote: `routes/session_routes.py`'s `PATCH /session/{sid}` (cambio de modelo entre turnos) recalcula `model_calibration.capabilities_for(nuevo)`/`(anterior)` y devuelve `capabilities` + `lost: [...]` (vision/tools nativas) — lectura del manifiesto existente, nunca una probe en vivo | No se halló UI específica que muestre `lost` al cambiar de modelo | tests/test_session_model_switch_capabilities.py (nuevo, lote 20) | Sigue sin existir recálculo DENTRO de una llamada en curso ni reconstrucción de estado del turno (lo que QA-28 exige íntegro); lo cerrado es el recálculo al cambiar de modelo ENTRE turnos vía REST — ver tests/qa/test_qa_28_cambio_de_modelo.py, que se deja xfail a propósito por no cubrir ese caso. |
| CALL-01 | parcial | src/llm_core.py:3841-3888 acumulación por index/call_id con guardas (índice nulo, args nulos); sin `object_pairs_hook` para claves duplicadas ni límites de bytes/profundidad en el parseo de argumentos | No se halló "preparando llamada" genérico en UI (solo `tool_call_delta` para create/update/edit_document) | tests/test_llm_core_streaming.py | Falta rechazo explícito de JSON con claves duplicadas y límites de bytes/profundidad; UI no muestra progreso de ensamblado para llamadas genéricas. |
| CALL-02 | parcial | Esquemas con enums en src/tool_schemas.py; validación por herramienta vía `ValueError`/`_resolve_tool_path` en src/agent_tools/filesystem_tools.py; scoping por owner (`owner_filter`) muy usado | No se halló UI de error junto al argumento | tests/test_*_owner_scope.py (múltiples) | No hay validador JSON-schema centralizado con tipos estrictos/rangos; sin evidencia de rechazo de "archivo obsoleto" (precondición de versión). |
| CALL-03 | ausente | No se encontró mecanismo de "reparación acotada sin ampliar destino/permiso" (solo repair de sintaxis JSON en src/tool_parsing.py, p.ej. `_parse_gemma_tool_call`) | No se halló distinción original/corrección/autorizada en UI | No se halló | Los errores de tool se re-exponen al modelo en la ronda siguiente sin límite ni garantía de mismo tool/alcance. |
| CALL-04 | parcial | src/agent_tools/subagent_tools.py `write_block_reason`/`_LOCK_CTX` — bloqueo por archivo entre sub-agentes, reclamo antes de ejecutar | studio (delegate_agents UI de progreso, no revisado en detalle) | tests/test_subagent_stop_and_locks.py; tests/test_subagent_permissions.py | El locking es solo entre sub-agentes delegados; llamadas nativas paralelas de un mismo turno se ejecutan secuencialmente (sin `asyncio.gather`), no hay agrupación explícita de lecturas independientes. |
| CALL-05 | parcial | Resultados ad hoc `{output/error/exit_code}` en src/agent_tools/*.py; no existe clase `ToolResult` ni campos `evidence_refs`/`side_effects` (0 ocurrencias en repo) | No se halló tarjeta de resultado con duración+enlace a evidencia | No se halló | Contrato tipado (status/data/evidence_refs/side_effects/error) de la spec no existe; solo dict libre por herramienta. |
| CALL-06 | parcial (lote 7, 10-09) | `src/retry_policy.py` (`classify_http`: retry_now/retry_backoff/no_retry/outcome_unknown, Retry-After, jitter, `RetryBudget`); enchufado en el bucle no-streaming de `src/llm_core.py::llm_call_async` (`on_outcome_unknown`) | mensaje «saturado, reintentando» vs «pudo completarse» aún sin tarjeta propia | tests/test_retry_policy.py (48), tests/test_llm_core_retries.py (10) | Los tres bucles de reintento de streaming en llm_core (~3300/3420/3570/3890) siguen sin la política; tool calls con efectos remotos aún no consultan la clase. |
| CALL-07 | parcial | src/agent_tools/interaction_tools.py `AskUserTool` (multi-select, sin texto libre, sin `question_id`); routes/chat_routes.py `_mark_tool_approval_resolved`/`_supersede_tool_approval_history` (persistencia y "superseded" en recarga) | studio/src/adapters/chat.ts `ask_user`/`ask_resolved` events | No se halló test dedicado a doble clic/timeout | Falta opción de respuesta libre y `question_id` explícito; solo el flujo de "tool_approval" está claramente protegido contra decisiones contradictorias. |
| CALL-08 | existente | src/prompt_security.py (`untrusted_context_message`); src/tool_capabilities.py taint/gate tras contexto externo; `parse_tool_blocks` (src/tool_parsing.py) solo se invoca sobre `round_response` del propio modelo (src/agent_loop.py:3450), nunca sobre texto de herramientas/documentos | studio marca sugerencias vs acción ejecutable (no revisado a fondo) | tests/test_external_context_tool_gate.py; tests/test_prompt_injection_evasion.py | Cobertura sólida; no se verificó explícitamente el caso literal "README con bloque de herramienta" pero el mecanismo lo cubre por diseño. |
| TOOL-01 | parcial | src/tool_capabilities.py (`ToolCapabilities`, `ToolEffect`, `capabilities_for_tool`); src/tool_schemas.py (schemas nativos); src/agent_tools/__init__.py `TOOL_TAGS`/aliases — fragmentado, sin clase `ToolDescriptor` única versionada | No se halló catálogo con búsqueda/favoritos/prueba segura | tests/test_external_context_tool_gate.py (`test_all_fence_tools_have_explicit_capabilities`, `test_all_native_schema_tools_have_explicit_capabilities`) | No hay un registro único versionado con idempotencia/timeout/salida/compatibilidad; no hay catálogo en Studio. |
| TOOL-03 | parcial | src/mcp_manager.py (`McpManager`, stdio/SSE/HTTP, OAuth, reconexión, `_honest_status`, estados connecting/connected/error/needs_auth/timeout) | studio/src/screens/settings/Integrations.tsx (connected/error/disconnected/needs authorisation) | tests relacionados con MCP (p.ej. test_mcp_cache_invalidation.py, test_mcp_reconnect_args.py) | No hay estado "Degradada" explícito ni "Probando" distinto en UI; no se verificó paginación de `list_tools`. |
| TOOL-04 | parcial | src/mcp_manager.py `build_server_env`/`minimal_env`/`scrub_private` (aislamiento de proceso stdio + control de secretos por env); no hay manifiesto con versión/hash/dependencias por extensión | No se halló UI de procedencia/diff de permisos al instalar | No se halló | No existe concepto de "extensión" con manifiesto versionado más allá de servidores MCP; sin bloqueo de broker probado contra lectura de credenciales fuera de ámbito. |
| HW-01 | parcial | src/vram_admission.py (`assess`, `Ticket`, `admit`, mensajes de RAM/commit); src/vram_fit.py (`kv_bytes_per_token_estimated`, notas "estimated from GGUF metadata") — sin lock/reserva atómica entre `admit()` concurrentes | studio/src/screens/VramAdmissionDialog.tsx | No se halló test de concurrencia de admisión | Sin `asyncio.Lock`/reserva atómica: dos `admit()` simultáneos pueden leer el mismo presupuesto libre; solo Ollama, no cubre RAM/commit global de forma unificada por GPU. |
| HW-02 | existente | src/agent_runs.py `model_state()` distingue loading/prefill/spill vía Ollama `/api/ps` (`"spill": vram < size*0.98`), documentado contra el caso real "35GB en VRAM y PCIe spill" | studio/src/adapters/chat.ts (mensajes "Loading the model"/"spilling to RAM"); studio/src/screens/studio/Vitals.tsx (`spilling`, "PCIe spill") | No se halló test dedicado a spill | No se verificó confirmación explícita antes de reducir contexto/cuantización silenciosamente. |
| PERF-02 | parcial | ~21 ficheros usan `asyncio.to_thread`/executors (p.ej. src/model_discovery.py, src/service_health.py); PERO src/chat_handler.py:284 llama `build_user_content` (→ src/document_processor.py `_process_pdf`, pypdf síncrono) directamente en el request async sin `to_thread` | No se halló verificación de que la UI siga respondiendo durante extracción de PDF | No se halló test de bloqueo del event loop | El path de adjuntos PDF/oficina en el chat corre pypdf/vision síncronos dentro de una corrutina sin offload — riesgo de bloqueo del event loop, aunque acotado por `MAX_PDF_ATTACHMENT_PAGES`. |
| PERF-04 | parcial | routes/system_usage_routes.py `_collect_host`/`_collect_gpu`/`health_signals` (CPU/RAM/disco/VRAM a nivel host, psutil); src/disk_ballast.py (cuarentena ante presión de disco); `src/bg_monitor.py::is_paused_for_resource_pressure()` (hysteresis-based, de una ola previa) ya existía sin consumidor; L29 lo conecta en `src/bg_jobs.py::launch()` — bajo presión, el job se guarda como `status="queued"` (no se descarta) y `refresh()` lo lanza (`_spawn_process`) en cuanto la presión cede | studio Vitals.tsx muestra avisos de disco/VRAM | tests/test_l29_bg_jobs_ram_pressure.py (nuevo, 4 tests: no spawnea bajo presión, encola, lanza al despejarse, `kill()` de un job encolado) | Sigue sin haber `psutil.Process(pid)` propio (RSS, fds, threads) del proceso Faustus ni límites explícitos de sesiones concurrentes; lo que L29 cierra es "aplazar, no descartar" un job de fondo bajo presión — un caso concreto, no la medición general. |

## Grupo C — Contexto, investigación y verificación: CTX, IDX, MEM, RES, WEB, VER

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| CTX-01 | parcial | src/context_budget.py (`compute_input_token_budget`, escala a ventana del modelo, respeta el tope explícito de settings — `budget_for` en el mismo módulo es un presupuesto itemizado real a partir del manifiesto del modelo, pero no es un reemplazo directo: ignora el tope explícito del usuario, así que ambos coexisten en vez de fusionarse; ver nota EDIT/CTX del lote 20 en `src/agent_loop.py`); src/model_context.py `estimate_tokens` (heurística chars*0.3+overhead, no tokenizer real por proveedor); src/context_ledger.py (`classify`, `SECTIONS`, y ahora `evidence_for_read` — CTX-03/EDIT-01, fija el hash exacto de una ventana de fichero leída) | studio/src/screens/studio/Transcript.tsx `Ledger()` (separa system/tools/instructions/skills/memory/documents/web/attachments/tool_results/conversation/user) | tests/test_context_budget.py, tests/test_context_ledger.py, tests/test_estimate_tokens_tool_calls.py | No usa tokenizer real (tiktoken/etc.) por modelo, solo heurística; no hay marca explícita "estimado" para imagen/audio distinta del cálculo normal; `budget_for` (presupuesto real itemizado) sigue sin integrarse en `_trim_route_request_messages` porque rompe el contrato explícito de settings que un test existente fija (ver Limitaciones del informe de este lote). |
| CTX-02 | parcial (lote 20) | src/context_compactor.py `maybe_compact` (resumen por LLM, con fallback intacto) + `compact_with_integrity` (plegado determinista que preserva identificadores, sin LLM) — `src/agent_loop.py::_build_route_request_state` intenta `compact_with_integrity` primero cuando hace falta compactar y sólo cae a `maybe_compact` si no hubo evidencia de integridad; el resultado plegado se referencia con `EVIDENCE_SOURCE_TYPES = "conversation"` (nuevo, `src/contracts/tool.py`) en vez de reusar `"tool"` | No se encontró UI para ver resumen, recuperar fuente o fijar fragmentos no comprimibles | tests/test_compaction_summary_failure.py, tests/test_context_compactor_regressions.py, tests/test_lote20_ctx02_integrity_before_llm_compaction.py (nuevo) | El orden integridad-antes-que-resumen-con-pérdida ya existe; sigue sin haber fijación de fragmentos por el usuario ni UI para inspeccionar qué se compactó. |
| CTX-03 | parcial | src/read_plan.py (lectura paginada offset/limit + índice de símbolos); src/context_engine/{compiler.py,candidates.py,store.py} (dedupe por `source_ref`, packet + manifest); routes/context_engine_routes.py (`/packets`, `/packets/{id}/manifest`, `/receipts`); src/context_ledger.py::evidence_for_read (nuevo en este lote: cada lectura de `ReadFileTool` adjunta un `EvidenceRef` con el hash exacto del rango leído, más `revision` — ver EDIT-01) | studio/src/screens/Context.tsx `ManifestPane` ("Why this packet said what it said", ítems del manifiesto) | tests/test_context_engine_sources.py, tests/test_context_engine_delta_source.py, tests/test_read_plan_outline.py | No se encontró herramienta de "búsqueda dentro del output" ni carga incremental explícita; la deduplicación conserva `source_ref` pero no hay endpoint que devuelva el fragmento exacto citado por un resumen. |
| IDX-01 | parcial | services/projects.py `_canonical_ref` (realpath+normcase, resuelve mayúsculas/D:\ vs d:/), `ProjectStore` (project_id estable, independiente de folder) | studio/src/screens/Project.tsx (texto "Source missing"); studio/src/screens/Projects.tsx (solo input de texto, sin selector nativo ni "recientes") | tests/test_project_identity.py (`test_project_id_survives_renaming_the_folder`) | No hay selector nativo de carpeta (`showOpenDialog`/`webkitdirectory`) ni lista de "recientes"; reubicar carpeta es editar el campo `workspace` a mano. |
| IDX-04 | parcial | src/upload_handler.py (`python-magic` para MIME, fallback si no está); src/document_processor.py `_process_text_file` (chardet para encoding, try/except por archivo sin bloquear otros; VL como OCR de imágenes de PDF) | studio/src/screens/studio/Composer.tsx (estados `queued`/`uploading`/`failed` con botón "Retry"; sin `extracting`/`ready`/`partial` distintos) | tests/test_upload_content_detection_magic.py, tests/test_pdf_extraction_budget.py, tests/test_native_docx_extraction.py | No se detectan PDFs cifrados/corruptos explícitamente; no hay señal de "solo se extrajo portada" (PDF escaneado marcado como leído igual); frontend no distingue Subiendo/Extrayendo/Listo/Parcial. |
| MEM-01 | parcial | src/memory_engine.py (`TRUST_CLASSES`: human_explicit/agent_validated/agent_assertion/legacy_import; `STATUSES`: active/deprecated/anti_pattern; owner/project/level) | studio/src/screens/Memory.tsx (`memory.source === 'user' ? 'yours' : 'extracted'`, `fs-mem__origin` con sessionId) | tests/test_memory_engine.py, tests/test_memory_owner_isolation.py, tests/test_consolidate_memory_explicit_drops.py | No hay campo de "sensibilidad" explícito; estados son active/deprecated/anti_pattern (no exactamente confirmado/inferido/obsoleto); no se verificó aislamiento cruzado por proyecto, solo por owner. |
| MEM-02 | existente (cerrado en L29 — implementación ya estaba de una ola previa, faltaba la ruta HTTP y el test de aceptación) | src/memory_engine.py `forget()`/`correct()` (tombstone table, `is_tombstoned` bloquea resurrección salvo `trust_class=human_explicit`) ya existían y no estaban expuestos; L29 añade `DELETE /api/memory-engine/items/{id}/forget` y `POST /api/memory-engine/items/{id}/correct` en routes/memory_engine_routes.py, y `ItemCreate` ahora expone `type`/`session_id`/`provenance` hacia `add_item()` | studio/src/screens/Memory.tsx `deleteMemory` sigue llamando al DELETE físico (`/items/{id}`), no al nuevo `/forget` — pendiente de conectar la UI | tests/test_memory_engine.py; tests/test_l29_memory_forget_correct.py (nuevo, 7 tests: tombstone+no-resurrección, correct+link-back, 404s, validación) | La ruta HTTP y el contrato ya existen y están probados; la UI de Memory.tsx todavía no distingue "borrar" (DELETE) de "olvidar" (forget) — sigue usando la primera. También: `engine.correct()` (foreign, src/memory_engine.py) tombstona el original ANTES de validar el texto nuevo — ver "Cambios necesarios en ficheros ajenos" del informe de L29; la ruta ya se defiende de ese caso, la función de base no. |
| MEM-05 | existente | src/changeset_store.py `create_changeset(..., incognito)` → `{"stored": False, "storage_reason": "incognito"}`; src/chat_processor.py (`incognito` desactiva skills/consolidación); routes/chat_routes.py (incognito bloquea tools que exponen memoria, no genera título); routes/session_routes.py (purge de sesiones incógnito huérfanas) | studio/src/screens/studio/Composer.tsx (`knobs.incognito`, chip dedicado, distinto de "Skip memory recall") | tests/test_chat_memory_recall_control.py, tests/test_review_regressions.py (`test_build_chat_context_incognito_*`) | Cobertura amplia (memoria, changesets, título, tools); no se verificó explícitamente exclusión de embeddings/logs de depuración de bajo nivel. |
| VER-01 | existente | src/agent_loop.py / src/scorecard.py (estados por check: verified/unverified/rejected/syntax_error/tests_failed/review_issues/target_substituted/unknown_tool) | studio/src/screens/studio/Harness.tsx `CheckList`/`HarnessCard` (badge "Verified" solo si `verified===true` y ningún check `unverified`; si no, badge "Unverified") | tests/test_harness_building_blocks.py, tests/test_studio_harness_status_js.py | No hay estado explícito "no aplicable" distinto de los demás; el veredicto agregado es a nivel de turno, no por criterio individual documentado. |
| VER-02 | existente | src/project_tests.py `detect_test_command` (descubre pytest/npm/Makefile), `compare_with_baseline` (baseline en checkpoint, clasifica `new_failures`/`pre_existing`/`pre_existing_only`) | studio/src/screens/studio/Harness.tsx / harness-status.ts (consume el resultado de project_tests) | tests/test_project_tests_scoping.py, tests/test_project_tests_output_oracle.py | No se confirmó ampliación automática de qué tests correr cuando cambia el "riesgo" más allá de `related_test_files`. |
| VER-04 | parcial | src/output_oracle.py (`apply`/`check`: exit 0 no basta, requiere string declarado; `EXIT_OUTPUT_MISMATCH`); src/chat_export_docx.py `render()` | routes/session_routes.py `export_session` (llama `chat_export.render` y devuelve directo, sin paso de validación) | tests/test_output_oracle.py, tests/test_chat_export_docx.py (reabren el .docx con zipfile en el test, no en producción) | El oráculo cubre pasos de comando (exit_code+string), pero el export de documentos no reabre/valida el artefacto antes de servir el enlace; frontend no distingue Generado/Guardado/Abre correctamente/Revisado/Verificado. |
| VER-05 | existente | src/research_citations.py (`audit_citations`, `check_claims`, `SourceRegistry`); src/claim_verify.py `verify`/`verify_claims` (juicio semántico de apoyo, no solo existencia del marcador) | studio/src/screens/studio/SavedEvidence.tsx ("This is the original evidence") | tests/test_citation_sentence_boundaries.py, tests/test_verify_claim_tool.py, tests/test_research_visible_citations.py | Cobertura sólida de audit+verify; UI de "fuente inaccesible/respaldo dudoso" no se localizó de forma explícita fuera de SavedEvidence. |
| VER-06 | parcial | services/review_state.py (`init`/`decide`/`get`, acepta/rechaza por archivo, separado de tests); routes/workspace_routes.py `GET/POST /api/workspace/review/{message_id}` (admin-only) | No se encontró UI en Studio que llame a `/api/workspace/review/...` (accept/reject no está cableado en Composer/Transcript) | (no se halló test directo de review_state en tests/) | Backend completo (accept/reject/restore por checkpoint) pero sin consumo desde Studio; no hay firma/hash del diff que invalide la aprobación si cambia el contenido. |
| WEB-01 | existente | routes/search/search_routes.py `GET /api/search/health` (usa `searxng_engine_health`, `single_engine` cuando solo respondió un motor) | studio/src/screens/Settings.tsx (`searchHealth()`, tarjeta `data-testid="search-health"`) | tests/test_search_engine_health.py, tests/test_service_health_search.py | Salud de motores está en Settings, no se confirmó su reutilización dentro del flujo de research/reporte (filtro fecha/dominio + aviso de cobertura parcial en el informe mismo). |
| WEB-02 | parcial | src/outbound_fetch.py (bloqueo SSRF post-DNS, IP privada/link-local/169.254.169.254/metadata, redirecciones re-evaluadas por hop, límites de bytes/tiempo/MIME) | studio/src/screens/studio/SavedEvidence.tsx (vista "original evidence" básica) | tests/test_webhook_ssrf_resilience.py, tests/test_web_fetch_size_caps.py, tests/test_deep_research_same_pages_again.py | Backend muy robusto contra SSRF/límites; no se encontró UI de "páginas duplicadas" ni "contenido desactualizado" en el flujo de research/lectura web. |
| WEB-03 | ausente | src/desktop_control_session.py / src/mcp_manager.py delegan la automatización de navegador a `@playwright/mcp` (proceso externo); no hay perfiles por owner/proyecto ni cifrado de cookies en el repo | No se halló UI de "ver navegador activo / tomar control" separada por proyecto | (sin tests específicos) | No existe aislamiento de sesión por propietario/proyecto ni sesiones efímeras opcionales en el código propio; la sesión de navegador es la del usuario vía MCP externo, sin capa de perfiles. |
| WEB-04 | parcial (de una ola previa, WEB-04 y aún no cableado — L29 confirma que sigue sin llamador real) | src/mcp_manager.py solo trunca/registra snapshots (`truncate_browser_snapshot`); src/browser_actions.py (`ActionPrecondition`, `check_precondition`, `run_with_precondition`) ya existe y está probado con snapshot/act inyectados, pero NADA en el árbol lo llama todavía — la invocación real de `mcp__builtin_browser__*` sigue siendo `src/tool_execution.py`'s `elif tool.startswith("mcp__"):` (~línea 1704), que llama `mcp.call_tool(tool, args)` directo, sin pasar por `run_with_precondition`; ver "Cambios necesarios en ficheros ajenos" del informe de L29 para el cableado exacto que falta ahí | No se halló resaltado de elemento objetivo ni aprobación contextual para envíos en Studio | tests/test_web_browser_actions_readback.py (la función en sí, con snapshot/act mockeados) | La resolución de elementos y el readback siguen dependiendo enteramente de `@playwright/mcp` externo en la práctica: la capa propia de precondición/postcondición EXISTE y está probada de forma aislada, pero no se ejecuta en ningún camino real todavía — cablearla requiere tocar `src/tool_execution.py`, fuera de la vía libre de L29. |
| RES-01 | parcial | src/deep_research.py `_outline_sections`/`_extract_subquestions`/`_clean_subquestions` (agrupa por encabezados antes de recortar, pero `kept[:MAX_SUBQUESTIONS]` con `MAX_SUBQUESTIONS=24` sigue siendo un tope numérico) | No se encontró un esquema editable (pendientes/cubiertos/insuficientes) en Studio para research | tests/test_deep_research_outline_sections.py (`test_luis_s_real_whiplash_brief_is_fifteen_sections`) | Sigue existiendo un límite duro (24) pese a agrupar por encabezados; sin UI de esquema editable con estado de cobertura por nodo. |
| RES-03 | parcial | src/research_citations.py `SourceRegistry.add` (url, title, summary, evidence, domain, fetched_at); marca citas "weak" (`audit_citations`) | No se localizó biblioteca de fuentes agrupable/filtrable en Studio | tests/test_research_citations.py, tests/test_services_research_low_quality_sources.py | Falta autor/organismo, fecha de publicación (solo `fetched_at`=consulta), DOI, versión y estado de acceso explícito; no distingue primaria/secundaria. |
| RES-04 | parcial | src/deep_research.py `_final_report_in_parts` (una sección fallida se reemplaza por su encabezado + nota, no borra el resto; si todo el join queda vacío devuelve el `report` previo) | studio/src/screens/studio/Transcript.tsx `ResearchLine`/`LiveLine` (solo progreso por fase, no vista de secciones completas/pendientes) | tests/test_deep_research_report_in_parts.py | No hay glosario compartido explícito; frontend no expone un "informe vivo" con secciones completas/pendientes, solo el indicador de fase en curso. |
| RES-05 | ausente | src/research_handler.py `recover_interrupted` (tras reinicio, marca runs "running" como "interrupted" y ofrece Retry desde cero); sin checkpoint de consultas/páginas/extractos/síntesis en src/deep_research.py | UI muestra tarjeta de fallo con botón "Retry" (reinicio completo), no "reanudar desde la última etapa" | tests/test_research_restart_survival.py | No hay recuperación real por etapas: un reinicio de backend pierde el progreso y "Retry" vuelve a gastar toda la inferencia, contradiciendo el criterio de aceptación. |

## Grupo D — Edición, ejecución, seguridad y operación: EDIT, EXEC, SEC, OPS, EVAL

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| EDIT-01 | parcial (lote 20 amplía el alcance) | routes/workspace_routes.py PUT /api/workspace/file (sha256 `revision`, lock, atomic_write_text, 409); routes/document/document_routes.py PUT/restore (`expected_content`, 409); src/agent_tools/filesystem_tools.py EditFileTool (ancla única old_string); nuevo: `write_file`/`edit_file`/`apply_patch` aceptan `base_revision` opcional en `src/tool_schemas.py` (lote 19), y ahora `ReadFileTool` adjunta `revision` (`sha256:<hex>` del fichero) y `evidence_refs` tras cada lectura (`src/context_ledger.py::evidence_for_read`) para que el modelo tenga qué pasar como `base_revision` en la siguiente edición | studio/src/screens/studio/SidePanel.tsx (muestra "The file changed outside this editor") | tests/test_document_editor_conflict.py, tests/test_edit_file.py, tests/test_lote20_read_file_evidence.py (nuevo) | studio/src/screens/documents/Editor.tsx (biblioteca) sigue llamando saveDoc sin expectedContent: sobrescribe sin detectar conflicto pese a que el backend lo soporta; el `base_revision` del lado herramientas no se ha conectado a esa pantalla. |
| EDIT-02 | existente (cerrado en este lote — implementación ya estaba, faltaba el test de aceptación) | src/edit_journal.py::EditJournal/apply_batch (registra intención de cada fichero del lote antes de escribir, aplica en orden, ante el primer OSError compensa cada fichero ya aplicado devolviéndolo a sus bytes previos o borrándolo); src/agent_tools/filesystem_tools.py ApplyPatchTool ejecuta su escritura a través del journal; src/workspace_checkpoints.py checkpoint()/restore() (shadow git repo, restaura por archivo); src/dispatch.py `_checkpoint` antes de cada job | studio/src/adapters/workspace.ts restoreCheckpoint (sin UI que la use fuera de Harness) | tests/test_edit_journal.py (journal aislado + escenario end-to-end de fallo de disco a mitad de lote); tests/qa/test_qa_17_lote_parcialmente_aplicado.py (nuevo, verde — antes xfail) | Journal y compensación por archivo dentro de un lote ya existen y están probados; sigue sin haber preview de lote con exclusión de cambios opcionales antes de aplicar. |
| EDIT-03 | parcial | src/agent_tools/filesystem_tools.py `_read_text_lf`/`_write_text_lf` preserva CRLF; atomic_write_text preserva permisos (no recrea el inodo) | studio/src/screens/documents/DiffView.tsx (diff simple) | tests/test_edit_file.py (diff added/removed) | Sin manejo explícito de BOM; DiffView no distingue cambios funcionales de formato ni tiene vista dedicada de binarios/archivos grandes. Sin cambios en este lote. |
| EDIT-04 | existente (cerrado en este lote — implementación ya estaba, faltaba el test de aceptación) | routes/workspace_routes.py GET /checkpoint/status, /checkpoint/changes ("what restore would touch"), /checkpoint/file (detecta binario), POST /checkpoint/restore; src/workspace_checkpoints.py::status(); `checkpoint_coverage(workspace, sha, external_effects=...)` (nuevo desde una ola previa, ahora probado por aceptación): reporta `covered`/`uncovered` (con razón, p.ej. `excluded_dir_never_snapshotted`) y los `external_effects` irreversibles que el llamador declare; `restore()` adjunta ese informe bajo `coverage` sin tocar sus claves históricas (`restored`/`deleted`/`failed`/`unchanged`) | studio/src/screens/studio/Harness.tsx usa restoreCheckpoint | tests/test_edit_checkpoint_coverage.py (incluye la prueba anti-regresión de que el shape antiguo de `restore()` no tenía `coverage`); tests/qa/test_qa_18_rollback_fuera_de_alcance.py (nuevo, verde — antes xfail) | `restore()` no reenvía `external_effects` suministrados por el llamador a su llamada interna a `checkpoint_coverage` — hay que declararlos aparte, en una llamada directa a `checkpoint_coverage(..., external_effects=...)`; no hay panel "Qué se restaurará/Qué no" en Studio todavía. |
| EXEC-01 | parcial | src/agent_tools/subprocess_tools.py (_create_bash_subprocess: Git Bash nativo en Windows, cwd estructural); src/native_env.py (allowlist de env, scrub_private); src/doctor.py (diagnóstico de dependencias) | (no localizada pantalla que muestre Windows/WSL/contenedor/remoto) | tests/test_agent_bash_windows.py, tests/test_native_env.py | Ninguna pantalla de Studio muestra dónde se ejecutará el comando (Windows/WSL/contenedor/remoto) ni ofrece abrir terminal equivalente. |
| EXEC-02 | parcial | src/agent_tools/subprocess_tools.py usa create_subprocess_exec (argv, no shell=True) para el spawn; src/command_guard.py clasifica SAFE..CRITICAL por regex/heurística (no es el mecanismo de contención, solo gate de aprobación) | (sin preview de comando efectivo ni máscara de secretos en UI) | tests/test_command_guard.py, tests/test_subprocess_hardening.py | No hay preview del comando/ámbito efectivo con secretos enmascarados en Studio; command_guard es una heurística de patrones, coherente con el diseño pero no reemplaza contención real. |
| EXEC-03 | existente | src/process_ownership.py (Ownership, TreeKill, creation_time, terminate_tree solo desciende por hijos propios); src/bg_jobs.py `_kill_job` usa process_ownership | studio/src/screens/Activity.tsx (botón Cancel), studio/src/screens/agents/Runners.tsx | tests/test_process_ownership.py, tests/test_process_tree_ownership.py | Terminal acoplable/buscar-copiar logs no verificado en detalle; el núcleo (árbol propio, no matar todo el sistema) está bien probado. |
| EXEC-04 | parcial | src/vram_admission.py (Ticket/admission solo para carga de modelos, VRAM); src/bg_jobs.py (jobs en background sin límite de memoria/concurrencia conjunto) | studio/src/screens/research/Research.tsx `startAll(sequential)` (solo research, no builds/tests/modelos) | tests/test_vram_admission.py | No existe cola/admisión conjunta para builds+tests+modelos grandes; solo hay admisión de VRAM por modelo y ejecución secuencial ad-hoc en research. |
| SEC-01 | parcial | src/agent_gate.py (GateServer/judge, hook externo independiente del modelo); src/approval_store.py (approve once/task, expiración TTL); routes/command_guard_routes.py allowlist GET/POST/DELETE | studio/src/screens/studio/Transcript.tsx AskCard ("Approve" / "Approve the whole task" / "Deny") | tests/test_agent_gate.py, tests/test_approval_store.py | Sin vista de "concesiones activas" ni revocación inmediata en Studio; los endpoints de allowlist de command_guard no están cableados a ninguna pantalla. |
| SEC-02 | existente | src/prompt_security.py `untrusted_context_message` (marca provenance, escapa marcadores de guardia, detecta evasión) | studio/checks/untrusted.check.mjs (sanitiza javascript:/data:/HTML hostil, fixpoint) | tests/test_studio_untrusted_content.py, tests/test_tool_output_prompt_injection.py, tests/test_prompt_injection_audit.py | Cobertura amplia; no se verificó cada superficie (email HTML, adjuntos) individualmente. |
| SEC-03 | parcial (gap de nombres reservados de Windows cerrado en lote 20) | src/tool_execution.py resuelve `os.path.realpath` y confina contra roots (symlinks); `_is_windows_reserved_name`/`_WINDOWS_RESERVED_NAMES_CF` rechazan CON/PRN/AUX/NUL/COM1-9/LPT1-9 en cualquier componente de la ruta, con o sin extensión, case-insensitive; src/sandbox_exec.py ejecuta "skills" en Docker con límites de memoria/timeout/red | studio/src/screens/studio/WorkspaceDialog.tsx dice "file tools and terminal are confined" | tests/test_personal_dir_symlink_escape.py, tests/test_file_mentions_symlink_containment.py, tests/test_sandbox_exec.py, tests/test_lote20_sec03_reserved_windows_names.py (nuevo), tests/test_sec_lote16_sec03_sec04.py (gap-tracking guard convertido a positivo) | src/agent_loop.py documenta explícitamente que el shell "is NOT sandboxed" (solo las tool-calls de archivo están confinadas); el texto de WorkspaceDialog.tsx sobre-afirma que el "terminal" está confinado. Nombres reservados: cerrado, sin gap nuevo conocido. |
| SEC-04 | parcial | src/outbound_fetch.py (pinning DNS, bloqueo de IP privada/metadata, límites de tamaño); src/url_safety.py, src/url_security.py (SSRF) | (sin pantalla de "perfil totalmente local") | tests/test_outbound_broker_ssrf.py, tests/test_webhook_dns_rebinding_pin.py, tests/test_gallery_endpoint_ssrf.py | No se encontró un perfil "local-only" único que cubra embeddings/OCR/reranker/telemetría además del LLM; el egress control es por-fetch, no una política global auditable. |
| SEC-05 | existente | core/log_safety.py (redact_secrets, SecretRedactingFilter/Formatter instalado en root loggers); src/secret_storage.py (Fernet), src/secret_files.py (harden_secret_files) | studio/src/screens/settings/SystemExtras.tsx (logs de diagnóstico) | tests/test_sec1_secret_hygiene.py (redacción de tracebacks/handlers), tests/test_api_key_file_permissions.py | No se localizó un export/"crash dump" dedicado con test explícito, pero la redacción está instalada globalmente en logging. |
| SEC-06 | existente (gap de `question_store` cerrado en lote 20) | core/authz.py (reglas API por scope); decenas de rutas con chequeo de owner (routes/artifact_routes.py, routes/memory/memory_routes.py); src/question_store.py::Store.get/resolve/cancel ahora reciben y comprueban `owner` (normalizado como src/tool_approvals.py; un mismatch lee como "not_found"/None, nunca revela existencia; `owner=None` no filtra, por compatibilidad); routes/chat_routes.py:1937 pasa `owner=owner` a `resolve_question` | (estado de usuario/proyecto visible en Studio, no auditado en detalle) | ~46 archivos test_*_owner_scope.py / IDOR (sessions, memorias, gallery, research, documentos, etc.); tests/test_sec_lote16_sec05_sec06.py (el xfail que demostraba el gap de `question_store` es ahora verde) | Cobertura muy amplia y consistente; no se auditó cada endpoint individualmente (miles de rutas). `question_store`: cerrado, sin gap nuevo conocido. |
| SEC-07 | existente | desktop/main.cjs (`contextIsolation:true, sandbox:true, nodeIntegration:false, webSecurity:true`, CSP en splash, setWindowOpenHandler/will-navigate controlados); desktop/preload.cjs (contextBridge mínimo) | studio/src/screens/documents/Editor.tsx, compare/Compare.tsx, email/Reader.tsx usan `<iframe sandbox="...">` para HTML/preview | desktop/desktop-control.test.cjs, tests/test_security_headers_middleware.py | Bien evidenciado; no se probó a fondo el puente IPC (`ipcMain.handle('faustus:window',...)`) más allá de lo visto. |
| OPS-01 | parcial | src/doctor.py (Finding por área: backends, sandbox image, modelos, memoria vectorial — distingue dependencia faltante de fallo); src/health.py; requirements.txt/pyproject.toml/package-lock.json versionados | (no se encontró ninguna referencia a "doctor" en studio/src) | tests/test_doctor.py, tests/test_doctor_services.py | doctor.py no está expuesto en ninguna pantalla de Studio (grep sin resultados): no hay "asistente de reparar entorno" visible ni preview de acciones para el usuario. |
| OPS-02 | parcial | scripts/update_database.py (ALTER TABLE condicional, sin backup previo); update_windows.bat (git pull + docker compose up, sin backup/rollback/modo mantenimiento) | (sin notas de cambios/permisos nuevos ni posposición por tareas activas) | tests/test_update_database_script.py (solo valida guard `__main__`), tests/test_windows_update_script.py | No hay preflight de versión, backup antes de migrar, transacción con rollback ni modo mantenimiento; el script de actualización no comprueba tareas activas. |
| OPS-03 | parcial | src/backup_service.py (`_sqlite_safe_copy` WAL-aware, write_manifest con HMAC+sha256, verify_archive, prune); routes/backup_routes.py (/api/backup/snapshot, /verify, /snapshots) | studio/src/screens/settings/SystemExtras.tsx `BackupCard` (solo export/import completo) | tests/test_backup_service.py, tests/test_backup_wiring.py | Studio no expone snapshots/verify/última-copia-comprobada ni restauración selectiva/exploración de contenido, solo un export/import de todo; no hay prueba periódica de restauración en sandbox, solo `verify_archive` (integridad, no restore real). |
| OPS-05 | parcial | src/crash_recovery.py (boot_scan detecta runs interrumpidos tras crash/reinicio, `verify_resumed` nunca afirma éxito sin sondear el pid); tools/MCP se pueden desactivar manualmente vía settings (`disabled_tools`) | studio/src/screens/settings/SystemExtras.tsx (logs de diagnóstico) | tests/test_crash_recovery.py (boot scan, nunca bloquea el arranque) | No hay arranque en "modo seguro" automático tras crashes repetidos ni desactivación automática de un plugin/MCP corrupto, ni pantalla de recuperación con "volver a versión anterior". |
| EVAL-01 | no verificable | src/scorecard.py (métricas reales por turno/modelo desde uso real, no fixtures); referencia a `agent-bench/` (run_task.py, run_matrix.py) en FAUSTUS.md, banco de pruebas propio no incluido en este repo | (sin pantalla "Lab" de resultados por tarea/perfil con baseline) | (ninguno localizado en tests/ para fixtures de bug/feature/refactor/investigación etc.) | `agent-bench/` (el banco de tareas representativas real) vive fuera de este checkout (`D:\LocalAI\agent-bench` según FAUSTUS.md): no se puede verificar su cobertura leyendo este código. |
| EVAL-03 | parcial | src/disk_ballast.py (simula presión de disco/urgencia); src/dispatch.py (idempotency_key evita duplicar efectos en reintento); src/crash_recovery.py (proceso muerto/expiración) | (sin botón "reproducir fixture" ni vista de qué se conservó/reanudó/incierto) | tests/test_disk_ballast.py, tests/test_dispatch_reliability.py (`test_a_retried_dispatch_with_the_same_idempotency_key...`) | No hay un framework unificado de caos (deltas partidos, eventos duplicados, OOM, timeout simultáneos); la cobertura existe dispersa por subsistema, sin fixture reproducible desde Studio. |
| EVAL-04 | parcial | studio/checks/*.mjs (~40 checks: activity, council, stateMirror, deltas, markdown, etc., unitarios de adapters/componentes vía node+esbuild) | tests/e2e/test_agent_flows.py, test_studio_baseline.py (Playwright, journeys reales de UI) | tests/test_studio_*_js.py (envuelven los .mjs), tests/e2e/conftest.py | Todos los e2e (tests/e2e/conftest.py `fake_llm`) corren SOLO contra un LLM simulado; no se encontró un segundo paso contra backend/modelo real, justo lo que la aceptación pide no considerar como integración final. |
| EVAL-05 | existente | tests/test_agent_asks_before_a_new_system.py (regla nace de incidente del 10-09-2026, verifica literalmente que el prompt sigue preguntando ante diseño ambiguo); src/output_oracle.py ("false success" no se puede colar por exit 0) | (sin pantalla que vincule bug/test/release) | tests/test_pr6020_rebase_regressions.py, tests/test_kv_cache_invalidation_2927.py, tests/test_agent_asks_before_a_new_system.py | Patrón de fixture-por-incidente bien establecido y con casos nombrados por PR/issue; no se halló vínculo bug↔test↔release en Studio ni mecanismo que impida re-listar un fallo ya corregido como pendiente. |

## Grupo E — Producto e interfaz: ART, MEDIA, CONN, AUTO, UX, SET, A11Y, LANG, BENCH

| ID | Estado | Rutas reales (backend) | Rutas reales (Studio) | Tests existentes | Qué falta / nota |
|---|---|---|---|---|---|
| ART-01 | parcial | src/artifact_store.py (`collect`/`persist`/`publish_copy`: hash, escritura temporal + link atómico, checksum verificado); src/contracts/artifact.py (`Artifact.partial: bool`); routes/artifact_routes.py (list/{id}/{id}/download por owner) | studio/src/screens/ArtifactInfo.tsx + adapters/artifact-info.ts (provenance, partial/completo, descarga) | docs/design/ART-1-artifacts.md cita 126+49+112 pruebas (no localizadas por nombre de fichero) | No hay estados preparando/generando/validando/fallido, solo bool `partial`. Frontend no tiene "abrir en app externa", "revelar en carpeta" ni "copiar referencia"; solo descarga y ficha de procedencia. |
| MEDIA-01 | parcial | src/chat_handler.py (`vision_enabled`, `model_supports_vision`, `analyze_image_with_vl_result`; despoja `image_url` para modelos de solo texto y usa descripción VL en su lugar) | Ninguna ruta encontrada que explique al usuario qué verá el modelo (Composer.tsx no menciona vision/caption) | tests/test_vision_model_detection.py, tests/test_lmstudio_vision.py, tests/test_vision_owner_scope.py | Backend evita "fingir que vio" la imagen (usa caption derivado), bien probado. Frontend no explica al usuario si verá imagen, transcripción o descripción derivada. |
| CONN-02 | parcial | routes/email_routes.py: `GET /search`, `GET /read/{uid}` (lectura) separados de `POST /draft`, `POST /send`, `GET/POST/DELETE /pending/{sid}` (agent_draft→approve, escritura/ejecución) | studio/src/screens/email/Compose.tsx (revisión de destinatarios/adjuntos antes de enviar), Outbox.tsx (borradores de agente pendientes de aprobación) | No localizados específicamente | Backend separa bien leer/redactar/enviar y agent_draft requiere aprobación explícita. No se verificó revisión de calendario/zona horaria en el compose ni que aprobar un borrador impida cambiar destinatario/adjunto. |
| CONN-03 | parcial | routes/email_routes.py `GET /resolve-contact` (busca en Sent/INBOX/Drafts, devuelve candidatos); routes/contacts/contacts_routes.py `GET /search` | studio/src/screens/email/Compose.tsx `Recipients()` (autocompletar con lista `role="listbox"`, selección explícita por teclado/click, no auto-elige) | No localizados | El picker de Compose.tsx exige selección explícita (nombre+email), pero no muestra "origen" y solo cubre contactos de email; el envío guiado por agente no usa `resolve-contact` (no hay desambiguación en la capa de herramientas). |
| AUTO-02 | parcial | src/approval_store.py (`status`: granted/denied/**expired** distintos, `uses_left` como presupuesto de usos, `expire_stale()`); src/agent_profiles/resolver.py `snapshot()` (fija permisos/resolución por ejecución, `identity`+`trace`) | studio/src/screens/Activity.tsx (filtro "Needs action", contador `waiting`, tarjeta de aprobación "Nothing happens until you decide") | No enumerados | Caducar nunca es "granted" (verificado en código). No se encontró presupuesto monetario/recursos (solo `uses_left` por número de usos) ni política explícita configurable para ausencia del usuario. |
| UX-01 | parcial | routes/editor_draft_routes.py es solo del editor de imágenes (canvas), no del texto del composer; no hay ruta server-side para el borrador de mensaje de chat | studio/src/screens/Studio.tsx (`readDraftFor`/`writeDraftFor` en `localStorage` por sessionId, excluye incógnito); studio/src/screens/studio/panel-storage.ts (`sessionStorage`); L29: `readAttachmentsFor`/`writeAttachmentsFor` (mismo patrón, misma exclusión de incógnito) ahora también persisten y restauran los adjuntos preparados por sesión — cerraba QA-35 (antes xfail) | tests/qa/test_qa_35_borrador_persistente.py (reescrito, ahora verde) | El texto del borrador y ahora los adjuntos viven en `localStorage` del navegador (sin backend, sin límites/limpieza server-side) — eso sigue sin cambiar. |
| UX-02 | parcial | No existe `client_message_id`/outbox; routes/chat_routes.py `GET /api/chat/stream_status/{id}` y `/resume` permiten reconectar a un stream activo sin reenviar | studio/src/screens/studio/Composer.tsx (`!event.nativeEvent.isComposing` evita Enter durante IME); studio/src/screens/Studio.tsx (`sendingMessage` ref + `busy` bloquean doble clic; espera `uploads.hasPending()` antes de enviar) | No localizados | No hay clave de idempotencia server-side ni outbox; el guard de doble envío es solo estado en memoria del cliente, no protege ante reconexión real duplicando la tarea en el backend. |
| UX-04 | parcial | routes/chat_routes.py `POST /api/chat/subagent/steer/{child_session_id}` (`steer_worker` en src/agent_tools/subagent_tools.py) — solo para subagentes | studio/src/screens/studio/Composer.tsx: solo botón "Stop" (`Square`) cuando `busy`; no hay botón "Dirigir tarea" ni "Enviar después" en el composer principal | No localizados | No existe steering de la tarea principal expuesto al usuario ni cola explícita "enviar después"; solo cancelar (Stop) está implementado en la UI principal. |
| UX-05 | parcial | n/a (lógica de cliente) | studio/src/screens/Studio.tsx (`pinnedRef`, `onScroll` umbral <80px, autoscroll solo si `pinnedRef.current`); studio/src/screens/studio/MessageNavigator.tsx (saltos entre turnos con teclado/slider, `aria-*`) | No localizados | Auto-scroll condicionado y navegador de turnos sí existen. No se encontró botón "nuevos mensajes" cuando el lector no está al final, ni búsqueda dentro de la conversación en Transcript.tsx. |
| UX-08 | parcial | routes/chat_routes.py maneja `sources` en el JSON de respuesta (~L2203); Harness recibe `summary`/`checks` desde el backend del turno; OBS-03's `src/contracts/errors.py` taxonomy ya existía sin llegar hasta el párrafo de error del turno | studio/src/screens/studio/Transcript.tsx: `CopyButton` (copiar respuesta), `IconButton RefreshCw` (Regenerar = reintentar); L29: el párrafo de error del turno ahora usa `describeError`/`friendlyError` (studio/src/components/errorTaxonomy.ts, ya usado por Activity.tsx) en vez de mostrar `turn.error` en crudo, con la MISMA clave i18n que Activity — leyendo `errorClass`/`traceId`/`stepId` que `chat.ts::decode()` ahora expone desde `error_class`/`trace_id`/`step_id` | tests/test_l29_studio_error_trace_decode_js.py (nuevo) | Copiar, regenerar y error inline categorizado están implementados. No se verificó "abrir fuentes"/descarga de artefactos citados directamente desde la respuesta ni diagnóstico técnico desplegable explícito. **Gap conocido de L29**: `studio/src/screens/studio/model.ts::errorTraceFields()` (fichero ajeno) sigue leyendo `raw.error_class`/`raw.trace_id`/`raw.step_id` en snake_case directamente del `ChatEvent`, así que NO recoge los nuevos campos camelCase de `chat.ts` — ver "Cambios necesarios en ficheros ajenos" del informe de L29. |
| BENCH-02 | parcial | src/changesets.py + routes/changesets_routes.py (`/build`, `/from-dispatch/{job_id}`, `/diff` con checkpoint sha) — verificación/auditoría de cambios, no revisión interactiva por hunk | studio/src/screens/documents/DiffView.tsx (`decide(id,accepted)` por chunk, botones "Take"/"Keep mine") — es para edición de documentos, no para cambios de código con dependencias | No localizados | `changesets` prueba/audita cambios ya hechos (fingerprint, checkpoint) pero no ofrece aceptar/rechazar por hunk ni abrir en editor externo. DiffView.tsx sí acepta/mantiene por chunk pero en otro dominio (documentos), sin relaciones de dependencia ni invalidación por edición externa. |
| SET-01 | parcial | src/doctor.py (`run()`/`_backends`/`_sandbox_image`/`_models`/etc.: chequeos no destructivos, reporta `absent` en vez de `fail` cuando falta Docker/ComfyUI) | No se encontró consumo de `/api/doctor` en studio/src; studio/src/screens/cookbook/* (Fit/Download/Servers/Models) gestiona modelos locales, no es un asistente de primer uso | No localizados | Diagnóstico backend no destructivo existe pero no está expuesto en la UI de Studio. No se identificó un recorrido guiado mínimo (conectar modelo → probar chat → herramienta segura) que salte módulos no usados. |
| SET-03 | existente | src/agent_profiles/contracts.py `PRECEDENCE=("system_policy","owner_policy","project_restriction","activity_policy","task_override","agent_default","project_default","global_default")`; resolver.py `explain()`/`explain_rows()`; src/agent_settings_schema.py (`coerce_setting_value`, `_clamp`, help por campo) | studio/src/screens/Settings.tsx (`data-changed` marca campos que difieren de `schema.defaults`) | No enumerados | Precedencia global→proyecto→agente→turno es dato auditable (`explain`), con validación/clamp al guardar. No se encontró buscador de ajustes, modo simple/avanzado ni "copiar perfil" en Settings.tsx. |
| SET-05 | parcial | src/chatgpt_subscription.py (`ChatGPTSubscriptionReauthRequired` se lanza en vez de caer a otra API); src/api_key_manager.py (claves por backend) | No se encontró indicador Local/Remoto por llamada ni "coste desconocido" en studio/src | No localizados | Un fallo de reauth de suscripción lanza excepción explícita en vez de fallback silencioso (buena señal), pero no hay UI con indicador Local/Remoto por llamada ni cálculo de categorías de datos antes de usar un backend remoto. |
| A11Y-01 | existente | n/a (funcionalidad de frontend) | studio/src/shell/CommandPalette.tsx (Ctrl+K); studio/src/screens/Settings.tsx `ShortcutsSection` (remapeable, detecta duplicados, botón "Defaults"); studio/src/components/Dialog.tsx (Radix: focus trap, `aria-modal`, restauración de foco) | tests/e2e/test_composer_shortcuts.py, tests/e2e/test_studio_baseline.py | Atajos remapeables, paleta de comandos y diálogos con trampa de foco real vía Radix están implementados y probados en e2e. No se verificó cobertura 100% teclado del flujo de revisión de diffs (ver BENCH-02). |
| LANG-01 | parcial | src/reply_language.py (directiva de idioma por turno, retrocede al último turno que fijó idioma, no fuerza nada sin señal ≥`MIN_SIGNAL`); src/action_intents.py (patrones en español limitados a edición de objetivos/metas de proyecto, ej. l.100) | studio/src/i18n/es.ts (5781 líneas); `locale()` usado en formateo de número/fecha (p.ej. ArtifactInfo.tsx) | tests/test_reply_language.py; tests/test_agent_loop_workspace_tool_floor.py ("Anade" sin tilde); tests/test_action_intents.py (`test_spanish_project_objective_order_is_unicode_normalization_safe`) | Preservación de idioma y pruebas con tildes/Unicode son sólidas. Los intents en español para activar capacidades ("impleméntame", "créame") solo existen para el dominio de objetivos de proyecto, no de forma genérica como los equivalentes en inglés. |

