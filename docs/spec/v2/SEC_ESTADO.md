# Estado de SEC-01..07 (Lote 16 - Seguridad)

Generado a mano (Lote 16) a partir de `docs/spec/v2/backlog.json` (SEC-01..08) y
`docs/spec/v2/MAPA_REUTILIZACION.md` (filas SEC-01..07), verificando cada
garantia contra el codigo real desplegado (TestClient / import directo de los
modulos, sin mocks de la autoridad que se prueba). SEC-08 no es de este lote.

Convenciones, igual que `QA_ESTADO.md`: **verde** = el mecanismo existe y un
test lo prueba contra el codigo real. **xfail** = una garantia concreta falta
hoy, con `xfail(strict=True)` senalando el gap exacto. Los tests nuevos de
este lote viven en `tests/test_sec_lote16_*.py`; los `tests/qa/test_qa_30..33`
(preexistentes, verdes) cubren SEC-01/02/04/06/07 desde el angulo de
aceptacion y no se han tocado.

| ID | Estado | Donde vive | Test | Que queda abierto |
|---|---|---|---|---|
| SEC-01 | verde | `core/authz.py` (matriz deny-by-default para tokens API, consultada por `app.py` antes de `call_next`); `src/contracts/tool.py` (`ToolInvocation.authorization_ref`) | `tests/test_auth1_token_matrix.py` (preexistente, matriz completa); `tests/test_sec_lote16_sec01_sec02.py` (nuevo: un `authorization_ref` inventado no lo lee ningun consumidor real; un scope inventado o el conjunto completo de scopes reales no abren una ruta no declarada) | Frontend: sin vista de "concesiones activas" ni revocacion inmediata en Studio (ya documentado en el mapa, fuera de alcance backend); los endpoints de allowlist de `command_guard_routes.py` no estan cableados a ninguna pantalla |
| SEC-02 | verde | `src/prompt_security.py` (`untrusted_context_message`, marcado de procedencia, escape de marcadores, invisibles); `src/tool_capabilities.py` (`ToolRunSecurityContext`) | `tests/test_prompt_security.py`, `tests/test_prompt_injection_evasion.py`, `tests/test_external_context_tool_gate.py` (preexistentes); `tests/qa/test_qa_30_inyeccion_en_documento.py` (preexistente); `tests/test_sec_lote16_sec01_sec02.py` (nuevo: superficie de email HTML con instruccion invisible en Unicode tags + intento de cierre prematuro del bloque guardado, verificados juntos como en QA-30, mas los 4 patrones de evasion del marcador de apertura) | Cobertura amplia; no se ha verificado cada superficie una por una (adjuntos PDF/DOCX renderizados a texto, por ejemplo) |
| SEC-03 | verde (lote 20) | `src/tool_execution.py` (confinamiento por `realpath` + roots, rechazo de raices de filesystem incl. UNC, `_is_windows_reserved_name` sobre cada componente de la ruta) | `tests/test_personal_dir_symlink_escape.py`, `tests/test_file_mentions_symlink_containment.py`, `tests/test_workspace_confine.py` (preexistentes); `tests/test_sec_lote16_sec03_sec04.py` (nuevo en L16: ya NO es xfail — confirma que `CON`/`PRN`/`AUX`/`NUL`/`COM1-9`/`LPT1-9` (con o sin extension, cualquier capitalizacion) se rechazan en cualquier componente de la ruta, no solo el nombre final) | Conocido y ya documentado: `src/agent_loop.py` deja el shell sin sandbox real (solo las tool-calls de archivo estan confinadas) |
| SEC-04 | verde (por-fetch) | `src/outbound_fetch.py` (`classify_destination`/`fetch`: resolucion unica por hop, pinning, bloqueo de IP privada/metadata, re-clasificacion en cada redireccion) | `tests/test_outbound_broker_ssrf.py`, `tests/test_webhook_dns_rebinding_pin.py` (preexistentes); `tests/qa/test_qa_31_ssrf_y_redireccion.py` (preexistente); `tests/test_sec_lote16_sec03_sec04.py` (nuevo: direccion IPv6 con mapeo IPv4 hacia metadata; cadena de 3 saltos donde solo el ultimo se vuelve hostil; redireccion en sentido inverso, de un perfil local hacia el internet publico) | `tests/qa/test_qa_29_privacidad_transitiva.py` (preexistente, xfail): no existe un perfil "local-only" unico que tambien cubra reranker/resumen remoto — el control sigue siendo por-fetch, no una politica global auditada; fuera de alcance de este lote (tocaria `src/endpoint_resolver.py`/`src/rerank.py`, no propios) |
| SEC-05 | verde | `core/log_safety.py` (`redact_secrets`, instalado en root loggers); `src/secret_storage.py`, `src/secret_files.py`; `src/mcp_manager.py::build_server_env` (scrub del token interno via `src/native_env.py::scrub_private`) | `tests/test_sec1_secret_hygiene.py`, `tests/test_api_key_file_permissions.py`, `tests/test_native_env.py` (preexistentes); `tests/test_effective_config.py::test_effective_route_returns_no_secret` (preexistente, ruta `/api/config/effective`); `tests/test_sec_lote16_sec05_sec06.py` (nuevo: el token interno real de `os.environ` no sobrevive a `build_server_env(..., inherit_env=True)`, ni en mayusculas ni en variantes de capitalizacion, ni el alias legado `ODYSSEUS_INTERNAL_TOKEN`) | No se localizo un export/"crash dump" dedicado con test explicito propio, pero la redaccion esta instalada globalmente en logging (ya sabido) |
| SEC-06 | verde (lote 20) | `core/authz.py`; decenas de `test_*_owner_scope.py`/IDOR; `src/tool_approvals.py` (owner-checked); `src/chat_outbox.py` (owner-checked); `routes/config_routes.py`, `routes/tool_registry_routes.py`, `routes/research/research_routes.py` (owner-checked, incl. `/resume`); `src/question_store.py` (`Store.get/resolve/cancel` ahora reciben y comprueban `owner`, normalizado igual que `src/tool_approvals.py`; `owner=None` sigue sin filtrar, para no romper llamadores preexistentes que aun no pasan dueno); `routes/chat_routes.py:1937` pasa `owner=owner` a `resolve_question` | `tests/qa/test_qa_33_aislamiento_de_dueno.py` (preexistente); `tests/test_sec_lote16_sec05_sec06.py` (control positivo con `chat_outbox`, preexistente; el xfail estricto que demostraba el gap de `question_store` ahora es `test_owner_b_cannot_read_or_resolve_owner_as_open_question`, verde) | Ninguno nuevo conocido para `question_store`; el aislamiento de dueno del resto del sistema sigue por-modulo, no una politica global auditada (fuera de alcance) |
| SEC-07 | verde | `desktop/main.cjs` (`contextIsolation:true, sandbox:true, nodeIntegration:false, webSecurity:true` en toda ventana; CSP sin `unsafe-eval`; IPC `faustus:window` limitado a la ventana propia y a 5 acciones fijas); `desktop/preload.cjs` (bridge minimo, sin `ipcRenderer` crudo) | `tests/qa/test_qa_32_preview_malicioso.py`, `desktop/desktop-control.test.cjs`, `tests/test_security_headers_middleware.py` (preexistentes); `tests/test_sec_lote16_sec07_electron.py` (nuevo: ninguna CSP declarada permite `unsafe-eval`; el bridge expone exactamente `command`+`subscribe`, nunca el objeto `ipcRenderer` completo ni `require`/`fs`/`child_process`; el handler IPC valida `senderFrame` y la lista fija de acciones) | Bien evidenciado; el puente IPC (`ipcMain.handle('faustus:window',...)`) esta ahora probado directamente (antes solo se habia "visto"), pero no hay un test end-to-end con Electron real ejecutando una pagina hostil |

Resumen (tras Lote 20): **7 verde** (SEC-01, SEC-02, SEC-03, SEC-04\*, SEC-05,
SEC-06, SEC-07) — los dos gaps reales que este documento rastreaba
(nombres reservados de Windows en SEC-03, `question_store` sin
comprobacion de dueno en SEC-06) estan cerrados. \*SEC-04 es "verde" para el
control por-fetch que pide el criterio de aceptacion SSRF/redireccion/DNS
rebinding; el perfil "local-only" global que tambien pide el backlog sigue
siendo xfail (QA-29), sin cambios de este lote.
