# WP34 — Exportación portable y OTIO

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP26, WP13, WP03. No ejecutar con dependencias sin cerrar.

Entregar proyectos editables y medios sin falsa completitud.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: AST09, AST10, AST11.

## Archivos y autoridad

Puntos de extensión iniciales: `src/project_export.py`, `src/creator/portable.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Manifest inventario hashes/versiones/licencias

2. Bundle con originales/proxies opcionales y faltantes explícitos

3. OTIO subset y reporte de pérdidas de efectos

4. Import seguro sin ejecutar hooks ni sobrescribir

## Criterios de cierre

- Bundle completo contiene todo lo declarado

- Missing assets bloquea export completo

- Reimport en carpeta nueva conserva relaciones autorizadas

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.