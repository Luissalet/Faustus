# WP06 — Identidad modelo frente a despliegue

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP00. No ejecutar con dependencias sin cerrar.

Separar metadatos de pesos y observaciones por configuración ejecutada.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: MOD01, MOD02, MOD19.

## Archivos y autoridad

Puntos de extensión iniciales: `src/model_capabilities.py`, `src/model_calibration.py`, `src/model_capability_readers`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Crear ModelSpec y DeploymentFingerprint compatibles con registros previos

2. Migrar probes viejos a scope legacy_unknown

3. Clave incluye motor, revisión, configuración y hardware

4. Conservar aliases/digests para deduplicar pesos

## Criterios de cierre

- Mismo digest en dos deployments no comparte resultados de rendimiento

- Un probe sin identidad suficiente no se promueve a verificado

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.