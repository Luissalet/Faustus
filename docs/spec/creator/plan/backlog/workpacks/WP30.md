# WP30 — Recursos físicos y modo conservador

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP09, WP00. No ejecutar con dependencias sin cerrar.

Representar contención real sin relajar la seguridad actual.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: MOD09, RES01, RES02, RES03.

## Archivos y autoridad

Puntos de extensión iniciales: `src/resource_admission.py`, `src/vram_admission.py`, `src/gpu_topology.py`, `src/memory_budget.py`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Inventario GPU UUID + RAM commit + scratch + CPU

2. Alias endpoint→recursos y avisos topología

3. Reservas predictivas y revalidación antes de ejecución

4. Mantener serialización actual por defecto

## Criterios de cierre

- Dos endpoints de la misma GPU no duplican capacidad

- 12+16+16 no se muestran como dispositivo único

- Un desconocido no se convierte en capacidad libre

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.