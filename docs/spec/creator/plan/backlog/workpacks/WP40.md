# WP40 — Publicación y entrega aprobada

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP34, WP32, WP09. No ejecutar con dependencias sin cerrar.

Preparar paquetes y conectores de publicación por destino.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: EXT09, ADV05.

## Archivos y autoridad

Puntos de extensión iniciales: `src/connector_outbox.py`, `src/creator/delivery.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Títulos/portadas/subtítulos/formatos por canal

2. Aprobación de destinatario/contenido/hash/fecha

3. Outbox, recibo remoto y reconciliación incierta

4. Reutilizar delivery donde exista

## Criterios de cierre

- Renderizar no publica

- Cambiar archivo invalida permiso de envío

- Aceptación remota no se confunde con entrega final

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.