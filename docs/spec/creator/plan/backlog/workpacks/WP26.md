# WP26 — Caché y reconstrucción incremental

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP12, WP14, WP22. No ejecutar con dependencias sin cerrar.

Recalcular sólo los derivados afectados, sin caché engañosa.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: AST05, ORC12.

## Archivos y autoridad

Puntos de extensión iniciales: `src/provenance_graph.py`, `src/creator/dependencies.py (nuevo)`, `src/workflows`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. DAG de inputs/revisiones/modelos/recetas

2. Fingerprint de ejecución completo

3. Invalidación por alcance temporal

4. Concurrencia de solicitudes iguales conserva occurrences y permisos

## Criterios de cierre

- Cambiar un título no regenera vídeo fuente

- Un backend cambiado invalida evidencia no comparable

- Cache hit explica procedencia y no cruza owners

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.