# WP33 — MCP Creator y captura opcional

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP32, WP10, WP05. No ejecutar con dependencias sin cerrar.

Exponer las mismas operaciones a clientes externos sin otra autoridad.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: EXT05, EXT06.

## Archivos y autoridad

Puntos de extensión iniciales: `src/builtin_mcp.py`, `src/tool_registry.py`, `src/creator/tool_api.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Tools discover/plan/edit/render/status con scopes

2. Mismo owner y mismo API que Studio

3. Captura explícita de página/selección mediante extensión opcional

4. Cambios de esquema versionados y referencias invalidadas

## Criterios de cierre

- MCP no recibe permisos superiores al usuario

- Sin cookies exportadas ni scraping de sesiones del proveedor

- Disponibilidad de cliente y cuotas no se prometen

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.