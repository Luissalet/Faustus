# WP12 — Operaciones no destructivas comunes

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP02, WP03, WP01. No ejecutar con dependencias sin cerrar.

Historial ordenado, revisiones y undo/redo para imagen y tiempo.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: AST07, IMG01.

## Archivos y autoridad

Puntos de extensión iniciales: `src/media_edit_projects.py`, `src/creator/operations.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Definir operaciones tipadas y versionadas

2. Aplicar en orden semántico explícito

3. Sustituir blobs inline por referencias mediante migración aditiva

4. CAS para cambios UI/agente

## Criterios de cierre

- Deshacer no pierde originales

- Capas y transforms producen el orden mostrado

- Reintento de command_id no duplica operación

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.