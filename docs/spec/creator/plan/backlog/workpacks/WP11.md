# WP11 — Recetas ComfyUI más expresivas

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP10. No ejecutar con dependencias sin cerrar.

Convertir referencia y máscaras de adjuntos en inputs verificables.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: IMG04, IMG09.

## Archivos y autoridad

Puntos de extensión iniciales: `config/media_workflows`, `src/media_workflows.py`, `src/media_backends/comfyui.py`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Resolver occurrence a upload propio del motor

2. Schemas dependientes de tarea y variantes

3. Catálogo de recipes con fingerprint completo

4. Review exacta de nodos y dependencias, sin instalar arbitrariamente

## Criterios de cierre

- Referencia enviada consta en recibo técnico

- Rol textual no se etiqueta como conditioning

- No se ejecutan nodos no revisados

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.