# WP00 — Reauditoría del HEAD y mapa de autoridades

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: ninguna. No ejecutar con dependencias sin cerrar.

Convertir la baseline fijada en un inventario contra el HEAD de implementación.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: EXT10.

## Archivos y autoridad

Puntos de extensión iniciales: `README.md`, `FAUSTUS.md`, `PENDIENTES.md`, `src`, `routes`, `studio/src`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Leer guías y calcular diff frente al commit base

2. Trazar callers de media/modelos/pools y pantallas reales

3. Clasificar capacidad documentada, localizada, integrada y probada

4. Actualizar decisiones sin borrar funciones ya existentes

## Criterios de cierre

- Inventario enlaza archivo, función, ruta, pantalla y prueba donde existan

- Toda ausencia no demostrada queda como no localizada

- No descarga modelos ni inicia inferencia

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.