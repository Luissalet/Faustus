# WP39 — LoRA y conjuntos creativos opcionales

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP24, WP21, WP30, WP36. No ejecutar con dependencias sin cerrar.

Personalización con datos autorizados y evaluación separada.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: MUS12, ADV03, ADV06.

## Archivos y autoridad

Puntos de extensión iniciales: `src/creator/training.py (nuevo)`, `src/media_consent.py`, `src/bench`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Dataset lineage/licencias/consentimiento

2. Preflight GPU/scratch y entornos separados

3. Entrenamiento recuperable sólo si backend soporta checkpoints

4. Comparador heldout antes de promover adaptador

## Criterios de cierre

- Nunca entrena como efecto de generar una imagen

- No memoriza por defecto toda biblioteca

- Checkpoint incompleto no se instala

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.