# WP14 — Renderer FFmpeg reproducible

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP10, WP13, WP04. No ejecutar con dependencias sin cerrar.

Compilar operaciones autorizadas a un render observable.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: VID10, VID12.

## Archivos y autoridad

Puntos de extensión iniciales: `src/media_transforms.py`, `src/creator/render.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Argv tipado y filtros permitidos, sin shell libre

2. Progreso parseado y colas/cancelación por proceso propio

3. Exportar perfiles explícitos y recibo de parámetros efectivos

4. Validar decodificación, duración y streams antes de completed

## Criterios de cierre

- Archivo final existe y decodifica

- Cancelación no mata procesos ajenos

- Salida parcial nunca se etiqueta final

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.