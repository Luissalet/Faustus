# WP23 — Adapters de vídeo especializados

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP22, WP10, WP07. No ejecutar con dependencias sin cerrar.

Integrar familias Wan/LTX mediante recipes/versiones probadas.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: VID05, VID06, VID07, MOD15.

## Archivos y autoridad

Puntos de extensión iniciales: `src/media_backends`, `config/media_workflows`, `src/creator/video.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Separar local y hosted por deployment

2. Matrices T2V/I2V/S2V/retake/extend reales

3. Probe barato y prueba GPU opt-in corta por variante

4. Exportar clips listos para timeline, sonido separable

## Criterios de cierre

- No se copia a local la capacidad exclusiva de API

- VRAM y componentes se miden en instalación

- Fallo conserva planos aprobados

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.