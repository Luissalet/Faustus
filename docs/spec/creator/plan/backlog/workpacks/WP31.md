# WP31 — Concurrencia medida y multiworker

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP30, WP10, WP36. No ejecutar con dependencias sin cerrar.

Habilitar paralelismo sólo para recursos realmente independientes.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: RES04, RES05, RES10.

## Archivos y autoridad

Puntos de extensión iniciales: `src/resource_admission.py`, `src/media_backends/pool.py`, `src/dispatch.py`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Medir contención y memoria con plan autorizado

2. Per-device leases y afinidad de modelo

3. Fairness y prioridad de chat sin matar efectos activos

4. Pools remotos separados, aislamiento y reconexión

## Criterios de cierre

- Activa opt-in sólo tras pruebas de dos recursos disjuntos

- Fallo de worker no libera reserva externa no reconciliada

- No promete pooling de VRAM por software

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.