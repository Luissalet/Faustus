# WP29 — Equipos con estado y propiedad claros

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP28, WP22. No ejecutar con dependencias sin cerrar.

Organizar especialistas de producción o código con vidas de contexto explícitas.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: ORC07, ORC08, ORC09, ORC10.

## Archivos y autoridad

Puntos de extensión iniciales: `src/dispatch.py`, `src/external_worker.py`, `src/agent_profiles`, `src/subagent_permissions.py`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Per-task/per-project context y límites

2. Propiedad de documentos/planos y worktrees para código

3. Steering durable por revisión y feedback

4. Único integrador aplica propuestas aceptadas

## Criterios de cierre

- Dos especialistas no pisan el mismo documento

- Cancelar padre cancela pasos futuros y reconcilia externos

- Una identidad persistente no implica permiso persistente

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.