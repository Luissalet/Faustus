# WP03 — Biblioteca y linaje multimedia

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP02. No ejecutar con dependencias sin cerrar.

Reutilizar occurrences para medios, proxies y derivados.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: AST01, AST02, AST08, AST12.

## Archivos y autoridad

Puntos de extensión iniciales: `src/artifact_store.py`, `src/artifact_identity.py`, `src/provenance_graph.py`, `src/creator/assets.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Añadir roles, padres y rangos temporales en metadatos

2. Separar identidad de bytes y pertenencia

3. Añadir miniaturas/waveforms como derivados

4. Respetar tombstones y alcance owner

## Criterios de cierre

- Bytes idénticos no comparten permisos

- Eliminar un enlace no destruye un medio compartido

- No se incrustan vídeos en JSON

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.