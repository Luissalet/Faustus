# WP15 — Transcripción alineada y workers ASR

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP10, WP04. No ejecutar con dependencias sin cerrar.

Añadir WhisperX opcional sin romper el flujo corto local.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: SUB01, SUB02, SUB03.

## Archivos y autoridad

Puntos de extensión iniciales: `services/local_video.py`, `services/speech_runtime.py`, `src/creator/transcripts.py (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Worker aislado por versiones

2. Segmentos/palabras/idioma/speaker_id con confianza nullable

3. Alineadores por idioma y diarización opt-in

4. Particiones con offsets y deduplicación de bordes

## Criterios de cierre

- Un speaker_id no se presenta como identidad humana

- No se descargan pesos al importar

- Regresión del exportador actual pasa

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.