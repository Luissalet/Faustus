# WP01 — Regresiones de contratos existentes

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP00. No ejecutar con dependencias sin cerrar.

Comprobar antes de modificar las observaciones estáticas.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: QA01.

## Archivos y autoridad

Puntos de extensión iniciales: `src/media_workflows.py`, `src/media_capabilities.py`, `src/media_edit_projects.py`, `tests`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Crear pruebas de fingerprint con cambios en requisitos/outputs

2. Probar ffmpeg -version no cero

3. Probar orden de capas/crop/rotación con imágenes mínimas

4. Corregir sólo comportamientos confirmados, conservar compatibilidad y versión

## Criterios de cierre

- Pruebas reproducen el comportamiento antes del parche

- No se presentan observaciones estáticas como exploits confirmados

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.