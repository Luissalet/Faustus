# WP13 — Timeline y relojes

Estado: propuesta. Baseline: `fa567977a00505b18a0fdf1b6e4d1a83f56270c6`.

Dependencias: WP12, WP05. No ejecutar con dependencias sin cerrar.

Un editor temporal correcto antes de añadir IA al montaje.

## Lecturas

Leer `README_START_HERE.md`, `docs/00_BASELINE_Y_GAPS.md`, `docs/03_ARQUITECTURA_Y_CONTRATOS.md` y el capítulo de dominio pertinente. Después buscar estos IDs en `backlog/features.json`: VID01, VID02, VID09, SUB10.

## Archivos y autoridad

Puntos de extensión iniciales: `src/creator/timeline.py (nuevo)`, `studio/src/screens/creator/timeline/ (nuevo)`. Identificar callers, tests y write set real antes de editar. No crear una autoridad paralela.

## Implementación

1. Frames racionales y muestras de audio

2. Pistas, clips, trims, splits, ripple, snapping y markers

3. Mapas de retiming y clips anidados acotados

4. Vista virtualizada y proxy seeking

## Criterios de cierre

- Cortes no acumulan error float

- VFR se indexa por PTS o proxy documentado

- Editar sólo cambia revisión y no renderiza sin orden

## Entrega del agente

Incluir diff, tests realmente ejecutados, resultados, limitaciones, migración/flag/rollback y puntos pendientes. No afirmar éxito de motores por un mock; no ejecutar modelos ni instalar dependencias sin plan autorizado. Un paquete grande se divide en PR homogéneos.