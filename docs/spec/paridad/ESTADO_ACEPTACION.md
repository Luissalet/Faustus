# Estado de los 36 casos de aceptación de paridad de aceptación (A01-A36)

Generado a mano (Lote T1, PR1) a partir de `docs/spec/paridad/acceptance_cases.json`
(copia sin cambios de `evidence/acceptance_cases.json` del paquete de
auditoría) y coherente con el marker `acceptance(case_id)` que cada test
declara (`pyproject.toml [tool.pytest.ini_options].markers`) —
`tests/test_acceptance_index.py` lee ese marker de todo `tests/*.py` y falla
si esta tabla se desincroniza con lo que realmente encuentra.

Regla del encargo (ver `docs/spec/paridad/README.md`): **ninguna fila pasa
a `verde` porque exista una clase o un test simulado del mecanismo**. Solo
cierra una fila un test que ejercita código real de Faustus (TestClient +
rutas reales, `agent_runs`/`approval_store`/`tool_approvals`/workflows
reales, fakes solo para el LLM y procesos externos) y que aparece en la
columna `Test` de esa fila.

- **verde**: el test existe, pasa y ejercita código real — la fila lo
  enlaza.
- **xfail**: el mecanismo no existe todavía; el test existe y está marcado
  `xfail(strict=True)` con el motivo en el propio test.
- **pendiente**: sin test todavía. Es el estado por defecto de este lote
  (PR1 solo entrega el manifiesto y el ejecutor, no los mecanismos de
  A01-A36); no implica que el caso esté descartado.

Resumen tras fusionar T1+T2+T3+S3+T4-ola1 (2026-09-16): **10 verde** (A01–A07,
A08, A09, A20), 26 `pendiente`. En la entrega de T1 (PR1) era **1 verde**
(A03), **0 xfail**, **35 pendiente**. De las 35 pendientes, 6 estaban
asignadas por `CONTRATO_PARIDAD_1.md` a los lotes T2 (A01, A04, A07) y T3
(A02, A05, A06); T4 (ola 1 de paridad, `CONTRATO.md`) cierra A08/A09 (tool
discovery); las 27 restantes quedan abiertas para un PR posterior.

| ID | Área | Estado | Test | Qué falta |
|---|---|---|---|---|
| A01 | turns | verde | `tests/acceptance/test_a01_turn_admission.py` | — (T2: lock de admisión por sesión en `routes/chat_routes.py::chat_stream` que envuelve solo la mutación hasta `agent_runs.start()`; un POST inválido nunca toca el run válido en curso; dos POST válidos concurrentes no se entrelazan; `client_message_id` en carrera sigue siendo un solo turno) |
| A02 | approvals | verde | `tests/acceptance/test_a02_approval_race.py` | — (T3: la misma decisión desde dos clientes concurrentes contra la ruta real → una fila decidida y el perdedor recibe 409 con el recibo del ganador; `tool_approvals.consume_with_reason` devuelve `consumed`/`already_consumed`/`expired`/`not_found`/`owner_mismatch`/`invalid_decision`) |
| A03 | approvals | verde | `tests/test_tool_approvals.py::test_dispatcher_rejects_modified_approved_action` | — (digest canónico de `src/tool_approvals.py:120-171,293-330` comprobado en `src/tool_execution.py:1179-1195`; test marcado `@pytest.mark.acceptance("A03")` en este lote, sin más cambios) |
| A04 | events | verde | `tests/acceptance/test_a04_replay_cursor.py` | — (T2: corte tras k eventos y reconexión real por `GET /api/chat/resume` con cursor → sin hueco ni duplicado, `[DONE]` una vez, LLM falso llamado una vez) |
| A05 | recovery | verde | `tests/acceptance/test_a05_unknown_effect.py` | — (T3: evento `tool_effect` `pending|confirmed|failed` por `call_id` con flush inmediato para tool-calls no `read`; `recover_interrupted_runs` marca `unknown_effects` en el parcial y en la nota; el siguiente turno recibe un bloque de sistema que prohíbe repetirlas sin comprobar) |
| A06 | recovery | verde | `tests/acceptance/test_a06_lease_fencing.py` | — (T3: `lease_generation` en `scheduled_tasks` y `workflow_node_runs` con migración idempotente; `still_owner` antes de cada efecto en el scheduler → estado `fenced`; handlers de workflow con `_check_fenced` — devuelve `failed` + `fenced: True` porque el engine solo admite cuatro estados de handler) |
| A07 | cancel | verde | `tests/acceptance/test_a07_cancel_cascade.py` | — (T2: `stop_workers_of_parent_by_level` transitivo; `chat_stop` retira aprobaciones de toda la jerarquía y cancela sus preguntas; `cleanup.workers_stopped` por nivel y `cleanup.approvals_retired`) |
| A08 | tools | verde | `tests/acceptance/test_a08_tool_discovery.py` | — (T4: `lookup_tools` — ya existente en `src/tool_serve.py` — narrowed to the EXACT permitted schema by `src/tool_discovery.py::is_permitted`/`audit_selection` — `tool_policy`/`disabled_tools`/admin denylist AND real-tool existence, not just `disabled_tools` — wired into `agent_loop.py`'s `lookup_tools` promotion step; selection+resolution audited via the new `tool_discovery` SSE event) |
| A09 | tools | verde | `tests/acceptance/test_a09_tool_discovery_no_match.py` | — (T4: `src/tool_discovery.py::audit_selection`'s unknown-tool branch — a name that is not a real tool never resolves/promotes; explicit bounded (N=5) "closest by name/capability" fallback via `nearest_by_name_or_capability`, no fabricated call, no schema dump) |
| A10 | code_mode | verde | `tests/acceptance/test_a10_code_mode_policy_gate.py` | — (T6: `run_code` compone llamadas en `src/code_mode/` — subproceso `python -I` aislado + `guest.py` + `bridge.dispatch_call`, que despacha cada `tools.call` por el mismo `execute_tool_block` real; un comando destructivo obtiene el mismo rechazo del guardia que una llamada directa y un tool en `disabled_tools` es inalcanzable. `security_context` se reconstruye como `ToolRunSecurityContext()` nueva en cada llamada del puente porque `ctx` de `TOOL_HANDLERS` no transporta la instancia real del turno — ver `T6_wiring.md`; no cambia el veredicto para el guardia de comandos destructivos, que corre antes de consultar el estado del contexto) |
| A11 | code_mode | verde | `tests/acceptance/test_a11_code_mode_quotas.py` | — (T6: `src/code_mode/runner.py` — timeout de pared (`agent_code_mode_timeout_seconds`), máximo de llamadas (`agent_code_mode_max_calls`), bytes de salida (`agent_code_mode_max_output_bytes`), RLIMIT_CPU/RLIMIT_AS por `preexec_fn` en POSIX; recibo diagnóstico `{terminated_by, calls_made, elapsed_ms, output_bytes, last_call}`; el pid del subproceso no sobrevive tras el kill (comprobado con `os.kill(pid, 0)`). CPU/memoria vía rlimits implementados pero sin test dedicado en este lote — no exigido por los 4 casos listados en el contrato) |
| A12 | artifacts | verde | `tests/acceptance/test_a12_tool_result_offload.py::test_two_parallel_oversized_results_survive_a_restart_with_matching_integrity` | — (T5: `src/tool_result_offload.offload_if_oversized` persiste el resultado íntegro en el artifact store real — owner, sha256, bytes, retention — antes de sustituirlo por un resumen acotado + `artifact_id`; dos tools en paralelo → dos artefactos distintos; tras recrear el engine/app desde el mismo sqlite en disco, `routes/artifact_routes.py` real + `read_artifact_range` recuperan el owner con sha256 recomputado == guardado. Cableado de la llamada en `src/agent_loop.py` (no es fichero de T5) y del tool `read_artifact` en `src/tool_schemas.py`/`src/agent_tools/__init__.py` (T6) documentado en `T5_wiring.md`, comprobado por `tests/test_t5_wiring.py` `xfail(strict=True)`) |
| A13 | artifacts | verde | `tests/acceptance/test_a13_artifact_tenant_isolation.py::test_wrong_tenant_and_nonexistent_id_get_identical_denials` y `::test_encoded_traversal_ids_never_reach_the_filesystem` | — (T5: `routes/artifact_routes.py` real vía TestClient; otro owner con el mismo id real → 404 byte-idéntico al de un id inexistente, sin metadatos (sha256/owner/tamaño) en el cuerpo, en `metadata`/`download`/`manifest`/`provenance`; ids con traversal codificado (`..%2F`, `%2e%2e/`, `..\\`, `%5c`, id con `/`) nunca resuelven una ruta (404 de enrutado) ni tocan el store; `src.artifact_store.path_of` rechaza la forma decodificada de forma independiente) |
| A14 | context | verde | `tests/acceptance/test_a14_compaction_preserve.py::test_compaction_preserves_pending_approval_constraints_objective_and_sources` | — |
| A15 | context | verde | `tests/acceptance/test_a15_reacquisition.py::test_spilled_body_is_reacquired_intact_and_the_run_report_records_its_cost` | Reacquisition tool (`read_overflow`) not yet wired to the agent's live tool loop — see `T8_wiring.md` |
| A16 | sandbox | verde | `tests/acceptance/test_a16_sandbox_unavailable.py` | — (U1 ola 2: `src/sandbox_provider.py` (nuevo) — puerto `SandboxProvider.probe()→Availability`; `BashTool` real vía `src/sandbox_exec.py` en `agent_sandbox_mode=strict`, con solo `DockerWorkspaceBackend._docker`/`DockerSandboxProvider._run_docker` falseados (daemon ausente, plataforma Windows no soportada): resultado `sandbox_refused`, mensaje literal `sandbox unavailable: <razón>`, y el lanzador de host (`_create_bash_subprocess`) espiado con 0 llamadas — nunca ejecución privilegiada en host por su cuenta) |
| A17 | sandbox | verde | `tests/acceptance/test_a17_sandbox_session_missing.py` | — (U1 ola 2: sandbox de sesión persistente opt-in (`agent_sandbox_persistent_session`, nuevo) sobre `DockerSandboxProvider` — contenedor `sleep infinity` por `session_id`; con el cliente Docker falseado para simular `docker rm` entre dos `BashTool().execute()` con el mismo `session_id`, la segunda ejecución detecta `status()==missing` tras haber existido, aplica `sandbox_missing_policy` (`recreate_empty` verde; `fail` cubierto por test adicional no marcado) y el resultado dice explícitamente que los ficheros previos NO sobrevivieron, listando los conocidos vía `exec(..., touches=[...])`) |
| A18 | mcp | verde | `tests/acceptance/test_a18_mcp_oauth_reauth.py::test_reauthorization_resumes_and_next_call_uses_new_token` | — (U2: `McpManager._oauth_ensure_valid` runs before every `call_tool` on an OAuth Streamable HTTP server against the real `mcp.client.auth.OAuthClientProvider`/`src.mcp_oauth.DbTokenStorage` — a valid token is a no-op, an expired one gets exactly one bounded manual refresh (`httpx.MockTransport` in the test, real `httpx.AsyncClient` in production), and anything else (no refresh_token, or the refresh itself failing) returns a typed `{"reauthorization_required": True, "server_id": ...}` with no token/header/auth-URL in it or in `format_tool_result`'s output, while kicking off a fresh authorize flow via the SAME `_start_http_connect`/`_connect_http` path used at initial connect — no new callback-route wiring. Because the preflight runs before the tool is ever dispatched, the failed attempt never reaches the server (`call_count == 0`), so the retry after reauthorization is the first real attempt, not a repeat) |
| A19 | mcp | verde | `tests/acceptance/test_a19_mcp_tool_catalog.py::test_schema_change_invalidates_cache_and_model_gets_new_schema` | — (U2: `McpManager._discover_tools_paginated` — used by every real connect path (stdio/SSE/HTTP) — follows `tools/list`'s `nextCursor` up to the new `mcp_discovery_max_pages` setting (default 20), marking `discovery_truncated` on the connection when the cap is hit instead of silently serving a partial catalog; the new `src/mcp_tool_cache.py` keys each server's catalog by `serverInfo.version` + a hash of the tool set, so a schema change under the SAME version is caught, not just a version bump. `notifications/tools/list_changed` is wired into every `ClientSession` (`_make_tools_changed_handler` via `_open_client_session`'s `message_handler`) and invalidates + re-discovers in the background, bumping `McpManager._generation` so `get_all_tools`/`get_all_openai_schemas` serve the new schema on the model's next turn) |
| A20 | sdk | verde | `tests/acceptance/test_a20_external_sdk_consumer.py` | — (S3: `npm pack` + `npm install --offline` en dos proyectos limpios, ESM y CommonJS, contra un servidor real con auth activada y un token `ody_…` real de perfil `sdk`; turno de agente con `workspace`, aprobación `tool_approval` real vía `approve_task`, edición que llega a disco, `sessions.export()` + `artifacts.list/get/download` con `sha256` verificado, `turn.cancel('task')` + `turns.resume()` → `RunNotActiveError`, y un token sin scope `sessions` rechazado con 403 al crear sesión; de paso corrigió un bug real en `routes/chat_routes.py::_resolve_request_workspace` que descartaba `workspace` en toda llamada autenticada por token — ver `MATRIZ_PARIDAD.md` fila 2 para las reservas honestas sobre el propio paquete) |
| A21 | embed | verde | `tests/acceptance/test_a21_embed_two_sessions.py` | `studio/embed/` (nuevo, paquete `@faustus/embed`): `<faustus-chat>` Web Component (Shadow DOM, propio cliente `faustus-sdk`, sin router global, sin CSS global), atributos/props `server`/`token`/`session`/`theme`/`mode`, slots `header`/`footer`/`empty`, eventos `faustus:turn-start`/`faustus:turn-end`/`faustus:error`/`faustus:event`, respuesta básica a `ask_user` (pregunta y `tool_approval`), token en memoria (nunca `localStorage`/URL/DOM ligero — se retira el atributo al leerlo). Cierre en tres capas: (1) `studio/checks/embed.check.mjs` monta dos instancias reales (bundle esbuild real) en `happy-dom` contra un `fetch` falso y prueba aislamiento de sesión/estilos/tokens y accesibilidad (`role=log`/`role=textbox`/`role=button`, Enter para enviar); (2) prueba HTTP real contra un servidor Faustus real con auth activada: dos cuentas admin reales, dos tokens `sdk` reales, sesiones y lecturas cruzadas de historial devuelven 403/404; (3) Playwright real (Chromium) abre `studio/embed/example.html` servido en su propio origen contra el servidor real (CORS real vía `ALLOWED_ORIGINS`, sin comodín) y conduce dos turnos reales en dos paneles, verificando que ninguna transcripción ni el DOM ligero ni `localStorage` filtran el token o la respuesta ajena. Ningún cambio en `app.py`/`core/`: `ALLOWED_ORIGINS` ya soporta el origen del host embebido (documentado en `studio/embed/README.md`, no se creó `embed_allowed_origins`) |
| A22 | auth | verde | `tests/acceptance/test_a22_oidc_login.py::test_a22_expired_wrong_audience_replay_and_forbidden_email` | — (U4: `core/oidc.py` — Authorization Code + PKCE, discovery/JWKS cached with their own TTLs, `verify_id_token` checks signature (RS256/ES256 matched by `kid`), `iss`, exact `aud`, `exp`/`nbf`/`iat` with ≤60s skew, and `nonce` bound to the login's `state`; the `state`/`nonce`/PKCE registry is one-shot — a replayed callback finds nothing and gets 400 before any token exchange. `routes/auth_routes.py`'s `GET /api/auth/oidc/{login,callback}` map a verified, allow-listed, `email_verified` identity to a local account keyed by `sub` (never email) via `core.auth.AuthManager.resolve_or_create_oidc_user`, re-syncing `oidc_admin_emails`/`oidc_admin_group` role on every login, then opens the SAME session `create_session_trusted` local login uses) |
| A23 | machine_auth | verde | `tests/acceptance/test_a23_service_identity.py::test_a23_insufficient_scope_denied_and_revocation_effective_within_bound` | — (U4: `src/service_identity.py` layers machine `subject`/explicit `scopes`/`expires_at`/`revoked_at` on the existing `ody_…` tokens via `POST /api/tokens` (`subject=`/`expires_in_seconds=`, `routes/api_token_routes.py`) without touching `core/database.py`'s `ApiToken` schema — a side JSON store, same `atomic_write_json` pattern as `core/auth.py`. A real server, real admin session, real token creation and the new real `POST /api/tokens/{id}/revoke`: a `chat`-only service token hits a real `sessions`-gated route (`POST /api/session`) → 403 naming `sessions` (the EXISTING `core/authz.py` bearer matrix, now reached through a service-identity token); a `sessions`-scoped one is admitted, then revoked, then the SAME token is refused 401 on the very next real request — the documented `token_revocation_bound_seconds` bound (default 0: no cache, immediate) holds for both the production bearer-auth cache and `service_identity.is_revoked()` itself) |
| A24 | schedule | verde | `tests/acceptance/test_a24_scheduler_dst_misfire.py` | — (U3: `src/task_scheduler.py::compute_next_run`/`_dst_resolve` ya resolvía el DST de `Europe/Madrid` con `dst_ambiguity_policy` por tarea; este lote añade `catch_up_max:k` a `misfire_policy` (`_parse_misfire_policy`, cola `catchup_pending_json` drenada por `_next_moment_after`) y la deduplicación por `fire_key` (`claim_fire_key`/`_claim_fire_key_literal`, `scheduler_fire_log` con `fire_key` PRIMARY KEY) aplicada justo antes del efecto en `_execute_task_locked` — no en `_claim_due_task`, donde habría bloqueado un reintento legítimo tras recuperar un lease, ver el docstring de ese método y `test_a_retry_after_lease_recovery_still_executes_once_not_zero_times`; política documentada en `docs/spec/paridad/SCHEDULER_POLITICA.md`) |
| A25 | skills | verde | `tests/acceptance/test_a25_skill_git_source.py` | — (U3: `src/skill_sources.py` (nuevo) — `install`/`check_updates`/`update(verify=True)`/`rollback` contra un repo git real en `tmp_path`; `pinned_revision` es el SHA exacto, nunca el nombre de la rama; `check_updates` es de solo lectura; `update` verifica en staging (digest + frontmatter + fixture de script si `run_fixture` se provee) antes de promover, guardando la revisión previa en `skill_sources_backups/` para `rollback`, que restaura byte a byte comprobando el digest antes y después. Metadatos de origen fuera de la carpeta de la skill a propósito, para no alterar el digest de ADP-25. HTTP: `routes/skill_source_routes.py` (nuevo) probado standalone en `tests/test_u3_skill_source_routes.py`; cableado en `app.py` pendiente — ver `U3_wiring.md`, comprobado por `tests/test_u3_wiring.py` xfail(strict=True)) |
| A26 | learning | verde | `tests/acceptance/test_a26_candidate_invalid_schema.py::test_invalid_schema_candidate_rejected_before_availability`, `test_a26_candidate_invalid_schema.py::test_privilege_escalation_skill_text_rejected_before_availability` | — (U5: `src/harness_evolution/validator.py` parses an `add_skill` `CandidatePatch` through `src.skills_runtime.bridge.manifest_from_markdown` — the real production schema parser — and `src.skill_governance.privilege_escalation_flags`/`evaluation_manifest`; a schema or privilege-escalation failure inserts the patch as `status="rejected"` with the field/reason in `trace`, never touching the active `HarnessRevision`. `routes/evolution_routes.py`'s `POST /api/harness/candidates` exercised via `TestClient`; not yet mounted on `app.py` (not U5-owned), see `U5_wiring.md`, checked by `tests/test_u5_wiring.py` (xfail)) |
| A27 | learning | verde | `tests/acceptance/test_a27_candidate_held_out_failure.py::test_held_out_failure_blocks_promotion_and_keeps_evidence`, `test_a27_candidate_held_out_failure.py::test_all_held_out_passing_is_required_not_just_nonempty_source` | — (U5: `src/harness_evolution/evaluator.py::evaluate` runs a real per-task-id fixture runner over source AND held-out task sets; a candidate that passes source but fails any held-out task lands at `status="evaluated_failed"` and `promote_candidate` refuses it (`PromotionRefused`) — the per-task `source`/`held_out` dict stays queryable through `GET /api/harness/candidates/{id}` afterwards) |
| A28 | learning | verde | `tests/acceptance/test_a28_promoted_candidate_regresses.py::test_dependency_digest_change_reverts_the_promoted_candidate`, `test_a28_promoted_candidate_regresses.py::test_unchanged_dependency_digest_leaves_candidate_promoted` | — (U5: `src/harness_evolution/promotion.py::recheck_applicability` recomputes every `required_capabilities` digest — proven against a REAL fixture skill folder and `src.skills_runtime.discovery.skill_digest` (ADP-25's own digest function, not a duplicate) — and on any mismatch reverts the active `HarnessRevision` back to `rollback_target` through the same CAS path `promote()` used, marking the `CandidatePatch` `status="reverted"` with `trace` naming the capability) |
| A29 | loops | verde | `tests/acceptance/test_a29_loop_breaker.py::test_twenty_identical_tool_calls_stop_the_turn_deterministically` | — (T7 + integración: `src/loop_breaker.py::LoopPolicy` cableada en `src/agent_loop.py` — observa cada (tool, args, resultado) tras ejecutar y cada duplicado que la recuperación de bucles salta; N idénticos → aviso, luego la tool se retira, luego el turno termina con `stop_reason=non_progressing_loop` y evento SSE `loop_breaker_stop`, sin depender de la auto-reflexión del modelo) |
| A30 | learning | verde | `tests/acceptance/test_a30_concurrent_refiner_promotions.py::test_two_threads_promoting_same_parent_one_wins_other_gets_stale_parent` | — (U5: two real OS threads (`threading.Barrier`-aligned) call `promote_candidate` against the same parent `HarnessRevision`; `src/harness_evolution/store.py::promote_cas`'s `BEGIN IMMEDIATE` + `UPDATE ... WHERE status='active'` — real SQLite locking, not a Python lock — lets exactly one succeed and the other raise `StaleParent` naming the winner; `rebase_and_retry` repoints the loser at the winner, re-validates and re-evaluates, and its promotion lands as a SECOND revision descended from the first — both refiners' skills present in the final refs, no lost update) |
| A31 | budget | verde | `tests/acceptance/test_a31_budget_reservations.py::test_parallel_children_reserve_and_reconcile_against_shared_run_budget` | `src/budget_account.py` reservation/reconciliation is wired into `src/agent_tools/subagent_tools.py`'s worker launch (real `delegate_agents`); `GET /api/runs/{run_id}/budget` (`routes/budget_routes.py`) works but is not yet mounted on `app.py` (not T7-owned) — one-line diff in `T7_wiring.md` §1, checked by `tests/test_t7_wiring.py` (xfail). Cost is always `unpriced_usage` today (no per-model price table found) — never a false 0.0. |
| A32 | benchmark | verde | `tests/acceptance/test_a32_a33_benchmark_matrix.py::test_failed_paid_attempt_followed_by_success_sums_both_known_costs`, `::test_any_unknown_cost_in_the_cell_reports_unknown_never_zero`, `::test_all_attempts_failing_still_reports_their_known_costs_and_fails` | — (U6: `scripts/benchmark_matrix.py::compute_total_cost` sums every attempt with a known/free cost — a failed paid attempt followed by a successful one counts BOTH — and returns the literal string `"unknown"`, never `0`, if any attempt's cost could not be priced) |
| A33 | benchmark | verde | `tests/acceptance/test_a32_a33_benchmark_matrix.py::test_missing_cell_is_reported_incomplete_and_denominator_is_not_reduced`, `::test_judge_verdict_whose_input_hash_no_longer_matches_is_stale_not_a_pass`, `::test_cli_run_writes_summary_and_report_and_exits_nonzero_when_incomplete` | — (U6: `scripts/benchmark_matrix.py::build_matrix`/`render_report_md` — a missing {case×system×model} cell is reported `MISSING` and stays in the pass-rate denominator; a judge verdict whose `input_hash` no longer matches `sha256(judged_output)` is `stale` and never counts as a pass) |
| A34 | migration | verde | `tests/acceptance/test_a34_project_migration.py::test_preview_classifies_unsupported_resource_and_connector_credential`, `::test_apply_never_writes_the_token_and_omits_nothing_from_the_report` | — (U6: `src/migration/` — new module, `preview()`/`apply()` against the documented interchange format in `docs/spec/paridad/MIGRACION.md`; connectors are always `requires_reauthorization`, the credential value is never written to `DATA_DIR`; `losses.md` names every excluded entry) |
| A35 | distribution | verde | `tests/acceptance/test_a35_lifecycle.py::test_backup_then_restore_round_trip_preserves_user_data_bit_for_bit`, `::test_restore_refuses_to_silently_overwrite_a_non_empty_data_dir`, `::test_uninstall_scripts_exist_and_never_delete_data_without_purge` | Clean install/upgrade on an actual clean machine stays documented-only in `docs/distribution/LIFECYCLE.md` (no clean machine available to this lot) — only backup/restore round trip and the uninstall scripts' data-safety property are exercised for real |
| A36 | product | verde | `tests/acceptance/test_a36_product_surface.py::test_cited_route_exists_on_the_real_app` (parametrized over 6 paths), `::test_surface_doc_names_every_cited_path_and_the_open_share_gap` | Share (a public/external link) stays an open gap, documented in `docs/spec/paridad/SUPERFICIE_PRODUCTO.md` with what would need to be built |

## Cómo T2 y T3 actualizan sus filas

1. Escribe el test en `tests/acceptance/test_a<NN>_<slug>.py` (o, si el caso
   ya vive en un fichero existente como A03, decóralo ahí) con exactamente
   un `@pytest.mark.acceptance("A0N")` por función de test. Usa
   `tests/acceptance/conftest.py::record_evidence(request, **refs)` para
   adjuntar referencias concretas (session/run ids, nodeids, ficheros) que
   `scripts/acceptance_run.py` recogerá en `evidence_refs`.
2. Actualiza la fila de este fichero: cambia `Estado` a `verde` (mecanismo
   real, test real, pasa) o `xfail` (mecanismo ausente; test
   `xfail(strict=True)` con el motivo dentro del propio test), rellena
   `Test` con `fichero::función` exacto, y `Qué falta` con lo que quede (o
   `—` si nada).
3. Corre `python3 -m pytest tests/test_acceptance_index.py -q` — falla con
   un mensaje exacto si la fila no nombra el fichero real, si el estado no
   coincide con el marker, o si `xfail` se declaró sin `@pytest.mark.xfail`
   en el test.
4. Si tu lote toca una fila que no era tuya (p. ej. porque el orquestador
   fusiona T2 y T3 sobre el mismo `ESTADO_ACEPTACION.md`), edita solo tu(s)
   fila(s) — cada lote es responsable únicamente de A01/A04/A07 (T2) o
   A02/A05/A06 (T3); si el fichero no existe todavía en tu worktree (porque
   T1 aún no se ha fusionado), créalo con estas mismas 36 filas y que el
   orquestador reconcilie el merge.
5. No toques la fila de A03 salvo que cambies el propio mecanismo que
   prueba — sigue siendo responsabilidad de quien la cierre originalmente
   documentar cualquier cambio ahí.
