# Matriz de paridad — Faustus vs. TrueForge (PR1 / Lote T1)

Las 22 filas de `04_COMPARATIVA_Y_DECISION.md` (paquete de auditoría), con la
columna «Faustus hoy» reauditada contra el árbol de este repo en el commit
`5b72467` (rama `lot/T1`). Regla del encargo: **ninguna fila se cierra por
existir una clase o un test simulado**; cierra una fila solo un test de
`ESTADO_ACEPTACION.md` marcado `verde` que ejercita código real y aparece
enlazado en la columna «Test de aceptación». Donde la cita de
`01_FAUSTUS_BASELINE.md` no se pudo reverificar línea por línea en este árbol
(auditado sobre otro checkout), la fila lo dice explícitamente en vez de
heredar el número sin comprobar.

Estados: **cerrada** = hay un test `verde` en `ESTADO_ACEPTACION.md` que
prueba exactamente esto contra código real. **parcial** = parte del
mecanismo existe y está verificado por lectura/tests focalizados
preexistentes, pero no hay (todavía) un caso de aceptación A01–A36 verde que
lo cubra end-to-end. **abierta** = brecha real o sin evidencia suficiente.

| # | Razón para elegir TrueForge | Evidencia upstream (doc/sección) | Faustus hoy (fichero:líneas, reauditado) | Cambio/PR | Test de aceptación | Estado | Fase |
|---|---|---|---|---|---|---|---|
| 1 | Incorporar componentes con licencia permisiva | `10_LICENCIA_Y_ADOPCION.md`; TrueForge `LICENSE` (MIT) | AGPL-3.0-or-later declarada en `README.md:298`, `README.es.md:299`, `LICENSE`; `ACKNOWLEDGMENTS.md:14-22` y `src/deep_research.py:1935` conservan atribución AGPL a Diogenes/CommanderTurtle (commit `87d01f6f`) — código adaptado, no solo un nombre de variable | TF17 — inventario de procedencia + decisión de los titulares; ningún PR técnico la cierra | Ninguno (decisión de producto/derechos, no de comportamiento) | **abierta — decisión del propietario** (no se ha tocado `LICENSE` ni `ACKNOWLEDGMENTS.md` en este lote) | P0/P1 |
| 2 | Consumir un SDK público | `02_TRUEFORGE.md` (cliente TS generado, CJS/ESM) | API versionada (`docs/api/versioning.md:1-50`: `X-Faustus-Api-Version`, 426 para clientes viejos) y guía de cliente + scripts Python (`docs/api/client-guide.md:1-40`); no hay paquete instalable con package metadata ni cliente TS generado en el árbol | TF02 | A20 (sdk), A25 (skills como parte del contrato de catálogo) — no asignados a T1/T2/T3 de este contrato | abierta | P1 |
| 3 | Insertar el chat en mi aplicación | `02_TRUEFORGE.md` (paquete UI embebible, slots, adapter) | Studio (`studio/`) es una app web completa, no un paquete de componentes embebibles; wrapper Electron en `desktop/`. Conectores pueden abrir una `ui_url` externa (`docs/api/connectors.md:37-39`), que es integración de otras apps, no embedding de Faustus | TF03 | A21 (embed) — no asignado a T1/T2/T3 de este contrato | abierta | P1 |
| 4 | Un núcleo reutilizable y pequeño | `02_TRUEFORGE.md` (core separado de servidor/UI/SDK) | Aplicación monolítica: `app.py` registra FastAPI y todos los managers; backend dividido en `core/`, `src/`, `services/`, `routes/` pero sin perfil headless empaquetado por separado. `src/contracts/`, `src/api_version.py` sí son puertos versionados reutilizables | TF01/TF16 — perfil headless sobre los mismos puertos | Ninguno directo (estructural, no de comportamiento de un caso concreto) | parcial | P1/P5 |
| 5 | Probarlo con un comando | `02_TRUEFORGE.md` (CLI npx) | Instalación soportada: Docker Compose, instalador nativo Windows/macOS, `launcher.py` (`README.md:27-48`); fricción de la primera tarea en instalación limpia no medida en este lote | TF14 | A35 (distribution) — no asignado a T1/T2/T3 de este contrato | parcial | P5 |
| 6 | Compartir una instancia en equipo | `02_TRUEFORGE.md` (Postgres/Redis, recetas multi-réplica) | Persistencia por defecto es SQLite/JSON local (`core/database.py`); sin perfil Team documentado con Postgres/Redis ni pruebas de cancelación entre workers en múltiples réplicas | TF12 | Ninguno de A01–A36 cubre despliegue multi-réplica explícitamente | abierta | P3 |
| 7 | Usar el login corporativo | `02_TRUEFORGE.md` (OIDC/PKCE/JWKS) | Auth local con usuarios, bcrypt, sesiones, TOTP (`core/auth.py:1-44`); `src/mcp_oauth.py:174-175` es OAuth/OIDC de **servidores MCP externos**, no sustituye el login de la aplicación — sin IdP central para usuarios de Faustus | TF12 | A22 (auth) — no asignado a T1/T2/T3 de este contrato | abierta | P3 |
| 8 | Aislar usuarios/organizaciones | `02_TRUEFORGE.md` (sesiones por owner, no multi-tenant completo) | Owner-scoping amplio en chat, memoria, calendario, email, tareas, documentos y API tokens (`routes/api_token_routes.py:13-40, 119-157`); `GET /api/mcp/servers` y `GET /api/app-connectors` exigen `require_admin` (`routes/connector_routes.py:142`, `routes/mcp/mcp_routes.py:201` — verificado en este árbol), pero siguen siendo superficies admin **globales**, no org-scoped; sin modelo de organización/rol | TF12 | A13 (artifacts, aislamiento por owner) toca una esquina; ninguno cubre aislamiento de organización completo | parcial | P3 |
| 9 | Reducir esquemas enviados al modelo | `02_TRUEFORGE.md` (deferred tools, preload/selectores) | `src/tool_index.py` y `src/tool_capabilities.py` existen y son reutilizables para discovery/selección; sin caso de aceptación que mida éxito de carga diferida con fallback acotado | TF04 | A08/A09 — no asignados a T1/T2/T3 de este contrato | parcial | P2 |
| 10 | Componer herramientas con código | `02_TRUEFORGE.md` (Code Mode ligado al sandbox) | No se encontró un puente código-aislado → `src/tool_execution.py` con mensaje tipado call_id/tool_id/args en el árbol auditado | TF05 | A10/A11 — no asignados a T1/T2/T3 de este contrato | abierta | P2 |
| 11 | Evitar resultados enormes en contexto | `02_TRUEFORGE.md` (offload y previews) | `src/artifact_store.py` existe (contenido, integridad, propietario); retención/lectura parcial tras reinicio sin caso de aceptación que lo pruebe end-to-end | TF06 | A12/A13 — no asignados a T1/T2/T3 de este contrato | parcial | P2 |
| 12 | Mantener contexto largo eficaz | `02_TRUEFORGE.md` (compaction integrada configurable) | `src/context_compactor.py` (1400 líneas) cableado; coste de recuperar información comprimida no medido en este lote | TF10/TF19 | A14/A15 — no asignados a T1/T2/T3 de este contrato | parcial | P2 |
| 13 | Ejecutar en sandbox enchufable | `02_TRUEFORGE.md` (proveedor, Daytona, fallback local condicionado) | Docker sandbox opt-in: si no hay daemon/imagen/workspace, rechaza en vez de caer al host (`src/sandbox_exec.py:1-29, 197-219`); `src/command_guard.py:1-34, 64-87` clasifica SAFE/CAUTION/DANGEROUS/CRITICAL con recibos encadenados; host Docker requiere `ODYSSEUS_ENABLE_HOST_DOCKER=true` explícito. Sin interfaz de proveedor local/remoto enchufable (un solo backend Docker) | TF07 | A16/A17 — no asignados a T1/T2/T3 de este contrato | parcial | P2 |
| 14 | Instalar skills reproducibles | `02_TRUEFORGE.md` (packs git-backed montados en sandbox) | Skills como directorios `data/skills/.../SKILL.md` con frontmatter, importación, auditoría de uso y gating por toolset (`src/workflows/skills.py`); catálogo git con revisión fijada y rollback no confirmado en este lote | TF08 | A25 (skills) — no asignado a T1/T2/T3 de este contrato | parcial | P2 |
| 15 | Conectar MCP con OAuth | `02_TRUEFORGE.md` (MCP remoto con auth, paginación, retries, políticas) | Transportes stdio/SSE/HTTP y OAuth MCP existen (`src/mcp_manager.py`, `src/mcp_oauth.py`); reconexión, selección y renovación de token no probadas con caso de aceptación real | TF09 | A18/A19 — no asignados a T1/T2/T3 de este contrato | parcial | P2 |
| 16 | Delegar trabajo sin inflar contexto | `02_TRUEFORGE.md` (subagentes dinámicos, concurrencia acotada) | Delegación existe (`src/agent_tools/subagent_tools.py`); `stop_workers_of_parent` (`:432-445`) para cascada de cancelación no es todavía transitiva a nietos (ver fila 18/A07); presupuesto global padre+hijos no unificado | TF10/TF20 | A07 (cancel) — asignado a T2 en este contrato (`tests/acceptance/test_a07_cancel_cascade.py`); A31 (budget) fuera de alcance | parcial | P2 |
| 17 | Preguntas/aprobaciones y UI generativa | `02_TRUEFORGE.md` (preguntas, approvals, Generative UI) | `src/approval_store.py:214-273` `decide` con CAS y recibo `already_<status>`/`decided_by` (verificado); `question_store` con `revision` → 409 (`routes/chat_routes.py:2401-2429`); `src/tool_approvals.py:120-171, 293-330` digest canónico comprobado en `src/tool_execution.py:1179-1195` (verificado, cierra A03); `consume` (`:497-545`, verificado) devuelve `None` sin motivo tipado — falta test HTTP con dos clientes concurrentes sobre la misma decisión | TF18 | A02 — asignado a T3 (`tests/acceptance/test_a02_approval_race.py`); **A03 — cerrada** (`tests/test_tool_approvals.py::test_dispatcher_rejects_modified_approved_action`, marcada `@pytest.mark.acceptance("A03")` en este lote) | parcial (A03 cerrada; A02 abierta) | P1/P2 |
| 18 | Sesiones, streaming y reconexión | `02_TRUEFORGE.md` (estado explícito, eventos, coordinación de executor) | Validación del POST de chat (`routes/chat_routes.py:1839-4061`) ocurre antes de `agent_runs.start()` (`:1093-1137`, único punto que cancela al predecesor, verificado); sin lock por sesión que serialice dos POST concurrentes válidos. `sequence` estable en `_publish` (`src/agent_runs.py:565-589`, verificado) y replay con cursor clamped en `subscribe` (`:1140-1224`, verificado) vía `GET /api/chat/resume/{sid}` (`routes/chat_routes.py:4083-4102`, verificado); el test de la ruta mockea `subscribe` en vez de ejercitarlo real | TF11/TF18 | A01 — asignado a T2 (`tests/acceptance/test_a01_turn_admission.py`); A04 — asignado a T2 (`tests/acceptance/test_a04_replay_cursor.py`) | abierta (ambas A01/A04 sin test aún al cierre de este lote) | P3 |
| 19 | Programar trabajos | `02_TRUEFORGE.md` (rutas y recursos de schedules) | `src/task_scheduler.py:778-902` claim/heartbeat/release por `lease_owner` con CAS; sin epoch/generación — un worker que pierde el lease deja de latir (`:904-909`) pero `_execute_task_locked`/`_execute_action`/`_execute_llm_task` no comprueban la propiedad antes de producir el efecto | TF11/TF20 | A06 — asignado a T3 (`tests/acceptance/test_a06_lease_fencing.py`); A24 (DST/misfire) fuera de alcance de este contrato | abierta | P1/P3 |
| 20 | Releases y mantenimiento predecibles | `02_TRUEFORGE.md` (changesets, CI, paquetes, OpenAPI/SDK generado) | Distribución self-hosted (Docker Compose, instalador nativo); sin matriz de compatibilidad ni smoke multiplataforma verificado en este lote | TF14 | A35 (distribution) — no asignado a T1/T2/T3 de este contrato | abierta | P5 |
| 21 | Ahorrar dinero con evidencia | `02_TRUEFORGE.md` (benchmark propio) | `RouterConfig` prioriza modelos locales y no escala en remoto por defecto (`src/model_router.py:137-149`, verificado); MOD-05 desactivado por defecto (`app.py:1143-1147`) y coexiste con `src/execution_router.py` — no hay un único router para turnos/workflows/agentes delegados; scorecard es append-only y local (`src/scorecard.py:17-18`) | TF01/TF24 | A32/A33 (benchmark) — no asignados a T1/T2/T3 de este contrato; `scripts/acceptance_run.py` (este lote) sienta el formato de `cost_status`/`total_cost` por caso como paso previo | parcial | P0/P2 |
| 22 | Migrar sin reconstruir agentes | `02_TRUEFORGE.md` (formatos/API existentes como punto de partida) | Sin importador ni guía de migración desde TrueForge en el árbol auditado | TF15 | A34 (migration) — no asignado a T1/T2/T3 de este contrato | abierta | P5 |

## Notas de reauditoría

- Las filas 2, 3, 5, 6, 7, 10, 15, 20, 22 reutilizan citas de
  `01_FAUSTUS_BASELINE.md` del paquete de auditoría (mismo snapshot, checkout
  distinto); se comprobó que los ficheros citados existen en este árbol y,
  donde fue barato hacerlo, que la cita de línea sigue siendo exacta o
  aproximada (p. ej. `routes/connector_routes.py:142` y
  `routes/mcp/mcp_routes.py:201` se reverificaron directamente). Donde no se
  reverificó línea por línea, la fila cita el fichero y se apoya en la
  clasificación EXISTE/PARCIAL/BRECHA/VALIDAR del propio baseline en lugar de
  inventar precisión que no se comprobó.
- Las filas 8, 16, 17, 18, 19, 21 incorporan directamente los hechos de la
  reauditoría de hoy fijada en `CONTRATO_TRUEFORGE_1.md` — cada cita de
  fichero:línea de esas filas fue releída contra este árbol (`5b72467`)
  durante este lote antes de escribirla aquí.
- Ninguna fila de esta tabla se marca `cerrada` salvo que exista una fila
  `verde` correspondiente en `ESTADO_ACEPTACION.md` con un test que la
  pruebe; hoy eso solo es cierto para A03 (fila 17).
