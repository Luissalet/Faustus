# Línea base de adaptación (ADP-01) — snapshot en `3b1c402`

Este documento es la fuente para que ninguna otra tarea del backlog de
adaptación (ADP-04 en adelante) duplique algo que ya existe en el repo.
Es la versión-de-repositorio del inventario que produjo ADP-01: léelo
ANTES de diseñar cualquier pieza nueva de las ADP-04..32 — si la fila dice
`cubierta` o `parcial`, hay código real que reutilizar o ampliar, no un
hueco en blanco.

- **Repo:** `faustus` · rama `master` · **HEAD en el momento del inventario:**
  `3b1c402c820aec7a460a30843ad931cc962b83f4` (forma corta: `3b1c402`).
- **Fuente del backlog:** `BACKLOG_FAUSTUS.md` (subido por Luis).
- **Método:** lectura incremental (Glob/Grep + fragmentos con Read), sin
  cargar ficheros completos de miles de líneas.
- **Vocabulario de clasificación:**
  - `cubierta` — los criterios de aceptación de la ficha ya los satisface
    código existente y verificado en este repo.
  - `parcial` — hay una pieza real y funcional, pero falta una parte no
    trivial del alcance de la ficha.
  - `nueva` — no se encontró implementación equivalente (se buscó por
    nombre, por concepto y por los ficheros que la propia ficha señala).
  - `descartar` — ninguna en este lote.
- Ningún hallazgo se declaró "ausente" solo por una búsqueda sin
  resultados, sin al menos dos estrategias de búsqueda (nombre exacto +
  concepto) y, donde aplica, lectura del fichero relevante.

Si tu tarea toca una fila marcada `cubierta` o `parcial`, **actualiza esta
tabla** en el mismo cambio (nueva evidencia, nuevo delta, o pásala a
`cubierta`) en vez de dejar que este documento quede desincronizado del
código. Si divide/reclasifica una fila, añade la fecha y el lote que lo
hizo en una nota bajo la tabla en vez de borrar la fila anterior.

**Nota (lote de integración, sin fecha exacta registrada):** la fila
ADP-22 de abajo describe el hueco `assert_outbound`-no-cableado como
todavía abierto ("xfail documentado", punto (4) de su columna Delta). Ese
hueco puntual (fallback de `foreground_model_routing.py`/`task_endpoint.py`)
quedó cerrado por el lote de integración — ver la nota transversal
correspondiente más abajo — sin reclasificar la fila entera, porque el
resto de su Delta (reconciliar MOD-05/`execution_router.py`, UI de
`max_price`, registro unificado de motivo/coste) sigue sin tocar.

## Tabla

| ID | Clasificación | Evidencia (ruta · símbolo · qué hace) | Delta concreto |
|---|---|---|---|
| ADP-04 | parcial | `studio/src/screens/documents/Editor.tsx` (696L): editor de texto plano sobre `<textarea>` (`editorRef`) con barra de comandos Markdown (`applyMarkdown(action, text, selStart, selEnd)`, línea 159). `SidePanel.tsx` ya monta un panel de documento (título, versión `v{doc.version}`, streaming). No hay parser que preserve construcciones desconocidas como nodos opacos, ni "modo fuente" distinto de "modo visual" (solo hay un modo: texto plano con atajos). Sin rastro de `OpenKnowledge` en el repo (`grep -i OpenKnowledge` → 0 resultados). | Construir el editor visual real (o justificar mantener solo texto) con: representación canónica por formato, nodos opacos para sintaxis no soportada, y separación editor-visual/modo-fuente. El "modo fuente" de la ficha ya existe de facto porque hoy solo hay ese modo. |
| ADP-05 | parcial | `studio/src/screens/studio/SidePanel.tsx` líneas 136-230: `doc.suggestions` es un array de `DocSuggestion {id, find, replace, reason}` (tipo en `studio/src/adapters/chat.ts:337` `DocSuggestion[]`, evento `doc_suggestions`). Muestra diff textual (`− {find}` / `+ {replace}`), botones Apply/Skip/Apply all (`applySuggestion`). Si `find` ya no está en el texto, se omite con aviso ("ya no está en el documento") en vez de aplicarse a ciegas. | No hay entidad "comentario" persistente ligada a documento+revisión base+cita+contexto (solo `find/replace/reason` efímero en el turno). `next.replace(sg.find, sg.replace)` sustituye la PRIMERA ocurrencia — no hay lógica explícita para "texto repetido no provoca reubicación arbitraria" ni marcado de "huérfano" (solo skip silencioso). Falta persistencia de hilos de comentarios y protección explícita "edición humana posterior no se pierde al aceptar". |
| ADP-06 | nueva | Sin resultados de `wikilink`/`backlink`/`[[` en `src/` ni `studio/src/` (búsquedas por ambos términos, 0 ficheros). `src/project_board.py::add_link/add_ref` enlaza *issues* del tablero (commits, refs), no documentos entre sí. `SidePanel.tsx` no referencia otros documentos por identidad. | Todo por construir: resolución por identidad estable, índice reconstruible de referencias/backlinks, separación enlace-existente vs sugerencia-del-modelo, navegación básica. |
| ADP-07 | cubierta (para el caso que ya cubre) / parcial en alcance total | `studio/src/adapters/fileConflict.ts` (59L): mecanismo tres-vías YA implementado y documentado como respuesta al 409 de `PUT /api/workspace/file` (`routes/workspace_routes.py::save_workspace_file`) — `ThreeWayLine{base,mine,theirs,status}`, `threeWayLines()`, `threeWaySummary()`; nunca descarta líneas en silencio (los faltantes quedan `null`). `src/chat_versions.py` guarda snapshots de versión por sesión (`save/list_versions/get/drop/clear`) — preserva borradores de conversación. | El three-way existente cubre ficheros de workspace en texto plano; falta extenderlo al editor visual (ADP-04) y a las propuestas (ADP-05), que hoy no pasan por este camino. No se debe (ni hace falta) sustituir el mecanismo — solo ampliarlo. |
| ADP-08 | nueva | `src/agent_tools/desktop_tools.py` (1196L): `DesktopBackend` es 100% basado en coordenadas — `click(x,y,button)`, `type_text`, `key_combo`, `scroll(x,y,dy)`, `list_windows`, `focus_window(title)`, `grab(region)`; `WindowsBackend` manda `SendInput`/Win32 crudo, `LinuxBackend` usa `xdotool`. Cero concepto de snapshot semántico, `ref`, precondición o "disposición de entrega". | Diseñar e implementar el contrato completo: snapshot/ref/precondiciones/disposición de entrega/observación posterior, límites de profundidad/tamaño, rechazo de ref de otra sesión/generación. Nada reutilizable salvo el propio `DesktopBackend` como punto de extensión (coordina bien con "ampliar con capacidades opcionales" de la ficha). |
| ADP-09 | nueva | Mismo fichero: `WindowsBackend` no usa `pywinauto` ni UIA (solo `SendInput`, `user32`/`gdi32` vía ctypes probablemente). Ningún `import pywinauto` ni `uia` en el repo (no localizado). Depende por completo de ADP-08. | Todo por construir: backend UIA opcional, aislado de COM, con degradación explícita sin `pywinauto` y en otros SO. |
| ADP-10 | parcial | `studio/src/screens/Activity.tsx` ya expone exportación/inspección de "record" de una ejecución (línea ~867-875: filtra claves internas y muestra JSON de campos objeto) y `src/tool_approvals.py::ExactToolApproval/PendingToolApproval` sellan la aprobación exacta por parámetros (`_canonical_digest`). Nada de esto es específico de acciones de escritorio. | Falta `src/desktop_semantics/evidence.py` (o equivalente): separar "disposición de entrega" de "resultado observado" para acciones de escritorio, exportación saneada y selectiva (sin claves/capturas no seleccionadas), y vínculo explícito a propietario/ejecución de origen. Depende de ADP-08 (que aún no existe). |
| ADP-11 | parcial | `studio/src/screens/Activity.tsx` líneas 31-33: filtros ya existentes — `accion` (`status==='waiting'`), `activo` (`running`/`queued`), `fallido` (`failed`); indicador `waiting` contado (línea 498); `live()` marca actividad "viva" si hay `running/queued/waiting/paused`. Es una priorización real, pero plana. | Falta separar explícitamente "espera humana" vs "cola local" vs "dependencia" vs "desconexión" vs "finalizado sin revisar" (hoy 3 categorías, no 5), y estado de lectura por usuario con motivo visible. Auditar antes de construir desde cero, tal como pide la propia ficha. |
| ADP-12 | parcial | Backend: `src/agent_runs.py::stop()/request_pause()` aceptan `expected_run_id` — una llamada tardía con el run_id equivocado no actúa sobre un run distinto (protección de "generación" a nivel de ejecución). `routes/agent_leases_routes.py` + `src/resource_ownership.py` dan leases de **recursos** entre workers delegados (no es lo mismo: previene colisión de ficheros/recursos, no respuestas asíncronas obsoletas en la UI). No se encontró `AbortController`/`connectionId` en `Activity.tsx` ni adapters. | Falta la capa de "identidad+generación de conexión" en el frontend (foco/reconexión) que la ficha pide — no existe `connection_lease.py` ni `connectionLease.ts`. Reutilizar el patrón de `expected_run_id` como precedente de diseño. |
| ADP-13 | nueva | Cero menciones de "Herdr" en código (`grep -ri Herdr` → solo `OpenKnowledge` coincidió por el patrón combinado; una búsqueda específica de "Herdr" en `OBJETIVOS.md` → 0). `OBJETIVOS.md:410` solo cita "herdr-studio" como candidato de investigación no adoptado, junto a "agent-desktop, open-knowledge". | Todo por construir; depende de ADP-12 (inexistente) y ADP-02. |
| ADP-14 | parcial (bastante cubierta) | `src/topology_export.py`: `workflow_to_mermaid(definition)` y `future_to_mermaid(future)` ya generan Mermaid desde `WorkflowDefinition`/runs reales, con `_status_class` por nodo (estado del run sobre el grafo). `studio/src/components/MermaidView.tsx` + `studio/src/adapters/topology.ts` (`workflowMermaid`, `workflowEstimate`) lo consumen dentro de `Activity.tsx` (línea 893: `<MermaidView code={diagramCode} .../>`). Esto ya es la "alternativa legible sin canvas" que pide el criterio de aceptación. `Automations.tsx` es, correctamente, un screen de "recetas" (lista/formulario), no el runtime de workflows durables — la ficha ya avisa de no confundirlos, y en este repo están efectivamente separados. | No existe canvas interactivo tipo React Flow (no se encontró `react-flow`/`reactflow` en `studio/` ni en `package.json`). Si el objetivo estricto exige edición visual arrastrable, falta construirla; si basta con proyección+estado (que los criterios permiten), el delta es pequeño. |
| ADP-15 | cubierta | `src/agent_profile_lint.py::lint_workflow()` (usa `find_cycles` con Tarjan, `_unreachable_nodes`) YA implementa exactamente lo pedido: `LINT-WF-EFFECT-NO-HUMAN` — un nodo con efecto externo (`EFFECTFUL_TYPES`) sin `human_approval` entre sus **ancestros reales** (`_ancestors`, recorrido hacia atrás por `needs`) se marca como finding; como `_ancestors` solo sigue la cadena de dependencias real, una aprobación en una rama ajena NO cuenta como cobertura (criterio de aceptación cumplido por diseño). `Finding` es tipado (`code/severity/subject/message/hint`, no scores arbitrarios). La UI (`studio/src/screens/agents/ProfileLint.tsx`) consume el mismo `Finding.to_dict()` vía `adapters/topology.ts::lintCatalog/lintProfile`, así que backend y UI muestran el mismo finding por construcción. | Verificar el cableado end-to-end (ruta HTTP real) y, si se quiere, portar heurísticas adicionales de "AI Graph Studio" que la ficha menciona; el núcleo (ciclos, inalcanzables, bypass de aprobación) ya existe y coincide con los criterios de aceptación casi literalmente. |
| ADP-16 | parcial | `src/workflow_cost_estimate.py::estimate()` ya hace: precio min/max, `unbounded_loops` (ciclos vía `find_cycles`, reutilizado), `unpriced_models` (modelo desconocido → no se pone a 0, se reporta aparte) — cumple "precio desconocido produce unknown y no cero". No ejecuta LLM/scripts (solo lee la definición). | Solo cubre coste de nodos `skill`. NO enumera conexiones, herramientas, permisos, entradas/salidas ni esperas (alcance de la ficha es más amplio que "solo precio"). No existía `src/workflows/preflight.py` — **hecho en W1-G**, ver abajo. |
| ADP-17 | parcial | Dirección de exportación ya sólida: `WorkflowDefinition.parse()/to_dict()` versionado por `schema_version` (`src/contracts/workflow.py`), `_find_cycle` rechaza ciclos en el `.parse()` (no se puede "habilitar" un ciclo sin saltarse el contrato), y `topology_export.py` exporta a Mermaid fielmente. | No había importación desde formato externo (`src/workflows/interchange.py` no existía) ni mapeo tipo-por-tipo con allowlist de handlers comprobados — **hecho en W1-G**, ver abajo. El "round-trip canónico" (propio JSON) ya funciona vía `parse`/`to_dict`. |
| ADP-18 | nueva | `src/project_board.py::Store` (1146L) tiene issues/comentarios/links/refs (`add_link`, `add_ref`, `import_sources`, `export_markdown`) pero ningún concepto de "requisito" versionado distinto de un issue — no hay campo `revision` de requisito, ni distinción propuesta/aprobado. `_KEYS`/vocabulario del board es cerrado (issues), tal como la propia ficha pide no romper. | Diseñar esquema mínimo de requisito (identidad/revisión/fuente), sidecars opcionales, enlazado desde issues existentes sin ampliar el vocabulario del board. Todo por construir sobre la base de `project_board.py`. |
| ADP-19 | nueva | No existe `src/requirements/` ni `src/agent_tools/requirement_tools.py`. `src/agent_tools/__init__.py` y `src/agent_loop.py` no tienen herramienta de lectura de requisito por fichero/ID/tarea. | Depende por completo de ADP-18 (inexistente). Falta construir lectura con scope de proyecto, priorización de restricciones esenciales, y presupuesto de contexto con truncación visible (hay precedente de presupuesto de contexto en `src/context_engine/budgets.py`, reutilizable como patrón). |
| ADP-20 | nueva | Sin `src/requirements/evidence.py` ni estados `linked/test-associated/verified/stale/unknown`. `project_board.py` no vincula pruebas/ejecuciones a un requisito versionado. Hay piezas reutilizables: `Activity.tsx` ya lista runs/ejecuciones reales que podrían servir de "evidencia" de origen. | Depende de ADP-18. Construir la matriz de cobertura completa: vínculo a evidencia real, invalidación de vigencia al cambiar requisito/código/prueba. |
| ADP-21 | cubierta | Muy completo ya: `src/model_capabilities.py` define `ModelCapability`/`CapabilityAssertion`/`DeterministicControl`/`CapabilityProbeResult`, todos con campo `source` normalizado (`SOURCE_UNKNOWN`, `SOURCE_ADMIN_OVERRIDE`, `SOURCE_ENDPOINT_CONFIG`, `SOURCE_CAPABILITY_PROBE`, `SOURCE_PROVIDER_READER`) y `confidence`. `src/model_capability_readers/` tiene un lector por proveedor (`openai.py`, `google.py`, `ollama.py`, `lmstudio.py`, `llamacpp.py`, `openrouter.py`, `generic_openai.py`). `src/model_calibration.py` añade manifiesto con clave `vendor+model_id+endpoint_id+digest`, `save_announced`/`save_tested`, `compute_degraded()` (marca información caducada/degradada) y `diff()`. Esto es, casi literalmente, "fuente, fecha, estado anunciado/probado/desconocido y override explícito". | Delta pequeño: confirmar que el fallback cuando el catálogo remoto no responde sigue permitiendo usar un modelo local configurado (diseño del módulo lo sugiere, no verificado end-to-end aquí). No hay lista de precios/modelos hardcodeada de este informe (correcto, cumple el límite). |
| ADP-22 | parcial | `src/model_router.py` (MOD-05) existe y está bien construido (scoring, `RouterConfig`, `choose()`, `explain()`, log de decisiones) pero su propio docstring dice explícitamente que está **inerte** (`enabled=False` por defecto) y que el ruteo real vive en `src/execution_router.py`, "ajeno, sin auditar contra MOD-05" — dos caminos de ruteo sin reconciliar, tal como advertía la pista del encargo. `src/openrouter_options.py` ya valida `require_parameters`, `max_price`, `data_collection`, `order/ignore` de proveedor por endpoint. `src/privacy_policy.py::assert_outbound/is_local_destination/get_privacy_profile` ya aplica la política "local no sale a la nube sin autorización" en alguna capa **auxiliar** (embedding lane, ChromaDB, compactación, reranker, OCR/visión, TTS/STT — ver su propio docstring) — **pero NO en la cadena de fallback del turno principal ni de tareas de fondo**: confirmado en W1-B (`tests/adaptations/test_contract_boundaries.py`, xfail documentado) que ni `src/foreground_model_routing.py` ni `src/task_endpoint.py` ni `routes/email_routes.py` importan `privacy_policy` en absoluto. | Falta: (1) cablear/reconciliar MOD-05 con `execution_router.py` o documentar por qué siguen separados; (2) verificar en UI que `max_price` por token no se presente como tope total del run (no auditado aquí); (3) registrar motivo de selección + coste estimado/confirmado de forma unificada entre ambos routers; (4) **nuevo, encontrado en W1-B**: cablear `assert_outbound` en la resolución de fallback del turno principal/tareas de fondo (hoy solo el auxiliar OCR/visión lo hace) — este es exactamente el trabajo de W1-F (`src/provider_policy.py`). |
| ADP-23 | nueva | `src/media_backends/` solo contiene `comfyui.py` y `pool.py` — ningún adaptador OpenRouter para imagen/audio/vídeo (`grep -l openrouter src/media_backends/*.py src/media_workflows.py` → 0). `src/media_runs.py`/`media_workflows.py` sí existen como gestores de trabajos reutilizables, tal como sugiere la ficha. | Construir el adaptador completo: contratos por modalidad, reconciliación de trabajos asíncronos, permisos de envío y procedencia — reutilizando los gestores de `media_runs.py`. |
| ADP-24 | parcial | Dos precedentes reales de OAuth+PKCE ya en el repo: `src/mcp_oauth.py` (registro, authorization-code+PKCE, refresh, para conexiones MCP) y `src/chatgpt_subscription.py::exchange_authorization_code(code, code_verifier)` (+ `routes/chatgpt_subscription_routes.py`) para la suscripción ChatGPT. Ningún flujo PKCE específico de OpenRouter (`src/provider_auth/openrouter.py` no existe). | Adaptar el patrón ya probado dos veces en el repo a OpenRouter, en vez de diseñar desde cero: revisar protocolo/retornos de OpenRouter, guardar secretos solo en backend, mostrar cuenta/ruta de cobro/revocación. |
| ADP-25 | parcial | `src/skills_runtime/discovery.py` (`discover()`, `roots_for()`, `shadowed()` — detecta colisiones de nombre entre raíces) + `bridge.py` (`manifest_from_markdown`, `manifest_from_skill`, `permissions_from_frontmatter`, `SkillBridgeResult`, `survey(check_backends=True)`) ya construyen un manifiesto con permisos declarados desde el frontmatter de cada skill. | No hay hash/digest en ningún fichero de `skills_runtime` (`grep -i "hash|digest|sha256"` → 0 resultados) — ningún pinning de contenido aprobado. Tampoco hay diff en actualización ni ruta HTTP que use `discover`/`survey` (`grep` en `routes/` para `skills_runtime` → 0 coincidencias): el descubrimiento existe pero no está expuesto ni gateado como instalación/activación revisable. |
| ADP-26 | cubierta | `tests/eval/tasks.py`: 6 tareas representativas reales (bug fix, feature, refactor, investigación, documento, tabular), cada una con `setup/script/verify`; las de código pasan por `src/verification.py::run_verifier` (verificador real, no juicio del modelo) y la de investigación por `verify_citation`. `tests/eval/harness.py::EvalApp` levanta la app real con modelo simulado/scriptado. `tests/eval/baseline.json` es una línea base grabada y reproducible offline, con nota explícita "not a live-model run" y exclusión de campos volátiles. Esto cumple casi literalmente "pruebas reproducibles offline con fallos tipados" y "el resultado registra qué parte fue simulada". | Extensión menor si se quieren más rutas (proveedores, más documentos/herramientas); el núcleo del corpus ya existe y pasa por Faustus real, no solo por el modelo. |
| ADP-27 | parcial (núcleo ya cubierto) | `tests/eval/ablation.py` (EVAL-02) reutiliza *las mismas* fixtures de `tasks.py`, corre `DIMENSIONS` con overrides de configuración, reporta tasa de éxito + p50/p95 de latencia/tokens sobre `repeats` runs, y `judge_improvement` implementa exactamente el criterio de la ficha: una variante solo cuenta como mejora si el éxito ≥ baseline Y el coste se mantiene dentro de `cost_ratio_max` (nunca solo por gastar más). `scripts/eval_run.py` es el runner, comparado contra `baseline.json`. | No hay integración con Promptfoo (`grep -ri promptfoo` → 0) ni `tools/evals/faustus_provider.py`. No se observó instrumentación explícita de memoria/residencia de modelos (la concurrencia=1 es de facto por diseño del harness, pero no está *medida* como métrica separada). |
| ADP-28 | cubierta (con duplicidad a resolver) | Hay DOS mecanismos que se solapan: **(a)** `src/repo_map.py` (460L) — árbol+símbolos con caché TTL de 20s por `(workspace, términos)`, invalidación **global** (`invalidate(workspace)` limpia todo el workspace, no por fichero), símbolos vía `ast` para Python y regex para el resto. **(b)** `src/context_engine/code_index.py` (1491L, mucho más rico) — `Symbol`/`Edge` en sqlite3, `_slice_hash` (hash de contenido por fragmento → invalidación **incremental** real vía `_forget_file`/`_drop_file`), `_is_secret_name` (exclusión de secretos ya implementada), `_coverage` (ranking de relevancia), extracción Python vía `ast` + fallback léxico para otros lenguajes — su propio comentario dice explícitamente "one parser, two readers — no tree-sitter is added" (decisión ya tomada y documentada de NO usar tree-sitter). `code_index.py` ya satisface casi literalmente los criterios de aceptación (invalidación por fichero, recuperación de símbolo relevante, exclusión de secretos). | El trabajo real no es construir de cero sino **auditar y consolidar** los dos mecanismos para no duplicar "el mapa ya documentado", tal como la propia ficha advierte — trabajo de W1-I. Si se insiste en tree-sitter, contradice una decisión ya registrada en el código — habría que justificarlo explícitamente. |
| ADP-29 | parcial (bastante cubierta) | `src/browser_view.py` ya implementa: `parse_snapshot_elements()` (extrae `{ref, role, name}` de un snapshot Playwright-MCP, dos formatos soportados), `element_present(ref, snapshot_text)` — comprobación **exacta** por ref contra un snapshot FRESCO (no substring), documentado explícitamente para evitar que un ref obsoleto de una página distinta se dé por válido (código "WEB-04"). También `provenance_enabled()`/`annotate_page_text` (evidencia de extracción). La propia ficha avisa "no presentar Playwright MCP como integración ausente" — en efecto, ya está presente y no trivial. | No se encontró comparación explícita del MCP actual contra alternativas, ni una herramienta que pida subárbol/búsqueda en vez de snapshot de página completa (no verificado en `agent_tools`) — trabajo de W1-H. El core de scoping de refs por snapshot ya existe. |
| ADP-30 | parcial (branding ya hecho, demos pendientes) | `README.md` YA cumple el punto 1 de implementación: usa marca "Faustus" (wordmark, capturas), declara explícitamente "Faustus builds on [Odysseus]... See ACKNOWLEDGMENTS.md" (línea 244) y aclara que nombres internos (`odysseus`, `ODYSSEUS_*`, rutas de API, claves de almacenamiento) se **mantienen por compatibilidad** mientras el nombre de producto es Faustus (línea 190) — exactamente lo que pide el criterio "sin renombrar claves internas compatibles". `ACKNOWLEDGMENTS.md` es un manifiesto de terceros real y detallado (repo/commit/licencia/destino por cada adaptación, p.ej. Diogenes AGPL-3.0, opencode MIT, llmfit MIT, Tongyi DeepResearch Apache-2.0). `THIRD_PARTY_NOTICES.md` (nuevo, W1-B/ADP-02) añade un segundo registro, estructurado y con esquema validado, para las piezas adaptadas bajo ADP-02 — complementa, no sustituye, a `ACKNOWLEDGMENTS.md`. | No existe `examples/showcase/` ni `docs/showcase.md` (`find -iname "*showcase*"` → 0), ni recorrido offline explícitamente simulado, ni las tres demos (documento/agente supervisado/escritorio). El trabajo de "corregir metadatos heredados" ya está hecho; falta construir las demostraciones. |
| ADP-31 | nueva (por diseño, XL correcto) | `docs/design/` existe con varios documentos (`workflow-script-skills.md`, `durable-change-evidence.md`, etc.) pero no hay `bounded-workflow-iterations.md`. La garantía que la ficha pide no tocar sigue intacta: `src/contracts/workflow.py::_find_cycle()` se invoca dentro de `WorkflowDefinition.parse()` y rechaza cualquier ciclo en la construcción — no hay ningún campo de "máximo de iteraciones" ni tipo de nodo de bucle en el esquema actual (confirmado también por el propio docstring de `workflow_cost_estimate.py`, que documenta la misma ausencia). | Todo el diseño está por escribir: alcance por iteración, topes, artefactos, joins, cancelación/recuperación, idempotencia por efecto/iteración, prototipo aislado. Es coherente que la ficha lo marque P3/XL — es un documento de diseño desde cero, no una implementación existente que ampliar. |
| ADP-32 | parcial | `src/llm_core.py:26` — `_LOCAL_MODEL_LOCK = asyncio.Lock()`, adquirido/liberado alrededor de las llamadas a modelo local: confirma que la serialización actual es un candado **global único** (justo lo que la ficha pide medir primero). `src/vram_admission.py` ya tiene un patrón de admisión por recurso: `_reservation_key(root, device)` normaliza por `root` (URL base del servidor), con `try_reserve/release_reservation/reservations_snapshot`, `Ticket`/`open_ticket`/`resolve` — esto ya evita, a nivel de VRAM, que "dos URLs del mismo servidor creen capacidad ficticia" (el criterio de aceptación más específico de la ficha ya está resuelto ahí). `src/gpu_policy.py` maneja orden/preferencia de GPU, no admisión de pools. | El patrón de `vram_admission.py` está acotado a presupuesto de VRAM de un servidor/dispositivo, no a "pools" generales de recursos independientes para llamadas a modelo. Falta generalizar: medir si el candado global limita tareas reales, definir pools explícitos reutilizando la normalización de alias ya existente en `_reservation_key`, y mantener ruta serial segura + prioridad de primer plano — trabajo de W1-I. |

## Notas transversales

- **README/ACKNOWLEDGMENTS/THIRD_PARTY_NOTICES** ya cubren buena parte de
  ADP-02/ADP-30: hay manifiesto de terceros real (`ACKNOWLEDGMENTS.md`) con
  repo, commit, licencia y destino por adaptación, y el README ya distingue
  Faustus de Odysseus y conserva nombres internos por compatibilidad.
  `THIRD_PARTY_NOTICES.md` + `docs/adaptations/provenance.json` (ambos
  nuevos, W1-B) añaden el registro estructurado que ADP-02 pedía sin
  reemplazar el narrativo.
- **Duplicidad detectada (relevante para ADP-01/ADP-28):** `src/repo_map.py`
  y `src/context_engine/code_index.py` resuelven un problema muy similar
  con madurez muy distinta; cualquier trabajo de ADP-28 debe empezar
  auditando cuál de los dos es el "mapa ya documentado" antes de tocar
  nada.
- **Dos routers de modelo sin reconciliar (ADP-21/ADP-22):** `src/model_router.py`
  (MOD-05, inerte por defecto) y `src/execution_router.py` (el que
  realmente decide hoy) — confirmado por el propio docstring de
  `model_router.py`, no es una suposición.
- **Privacidad no cableada en la cadena de fallback del turno principal
  (encontrado en W1-B, ADP-03) — CERRADO en el lote de integración:**
  `src/privacy_policy.py::assert_outbound` protegía solo los auxiliares que
  su propio docstring lista (embedding, Chroma, compactación, reranker,
  OCR/visión, TTS/STT), no la resolución de fallback del modelo de
  chat/agente en primer plano ni de tareas de fondo. El lote de integración
  cableó `assert_outbound` directamente en
  `src/foreground_model_routing.py::resolve_foreground_model_policy` y en
  `src/task_endpoint.py::resolve_task_candidates` (el candidato alternativo
  que no es local se descarta con motivo registrado, nunca se llama); los
  dos tests que documentaban esto como `xfail(strict=True)` en
  `tests/adaptations/test_contract_boundaries.py` ahora pasan como tests
  normales. `routes/email_routes.py`'s utility fallback y el endpoint que
  una sesión SELECCIONA explícitamente (no alcanzado por fallback, cubierto
  por la invariante 1 de `src/provider_policy.py::resolve_route`) quedan
  fuera de este cierre puntual.
