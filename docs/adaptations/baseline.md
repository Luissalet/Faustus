# Línea base de adaptación (ADP-01) — snapshot en `3b1c402`

Este documento es la fuente para que ninguna otra tarea del backlog de
adaptación (ADP-04 en adelante) duplique algo que ya existe en el repo.
Es la versión-de-repositorio del inventario que produjo ADP-01: léelo
ANTES de diseñar cualquier pieza nueva de las ADP-04..32 — si la fila dice
`cubierta` o `parcial`, hay código real que reutilizar o ampliar, no un
hueco en blanco.

- **Repo:** `faustus` · rama `master` · **HEAD en el momento del inventario:**
  `3b1c402c820aec7a460a30843ad931cc962b83f4` (forma corta: `3b1c402`).
- **Reconciliado (11-09-2026, lote W3-E):** las filas se recontrastaron
  contra `95747d9` (ola ADP, "ADP ola 1") y `757262e` (ola CMP, "CMP ola
  2") — ambos posteriores al inventario original. Ver "Notas
  transversales" al final de la tabla para el resumen del lote.
- **Fuente del backlog:** `BACKLOG_FAUSTUS.md` (subido por Luis).
- **Método:** lectura incremental (Glob/Grep + fragmentos con Read), sin
  cargar ficheros completos de miles de líneas.
- **Vocabulario de clasificación:**
  - `cubierta` — los criterios de aceptación de la ficha ya los satisface
    código existente y verificado en este repo.
  - `parcial` — hay una pieza real y funcional, pero falta una parte no
    trivial del alcance de la ficha.
  - `nueva` — no se encontró implementación equivalente (se buscó por
    nombre, por concepto y por los ficheros que la propia ficha señala).
  - `descartar` — ninguna en este lote.
- Ningún hallazgo se declaró "ausente" solo por una búsqueda sin
  resultados, sin al menos dos estrategias de búsqueda (nombre exacto +
  concepto) y, donde aplica, lectura del fichero relevante.

Si tu tarea toca una fila marcada `cubierta` o `parcial`, **actualiza esta
tabla** en el mismo cambio (nueva evidencia, nuevo delta, o pásala a
`cubierta`) en vez de dejar que este documento quede desincronizado del
código. Si divide/reclasifica una fila, añade la fecha y el lote que lo
hizo en una nota bajo la tabla en vez de borrar la fila anterior.

**Nota (lote de integración, sin fecha exacta registrada):** la fila
ADP-22 de abajo describe el hueco `assert_outbound`-no-cableado como
todavía abierto ("xfail documentado", punto (4) de su columna Delta). Ese
hueco puntual (fallback de `foreground_model_routing.py`/`task_endpoint.py`)
quedó cerrado por el lote de integración — ver la nota transversal
correspondiente más abajo — sin reclasificar la fila entera, porque el
resto de su Delta (reconciliar MOD-05/`execution_router.py`, UI de
`max_price`, registro unificado de motivo/coste) sigue sin tocar.

## Tabla

| ID | Clasificación | Evidencia (ruta · símbolo · qué hace) | Delta concreto |
|---|---|---|---|
| ADP-04 | parcial | `studio/src/screens/documents/Editor.tsx` (696L): editor de texto plano sobre `<textarea>` (`editorRef`) con barra de comandos Markdown (`applyMarkdown(action, text, selStart, selEnd)`, línea 159). `SidePanel.tsx` ya monta un panel de documento (título, versión `v{doc.version}`, streaming). No hay parser que preserve construcciones desconocidas como nodos opacos, ni "modo fuente" distinto de "modo visual" (solo hay un modo: texto plano con atajos). Sin rastro de `OpenKnowledge` en el repo (`grep -i OpenKnowledge` → 0 resultados). | Construir el editor visual real (o justificar mantener solo texto) con: representación canónica por formato, nodos opacos para sintaxis no soportada, y separación editor-visual/modo-fuente. El "modo fuente" de la ficha ya existe de facto porque hoy solo hay ese modo. |
| ADP-05 | cubierta (11-09, W1-E + W2-A1) | `src/document_comments.py` + `routes/document_comments_routes.py` (ola ADP, W1-E): hilos de comentario persistentes anclados por cita+contexto a documento+revisión, `accept()` que falla cerrado (409 `document_comments.base_changed`) si el anclaje ya no es inequívoco, huérfano explícito en vez de reubicación. `studio/src/screens/studio/SidePanel.tsx`'s `DocTab.applySuggestion` (ola CMP, W2-A1, `docs/adaptations/decisions/CMP-01.md`) ya NO sustituye la primera ocurrencia a ciegas: `findOccurrences` + `OccurrencePicker` muestran todas las apariciones con contexto y piden elegir (o "aplicar a todas"). `studio/src/screens/documents/ReviewPane.tsx` (nuevo, W2-A1) es la superficie de revisión: lista de comentarios, aceptar propuesta con diff, resolver, huérfano visible como "anclaje perdido", enlaces salientes/entrantes. | Sigue sin poder editar el `find` alrededor en la vista previa (HTML renderizado) — el anclaje por rango solo funciona en modo `<textarea>` (límite documentado en `CMP-01.md`, punto 1 de Cobertura parcial). El `anchor` (quote+before+after) que permitiría que la SUGERENCIA del agente traiga su propia desambiguación automática sin mostrar el selector no está cableado — es exactamente el encargo de W3-A de esta ola. |
| ADP-06 | cubierta (11-09, W1-E) | `src/document_links.py` (347L) + `routes/document_links_routes.py` (ola ADP, W1-E, `docs/api/document_links.md`): `[[wiki-links]]` por id y por título, con backlinks reconstruibles por índice, `ambiguous`/`broken` explícitos sin adivinar el destino, separación enlace-existente vs sugerencia. `ReviewPane.tsx` (W2-A1) consume `listDocLinks`/`listDocBacklinks` y los muestra como sección "Links" (salientes con su `status`, y "documentos que enlazan aquí"). | Ninguno relevante detectado en esta ola; navegación básica y resolución por identidad cumplidas tal como pedía la ficha. |
| ADP-07 | cubierta (para el caso que ya cubre) / parcial en alcance total | `studio/src/adapters/fileConflict.ts` (59L): mecanismo tres-vías YA implementado y documentado como respuesta al 409 de `PUT /api/workspace/file` (`routes/workspace_routes.py::save_workspace_file`) — `ThreeWayLine{base,mine,theirs,status}`, `threeWayLines()`, `threeWaySummary()`; nunca descarta líneas en silencio (los faltantes quedan `null`). `src/chat_versions.py` guarda snapshots de versión por sesión (`save/list_versions/get/drop/clear`) — preserva borradores de conversación. | El three-way existente cubre ficheros de workspace en texto plano; falta extenderlo al editor visual (ADP-04) y a las propuestas (ADP-05), que hoy no pasan por este camino. No se debe (ni hace falta) sustituir el mecanismo — solo ampliarlo. |
| ADP-08 | cubierta (11-09, ola ADP) | `src/desktop_semantics/` (nuevo paquete: `contracts.py` 244L, `session.py` 270L, `evidence.py` 107L, `fake_backend.py`) + `src/agent_tools/desktop_semantic_tools.py` (298L): snapshot acotado por profundidad/tamaño, `ref` con sesión+generación, `STALE_REF`/`AMBIGUOUS_TARGET` explícitos, `delivered`/`not_delivered`/`unknown` separado de `verified`, tools `desktop_snapshot`/`desktop_find`/`desktop_act` (`docs/api/desktop_semantics.md`). CMP-10 (W2-H, ola CMP) añade encima `src/desktop_semantics/channel.py::choose_channel` — decisión explícita de canal (`app_api`/`dom_cdp`/`native_a11y`/`pixels`), `risk_change` visible cuando el fallback baja de semántico a píxeles. El antiguo `DesktopBackend` por coordenadas (`src/agent_tools/desktop_tools.py`) sigue existiendo aparte, como alternativa de coordenadas cuando `desktop_act` rechaza por canal no semántico. | Verificación física contra un backend UIA real en Windows sigue pendiente (ver ADP-09) — el contrato y los fakes están probados, no una sesión Windows real. `app_api`/`dom_cdp` en `choose_channel` son lógica pura con tests, sin llamador real todavía (solo `native_a11y`/`pixels` tienen consumidor). El disparador "el usuario toma el control" (`src/desktop_control_session.py::invalidate_generation` en el punto de foco/cambio de ventana) es el encargo de W3-D de esta ola. |
| ADP-09 | parcial (código cubierto, validación física pendiente) | `src/desktop_semantics/windows_uia.py` (182L, ola ADP, W1-B/E): backend UIA opcional vía `pywinauto`, aislado, con degradación explícita cuando `pywinauto` no está instalado o el SO no es Windows. | No verificado contra una instancia Windows real con UIA activo — el hueco que la propia ficha señalaba ("todo por construir: backend UIA opcional, aislado de COM, con degradación explícita") está construido y con tests contra fakes, pero la validación física en hardware Windows de verdad sigue sin hacerse (ver `PENDIENTES.md`). |
| ADP-10 | cubierta (11-09, ola ADP) | `src/desktop_semantics/evidence.py` (107L, ola ADP): separa "disposición de entrega" (`delivered`/`not_delivered`/`unknown`) de "resultado observado" (verificación posterior), exportación saneada y selectiva sin capturas no seleccionadas, vínculo explícito a propietario/ejecución de origen — exactamente lo que la ficha pedía y que dependía de ADP-08 (ya construido). | Ninguno relevante detectado en esta ola para el alcance concreto de la ficha (evidencia de acciones de escritorio); el resto de exportación/inspección de runs (`Activity.tsx`) sigue siendo genérico, no específico de escritorio, tal como ya anotaba la fila original — eso no era parte del criterio de esta ficha. |
| ADP-11 | cubierta (11-09, ola ADP + W2-C) | `src/attention.py` (481L, ola ADP): `classify()` con ocho estados, motivo, prioridad y no-leídos por owner (`docs/api/attention.md`). CMP-05 (W2-C, ola CMP) profundiza sin sustituir: cuatro ejes separados (`lifecycle`/`wait_cause`/`connection_health`+`signal`/`next_action`), marcador durable de "terminado" (`src/agent_runs.py::_record_finished_marker`), vista por proyecto, orden estable durante interacción, MRU, guardia contra respuesta tardía (`nextRunParam`) — exactamente las cinco categorías (espera humana/cola local/dependencia/desconexión/finalizado sin revisar) que la fila original echaba en falta. | Vista por proyecto parcial: `task`/`render`/`approval` no llevan `project_id` todavía, caen en "Sin proyecto" (documentado en `CMP-05.md`). "Comparación de varias tareas lado a lado" no tiene vista dedicada — lectura conservadora de una frase corta del informe, no un criterio de aceptación propio. |
| ADP-12 | parcial | Backend: `src/agent_runs.py::stop()/request_pause()` aceptan `expected_run_id` — una llamada tardía con el run_id equivocado no actúa sobre un run distinto (protección de "generación" a nivel de ejecución). `routes/agent_leases_routes.py` + `src/resource_ownership.py` dan leases de **recursos** entre workers delegados (no es lo mismo: previene colisión de ficheros/recursos, no respuestas asíncronas obsoletas en la UI). CMP-05 (W2-C) añadió una guardia puntual y distinta en Activity: `adapters/activity.ts::nextRunParam` captura la clave del run en el momento del clic para que una resolución tardía no cierre ni sustituya la vista si el usuario ya navegó a otro run — cubre el síntoma en ESA pantalla concreta, no la capa general de identidad+generación de conexión que la ficha pide para todo el frontend. | Sigue sin existir `connection_lease.py`/`connectionLease.ts` como capa general de foco/reconexión — solo el caso puntual de Activity (arriba) tiene protección explícita. Reutilizar el patrón de `expected_run_id` (backend) y `nextRunParam` (frontend, ya construido en CMP-05) como precedentes de diseño. |
| ADP-13 | cubierta (contrato propio, sin validar contra Herdr real — 11-09, W2-H) | CMP-06 (`docs/adaptations/decisions/CMP-06.md`, ola CMP): `src/external_runtimes/herdr.py` — `HerdrClient` con transporte inyectable (nunca abre socket en tests), `negotiate_version()` rechaza versión desconocida, `list_presence()` con `certainty ∈ {structured, heuristic}` y `signal_age_s`, solo lectura (ningún verbo de escritura existe), token cifrado en `settings.py`. `routes/external_runtimes_routes.py` expone GET/PUT config y GET version/sessions. | El contrato de cable (`/version`, `/sessions`) está inferido ESTRICTAMENTE del texto del informe — nunca se llamó a un Herdr real (declarado explícitamente en `CMP-06.md`, "sin investigación externa, por instrucción del encargo"). Cableado de UI más allá del adaptador tipado, dejado a propósito para un lote posterior. |
| ADP-14 | cubierta (11-09, W2-E) | CMP-07 (`docs/adaptations/decisions/CMP-07.md`, ola CMP) construyó el canvas interactivo que esta fila señalaba como ausente: `studio/src/screens/workflows/PlanGraph.tsx` — SVG propio por capas de profundidad (sin `react-flow`/`d3`/dependencia nueva), navegación por teclado, tabla `<details>` como alternativa accesible; `NodeInspector.tsx` (re-lint al cambiar config) y `RunOverlay.tsx` (mismo grafo con estado de un run real superpuesto) sobre la pantalla nueva `/workflows` con tres modos (Diseño/Simulación/Ejecución). `src/topology_export.py`/`MermaidView.tsx` (ya existentes) siguen intactos como alternativa de solo-lectura. | Comparación de usabilidad frente a un canvas dedicado tipo React Flow no medida (hipótesis de mejora, no producto real contrastado). Persistencia de `layout` de nodos (`exportWorkflowDefinition` existe pero ningún botón la invoca) es el encargo de W3-F de esta ola. |
| ADP-15 | cubierta | `src/agent_profile_lint.py::lint_workflow()` (usa `find_cycles` con Tarjan, `_unreachable_nodes`) YA implementa exactamente lo pedido: `LINT-WF-EFFECT-NO-HUMAN` — un nodo con efecto externo (`EFFECTFUL_TYPES`) sin `human_approval` entre sus **ancestros reales** (`_ancestors`, recorrido hacia atrás por `needs`) se marca como finding; como `_ancestors` solo sigue la cadena de dependencias real, una aprobación en una rama ajena NO cuenta como cobertura (criterio de aceptación cumplido por diseño). `Finding` es tipado (`code/severity/subject/message/hint`, no scores arbitrarios). La UI (`studio/src/screens/agents/ProfileLint.tsx`) consume el mismo `Finding.to_dict()` vía `adapters/topology.ts::lintCatalog/lintProfile`, así que backend y UI muestran el mismo finding por construcción. | Verificar el cableado end-to-end (ruta HTTP real) y, si se quiere, portar heurísticas adicionales de "AI Graph Studio" que la ficha menciona; el núcleo (ciclos, inalcanzables, bypass de aprobación) ya existe y coincide con los criterios de aceptación casi literalmente. |
| ADP-16 | parcial (mucho más cubierta tras W2-B) | `src/workflows/preflight.py` (249L, ola ADP W1-G): conexiones/herramientas/permisos/entradas-salidas/esperas alrededor de `estimate()`, "unknown" si CUALQUIER skill invocada carece de precio. CMP-08 (W2-B, ola CMP) añade `estimate_detailed()` al lado (sin tocar el contrato de `estimate()`): cuentas separadas `node_activations`/`model_calls`/`external_ops`/`tokens{in,out}`, precio estructurado con procedencia (`StructuredPrice{source, as_of}`), `CallsProfile` para skills compuestas (nunca "1 llamada" por defecto sin perfil), `structural_bounds`/`forecast_with_assumptions`/`measured` distintos, y `src/plan_compare.py` (`POST /api/workflows/compare-plans`) con tabla `computed`/`estimated`/`unknown` por celda. | `local_latency` se enhebra end-to-end pero nadie lo calcula todavía desde `resource_admission.status()`/`llm_core.local_speed()`/`vram_admission.reservations_snapshot()`/`gpu_policy.model_sizes()` — es el encargo de W3-C de esta ola (`src/workflow_cost_estimate.py` SOLO `local_latency`). `skill_calls_profiles` "declarado" no se descubre automáticamente desde el manifiesto de una skill (falta `calls_profile` en el frontmatter) y `DATA_DIR/skill_call_history.json` no lo escribe nadie todavía — ambos también encargo de W3-C. Sin UI nueva para las cuentas separadas ni para `compare-plans` (adaptador y tipos listos, sin vista que los consuma). |
| ADP-17 | cubierta (11-09, ola ADP W1-G) | `src/workflows/interchange.py` (484L, ola ADP): importación desde formato externo con `design_only` y allowlist de handlers comprobados, ciclos no ejecutables rechazados igual que en `.parse()`. `WorkflowDefinition.parse()/to_dict()` (ya existente) sigue como round-trip canónico intacto; `topology_export.py` exporta a Mermaid. CMP-07 (W2-E) reutiliza `interchange.export_canonical` para `layout` aparte, sin fusionarlo con la definición. | Importación real de un export de aigraphstudio en su formato EXACTO sigue no verificada (forma asumida, no contrastada contra el exportador real de esa herramienta) — límite ya declarado en `docs/api/topology.md` y heredado sin cambios por CMP-07. |
| ADP-18 | cubierta (11-09, ola ADP) | `src/requirements/store.py` (732L, ola ADP): `REQ-N` por proyecto con revisiones inmutables, identidad/fuente/estado propuesta-aprobado, enlaces `implements`/`tests`/`evidences` — el esquema mínimo que la fila pedía, sin tocar el vocabulario cerrado de `project_board.py` (`docs/requirements-format.md`, `docs/api/requirements.md`). CMP-04 (W2-D, ola CMP) lee este store (nunca lo reimplementa) para construir `knowledge_neighborhood.py`. | Ninguno relevante detectado en esta ola para el esquema en sí; el trabajo de composición (vecindario tipado) es CMP-04, fila aparte de esta tabla por no tener ID ADP propio. |
| ADP-19 | cubierta (11-09, ola ADP) | `src/requirements/context.py` (129L) + `src/agent_tools/requirement_tools.py` (233L, ola ADP): lectura de requisito por fichero/ID/tarea con scope de proyecto, presupuesto de contexto con `omitted` explícito (mismo patrón que `context_engine/budgets.py`, reutilizado como precedente tal como la fila original sugería). `routes/requirements_routes.py` (247L) expone la API. | Ninguno relevante detectado en esta ola para el alcance concreto de la ficha. |
| ADP-20 | cubierta (11-09, ola ADP) | `src/requirements/evidence.py` (254L, ola ADP): `matrix()` calcula `linked`/`implemented`/`tested`/`verified`/`stale` por requisito, con `resolve_link_state` distinguiendo `stale` de `unknown` y cuatro razones de caducidad nombradas. CMP-04 (W2-D) traduce ese matrix (nunca lo recalcula) a los vecinos tipados `requirement→decision→symbol→test→run` con la prueba decisiva exacta del informe: requisito cambiado + test/código sin tocar → arista `evidences` marcada `stale=True` con "evidencia caducada", nunca "verificado". | `run` nodes del vecindario solo guardan el id del link (`run_123`) sin cruzarlo contra `agent_runs.py`/`changeset_store.py` para estado/fecha real — ni la ficha original ni `evidence.py` lo pedían, así que no se inventó ese cruce (documentado en `CMP-04.md`, punto 3). |
| ADP-21 | cubierta | Muy completo ya: `src/model_capabilities.py` define `ModelCapability`/`CapabilityAssertion`/`DeterministicControl`/`CapabilityProbeResult`, todos con campo `source` normalizado (`SOURCE_UNKNOWN`, `SOURCE_ADMIN_OVERRIDE`, `SOURCE_ENDPOINT_CONFIG`, `SOURCE_CAPABILITY_PROBE`, `SOURCE_PROVIDER_READER`) y `confidence`. `src/model_capability_readers/` tiene un lector por proveedor (`openai.py`, `google.py`, `ollama.py`, `lmstudio.py`, `llamacpp.py`, `openrouter.py`, `generic_openai.py`). `src/model_calibration.py` añade manifiesto con clave `vendor+model_id+endpoint_id+digest`, `save_announced`/`save_tested`, `compute_degraded()` (marca información caducada/degradada) y `diff()`. Esto es, casi literalmente, "fuente, fecha, estado anunciado/probado/desconocido y override explícito". | Delta pequeño: confirmar que el fallback cuando el catálogo remoto no responde sigue permitiendo usar un modelo local configurado (diseño del módulo lo sugiere, no verificado end-to-end aquí). No hay lista de precios/modelos hardcodeada de este informe (correcto, cumple el límite). |
| ADP-22 | parcial | `src/model_router.py` (MOD-05) existe y está bien construido (scoring, `RouterConfig`, `choose()`, `explain()`, log de decisiones) pero su propio docstring dice explícitamente que está **inerte** (`enabled=False` por defecto) y que el ruteo real vive en `src/execution_router.py`, "ajeno, sin auditar contra MOD-05" — dos caminos de ruteo sin reconciliar, tal como advertía la pista del encargo. `src/openrouter_options.py` ya valida `require_parameters`, `max_price`, `data_collection`, `order/ignore` de proveedor por endpoint. `src/privacy_policy.py::assert_outbound/is_local_destination/get_privacy_profile` ya aplica la política "local no sale a la nube sin autorización" en alguna capa **auxiliar** (embedding lane, ChromaDB, compactación, reranker, OCR/visión, TTS/STT — ver su propio docstring) — **pero NO en la cadena de fallback del turno principal ni de tareas de fondo**: confirmado en W1-B (`tests/adaptations/test_contract_boundaries.py`, xfail documentado) que ni `src/foreground_model_routing.py` ni `src/task_endpoint.py` ni `routes/email_routes.py` importan `privacy_policy` en absoluto. | Falta: (1) cablear/reconciliar MOD-05 con `execution_router.py` o documentar por qué siguen separados; (2) verificar en UI que `max_price` por token no se presente como tope total del run (no auditado aquí); (3) registrar motivo de selección + coste estimado/confirmado de forma unificada entre ambos routers; (4) **nuevo, encontrado en W1-B**: cablear `assert_outbound` en la resolución de fallback del turno principal/tareas de fondo (hoy solo el auxiliar OCR/visión lo hace) — este es exactamente el trabajo de W1-F (`src/provider_policy.py`), **cerrado** en el lote de integración de la ola ADP (ver "Notas transversales" más abajo). **Actualización 11-09 (ola ADP, `src/foreground_model_routing.py`/`src/agent_loop.py`):** `model_router.choose()` quedó cableado a `/api/chat`/`/api/chat_stream` para el caso concreto de sesión con `model=='auto'`, con evento SSE `model_router` y `record_outcome` — cierra parcialmente el punto (1), pero SOLO para ese caso; una sesión con modelo explícito sigue sin pasar por `execution_router.py` reconciliado, así que los puntos (1)-(3) siguen abiertos para el caso general. Ninguna fila de CMP ola 2 toca `model_router.py`/`execution_router.py` directamente. |
| ADP-23 | nueva | `src/media_backends/` solo contiene `comfyui.py` y `pool.py` — ningún adaptador OpenRouter para imagen/audio/vídeo (`grep -l openrouter src/media_backends/*.py src/media_workflows.py` → 0). `src/media_runs.py`/`media_workflows.py` sí existen como gestores de trabajos reutilizables, tal como sugiere la ficha. | Construir el adaptador completo: contratos por modalidad, reconciliación de trabajos asíncronos, permisos de envío y procedencia — reutilizando los gestores de `media_runs.py`. |
| ADP-24 | parcial | Dos precedentes reales de OAuth+PKCE ya en el repo: `src/mcp_oauth.py` (registro, authorization-code+PKCE, refresh, para conexiones MCP) y `src/chatgpt_subscription.py::exchange_authorization_code(code, code_verifier)` (+ `routes/chatgpt_subscription_routes.py`) para la suscripción ChatGPT. Ningún flujo PKCE específico de OpenRouter (`src/provider_auth/openrouter.py` no existe). | Adaptar el patrón ya probado dos veces en el repo a OpenRouter, en vez de diseñar desde cero: revisar protocolo/retornos de OpenRouter, guardar secretos solo en backend, mostrar cuenta/ruta de cobro/revocación. |
| ADP-25 | cubierta (11-09, ola ADP) | `src/skill_import_review.py` (258L, ola ADP): digest sha256 por skill, aprobación pinneada al digest exacto, diff en actualización, `privilege_request` rechazado cuando la skill pide más permisos de los aprobados; gate cableado en `workflows/skills.py::run` (nunca se ejecuta una skill no aprobada) y en `routes/skills_routes.py` — exactamente el hash/pinning/diff/ruta HTTP que la fila original echaba en falta. `discovery.py`/`bridge.py` (ya existentes) siguen siendo la fuente del manifiesto de permisos declarados; este módulo no los reimplementa, los consume. | Ninguno relevante detectado en esta ola. |
| ADP-26 | cubierta | `tests/eval/tasks.py`: 6 tareas representativas reales (bug fix, feature, refactor, investigación, documento, tabular), cada una con `setup/script/verify`; las de código pasan por `src/verification.py::run_verifier` (verificador real, no juicio del modelo) y la de investigación por `verify_citation`. `tests/eval/harness.py::EvalApp` levanta la app real con modelo simulado/scriptado. `tests/eval/baseline.json` es una línea base grabada y reproducible offline, con nota explícita "not a live-model run" y exclusión de campos volátiles. Esto cumple casi literalmente "pruebas reproducibles offline con fallos tipados" y "el resultado registra qué parte fue simulada". | Extensión menor si se quieren más rutas (proveedores, más documentos/herramientas); el núcleo del corpus ya existe y pasa por Faustus real, no solo por el modelo. |
| ADP-27 | parcial (núcleo ya cubierto) | `tests/eval/ablation.py` (EVAL-02) reutiliza *las mismas* fixtures de `tasks.py`, corre `DIMENSIONS` con overrides de configuración, reporta tasa de éxito + p50/p95 de latencia/tokens sobre `repeats` runs, y `judge_improvement` implementa exactamente el criterio de la ficha: una variante solo cuenta como mejora si el éxito ≥ baseline Y el coste se mantiene dentro de `cost_ratio_max` (nunca solo por gastar más). `scripts/eval_run.py` es el runner, comparado contra `baseline.json`. | No hay integración con Promptfoo (`grep -ri promptfoo` → 0) ni `tools/evals/faustus_provider.py`. No se observó instrumentación explícita de memoria/residencia de modelos (la concurrencia=1 es de facto por diseño del harness, pero no está *medida* como métrica separada). |
| ADP-28 | cubierta (duplicidad resuelta, 11-09 ola ADP) | Auditado y consolidado en la ola ADP (commit: "repo_map.symbols_for sobre code_index"): `src/repo_map.py::symbols_for` ahora lee de `src/context_engine/code_index.py` (el mecanismo más rico — sqlite3, `_slice_hash`, invalidación incremental por fichero, exclusión de secretos) en vez de mantener su propio índice paralelo con invalidación global; `tests/test_adp28_repo_map_consolidation.py` (223L) fija el comportamiento. La duplicidad que esta fila y las "Notas transversales" señalaban como pendiente de auditar ya no existe como dos fuentes de verdad. | Ninguno relevante detectado en esta ola; la decisión de no adoptar tree-sitter (ya registrada en `code_index.py`) sigue vigente y no fue revisitada. |
| ADP-29 | cubierta (11-09, ola ADP) | `src/browser_view.py` extendido con `subtree()`/`search()` (ola ADP, commit: "browser_view.subtree/search (ADP-29)"): pide subárbol o búsqueda en vez de forzar un snapshot de página completa, sobre el mismo `parse_snapshot_elements()`/`element_present()` (comprobación exacta por ref, ya cubierta) que la fila original documentaba como sólido. | No se encontró comparación explícita del MCP actual contra alternativas (Puppeteer, CDP directo) — no era un criterio de aceptación con entregable propio, solo una observación de contexto de la ficha original. |
| ADP-30 | cubierta (11-09, W2-H) | Branding (`README.md`, `ACKNOWLEDGMENTS.md`, `THIRD_PARTY_NOTICES.md`) ya estaba hecho antes de esta ola. CMP-14 (W2-H, ola CMP, `docs/adaptations/decisions/CMP-14.md`) construyó lo que faltaba: `docs/showcase.md` con los tres recorridos (documento con revisión real, agente supervisado, escritorio semántico) citando ficheros/tests reales de W2-A1/W2-C/W2-H; `examples/showcase/sample-project/` (README con ocurrencia repetida a propósito, `requirement.json` sin tests, `desktop-scenario.json` con nombres ambiguos — fixtures mínimas, marcadas DEMO); `src/doctor.py::_showcase_demo` comprueba su existencia. Separa explícitamente "conectar modelo existente" / "instalar motor opcional" / "probar una demo", y aclara que Faustus no es un instalador universal de un runtime de terceros. | El check de `doctor.py` es deliberadamente superficial (existencia de fichero, no ejecución del recorrido) — ampliarlo a ejecutar los tests citados sería un cambio de alcance distinto, no pedido por esta ficha. Las rutas citadas en `docs/showcase.md` hacia W2-A1/W2-C quedan desincronizadas si esos ficheros cambian de nombre (riesgo declarado en `CMP-14.md`, no oculto). |
| ADP-31 | cubierta como diseño, sin cablear al motor (por diseño — 11-09, W2-E) | CMP-07 (W2-E, ola CMP) entregó exactamente el documento que esta fila pedía: `docs/design/bounded-workflow-iterations.md` + `src/contracts/workflow_iteration.py` (dataclasses `IterationState`/`loop_effect_idempotency_key`, `max_iterations` siempre requerido — nunca "0 = ilimitado" — reutilizando `NODE_STATUSES`/`TERMINAL_NODE` de `contracts/workflow.py` en vez de un segundo vocabulario). La garantía que la ficha pedía no tocar sigue intacta: `_find_cycle()` sigue rechazando cualquier ciclo en `.parse()`, sin entrada `"loop"` en `NODE_TYPES`, sin import de `workflows/engine.py`/`handlers.py` (verificado por test con spy). | Deliberadamente **no cableado al motor/scheduler** — es un PROPOSAL, tal como pedía la ficha (P3/XL, decisión de diseño explícita, no tarea de código de esta ola). Wiring real de un tipo de nodo `"loop"` queda para una decisión futura. |
| ADP-32 | cubierta (11-09, ola ADP) | `src/resource_admission.py` (383L, ola ADP, commit: "resource_admission.py pools + /api/ops/admission (ADP-28/32)"): pools explícitos de recursos para llamadas a modelo, reutilizando la normalización de alias que `vram_admission._reservation_key` ya tenía, con ruta serial segura y prioridad de primer plano — generaliza el patrón que antes solo cubría VRAM de un servidor. `routes/ops_routes.py` expone `/api/ops/admission`; `tests/test_adp32_resource_pools.py` (203L) fija el comportamiento. | El candado global único de `llm_core.py::_LOCAL_MODEL_LOCK` sigue siendo el mecanismo real de serialización de llamadas locales — `resource_admission.py` define los pools y la prioridad, pero no se ha medido en esta ola si ese candado global limita tareas reales en producción (medición, no construcción, es lo que quedaría pendiente). |

## Notas transversales

- **README/ACKNOWLEDGMENTS/THIRD_PARTY_NOTICES** ya cubren buena parte de
  ADP-02/ADP-30: hay manifiesto de terceros real (`ACKNOWLEDGMENTS.md`) con
  repo, commit, licencia y destino por adaptación, y el README ya distingue
  Faustus de Odysseus y conserva nombres internos por compatibilidad.
  `THIRD_PARTY_NOTICES.md` + `docs/adaptations/provenance.json` (ambos
  nuevos, W1-B) añaden el registro estructurado que ADP-02 pedía sin
  reemplazar el narrativo.
- **Duplicidad `repo_map.py`/`code_index.py` (ADP-28) — CERRADA (11-09, ola
  ADP):** `repo_map.symbols_for` pasó a leer de `code_index.py` (el
  mecanismo más rico) en vez de mantener un segundo índice con invalidación
  global; ver fila ADP-28 arriba y `tests/test_adp28_repo_map_consolidation.py`.
- **Dos routers de modelo, reconciliación parcial (ADP-21/ADP-22):**
  `src/model_router.py` (MOD-05) dejó de estar completamente inerte — la
  ola ADP lo cableó a `/api/chat`/`/api/chat_stream` para el caso
  `model=='auto'` — pero `src/execution_router.py` sigue siendo quien
  decide cuando la sesión tiene un modelo explícito; los dos caminos
  siguen sin una reconciliación completa (ver fila ADP-22 arriba).
- **Reconciliación de esta tabla (11-09, lote W3-E, ola de integración
  final):** todas las filas se contrastaron contra lo que produjeron
  realmente la ola ADP (commit `95747d9`) y la ola CMP (commit `757262e`,
  fichas `docs/adaptations/decisions/CMP-*.md`) — no solo contra el
  snapshot original en `3b1c402`. Los cambios de clasificación quedan en
  cada fila, con el lote que cerró el hueco nombrado explícitamente; nada
  se marcó `cubierta` sin una ruta de fichero real y, donde procede
  (ADP-09, ADP-14, ADP-31), el límite de verificación pendiente
  (hardware físico, comparación de usabilidad, wiring al motor) queda
  igual de explícito que antes.
- **Privacidad no cableada en la cadena de fallback del turno principal
  (encontrado en W1-B, ADP-03) — CERRADO en el lote de integración:**
  `src/privacy_policy.py::assert_outbound` protegía solo los auxiliares que
  su propio docstring lista (embedding, Chroma, compactación, reranker,
  OCR/visión, TTS/STT), no la resolución de fallback del modelo de
  chat/agente en primer plano ni de tareas de fondo. El lote de integración
  cableó `assert_outbound` directamente en
  `src/foreground_model_routing.py::resolve_foreground_model_policy` y en
  `src/task_endpoint.py::resolve_task_candidates` (el candidato alternativo
  que no es local se descarta con motivo registrado, nunca se llama); los
  dos tests que documentaban esto como `xfail(strict=True)` en
  `tests/adaptations/test_contract_boundaries.py` ahora pasan como tests
  normales. `routes/email_routes.py`'s utility fallback y el endpoint que
  una sesión SELECCIONA explícitamente (no alcanzado por fallback, cubierto
  por la invariante 1 de `src/provider_policy.py::resolve_route`) quedan
  fuera de este cierre puntual.
